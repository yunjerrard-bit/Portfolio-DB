---
phase: 01-foundation-single-ticker
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - .gitignore
  - tickers.txt
  - tests/conftest.py
  - tests/test_input.py
  - tests/test_config.py
  - tests/test_market.py
  - tests/test_ema.py
  - tests/test_stats.py
  - tests/test_indicators.py
  - tests/test_color_rules.py
  - tests/test_smoke_end_to_end.py
  - tests/fixtures/rsi_golden.json
autonomous: true
requirements:
  - EXEC-02
user_setup: []
tags: [python, uv, pytest, scaffold, walking-skeleton]

must_haves:
  truths:
    - "uv run pytest는 testpaths=tests, pythonpath=src로 동작한다"
    - "Wave 1~4 모든 task의 <automated> 명령이 가리키는 test 함수가 RED 상태로 존재한다"
    - "구현 코드가 한 줄도 없어도 `uv sync` 후 `uv run pytest --collect-only`가 모든 테스트를 발견한다"
  artifacts:
    - path: "pyproject.toml"
      provides: "uv 의존성 핀 (Pattern 1) + [tool.pytest.ini_options]"
      contains: "[tool.pytest.ini_options]"
    - path: "tests/conftest.py"
      provides: "mock yfinance DataFrame fixture, 임시 tickers.txt fixture, 임시 .env fixture"
      min_lines: 30
    - path: "tests/test_input.py"
      provides: "INPUT-01/02/03 RED stubs"
    - path: "tests/test_config.py"
      provides: "INPUT-05 RED stub"
    - path: "tests/test_market.py"
      provides: "MKTD-01/02/03 RED stubs (network 미사용, mock yf.Ticker)"
    - path: "tests/test_ema.py"
      provides: "COMP-01/02/03 RED stubs + EMA 골든 입력 [1,2,3,4,5]→[1.0,1.5,2.25,3.125,4.0625]"
    - path: "tests/test_stats.py"
      provides: "COMP-04/05/06 RED stubs"
    - path: "tests/test_indicators.py"
      provides: "TECH-01/02 RED stubs + Wilder RSI/Stoch golden fixtures"
    - path: "tests/test_color_rules.py"
      provides: "COLOR-01~07 + TECH-04/05 RED stubs"
    - path: "tests/test_smoke_end_to_end.py"
      provides: "end-to-end + 3개 행 색 검증 RED stubs (openpyxl로 .xlsx 검증)"
    - path: "tests/fixtures/rsi_golden.json"
      provides: "Wilder RSI(14) 표준 입력→기대 출력 fixture"
    - path: ".env.example"
      contains: "EDGAR_USER_AGENT_EMAIL"
    - path: ".gitignore"
      contains: "output/"
    - path: "tickers.txt"
      provides: "예시 AAPL 한 줄"
  key_links:
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "tests/ 디렉토리"
      via: "testpaths=['tests'] + pythonpath=['src']"
      pattern: "testpaths.*tests"
    - from: "모든 test_*.py"
      to: "tests/conftest.py"
      via: "pytest fixture autodiscovery"
      pattern: "def test_"
---

<objective>
Walking Skeleton의 토대 (Wave 0). 프로젝트 부트스트랩 + 모든 후속 wave가 의존하는 test stub을 RED 상태로 생성한다. **구현 코드는 한 줄도 작성하지 않는다** — 본 plan의 산출물은 (1) `pyproject.toml` 의존성 + pytest 설정, (2) 9개 test 파일 + conftest, (3) 사용자 입력 파일(.env.example, tickers.txt, .gitignore).

Purpose: 모든 Wave 1~4 task의 `<automated>` 명령이 가리키는 test 함수가 이미 존재해야 TDD RED→GREEN 사이클이 닫힌다. VALIDATION.md "Per-Task Verification Map"의 11개 test 함수 이름은 single source of truth — 본 plan이 그것을 코드로 고정한다.

Output: 부트스트랩된 uv 프로젝트 (`uv sync` 성공), `uv run pytest --collect-only`가 11개 이상 test 함수를 발견, 모든 test가 RED (구현 없음 → ImportError 또는 NotImplementedError).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-single-ticker/01-CONTEXT.md
@.planning/phases/01-foundation-single-ticker/01-RESEARCH.md
@.planning/phases/01-foundation-single-ticker/01-VALIDATION.md
@.planning/phases/01-foundation-single-ticker/01-SKELETON.md
@CLAUDE.md

<interfaces>
<!-- 본 plan은 신규 프로젝트 부트스트랩이므로 기존 인터페이스 없음. -->
<!-- Wave 0이 정의하는 test 함수 시그니처는 후속 wave의 구현 계약: -->

Test 함수 이름 (VALIDATION.md Per-Task Verification Map에서 그대로 인용 — 변경 금지):

tests/test_input.py:
  - test_read_single_ticker        # INPUT-01
  - test_read_kr_suffix            # INPUT-02
  - test_empty_file_exits_nonzero  # INPUT-03

tests/test_config.py:
  - test_missing_env_fails         # INPUT-05

tests/test_market.py:
  - test_fetch_ohlcv_date_window   # MKTD-01
  - test_uses_curl_cffi_session    # MKTD-02
  - test_retries_on_rate_limit     # MKTD-03

tests/test_ema.py:
  - test_ema_matches_tradingview_formula  # COMP-01
  - test_diff_columns                     # COMP-02
  - test_daily_change                     # COMP-03

tests/test_stats.py:
  - test_expanding_median_std     # COMP-04
  - test_cumulative_scalars       # COMP-05
  - test_expanding_volume         # COMP-06

tests/test_indicators.py:
  - test_stoch_slow_known_input   # TECH-01
  - test_rsi_wilder_known_input   # TECH-02

tests/test_color_rules.py:
  - test_tech_buckets             # TECH-04/05
  - test_sigma_buckets            # COLOR-01~07

tests/test_smoke_end_to_end.py:
  - test_single_ticker_workbook   # SHEET-01~08, TECH-03/06, OUT-01~03
  - test_color_at_three_rows      # Success Criteria #2/#3
</interfaces>

<pyproject_template>
<!-- Pattern 1 (RESEARCH.md §263-293)에서 인용 — 그대로 사용 -->
[project]
name = "stocksig"
version = "0.1.0"
description = "표준편차 기반 주식 매매신호 + 포트폴리오 관리 시트"
requires-python = ">=3.13"
dependencies = [
    "yfinance>=0.2.66",
    "curl_cffi>=0.15,<0.16",
    "pandas>=2.2",
    "numpy>=2.0",
    "XlsxWriter>=3.2",
    "tenacity>=9.0",
    "python-dotenv>=1.0",
]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-mock", "openpyxl"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/stocksig"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
</pyproject_template>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로젝트 부트스트랩 (pyproject.toml + uv sync + 사용자 입력 파일)</name>
  <files>pyproject.toml, .env.example, .gitignore, tickers.txt</files>
  <action>
    `pyproject.toml`을 Write tool로 생성하되 본 plan의 `<pyproject_template>` 블록을 그대로 사용 (의존성 핀 + hatchling backend + `[tool.pytest.ini_options]` 포함). 그 다음 PowerShell에서 `uv sync` 실행 — 첫 실행 시 가상환경 생성 + 모든 핀된 의존성 설치. lock 파일(`uv.lock`)이 생성되는 것이 정상 (커밋 대상).

    `.env.example`을 Write로 생성 — 두 줄:
    ```
    EDGAR_USER_AGENT_EMAIL=your-name your-email@example.com
    OPENDART_API_KEY=
    ```
    (실제 `.env`는 사용자가 작성하며 `.gitignore`로 제외됨 — INPUT-05의 fail-fast는 Wave 1에서 구현)

    `.gitignore`을 Write로 생성 — 최소 다음 항목 포함: `output/`, `.env`, `*.xlsx`, `__pycache__/`, `.venv/`, `*.pyc`, `.pytest_cache/`, `uv.lock` 은 **포함 안 함**(커밋 대상). RESEARCH.md Pitfall "포트폴리오 파일 commit 금지" 및 Security 권고 반영.

    `tickers.txt`을 Write로 생성 — 단일 라인 `AAPL` (Phase 1 검증 티커, CONTEXT.md `<specifics>`).

    Decision references: D-01(레이어 구조 — 본 task에서는 디렉토리 미생성, Wave 1+에서 모듈과 함께 생성), D-06(yfinance 핀), EXEC-02(uv + pyproject).
  </action>
  <verify>
    <automated>uv sync &amp;&amp; uv run python -c "import yfinance, curl_cffi, pandas, xlsxwriter, tenacity, dotenv; print('ok')"</automated>
  </verify>
  <done>
    `pyproject.toml` 존재 + `uv.lock` 생성됨. `uv run python -c "import ..."`가 `ok` 출력. `.env.example`/`.gitignore`/`tickers.txt` 존재.
  </done>
</task>

<task type="auto">
  <name>Task 2: conftest.py + 공유 fixtures (mock yfinance DataFrame, 임시 tickers.txt, 임시 .env, RSI golden)</name>
  <files>tests/conftest.py, tests/fixtures/rsi_golden.json</files>
  <action>
    `tests/conftest.py`를 Write로 생성. 다음 fixture를 정의:

    1. `mock_ohlcv_df` — `pd.DataFrame`(2700 rows, columns=[Open, High, Low, Close, Volume], DatetimeIndex)를 결정적 시드로 생성 (`numpy.random.default_rng(seed=42)`). 종가는 100±drift, high=close*1.02, low=close*0.98, volume=정수. yfinance 응답을 흉내내는 구조이며 단위 테스트와 smoke test에서 공유. min_lines: 위해 약 30줄.
    2. `tmp_tickers_file` — pytest `tmp_path` 사용. 함수가 `(content: str) -> Path`를 반환하는 factory fixture. content 그대로 파일에 쓰고 path 반환.
    3. `tmp_env_file` — 동일 패턴. content (예: `EDGAR_USER_AGENT_EMAIL=x\nOPENDART_API_KEY=y`)를 받아 `.env` 작성, path 반환.
    4. `rsi_golden` — `tests/fixtures/rsi_golden.json`을 로드해서 `{"inputs": [...], "expected_rsi": [...]}` dict 반환.

    `tests/fixtures/rsi_golden.json`을 Write로 생성. Wilder RSI(14) 검증용 골든 셋: Wilder 1978 "New Concepts" 표준 예제 또는 합리적으로 도출 가능한 시퀀스. 단, **Wave 2에서 RSI 구현이 완료된 시점**에 구현팀이 실제 계산 값으로 fixture를 backfill하는 것이 정상 — 현재는 다음 sanity 입력만 fix하고 expected는 `null` placeholder로 두며 `test_rsi_wilder_known_input`이 fixture를 읽어 비교한다:
    ```json
    {
      "description": "Wilder RSI(14) golden. expected_rsi는 Wave 2에서 Wilder 1978 표준 예제 또는 TradingView 캡처 값으로 backfill.",
      "period": 14,
      "closes": [44.34,44.09,44.15,43.61,44.33,44.83,45.10,45.42,45.84,46.08,45.89,46.03,45.61,46.28,46.28,46.00,46.03,46.41,46.22,45.64,46.21,46.25,45.71,46.45,45.78,45.35,44.03,44.18,44.22,44.57],
      "expected_rsi_at_index_14": null,
      "tolerance": 0.5
    }
    ```
    Wave 2 RSI task가 first GREEN 시 expected를 backfill (구현 결과 그대로가 아니라 외부 참조와 일치해야 함 — Wave 2 task가 이를 명시).

    Decision references: D-05(한국어 메시지 — fixture는 코드만이라 영향 없음), D-06(yfinance 응답 mock 구조).
  </action>
  <verify>
    <automated>uv run python -c "import json; data = json.load(open('tests/fixtures/rsi_golden.json', encoding='utf-8')); assert data['period']==14 and len(data['closes'])&gt;=15"</automated>
  </verify>
  <done>
    `tests/conftest.py` 존재 + 4개 fixture 정의됨. `tests/fixtures/rsi_golden.json`이 JSON parseable이며 `period=14`, `closes` 길이 ≥ 15. `uv run pytest --collect-only`가 fixture 관련 collection 에러 없이 동작.
  </done>
</task>

<task type="auto">
  <name>Task 3: 9개 test 파일 RED stubs (VALIDATION.md test name 매핑 single source of truth 고정)</name>
  <files>tests/test_input.py, tests/test_config.py, tests/test_market.py, tests/test_ema.py, tests/test_stats.py, tests/test_indicators.py, tests/test_color_rules.py, tests/test_smoke_end_to_end.py</files>
  <action>
    9개 test 파일을 각각 Write로 생성. 각 파일은 본 plan의 `<interfaces>` 블록에 나열된 test 함수 이름을 **정확히 그대로** 정의해야 한다 (VALIDATION.md Per-Task Verification Map의 `pytest tests/test_X.py::test_Y -x` 명령이 그대로 동작해야 하므로).

    모든 stub 함수는 다음 형태로 작성:
    ```python
    import pytest

    @pytest.mark.xfail(reason="Wave N: 구현 대기 (PLAN 0X)", strict=False)
    def test_<name>(...):
        # GIVEN/WHEN/THEN 주석으로 기대 behavior 명시 (구현 task가 채울 슬롯)
        # 본 task에서는 stub만 — 실제 assertion은 후속 wave 구현 task가 추가
        from stocksig.io.input import read_tickers  # noqa
        raise NotImplementedError("Wave N에서 구현")
    ```
    `xfail(strict=False)`로 두면 RED 상태가 "expected failure"로 잡혀 wave 진행이 막히지 않음. 후속 wave 구현 task는 `xfail` 데코레이터 제거 + 실제 assertion 작성 + import 활성화로 GREEN 전환.

    각 파일별로 다음 import-target 모듈을 docstring에 명시 (후속 wave 구현 계약):

    - `tests/test_input.py` → `from stocksig.io.input import read_tickers`. 3 함수 (INPUT-01/02/03). `test_empty_file_exits_nonzero`는 `pytest.raises(SystemExit)` + `excinfo.value.code != 0` + 한국어 메시지 stderr 포함 검증 슬롯.
    - `tests/test_config.py` → `from stocksig.config import load_env`. 1 함수 (INPUT-05). `tmp_env_file("")` 입력 시 `SystemExit` + 한국어 메시지.
    - `tests/test_market.py` → `from stocksig.io.market import fetch_ohlcv`. 3 함수 (MKTD-01 date window today±4000d 검증; MKTD-02 `_SESSION` 모듈 attribute가 `curl_cffi.requests.Session` 인스턴스 + `impersonate="chrome"`; MKTD-03 `YFRateLimitError` mock side_effect 시 tenacity 재시도 5회). **네트워크 호출 없음** — `mocker.patch('yfinance.Ticker')`로 차단.
    - `tests/test_ema.py` → `from stocksig.compute.ema import add_ema_columns`. 3 함수. `test_ema_matches_tradingview_formula`는 골든 입력 `[1,2,3,4,5]`로 EMA(span=3, adjust=False) → `[1.0,1.5,2.25,3.125,4.0625]` 비교 (tolerance 1e-9). `test_diff_columns`는 가격-EMA, `test_daily_change`는 `EMA.diff()`.
    - `tests/test_stats.py` → `from stocksig.compute.stats import add_expanding_stats, cumulative_scalars`. 3 함수. `test_expanding_median_std`는 단순 입력으로 expanding median/std 값 검증. `test_cumulative_scalars`는 3행/4행용 스칼라. `test_expanding_volume`은 거래량 컬럼이 동일 처리되는지.
    - `tests/test_indicators.py` → `from stocksig.compute.indicators import stoch_slow, rsi_wilder`. 2 함수. `test_stoch_slow_known_input`은 RESEARCH.md Pattern 5 sanity (close=high → %K≈100, close=low → %K≈0, 일정 가격 → %K=50). `test_rsi_wilder_known_input`은 `rsi_golden` fixture 사용.
    - `tests/test_color_rules.py` → `from stocksig.compute.color_rules import decide_sigma_bucket, decide_stoch_bucket, decide_rsi_bucket, SigmaBucket, TechBucket`. 2 함수. `test_sigma_buckets`는 7개 케이스 (DEFAULT/SOFT_GREEN/HARD_GREEN/SOFT_RED/HARD_RED, NaN 입력, std=0 입력 — **D-02** 검증). `test_tech_buckets`는 Stoch≤20/≥80, RSI≤30/≥70 경계값.
    - `tests/test_smoke_end_to_end.py` → `from stocksig.main import run` 또는 `from main import run`. 2 함수. `test_single_ticker_workbook`은 mock yfinance(`mock_ohlcv_df` fixture) → `main.run()` 실행 → openpyxl로 `output/portfolio_*.xlsx` 열기 → 시트 이름=`AAPL`, A1=`AAPL`, 5행 한국어 헤더 존재, 6행+ 데이터 검증. `test_color_at_three_rows`는 가장 최근/중간/오래된 3개 행에 대해 expected SigmaBucket을 직접 계산 → 워크북에서 해당 셀의 `cell.font.color.rgb`가 D-04 hex와 일치 검증 (Success Criteria #2/#3).

    Decision references: D-01 (import 경로 = `stocksig.io.*` 등), D-02 (test_sigma_buckets NaN/0 케이스), D-04 (color hex 값을 test에서 import해서 비교 — `from stocksig.compute.color_rules import GREEN_800, RED_800, ...`), D-05 (한국어 stderr 메시지 검증), D-06 (test_fetch_ohlcv_date_window date 인자).
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q 2&gt;&amp;1 | grep -E "test_(read_single_ticker|read_kr_suffix|empty_file_exits_nonzero|missing_env_fails|fetch_ohlcv_date_window|uses_curl_cffi_session|retries_on_rate_limit|ema_matches_tradingview_formula|diff_columns|daily_change|expanding_median_std|cumulative_scalars|expanding_volume|stoch_slow_known_input|rsi_wilder_known_input|tech_buckets|sigma_buckets|single_ticker_workbook|color_at_three_rows)" | wc -l</automated>
  </verify>
  <done>
    위 verify 명령이 ≥ 19를 출력 (19개 test 함수 모두 collection됨). 추가로 `uv run pytest -q --no-header 2>&1 | tail -5`에서 모든 테스트가 `XFAIL` (or `xfailed`) 상태 — 즉 collection 단계 import 에러 없음, runtime 단계 NotImplementedError가 xfail로 흡수됨.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→Python | `tickers.txt`/`.env` (사용자 작성) 읽기. Phase 1은 단일 미국 티커지만 파서는 신뢰 경계로 취급 |
| PyPI→local venv | `uv add` 7 런타임 + 3 dev 의존성 설치. 본 plan에서 신규 패키지 도입 없음 (모두 STACK.md verified pins) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | `.env` 파일 (EDGAR/DART 키) | mitigate | `.gitignore`에 `.env` 명시 (본 task 1에서 작성). `.env.example`만 커밋. PITFALLS.md "Security Mistake: 포트폴리오 파일 commit 금지" 참조 |
| T-01-02 | Information Disclosure | `output/*.xlsx` (보유 종목 노출) | mitigate | `.gitignore`에 `output/`, `*.xlsx` 명시 |
| T-01-SC | Tampering | uv 패키지 설치 (yfinance, curl_cffi, pandas, numpy, XlsxWriter, tenacity, python-dotenv, pytest, pytest-mock, openpyxl) | accept | 모든 패키지는 STACK.md / RESEARCH.md §Package Legitimacy Audit에서 verified (Approved disposition, mainstream PyPI 다년 이력). slopcheck 미실행이나 STACK.md verified pins 그대로 사용 |
</threat_model>

<verification>
- `uv sync` 성공 + 7 런타임 + 3 dev 패키지 설치 완료
- `uv run pytest --collect-only`가 19개 test 함수 발견
- `uv run pytest -q`가 collection 에러 없이 모두 XFAIL/xfailed (Wave 1+ 구현 대기)
- `tests/fixtures/rsi_golden.json` parseable
- `.gitignore`에 `output/`, `.env`, `*.xlsx` 모두 포함
- `pyproject.toml`에 `[tool.pytest.ini_options] testpaths=["tests"] pythonpath=["src"]` 존재
</verification>

<success_criteria>
- 19개 test 함수가 VALIDATION.md Per-Task Verification Map의 이름과 **1:1 정확히 일치** (Wave 1~4가 `pytest tests/test_X.py::test_Y -x`를 명령 변경 없이 사용 가능)
- 후속 wave 모든 구현 task가 `xfail` 데코레이터 제거 + assertion 채우기만으로 GREEN 전환 가능
- 부트스트랩 reproducibility: 클린 클론 후 `uv sync && uv run pytest --collect-only`로 동일 결과
</success_criteria>

<output>
Create `.planning/phases/01-foundation-single-ticker/01-01-SUMMARY.md` when done. SUMMARY는 19개 test 함수의 정확한 이름 + import target 모듈을 표로 기록 (후속 wave의 single source of truth).
</output>
