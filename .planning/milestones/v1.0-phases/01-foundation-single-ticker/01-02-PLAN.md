---
phase: 01-foundation-single-ticker
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - src/stocksig/__init__.py
  - src/stocksig/config.py
  - src/stocksig/io/__init__.py
  - src/stocksig/io/input.py
  - src/stocksig/io/market.py
autonomous: true
requirements:
  - INPUT-01
  - INPUT-02
  - INPUT-03
  - INPUT-05
  - MKTD-01
  - MKTD-02
  - MKTD-03
user_setup: []
tags: [python, yfinance, curl-cffi, tenacity, dotenv]

must_haves:
  truths:
    - "tickers.txt 한 줄당 티커가 읽혀 list[str]으로 반환된다 (US/KR suffix-agnostic)"
    - "빈/없는 tickers.txt 실행 시 한국어 에러 + 비0 exit"
    - ".env에서 EDGAR_USER_AGENT_EMAIL/OPENDART_API_KEY가 비어있으면 한국어 에러 + 비0 exit"
    - "fetch_ohlcv(ticker)는 today-4000d~today 범위 OHLCV DataFrame을 반환한다"
    - "yfinance.Ticker는 curl_cffi.requests.Session(impersonate='chrome') session으로 호출된다"
    - "YFRateLimitError 발생 시 tenacity 정책으로 최대 5회 재시도된다"
  artifacts:
    - path: "src/stocksig/__init__.py"
      provides: "패키지 마커"
    - path: "src/stocksig/config.py"
      provides: "load_env() — python-dotenv 로드 + 한국어 fail-fast"
      min_lines: 20
    - path: "src/stocksig/io/__init__.py"
      provides: "io 서브패키지 마커"
    - path: "src/stocksig/io/input.py"
      provides: "read_tickers(path: str | Path) -> list[str]"
      min_lines: 25
    - path: "src/stocksig/io/market.py"
      provides: "fetch_ohlcv(ticker: str) -> pd.DataFrame + 모듈 attribute _SESSION"
      min_lines: 30
  key_links:
    - from: "src/stocksig/io/market.py:_SESSION"
      to: "curl_cffi.requests.Session(impersonate='chrome')"
      via: "모듈 레벨 단일 인스턴스 (재사용)"
      pattern: "Session\\(impersonate=\"chrome\""
    - from: "src/stocksig/io/market.py:fetch_ohlcv"
      to: "yfinance.Ticker(ticker, session=_SESSION).history"
      via: "session= kw 전달 (yfinance ≥0.2.60 API)"
      pattern: "Ticker\\(.*session=_SESSION"
    - from: "src/stocksig/io/market.py:fetch_ohlcv"
      to: "tenacity @retry"
      via: "wait_exponential + wait_random + stop_after_attempt(5) + retry_if_exception_type(YFRateLimitError)"
      pattern: "@retry"
---

<objective>
Wave 1: io 레이어 (외부 입력·시세 어댑터) 구현. 단일 미국 티커가 yfinance에서 실제 OHLCV로 수신될 수 있는 지점까지의 vertical 통로를 연다. **TDD GREEN phase** — Wave 0 RED stub을 GREEN으로 전환.

Purpose: 사용자가 작성한 `tickers.txt`/`.env`를 안전하게 읽고, curl_cffi Chrome impersonation + tenacity 재시도로 yfinance OHLCV를 받아오는 단일 어댑터를 제공. 후속 wave의 모든 계산은 이 DataFrame에 의존.

Output: GREEN 상태의 7개 unit test (INPUT-01/02/03/05, MKTD-01/02/03), 3개 import-able 모듈 (`config`, `io.input`, `io.market`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-single-ticker/01-CONTEXT.md
@.planning/phases/01-foundation-single-ticker/01-RESEARCH.md
@.planning/phases/01-foundation-single-ticker/01-SKELETON.md
@.planning/phases/01-foundation-single-ticker/01-01-SUMMARY.md
@CLAUDE.md
@tests/conftest.py
@tests/test_input.py
@tests/test_config.py
@tests/test_market.py

<interfaces>
<!-- 본 plan이 정의하는 공개 API. main.py 및 후속 wave가 이를 import한다. -->

src/stocksig/config.py:
  def load_env(env_path: str | Path | None = None) -> dict[str, str]
      """python-dotenv로 .env 로드. 필수 키(EDGAR_USER_AGENT_EMAIL, OPENDART_API_KEY)가
      비어있으면 한국어 메시지 logging.error + sys.exit(1).
      반환: 로드된 환경변수 dict."""

src/stocksig/io/input.py:
  def read_tickers(path: str | Path) -> list[str]
      """tickers.txt 읽기. 한 줄당 1 티커, 빈 줄 skip, '#' 시작 줄 주석 skip.
      파일 부재 또는 유효 티커 0개면 logging.error + sys.exit(1).
      반환: list[str] (대문자 변환 안 함 — '.KS'/'.KQ' 케이스 보존, suffix-agnostic)."""

src/stocksig/io/market.py:
  _SESSION: curl_cffi.requests.Session  # 모듈 레벨 (impersonate="chrome")

  @retry(...)
  def fetch_ohlcv(ticker: str) -> pd.DataFrame
      """yfinance.Ticker(ticker, session=_SESSION).history(
            start=(today - timedelta(days=4000)).isoformat(),
            end=today.isoformat(),
            auto_adjust=True)
         df.empty이면 ValueError raise (Phase 1 fail-fast, MKTD-04 deferred to Phase 2).
         로깅: logging.info(f"... | {ticker} | OHLCV {len(df)} 거래일 수신 완료")"""
</interfaces>

<patterns_from_research>
<!-- RESEARCH.md Pattern 2에서 직접 인용 — 그대로 구현 -->
Pattern 2 (market.py 핵심):
  from curl_cffi import requests as curl_requests
  import yfinance as yf
  from yfinance.exceptions import YFRateLimitError
  from tenacity import retry, wait_exponential, wait_random, stop_after_attempt, retry_if_exception_type
  from datetime import date, timedelta

  _SESSION = curl_requests.Session(impersonate="chrome")

  @retry(
      wait=wait_exponential(multiplier=1, min=2, max=30) + wait_random(0, 1),
      stop=stop_after_attempt(5),
      retry=retry_if_exception_type(YFRateLimitError),
      reraise=True,
  )
  def fetch_ohlcv(ticker: str) -> pd.DataFrame:
      ...

D-05 로깅 포맷: [LEVEL] YYYY-MM-DD HH:MM:SS | TICKER | 메시지
  - input.py: TICKER 자리에 `tickers.txt` 또는 `-` 사용
  - market.py: TICKER 자리에 ticker
  - config.py: TICKER 자리에 `config` 또는 `-`
</patterns_from_research>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: config.py + io/input.py 구현 (INPUT-01/02/03/05 GREEN)</name>
  <files>src/stocksig/__init__.py, src/stocksig/config.py, src/stocksig/io/__init__.py, src/stocksig/io/input.py</files>
  <behavior>
    - `load_env()` with valid .env (둘 다 채워짐) → returns dict with both keys, no exit
    - `load_env()` with empty `EDGAR_USER_AGENT_EMAIL` → `SystemExit(code != 0)` + 한국어 stderr 메시지 (예: `[ERROR] ... | config | .env의 EDGAR_USER_AGENT_EMAIL 값이 비어있습니다.`)
    - `load_env()` with empty `OPENDART_API_KEY` → 동일 한국어 fail-fast (INPUT-05)
    - `read_tickers(tmp_tickers_file("AAPL"))` → `["AAPL"]` (INPUT-01)
    - `read_tickers(tmp_tickers_file("005930.KS\n035720.KQ\nAAPL"))` → `["005930.KS","035720.KQ","AAPL"]` (INPUT-02: suffix 보존, 대소문자 변환 없음, 순서 유지)
    - `read_tickers(tmp_tickers_file(""))` → `SystemExit(code != 0)` + 한국어 stderr (예: `tickers.txt 파일이 비어있습니다.`) (INPUT-03)
    - `read_tickers(Path("/nonexistent.txt"))` → `SystemExit(code != 0)` + 한국어 stderr (`tickers.txt 파일을 찾을 수 없습니다: ...`)
    - `read_tickers(tmp_tickers_file("# 주석\n\nAAPL\n  TSLA  \n"))` → `["AAPL","TSLA"]` (빈 줄/주석 skip + strip)
  </behavior>
  <action>
    `src/stocksig/__init__.py`와 `src/stocksig/io/__init__.py`를 빈 파일로 Write (패키지 마커).

    `src/stocksig/config.py` Write — D-05 한국어 로깅 포맷 사용. `load_env(env_path=None)` 함수:
    - `from dotenv import load_dotenv`
    - 인자 `env_path`가 None이면 cwd의 `.env`. 파일 부재 시 `load_dotenv()`는 silent — 본 함수는 `os.environ.get(...)`으로 직접 검증.
    - 두 필수 키 (`EDGAR_USER_AGENT_EMAIL`, `OPENDART_API_KEY`) 각각이 None or "" or whitespace-only이면 `logging.error(f"... | config | .env의 {key} 값이 비어있습니다.")` + `sys.exit(1)`.
    - 모두 valid면 `{"EDGAR_USER_AGENT_EMAIL": ..., "OPENDART_API_KEY": ...}` 반환.
    - 한국어 메시지는 D-05 포맷 그대로. stderr 출력은 `logging`의 기본 StreamHandler가 stderr이므로 자동.

    `src/stocksig/io/input.py` Write. `read_tickers(path)` 함수:
    - `Path(path).read_text(encoding='utf-8')` (FileNotFoundError catch → 한국어 메시지 + `sys.exit(1)`)
    - 줄 단위 split, 각 줄 `strip()`, 빈 줄 skip, `#` 시작 줄 skip
    - 결과 list가 빈이면 한국어 메시지 + `sys.exit(1)` ("tickers.txt 파일이 비어있습니다.")
    - 그렇지 않으면 list 반환 (대소문자 변환 안 함 — INPUT-02 suffix 보존 보장)

    Tests 활성화: `tests/test_input.py`와 `tests/test_config.py`의 4개 함수에서 `@pytest.mark.xfail` 데코레이터 제거 + Wave 0이 남긴 슬롯에 실제 assertion 작성 (behavior 블록의 8개 케이스 모두 커버). 한국어 메시지 검증은 `caplog` 또는 `capsys` fixture로 stderr 캡처 후 부분 문자열 매치.

    Decision references: D-01 (`src/stocksig/io/input.py` 위치), D-05 (한국어 로깅 포맷), INPUT-01/02/03 직접 구현, INPUT-05 fail-fast.
  </action>
  <verify>
    <automated>uv run pytest tests/test_input.py tests/test_config.py -x -q</automated>
  </verify>
  <done>
    `tests/test_input.py`의 3개 함수 + `tests/test_config.py`의 1개 함수 모두 GREEN (xfail 데코레이터 제거됨). `uv run python -c "from stocksig.io.input import read_tickers; from stocksig.config import load_env; print('ok')"` 출력 ok.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: io/market.py 구현 — yfinance + curl_cffi + tenacity (MKTD-01/02/03 GREEN)</name>
  <files>src/stocksig/io/market.py</files>
  <behavior>
    - 모듈 import 시 `_SESSION`이 `curl_cffi.requests.Session` 인스턴스로 생성됨 + `impersonate="chrome"` 확인 가능 (MKTD-02)
    - `fetch_ohlcv("AAPL")` 호출 시 (yfinance.Ticker mock) — `Ticker(ticker="AAPL", session=_SESSION)`이 호출되었음 (MKTD-02)
    - `Ticker(...).history` 호출 인자가 `start=(today-4000d).isoformat()`, `end=today.isoformat()`, `auto_adjust=True` (MKTD-01)
    - `Ticker(...).history`가 `YFRateLimitError` raise를 5번 반복하면 tenacity가 5회 시도 후 `YFRateLimitError` 그대로 재raise (`reraise=True`) (MKTD-03)
    - `Ticker(...).history`가 처음 2번 `YFRateLimitError`, 3번째에 valid DataFrame 반환 → `fetch_ohlcv`가 그 DataFrame 반환 (MKTD-03 성공 경로)
    - `Ticker(...).history`가 빈 DataFrame 반환 → `ValueError` raise (Pitfall B, Phase 1 fail-fast)
    - 성공 시 `logging.info(f"... | AAPL | OHLCV {N} 거래일 수신 완료")` 호출됨 (Pitfall F)
  </behavior>
  <action>
    `src/stocksig/io/market.py` Write. RESEARCH.md Pattern 2 (`<patterns_from_research>` 블록)를 그대로 구현. 추가 사항:
    - `from datetime import date, timedelta` + `today = date.today()` 함수 내부에서 호출 (모듈 import 시점이 아님 — 테스트가 freezegun 없이 today를 검증 가능하도록 `date.today()` 직접 patch 가능한 구조).
    - 성공 후 `logger.info(f"%(asctime)s | {ticker} | OHLCV {len(df)} 거래일 수신 완료")` — D-05 포맷.

    yfinance mock 전략 (`tests/test_market.py`에서 사용):
    - `mocker.patch('stocksig.io.market.yf.Ticker')` 로 Ticker 클래스 자체를 mock
    - `mock_ticker = mocker.patch('stocksig.io.market.yf.Ticker').return_value`
    - `mock_ticker.history.return_value = mock_ohlcv_df` (conftest fixture)
    - `test_uses_curl_cffi_session`: 모듈 attribute 직접 검증 — `from stocksig.io import market; from curl_cffi.requests import Session; assert isinstance(market._SESSION, Session)`. Chrome impersonation 검증은 `Session`이 `impersonate="chrome"`로 생성됐는지 구현 코드 read로 cross-check (curl_cffi가 attribute로 노출하지 않으면 source 검증으로 대체) — 또는 test에서 `market._SESSION.impersonate == "chrome"` (curl_cffi가 노출하는 attribute) 우선 시도, 없으면 mocker spy on `curl_requests.Session` at module import time. **권장:** `tests/test_market.py`에서 `mocker.patch.object(market, '_SESSION')` 없이 모듈을 import한 뒤 `assert market._SESSION is not None`만 검증하고, Chrome impersonation은 fmt 검증 (예: `assert hasattr(market._SESSION, 'impersonate') or 'Chrome' in repr(market._SESSION)`)으로 약하게. 엄격 검증은 integration test에서.
    - `test_retries_on_rate_limit`: `mock_ticker.history.side_effect = [YFRateLimitError(...)] * 4 + [mock_ohlcv_df]` → `fetch_ohlcv("AAPL")` 호출, history가 5번 호출됐는지 (`assert mock_ticker.history.call_count == 5`), 반환값이 DataFrame인지. **테스트 속도 주의:** tenacity wait이 min=2~30s이므로 실제 5회 시도하면 ~60s. `tests/test_market.py`에서 `mocker.patch('stocksig.io.market.wait_exponential')` 또는 `monkeypatch.setattr(market.fetch_ohlcv.retry, 'wait', lambda *a, **kw: 0)` 같은 방식으로 wait을 0으로 override (tenacity의 `retry.wait = ...` 또는 `@retry(...)` 데코레이터를 test에서 다시 적용). 가장 안전한 패턴: `fetch_ohlcv.retry.wait = wait_none()` (tenacity 9.x API).

    Tests 활성화: `tests/test_market.py`의 3개 함수에서 `@pytest.mark.xfail` 제거 + behavior 블록 7케이스 assertion 작성. tenacity wait override를 fixture로 분리 (`@pytest.fixture(autouse=True)` for this file).

    Decision references: D-06 (date window today±4000d, tenacity 정책, auto_adjust=True), MKTD-01/02/03 직접 구현. Pitfall B (빈 DataFrame fail-fast), Pitfall F (거래일 수 로깅).
  </action>
  <verify>
    <automated>uv run pytest tests/test_market.py -x -q --timeout=10</automated>
  </verify>
  <done>
    `tests/test_market.py`의 3개 함수 모두 GREEN. tenacity wait override 덕에 전체 실행 < 10초. `uv run pytest -x -q` 전체에서 7개 io 레이어 테스트 GREEN, 나머지 12개는 여전히 xfail (Wave 2~4 대기).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→Python (`tickers.txt`, `.env`) | 사용자 입력 — strip + 빈 줄 skip + 주석 skip + UTF-8 강제 |
| local→Yahoo Finance (외부 HTTPS) | yfinance + curl_cffi (Chrome 임퍼소네이션, 차단 회피 자체가 보안이 아님). API 키 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-V1 | Input Validation (ASVS V5) | `read_tickers` parse | mitigate | strip + 빈 줄 skip + 주석 skip. 형식 검증(잘못된 티커)은 INPUT-04 (Phase 2). Phase 1은 단일 미국 티커 가정이므로 단순 파서로 충분 |
| T-01-DoS | Denial of Service | yfinance rate-limit polling | mitigate | tenacity `stop_after_attempt(5)` — 무한 재시도 방지. `wait_exponential(max=30)` 캡 |
| T-01-INFO | Information Disclosure | 로깅 메시지에 .env 값 노출 | mitigate | `config.load_env`는 로깅 시 키 이름만 출력 (`.env의 EDGAR_USER_AGENT_EMAIL 값이 비어있습니다`). 값 자체는 절대 로깅하지 않음 |
| T-01-NET | Spoofing | yfinance 응답 위조 | accept | yfinance는 HTTPS, Yahoo가 신뢰 루트. 추가 검증 비현실적이고 Phase 1 범위 밖 |
</threat_model>

<verification>
- `uv run pytest tests/test_input.py tests/test_config.py tests/test_market.py -x -q` 모두 GREEN (7/7)
- `uv run pytest -x -q` 전체 19개 중 7 GREEN + 12 xfail
- `uv run python -c "from stocksig.io.market import fetch_ohlcv, _SESSION; print(type(_SESSION).__module__)"` 출력에 `curl_cffi` 포함
- 한국어 에러 메시지 수동 확인: 빈 `tickers.txt` 만들고 `uv run python -c "from stocksig.io.input import read_tickers; read_tickers('tickers.txt')"` → stderr에 한국어 메시지
</verification>

<success_criteria>
- INPUT-01/02/03/05, MKTD-01/02/03 모두 자동 검증 GREEN
- `_SESSION`이 모듈 레벨 단일 인스턴스 (재사용성 — 후속 wave에서 별도 session 생성 금지)
- tenacity 데코레이터가 함수 attribute로 노출 (`fetch_ohlcv.retry` accessible) — test override 가능
</success_criteria>

<output>
Create `.planning/phases/01-foundation-single-ticker/01-02-SUMMARY.md` when done. SUMMARY에 io 레이어의 공개 시그니처 3개 (load_env, read_tickers, fetch_ohlcv) + 한국어 에러 메시지 문자열 목록 (후속 Phase 2 INPUT-04에서 메시지 톤 일관성 유지용)를 기록.
</output>
