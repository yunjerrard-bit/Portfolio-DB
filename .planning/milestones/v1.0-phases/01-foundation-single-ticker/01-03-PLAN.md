---
phase: 01-foundation-single-ticker
plan: 03
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - src/stocksig/compute/__init__.py
  - src/stocksig/compute/ema.py
  - src/stocksig/compute/stats.py
  - src/stocksig/compute/indicators.py
  - src/stocksig/compute/color_rules.py
  - tests/fixtures/rsi_golden.json
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05
  - COMP-06
  - TECH-01
  - TECH-02
  - TECH-04
  - TECH-05
  - COLOR-01
  - COLOR-02
  - COLOR-03
  - COLOR-04
  - COLOR-05
  - COLOR-06
  - COLOR-07
user_setup: []
tags: [pandas, ewm, expanding, stochastic, rsi, color, pure-functions]

must_haves:
  truths:
    - "add_ema_columns(df)는 종/고/저 × EMA11/22/96/192 = 12개 EMA + 12개 차이 + 12개 일변동 컬럼을 추가한다 (pandas.ewm span=N adjust=False)"
    - "add_expanding_stats(df, cols)는 각 col별 _median, _std 컬럼을 추가한다 (look-ahead-free)"
    - "cumulative_scalars(df, cols)는 각 col의 전체 누적 median/std 스칼라 dict를 반환한다"
    - "stoch_slow(df)는 표준 (14,3,3) 파라미터로 Slow %K, %D를 계산한다 (rolling min_periods 명시)"
    - "rsi_wilder(df)는 ewm(alpha=1/14, adjust=False) Wilder smoothing으로 RSI(14)를 계산한다"
    - "decide_sigma_bucket(value, median, std)는 D-02에 따라 NaN/0 입력 시 SigmaBucket.DEFAULT 반환, 그 외 ±1σ/±2σ 4단계로 분류한다"
    - "decide_stoch_bucket / decide_rsi_bucket는 D-04 임계값(20/80, 30/70)으로 TechBucket 3단 분류한다"
    - "색 상수 (GREEN_800/900/100, RED_800/900/100, DEFAULT_BLACK)는 모듈 레벨에서 import 가능하다 (D-04 single source of truth)"
  artifacts:
    - path: "src/stocksig/compute/__init__.py"
      provides: "compute 서브패키지 마커"
    - path: "src/stocksig/compute/ema.py"
      provides: "add_ema_columns(df) + EMA_PERIODS=[11,22,96,192] 상수"
      min_lines: 30
    - path: "src/stocksig/compute/stats.py"
      provides: "add_expanding_stats(df, cols) + cumulative_scalars(df, cols)"
      min_lines: 25
    - path: "src/stocksig/compute/indicators.py"
      provides: "stoch_slow(df, 14, 3, 3) + rsi_wilder(df, 14)"
      min_lines: 40
    - path: "src/stocksig/compute/color_rules.py"
      provides: "SigmaBucket Enum, TechBucket Enum, decide_sigma_bucket, decide_stoch_bucket, decide_rsi_bucket, 7개 색 hex 상수"
      min_lines: 60
  key_links:
    - from: "src/stocksig/compute/ema.py:EMA_PERIODS"
      to: "[11, 22, 96, 192]"
      via: "모듈 상수"
      pattern: "EMA_PERIODS\\s*=\\s*\\[11"
    - from: "src/stocksig/compute/color_rules.py 색 상수"
      to: "D-04 Material Design hex"
      via: "GREEN_800='#2E7D32', GREEN_900='#1B5E20', GREEN_100='#C8E6C9', RED_800='#C62828', RED_900='#B71C1C', RED_100='#FFCDD2'"
      pattern: "GREEN_800\\s*=\\s*[\"']#2E7D32[\"']"
    - from: "decide_sigma_bucket"
      to: "SigmaBucket.DEFAULT (NaN/0 분기)"
      via: "math.isnan(std) or std==0"
      pattern: "math\\.isnan|std\\s*==\\s*0"
---

<objective>
Wave 2: compute 레이어 (순수 계산) 구현. EMA·차이·일변동·expanding median/std·누적 스칼라·Stoch Slow(14,3,3)·RSI(14 Wilder)·색 결정 로직 — 모두 외부 의존성 없는 pure function. **TDD 본격 구간** — golden 입력에 의한 정확성 검증이 Phase 1의 시간 위험 1순위 (RESEARCH.md: "Phase 1의 시간 위험은 외부 API가 아니라 계산 정확성 검증").

Purpose: Core Value(중앙값±σ 색 신호)의 수치 정확성을 단위 테스트로 잠근다. 본 wave가 GREEN이면 색 신호가 잘못 발화될 가능성은 거의 0.

Output: GREEN 상태의 9개 unit test (test_ema 3, test_stats 3, test_indicators 2, test_color_rules 2 — 단 test_indicators 2개와 test_color_rules 2개가 RSI golden backfill을 포함), 4개 import-able 모듈.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-single-ticker/01-CONTEXT.md
@.planning/phases/01-foundation-single-ticker/01-RESEARCH.md
@.planning/phases/01-foundation-single-ticker/01-SKELETON.md
@.planning/phases/01-foundation-single-ticker/01-01-SUMMARY.md
@CLAUDE.md
@tests/test_ema.py
@tests/test_stats.py
@tests/test_indicators.py
@tests/test_color_rules.py
@tests/fixtures/rsi_golden.json

<interfaces>
src/stocksig/compute/ema.py:
  EMA_PERIODS: list[int] = [11, 22, 96, 192]
  def add_ema_columns(df: pd.DataFrame) -> pd.DataFrame
      """입력 df는 OHLCV 컬럼 (Close, High, Low) 보유.
      반환: df의 copy + 36 신규 컬럼 (12 EMA + 12 DIFF + 12 dailychg).
      컬럼 명명: EMA_{price}_{N}, DIFF_{price}_{N}, EMA_{price}_{N}_dailychg.
      price ∈ {Close, High, Low}, N ∈ {11, 22, 96, 192}."""

src/stocksig/compute/stats.py:
  def add_expanding_stats(df: pd.DataFrame, data_cols: list[str]) -> pd.DataFrame
      """각 col별 {col}_median, {col}_std 신규 컬럼 추가 (expanding window).
      pandas .expanding().median() / .std() 사용. ddof=1 (default)."""

  def cumulative_scalars(df: pd.DataFrame, data_cols: list[str]) -> dict[str, dict[str, float]]
      """{col: {'median': float, 'std': float}} — 3·4행용."""

src/stocksig/compute/indicators.py:
  def stoch_slow(df: pd.DataFrame, k_period: int = 14, slowing: int = 3,
                 d_period: int = 3) -> pd.DataFrame
      """반환 df는 컬럼 Stoch_%K, Stoch_%D (index=df.index)."""

  def rsi_wilder(df: pd.DataFrame, period: int = 14) -> pd.Series
      """반환 Series name='RSI'. Wilder smoothing == ewm(alpha=1/period, adjust=False)."""

src/stocksig/compute/color_rules.py:
  # 색 상수 (D-04 hex 그대로)
  GREEN_800 = "#2E7D32"; GREEN_900 = "#1B5E20"; GREEN_100 = "#C8E6C9"
  RED_800   = "#C62828"; RED_900   = "#B71C1C"; RED_100   = "#FFCDD2"
  DEFAULT_BLACK = "#000000"

  class SigmaBucket(Enum):
      DEFAULT, SOFT_GREEN, HARD_GREEN, SOFT_RED, HARD_RED

  class TechBucket(Enum):
      DEFAULT, SOFT_GREEN, SOFT_RED

  def decide_sigma_bucket(value, median, std) -> SigmaBucket
  def decide_stoch_bucket(value) -> TechBucket
  def decide_rsi_bucket(value) -> TechBucket
</interfaces>

<patterns_from_research>
<!-- Pattern 3 (EMA), 4 (stats), 5 (Stoch), 6 (RSI), 7 (color) RESEARCH.md 그대로 -->
- Pattern 3: `out[price_col].ewm(span=n, adjust=False).mean()` 12회 루프
- Pattern 4: `.expanding().median()` / `.std()`, ddof=1 default
- Pattern 5: `Low.rolling(14, min_periods=14).min()`, `High.rolling(14, min_periods=14).max()`, denom 0→NA, `fast_k.rolling(3, min_periods=3).mean()` for slow %K, `slow_k.rolling(3, min_periods=3).mean()` for %D
- Pattern 6: `delta = Close.diff(); gain = delta.clip(lower=0); loss = -delta.clip(upper=0); avg_gain = gain.ewm(alpha=1/14, adjust=False, min_periods=14).mean(); ... RSI = 100 - 100/(1+RS)`
- Pattern 7: `decide_sigma_bucket` — math.isnan / std==0 → DEFAULT. deviation < -2σ → HARD_GREEN, < -σ → SOFT_GREEN, > 2σ → HARD_RED, > σ → SOFT_RED, else DEFAULT.

Pitfall A: `decide_sigma_bucket`에 NaN/0 명시 분기 (실수로 누락하면 우연히 동작하지만 의도 미반영).
Pitfall C: EMA192 첫 192행은 seed bias로 expanding std가 좁음 — D-02가 자연 흡수 (DEFAULT). golden test가 첫 ~200행 대부분 DEFAULT 임을 확인 권장.

Golden 입력:
- EMA(span=3, adjust=False, [1,2,3,4,5]) → [1.0, 1.5, 2.25, 3.125, 4.0625]
- Stoch sanity: close=high → %K≈100, close=low → %K≈0, close=100/high=110/low=90 → %K=50
- RSI Wilder(14): tests/fixtures/rsi_golden.json — closes 30개. expected_rsi_at_index_14는 본 task가 backfill.
</patterns_from_research>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compute/ema.py + compute/stats.py 구현 (COMP-01~06 GREEN)</name>
  <files>src/stocksig/compute/__init__.py, src/stocksig/compute/ema.py, src/stocksig/compute/stats.py</files>
  <behavior>
    EMA (Pattern 3):
    - 입력 df (5행, Close=[1,2,3,4,5], High=High, Low=Low)에 `add_ema_columns(df)` 적용
    - 결과에 12개 EMA, 12개 DIFF, 12개 dailychg 컬럼 존재 (총 36 신규 컬럼)
    - `EMA_Close_3`(span=3 — 본 테스트 한정 override 또는 별도 helper로) 결과가 [1.0, 1.5, 2.25, 3.125, 4.0625] (tolerance 1e-9) — 또는 EMA_PERIODS=[11,22,96,192]는 그대로 두고 단순한 5행 입력에서는 EMA11도 같은 공식으로 계산되는지만 검증
    - `DIFF_Close_11 = Close - EMA_Close_11` (해당 비교)
    - `EMA_Close_11_dailychg = EMA_Close_11.diff()` (해당 비교)

    Stats (Pattern 4):
    - 입력 df (Close=[10, 20, 30, 40, 50])에 `add_expanding_stats(df, ['Close'])` 적용
    - `Close_median[0] = 10, Close_median[1] = 15, Close_median[2] = 20, Close_median[3] = 25, Close_median[4] = 30`
    - `Close_std[0] = NaN` (표본 1, ddof=1), `Close_std[1] = sqrt(50) ≈ 7.07107`, `Close_std[4] = std([10..50], ddof=1) ≈ 15.811`
    - `cumulative_scalars(df, ['Close'])` → `{'Close': {'median': 30.0, 'std': ≈15.811}}`
    - Volume 컬럼 입력 시 동일 처리 (COMP-06)
  </behavior>
  <action>
    `src/stocksig/compute/__init__.py` 빈 파일 Write.

    `src/stocksig/compute/ema.py` Write — RESEARCH.md Pattern 3 그대로:
    - `EMA_PERIODS = [11, 22, 96, 192]` 모듈 상수
    - `add_ema_columns(df)`: copy 후 `for price_col in ["Close", "High", "Low"]: for n in EMA_PERIODS:` 이중 루프로 36 컬럼 추가
    - 모든 ewm 호출은 `adjust=False` 필수 (Pitfall: adjust=True 금지)
    - DIFF는 `df[price_col] - ema`, dailychg는 `ema.diff()`

    Test 골든 처리: `test_ema_matches_tradingview_formula`는 `EMA_PERIODS`를 monkeypatch로 `[3]`로 일시 변경하거나, 헬퍼 `_compute_ema_series(series, span)`를 별도 export하여 테스트가 `[1,2,3,4,5]` + span=3으로 직접 호출. **권장:** ema.py에 `def compute_ema(series: pd.Series, span: int) -> pd.Series: return series.ewm(span=span, adjust=False).mean()` helper export → 단위 테스트가 직접 호출.

    `src/stocksig/compute/stats.py` Write — Pattern 4 그대로:
    - `add_expanding_stats(df, data_cols)`: 각 col별 `df[col].expanding().median()` 및 `.std()` 컬럼 추가
    - `cumulative_scalars(df, data_cols)`: dict comprehension `{col: {'median': df[col].median(), 'std': df[col].std()}}`

    Tests 활성화: `tests/test_ema.py` 3 함수 + `tests/test_stats.py` 3 함수의 `@pytest.mark.xfail` 제거 + behavior 블록 assertion 작성.

    Decision references: COMP-01~06 직접 구현. Pitfall C는 코드에 영향 없음 (color_rules가 흡수).
  </action>
  <verify>
    <automated>uv run pytest tests/test_ema.py tests/test_stats.py -x -q</automated>
  </verify>
  <done>
    6/6 GREEN. `uv run python -c "from stocksig.compute.ema import add_ema_columns, EMA_PERIODS; from stocksig.compute.stats import add_expanding_stats, cumulative_scalars; print(EMA_PERIODS)"` → `[11, 22, 96, 192]`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute/indicators.py 구현 + RSI golden backfill (TECH-01/02 GREEN)</name>
  <files>src/stocksig/compute/indicators.py, tests/fixtures/rsi_golden.json</files>
  <behavior>
    Stoch Slow (Pattern 5):
    - close=high=low=100 일정 → 분모 0 → %K 모두 NaN (denom 0→NA replace)
    - close 시퀀스가 high에 가까움 → Slow %K[-1] ≈ 100 (tolerance 5)
    - close 시퀀스가 low에 가까움 → Slow %K[-1] ≈ 0 (tolerance 5)
    - close=100, high=110, low=90 일정 (30일) → 14일 후 %K = 50.0 (정확)
    - 처음 13행 (rolling(14, min_periods=14) 영향)은 Stoch_%K NaN. 처음 ~15행 Stoch_%D NaN.

    RSI Wilder (Pattern 6):
    - `rsi_golden` fixture의 closes 30개 → 본 task가 구현 후 RSI[14] 또는 RSI[29] 실제 값 계산 → fixture에 backfill (`expected_rsi_at_index_14`). 이후 test는 `assert abs(rsi[14] - fixture['expected_rsi_at_index_14']) < fixture['tolerance']` (tolerance=0.5).
    - 처음 13행 (min_periods=14) NaN. 14번째 행부터 valid.
    - 가격이 monotonic 상승 시 RSI → 100 근접 (tolerance 5)
    - 가격이 monotonic 하락 시 RSI → 0 근접 (tolerance 5)

    Wilder 동등성 sanity: `rsi_wilder(df, 14)`와 `rsi_wilder` 직접 구현 + ewm(alpha=1/14) 결과가 동일 (수학적 동등성, A4 ASSUMED 검증).
  </behavior>
  <action>
    `src/stocksig/compute/indicators.py` Write — RESEARCH.md Pattern 5 + 6 그대로:

    Stoch:
    ```python
    def stoch_slow(df, k_period=14, slowing=3, d_period=3):
        low_min = df["Low"].rolling(window=k_period, min_periods=k_period).min()
        high_max = df["High"].rolling(window=k_period, min_periods=k_period).max()
        denom = (high_max - low_min).replace(0, pd.NA)
        fast_k = 100 * (df["Close"] - low_min) / denom
        slow_k = fast_k.rolling(window=slowing, min_periods=slowing).mean()
        slow_d = slow_k.rolling(window=d_period, min_periods=d_period).mean()
        return pd.DataFrame({"Stoch_%K": slow_k, "Stoch_%D": slow_d}, index=df.index)
    ```

    RSI:
    ```python
    def rsi_wilder(df, period=14):
        delta = df["Close"].diff()
        gain = delta.clip(lower=0)
        loss = -delta.clip(upper=0)
        avg_gain = gain.ewm(alpha=1/period, adjust=False, min_periods=period).mean()
        avg_loss = loss.ewm(alpha=1/period, adjust=False, min_periods=period).mean()
        rs = avg_gain / avg_loss.replace(0, pd.NA)
        rsi = 100 - (100 / (1 + rs))
        return rsi.rename("RSI")
    ```

    Wilder 동등성: `pandas.ewm(alpha=1/N, adjust=False)`가 Wilder smoothing과 수학적으로 동등 (RESEARCH.md A4). 별도 구현 비교 sanity check를 단위 테스트에 추가 가능하나 필수 아님.

    RSI golden backfill:
    1. 구현 완료 후 별도 스크립트 (`uv run python -c "..."`)로 fixture closes 입력 → `rsi_wilder` 계산 → `rsi[14]` 값 추출.
    2. `tests/fixtures/rsi_golden.json`의 `expected_rsi_at_index_14`를 그 값으로 backfill.
    3. **검증:** TradingView 또는 Wilder 1978 표준 예제와 0.5 tolerance 내 일치하는지 cross-check. 일치 안 하면 구현 버그 — 일치 시까지 수정. 일치 후 fixture에 `"cross_checked_source": "TradingView screenshot" 또는 "Wilder 1978 p.X"` note 추가.

    Tests 활성화: `tests/test_indicators.py` 2 함수의 xfail 제거 + assertion. `test_stoch_slow_known_input`은 위 4 케이스 + NaN 초기 행 검증. `test_rsi_wilder_known_input`은 fixture 사용.

    Decision references: TECH-01/02 직접 구현, pandas-ta 미사용 (CLAUDE.md "What NOT to Use"). A4 ASSUMED 검증 (Wilder == ewm(alpha=1/N)).
  </action>
  <verify>
    <automated>uv run pytest tests/test_indicators.py -x -q</automated>
  </verify>
  <done>
    2/2 GREEN. `tests/fixtures/rsi_golden.json`의 `expected_rsi_at_index_14`가 null이 아닌 float 값으로 backfill됨. cross-check source가 fixture에 기록됨.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: compute/color_rules.py 구현 (COLOR-01~07 + TECH-04/05 GREEN, D-02/D-04 잠금)</name>
  <files>src/stocksig/compute/color_rules.py</files>
  <behavior>
    sigma buckets (decide_sigma_bucket(value, median, std)):
    - value=None or median=None or std=None → DEFAULT
    - any of (value, median, std) is NaN (float) → DEFAULT
    - std == 0 → DEFAULT  (D-02)
    - value=100, median=100, std=10 (|dev|=0 ≤ 1σ) → DEFAULT (COLOR-06)
    - value=110.01, median=100, std=10 (1σ < dev=10.01 ≤ 2σ) → SOFT_RED (COLOR-03)
    - value=119.99, median=100, std=10 (1σ < dev=19.99 ≤ 2σ) → SOFT_RED (boundary)
    - value=120.01, median=100, std=10 (dev=20.01 > 2σ) → HARD_RED (COLOR-05)
    - value=89.99, median=100, std=10 (-2σ ≤ dev=-10.01 < -1σ) → SOFT_GREEN (COLOR-02)
    - value=79.99, median=100, std=10 (dev=-20.01 < -2σ) → HARD_GREEN (COLOR-04)

    tech buckets:
    - decide_stoch_bucket(NaN) → DEFAULT
    - decide_stoch_bucket(20) → SOFT_GREEN (≤20)
    - decide_stoch_bucket(50) → DEFAULT
    - decide_stoch_bucket(80) → SOFT_RED (≥80)
    - decide_rsi_bucket(NaN) → DEFAULT
    - decide_rsi_bucket(30) → SOFT_GREEN (≤30)
    - decide_rsi_bucket(70) → SOFT_RED (≥70)

    색 상수 노출:
    - GREEN_800 == "#2E7D32"
    - GREEN_900 == "#1B5E20"
    - GREEN_100 == "#C8E6C9"
    - RED_800   == "#C62828"
    - RED_900   == "#B71C1C"
    - RED_100   == "#FFCDD2"
    - DEFAULT_BLACK == "#000000"
  </behavior>
  <action>
    `src/stocksig/compute/color_rules.py` Write — RESEARCH.md Pattern 7 그대로. 명시적으로:
    - 모듈 최상단: 7개 색 hex 상수 (D-04 정책 single source of truth, Phase 4 튜닝 지점)
    - `SigmaBucket(Enum)` 5 멤버
    - `TechBucket(Enum)` 3 멤버
    - `decide_sigma_bucket(value, median, std)`: 명시적 NaN 분기 (`isinstance(x, float) and math.isnan(x)`) + std==0 분기 (D-02 Pitfall A) → DEFAULT. 그 외 deviation 비교. 경계 처리: `<` vs `<=` 정확히 (behavior 블록의 boundary 케이스에서 119.99 SOFT_RED, 120.01 HARD_RED). 코드 패턴:
      ```python
      if deviation < -2 * std: return SigmaBucket.HARD_GREEN
      if deviation < -std:     return SigmaBucket.SOFT_GREEN
      if deviation > 2 * std:  return SigmaBucket.HARD_RED
      if deviation > std:      return SigmaBucket.SOFT_RED
      return SigmaBucket.DEFAULT
      ```
      (boundary `dev = ±1σ` 정확히는 DEFAULT — COLOR-06 |값-중앙값|≤1σ 정확히 매치)
    - `decide_stoch_bucket(value)`: NaN → DEFAULT, ≤20 → SOFT_GREEN, ≥80 → SOFT_RED, else DEFAULT
    - `decide_rsi_bucket(value)`: NaN → DEFAULT, ≤30 → SOFT_GREEN, ≥70 → SOFT_RED, else DEFAULT

    Tests 활성화: `tests/test_color_rules.py`의 2 함수 xfail 제거 + behavior 블록의 모든 케이스 (sigma 9 + tech 7 + 색 상수 검증 7) assertion.

    Decision references: **D-02** (NaN/0 → DEFAULT 명시 분기), **D-04** (Material Design hex 7개 잠금), COLOR-01~07 + TECH-04/05 직접 구현. Pitfall A (NaN 분기 명시).
  </action>
  <verify>
    <automated>uv run pytest tests/test_color_rules.py -x -q</automated>
  </verify>
  <done>
    2/2 GREEN. `uv run python -c "from stocksig.compute.color_rules import GREEN_800, RED_900, GREEN_100, RED_100, SigmaBucket, decide_sigma_bucket; print(GREEN_800, RED_900, decide_sigma_bucket(120.01, 100, 10).name)"` → `#2E7D32 #B71C1C HARD_RED`.
    전체 `uv run pytest -x -q` → 7 + 6 + 2 + 2 = 17 GREEN, 2 (smoke) xfail.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | compute 레이어는 외부 입력 없는 pure function. DataFrame in/out. ASVS V5는 io 레이어가 책임 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-CALC | Tampering (계산 오류 = 잘못된 색 신호 = 잘못된 매매 판단) | EMA, Stoch, RSI, sigma bucket | mitigate | Golden test (EMA [1..5]→[1.0,1.5,2.25,3.125,4.0625], Stoch sanity 4케이스, RSI fixture cross-checked, sigma 9 boundary 케이스). Wilder == ewm(alpha=1/N) 동등성은 수학적 사실 + golden 일치로 이중 잠금 |
| T-01-NAN | Tampering | expanding std==0 또는 NaN 행 색 잘못 발화 | mitigate | D-02: `decide_sigma_bucket`이 NaN/0 명시 분기 → DEFAULT. Pitfall A 직접 대응. test_sigma_buckets에 NaN/0 케이스 포함 |
</threat_model>

<verification>
- `uv run pytest tests/test_ema.py tests/test_stats.py tests/test_indicators.py tests/test_color_rules.py -x -q` 10/10 GREEN
- `tests/fixtures/rsi_golden.json`의 expected 값이 backfill됨 + cross-check source 기록
- 색 상수 7개 hex가 D-04 표와 정확히 일치
- `uv run python -c "from stocksig.compute.color_rules import decide_sigma_bucket; assert decide_sigma_bucket(float('nan'), 100, 10).name == 'DEFAULT'; assert decide_sigma_bucket(100, 100, 0).name == 'DEFAULT'"` 실행 성공 (D-02 검증)
</verification>

<success_criteria>
- 계산 정확성 위험 (RESEARCH.md "Phase 1의 시간 위험 1순위")이 단위 테스트로 잠김
- D-04 색 hex가 import 가능한 모듈 상수 (Phase 4 튜닝 단일 지점 확보)
- D-02 (NaN/std=0 → DEFAULT)가 명시 분기로 잠김 (Pitfall A 회피)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-single-ticker/01-03-SUMMARY.md` when done. SUMMARY에 compute 레이어 공개 시그니처 + RSI golden backfill 값 + cross-check source를 기록.
</output>
