---
phase: 01-foundation-single-ticker
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - src/stocksig/output/__init__.py
  - src/stocksig/output/writer.py
  - src/stocksig/output/sheet_per_ticker.py
  - .planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md
autonomous: true
requirements:
  - SHEET-01
  - SHEET-02
  - SHEET-03
  - SHEET-04
  - SHEET-05
  - SHEET-06
  - SHEET-07
  - SHEET-08
  - TECH-03
  - TECH-06
  - OUT-01
  - OUT-02
  - OUT-03
user_setup: []
tags: [xlsxwriter, output, format-cache, sheet-layout]

must_haves:
  truths:
    - "make_workbook(path)은 XlsxWriter Workbook + Format 캐시 dict를 반환한다"
    - "Format 캐시는 SigmaBucket 5개 + TechBucket 3개 + header = 9개 키를 가지며 add_format 호출은 워크북당 9회로 제한된다"
    - "write_sheet_for_ticker(wb, formats, ticker, enriched_df, scalars)는 시트 1개를 D-03 컬럼 순서로 작성한다"
    - "시트 이름 = ticker, A1 = ticker, 3행 = 누적 median, 4행 = 누적 std, 5행 = 한국어 헤더, 6행+ = 날짜 내림차순 데이터"
    - "각 OHLCV/EMA/DIFF/dailychg 데이터 컬럼 우측에 _median, _std 컬럼이 인접 배치된다 (SHEET-08)"
    - "Stoch_%K, Stoch_%D, RSI 컬럼이 우측 끝에 위치 (D-03 그룹 6)"
    - "각 데이터 셀에 SigmaBucket 또는 TechBucket이 결정되어 Format이 적용된다 (DEFAULT는 fmt 미지정)"
    - "output/portfolio_YYYYMMDD.xlsx 경로로 저장된다 (output/ 폴더는 자동 생성)"
  artifacts:
    - path: "src/stocksig/output/__init__.py"
      provides: "output 서브패키지 마커"
    - path: "src/stocksig/output/writer.py"
      provides: "make_workbook(path) → (Workbook, formats_dict), 출력 경로 생성, close 헬퍼"
      min_lines: 35
    - path: "src/stocksig/output/sheet_per_ticker.py"
      provides: "write_sheet_for_ticker(wb, formats, ticker, df, scalars), build_column_layout(df), KOREAN_HEADERS dict"
      min_lines: 80
    - path: ".planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md"
      provides: "D-03 컬럼 인덱스 매핑 표 — 사용자/Phase 2 hyperlink 작성용 reference"
      contains: "컬럼 인덱스"
  key_links:
    - from: "writer.make_workbook"
      to: "compute.color_rules (SigmaBucket, TechBucket, GREEN_800, ...)"
      via: "Format dict 생성 시 hex import"
      pattern: "from stocksig\\.compute\\.color_rules import"
    - from: "sheet_per_ticker.write_sheet_for_ticker"
      to: "color_rules.decide_sigma_bucket / decide_stoch_bucket / decide_rsi_bucket"
      via: "각 셀당 1회 호출 + formats[bucket] lookup"
      pattern: "decide_sigma_bucket|decide_stoch_bucket|decide_rsi_bucket"
    - from: "Format dict"
      to: "9개 키 (5 SigmaBucket + 3 TechBucket + 1 header), workbook당 1회 생성"
      via: "writer.make_workbook 내부 add_format 9회 호출"
      pattern: "add_format"
---

<objective>
Wave 3: output 레이어 — XlsxWriter Workbook 라이프사이클 + 시트 1개 작성. Format 캐싱 (Pattern 8) + D-03 컬럼 배치 + 정적 색 베이킹 (per-cell). 본 wave 종료 시 (Wave 4 main.py 통합 없이도) `make_workbook → write_sheet_for_ticker → wb.close()`로 실제 .xlsx 파일이 생성 가능.

Purpose: Core Value(중앙값±σ 색 신호)가 실제로 .xlsx 셀에 베이킹되는 지점. Phase 1 산출물의 출력 형식이 여기서 잠긴다. D-03 컬럼 인덱스 매핑 표(별도 reference 문서)도 본 wave가 산출 — Phase 2의 시트1 hyperlink가 이를 사용.

Output: GREEN 상태 추가 없음 (Wave 4 smoke test가 종합 검증). 하지만 본 wave 종료 시점에 `output/portfolio_*.xlsx`를 수동 생성 가능 + Excel에서 D-03 레이아웃 + 색 베이킹 확인 가능.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-single-ticker/01-CONTEXT.md
@.planning/phases/01-foundation-single-ticker/01-RESEARCH.md
@.planning/phases/01-foundation-single-ticker/01-SKELETON.md
@.planning/phases/01-foundation-single-ticker/01-01-SUMMARY.md
@.planning/phases/01-foundation-single-ticker/01-02-SUMMARY.md
@.planning/phases/01-foundation-single-ticker/01-03-SUMMARY.md
@src/stocksig/compute/color_rules.py
@src/stocksig/compute/ema.py
@src/stocksig/compute/stats.py
@src/stocksig/compute/indicators.py

<interfaces>
src/stocksig/output/writer.py:
  def make_workbook(path: str | Path) -> tuple[xlsxwriter.Workbook, dict]
      """output/ 폴더 자동 생성. Workbook(path, {'constant_memory': False}).
      formats dict 구조:
        {
          SigmaBucket.DEFAULT:    Format({}),
          SigmaBucket.SOFT_GREEN: Format({font_color: GREEN_800}),
          SigmaBucket.SOFT_RED:   Format({font_color: RED_800}),
          SigmaBucket.HARD_GREEN: Format({font_color: GREEN_900, bg_color: GREEN_100}),
          SigmaBucket.HARD_RED:   Format({font_color: RED_900,   bg_color: RED_100}),
          TechBucket.DEFAULT:     Format({}),
          TechBucket.SOFT_GREEN:  Format({font_color: GREEN_800}),
          TechBucket.SOFT_RED:    Format({font_color: RED_800}),
          'header':               Format({bold: True, align: 'center'}),
        }
      반환: (wb, formats)"""

src/stocksig/output/sheet_per_ticker.py:
  KOREAN_HEADERS: dict[str, str]
      """원본 컬럼명 → 한국어 헤더 매핑. 예:
        'Close' → '종가', 'High' → '고가', 'Low' → '저가', 'Volume' → '거래량',
        'EMA_Close_11' → '종가 EMA11', 'DIFF_Close_11' → '종가-EMA11 차이',
        'EMA_Close_11_dailychg' → '종가 EMA11 일변동',
        'Close_median' → '종가 일별 중앙값', 'Close_std' → '종가 일별 표준편차',
        'Stoch_%K' → 'Stoch %K', 'Stoch_%D' → 'Stoch %D', 'RSI' → 'RSI',
        ... (모든 컬럼)"""

  def build_column_layout(enriched_df: pd.DataFrame) -> list[str]
      """D-03 순서대로 컬럼 이름 list 반환:
        [Date, Close, Close_median, Close_std,
              High,  High_median,  High_std,
              Low,   Low_median,   Low_std,
              Volume, Volume_median, Volume_std,
              EMA_Close_11, EMA_Close_11_median, EMA_Close_11_std, ... (12 EMA × 3 = 36),
              DIFF_Close_11, ..., ... (12 DIFF × 3 = 36),
              EMA_Close_11_dailychg, ..., ... (12 dailychg × 3 = 36),
              Stoch_%K, Stoch_%D, RSI]
        총 1 (Date) + 4×3 (OHLCV+stats) + 12×3 (EMA+stats) + 12×3 (DIFF+stats)
              + 12×3 (dailychg+stats) + 3 (tech) = 1 + 12 + 36 + 36 + 36 + 3 = 124 컬럼"""

  def write_sheet_for_ticker(
      wb, formats, ticker: str, enriched_df: pd.DataFrame,
      scalars: dict[str, dict[str, float]]
  ) -> None
      """1. ws = wb.add_worksheet(ticker)
         2. ws.write(0, 0, ticker)                    # A1 (SHEET-02)
         3. layout = build_column_layout(enriched_df)
         4. 3행 (row=2): 각 데이터 컬럼 위치에 scalars[col]['median'] (SHEET-03)
         5. 4행 (row=3): 각 데이터 컬럼 위치에 scalars[col]['std']    (SHEET-04)
         6. 5행 (row=4): KOREAN_HEADERS[col]                          (SHEET-05) with formats['header']
         7. 6행+ (row=5+): enriched_df.sort_index(ascending=False) 한 행씩 — 날짜는 첫 열에,
            데이터는 layout 순서대로. 각 셀에 대해:
              - OHLCV/EMA/DIFF/dailychg 데이터 셀: decide_sigma_bucket(value, row[f'{col}_median'], row[f'{col}_std']) → fmt
              - Stoch_%K, Stoch_%D 셀: decide_stoch_bucket(value) → fmt
              - RSI 셀: decide_rsi_bucket(value) → fmt
              - _median, _std 셀: 색 없음
            fmt가 DEFAULT bucket이면 fmt 없이 write, 아니면 with fmt.
         8. ws.set_column(0, len(layout)-1, 12)  # 합리적 폭 (Pitfall D)"""
</interfaces>

<patterns_from_research>
Pattern 8 (Format 캐싱 — RESEARCH.md §503-547):
- `wb = xlsxwriter.Workbook(path, {"constant_memory": False})`
- formats dict 8개 Bucket + 'header' = 9개 키
- 셀별: `ws.write(row, col, value, fmt)` (fmt 있을 때) 또는 `ws.write(row, col, value)` (DEFAULT)
- 절대 `wb.add_format(...)`을 셀마다 호출 금지 (메모리·styles.xml 폭증)

D-03 컬럼 순서 (CONTEXT.md decisions B):
1. 날짜
2. 원천 OHLCV (4 데이터 × 3 = 12 컬럼): Close+med+std → High+med+std → Low+med+std → Volume+med+std
3. 1차 EMA (12 데이터 × 3 = 36 컬럼): EMA_Close_11+med+std → EMA_Close_22+med+std → ... → EMA_Low_192+med+std
4. 2차 차이 DIFF (12 × 3 = 36)
5. 2차 EMA 일변동 dailychg (12 × 3 = 36)
6. Stoch %K, Stoch %D, RSI (3 컬럼, med/std 없음 — 임계값 색)

총 124 컬럼. 각 데이터 컬럼 옆에 즉시 med/std (SHEET-08).

D-04 색 (writer가 import):
- GREEN_800/900/100, RED_800/900/100 (color_rules에서 import)

Pitfall D: 시트 폭 과대 — `set_column(0, last, 12)` 또는 적절한 폭.
Pitfall G: 3·4행 = `scalars` dict (별도 인자), expanding의 마지막 값이 **아님**.
</patterns_from_research>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: writer.py + Format 캐싱 + 컬럼 레이아웃 헬퍼</name>
  <files>src/stocksig/output/__init__.py, src/stocksig/output/writer.py, src/stocksig/output/sheet_per_ticker.py (KOREAN_HEADERS + build_column_layout만 — write_sheet_for_ticker stub)</files>
  <behavior>
    - `make_workbook("/tmp/test.xlsx")` 호출 시 (1) `output/` 또는 부모 dir 자동 생성 (이미 존재하면 OK), (2) `Workbook` 객체 반환, (3) `formats` dict 키 9개 (5 SigmaBucket + 3 TechBucket + 'header').
    - 각 Format 객체의 properties 검증 (xlsxwriter Format은 internal — `repr` 또는 workbook close 후 styles.xml inspection은 통합 테스트에서. 단위 테스트는 키 존재 + dict size 검증으로 충분).
    - `build_column_layout(df)` (df는 36 EMA/DIFF/dailychg + 12 EMA × 3 stats 형태) → 124 element list. 첫 element는 'Date', 다음 12개는 OHLCV+stats 그룹, ..., 마지막 3개는 Stoch_%K, Stoch_%D, RSI.
    - `KOREAN_HEADERS["Close"] == "종가"`, `KOREAN_HEADERS["Stoch_%K"] == "Stoch %K"`, `KOREAN_HEADERS["EMA_Close_11_dailychg"] == "종가 EMA11 일변동"` 등.
  </behavior>
  <action>
    `src/stocksig/output/__init__.py` 빈 파일 Write.

    `src/stocksig/output/writer.py` Write — RESEARCH.md Pattern 8 그대로:
    ```python
    from pathlib import Path
    import xlsxwriter
    from stocksig.compute.color_rules import (
        SigmaBucket, TechBucket,
        GREEN_800, GREEN_900, GREEN_100, RED_800, RED_900, RED_100,
    )

    def make_workbook(path):
        path = Path(path)
        path.parent.mkdir(parents=True, exist_ok=True)
        wb = xlsxwriter.Workbook(str(path), {"constant_memory": False})
        formats = {
            SigmaBucket.DEFAULT:    wb.add_format({}),
            SigmaBucket.SOFT_GREEN: wb.add_format({"font_color": GREEN_800}),
            SigmaBucket.SOFT_RED:   wb.add_format({"font_color": RED_800}),
            SigmaBucket.HARD_GREEN: wb.add_format({"font_color": GREEN_900, "bg_color": GREEN_100}),
            SigmaBucket.HARD_RED:   wb.add_format({"font_color": RED_900,   "bg_color": RED_100}),
            TechBucket.DEFAULT:     wb.add_format({}),
            TechBucket.SOFT_GREEN:  wb.add_format({"font_color": GREEN_800}),
            TechBucket.SOFT_RED:    wb.add_format({"font_color": RED_800}),
            "header":               wb.add_format({"bold": True, "align": "center"}),
        }
        return wb, formats
    ```

    `src/stocksig/output/sheet_per_ticker.py` Write — 본 task에서는 KOREAN_HEADERS + build_column_layout만 구현. write_sheet_for_ticker는 다음 task.

    `KOREAN_HEADERS` 모듈 dict 작성. 키 후보 (124 컬럼 모두 매핑 필요):
    - 'Date' → '날짜'
    - 'Close'/'High'/'Low'/'Volume' → '종가'/'고가'/'저가'/'거래량'
    - `EMA_{p}_{n}` → `{한국어_p} EMA{n}` (예: 'EMA_Close_11' → '종가 EMA11')
    - `DIFF_{p}_{n}` → `{한국어_p}-EMA{n} 차이`
    - `EMA_{p}_{n}_dailychg` → `{한국어_p} EMA{n} 일변동`
    - `{col}_median` → `{원본_한국어} 일별 중앙값` (예: `Close_median` → `종가 일별 중앙값`)
    - `{col}_std` → `{원본_한국어} 일별 표준편차`
    - `Stoch_%K` → 'Stoch %K', `Stoch_%D` → 'Stoch %D', 'RSI' → 'RSI'

    실용적으로 `KOREAN_HEADERS`는 정적 dict가 아니라 **lookup 함수** `def korean_header(col: str) -> str:`로 구현하는 것이 124 항목 명시보다 깔끔. 본 plan은 함수 또는 dict 어느 쪽도 허용 (dict이면 dict comprehension으로 생성).

    `build_column_layout(df)`:
    ```python
    def build_column_layout(df):
        layout = ["Date"]
        # 원천 OHLCV
        for col in ["Close", "High", "Low", "Volume"]:
            layout += [col, f"{col}_median", f"{col}_std"]
        # 1차 EMA
        for price in ["Close", "High", "Low"]:
            for n in [11, 22, 96, 192]:
                base = f"EMA_{price}_{n}"
                layout += [base, f"{base}_median", f"{base}_std"]
        # 2차 차이
        for price in ["Close", "High", "Low"]:
            for n in [11, 22, 96, 192]:
                base = f"DIFF_{price}_{n}"
                layout += [base, f"{base}_median", f"{base}_std"]
        # 2차 EMA 일변동
        for price in ["Close", "High", "Low"]:
            for n in [11, 22, 96, 192]:
                base = f"EMA_{price}_{n}_dailychg"
                layout += [base, f"{base}_median", f"{base}_std"]
        # 기술적 지표
        layout += ["Stoch_%K", "Stoch_%D", "RSI"]
        return layout
    ```

    **중요:** Wave 2 stats.py의 `add_expanding_stats`는 데이터 컬럼들에 대해 `{col}_median`, `{col}_std`를 추가하므로, **layout이 요구하는 모든 데이터 컬럼이 stats 적용 대상이어야 한다**. main.py에서 stats 적용 대상 컬럼 list는 layout의 데이터 컬럼들 (Date 제외, *_median/*_std 제외, Stoch/RSI 제외) — 즉 4 OHLCV + 12 EMA + 12 DIFF + 12 dailychg = 40 컬럼. Wave 4가 이 list를 명시한다.

    Decision references: D-03 (컬럼 순서 그대로), D-04 (Format에 hex 적용), Pattern 8 (Format 캐싱).
  </action>
  <verify>
    <automated>uv run python -c "from stocksig.output.writer import make_workbook; from stocksig.output.sheet_per_ticker import build_column_layout, KOREAN_HEADERS; import pandas as pd; layout = build_column_layout(pd.DataFrame()); print(len(layout), layout[0], layout[1], layout[12], layout[-3], layout[-1])"</automated>
  </verify>
  <done>
    위 명령 출력이 `124 Date Close EMA_Close_11 Stoch_%K RSI` (또는 동등 — 124, Date, 두 번째 컬럼 Close, 13번째 EMA_Close_11, 끝에서 3번째 Stoch_%K, 마지막 RSI). `from stocksig.output.writer import make_workbook` import 에러 없음.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write_sheet_for_ticker — 정적 색 베이킹 + D-03 레이아웃 적용</name>
  <files>src/stocksig/output/sheet_per_ticker.py, .planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md</files>
  <behavior>
    Smoke test (Wave 4가 자동화)가 검증할 사실:
    - 시트 이름 = ticker (SHEET-01)
    - A1 셀 값 = ticker (SHEET-02)
    - 3행 (row=2): 데이터 컬럼 위치에 scalars[col]['median'] 값. _median/_std 컬럼 위치는 빈 셀. Date 위치(col=0)는 빈 셀.
    - 4행 (row=3): scalars[col]['std'] (SHEET-04)
    - 5행 (row=4): KOREAN_HEADERS, 'header' Format 적용 (bold + center)
    - 6행+ (row=5..): df.sort_index(ascending=False) 한 행씩. Date 컬럼은 enriched_df.index (DatetimeIndex). 데이터 셀: SigmaBucket 적용, Stoch/RSI 셀: TechBucket 적용, _median/_std 셀: 색 없음.
    - 셀의 Format이 D-04 hex와 일치 (openpyxl로 워크북 read 후 cell.font.color.rgb 검증)
    - 워크북 close 후 `output/{path}.xlsx` 존재

    수동 검증 추가 사항 (Wave 4 manual point 4):
    - Excel "조건부 서식 → 규칙 관리"에 항목 0개 (정적 베이킹이므로)
  </behavior>
  <action>
    `src/stocksig/output/sheet_per_ticker.py`에 `write_sheet_for_ticker` 추가.

    구현 단계 (interfaces 블록의 8 단계 그대로):
    1. `ws = wb.add_worksheet(ticker)`
    2. `ws.write(0, 0, ticker)`
    3. `layout = build_column_layout(enriched_df)`
    4. 3행/4행 작성:
       ```python
       for col_idx, col_name in enumerate(layout):
           if col_name == "Date" or col_name.endswith(("_median", "_std")):
               continue  # 3·4행에 빈 셀
           if col_name in scalars:
               ws.write(2, col_idx, scalars[col_name]["median"])
               ws.write(3, col_idx, scalars[col_name]["std"])
           # Stoch/RSI는 scalars에 없으므로 빈 셀 (SHEET-03/04는 OHLCV/EMA/DIFF/dailychg만 대상)
       ```
    5. 5행 (한국어 헤더):
       ```python
       for col_idx, col_name in enumerate(layout):
           ws.write(4, col_idx, korean_header(col_name), formats["header"])
       ```
    6. 6행+ (날짜 내림차순 데이터):
       ```python
       sorted_df = enriched_df.sort_index(ascending=False)
       for row_offset, (idx, row) in enumerate(sorted_df.iterrows()):
           excel_row = 5 + row_offset
           for col_idx, col_name in enumerate(layout):
               if col_name == "Date":
                   ws.write_datetime(excel_row, col_idx, idx.to_pydatetime() if hasattr(idx, 'to_pydatetime') else idx)
                   continue
               if col_name not in row.index:
                   continue  # 컬럼이 df에 없으면 skip (안전망)
               value = row[col_name]
               if pd.isna(value):
                   ws.write_blank(excel_row, col_idx, None)
                   continue
               # 색 결정
               if col_name == "Stoch_%K" or col_name == "Stoch_%D":
                   bucket = decide_stoch_bucket(value)
                   fmt = formats[bucket] if bucket != TechBucket.DEFAULT else None
               elif col_name == "RSI":
                   bucket = decide_rsi_bucket(value)
                   fmt = formats[bucket] if bucket != TechBucket.DEFAULT else None
               elif col_name.endswith(("_median", "_std")):
                   fmt = None  # med/std 자체에는 색 없음
               else:
                   # 데이터 셀 (OHLCV/EMA/DIFF/dailychg)
                   med = row.get(f"{col_name}_median")
                   std = row.get(f"{col_name}_std")
                   bucket = decide_sigma_bucket(value, med, std)
                   fmt = formats[bucket] if bucket != SigmaBucket.DEFAULT else None
               if fmt is None:
                   ws.write(excel_row, col_idx, value)
               else:
                   ws.write(excel_row, col_idx, value, fmt)
       ```
    7. `ws.set_column(0, len(layout)-1, 12)` (Pitfall D)

    **컬럼 인덱스 매핑 표:** `.planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md` Write — 124 컬럼의 (index, 원본 컬럼명, 한국어 헤더, 색 적용 규칙) 표. CONTEXT.md "<deferred>" 직전 "Note: Planner는 컬럼 인덱스 매핑 표를 별도 산출할 것" 직접 대응. 형식:
    ```markdown
    # D-03 컬럼 인덱스 매핑 표 (Phase 1)

    | Col Index | 원본 컬럼명 | 한국어 헤더 | 색 규칙 | 그룹 |
    |-----------|------------|------------|--------|------|
    | 0 | Date | 날짜 | (없음) | 0 |
    | 1 | Close | 종가 | SigmaBucket | 원천 OHLCV |
    | 2 | Close_median | 종가 일별 중앙값 | (없음) | 원천 OHLCV |
    | 3 | Close_std | 종가 일별 표준편차 | (없음) | 원천 OHLCV |
    | 4 | High | 고가 | SigmaBucket | 원천 OHLCV |
    ... (124 행)
    ```
    표는 `build_column_layout`에서 derive 가능하므로 `uv run python` 한 줄 스크립트로 생성 + 한국어 헤더 매핑 + 색 규칙 매핑.

    Decision references: D-01 (output 레이어 위치), D-02 (DEFAULT bucket fmt 미지정 = 기본색), D-03 (124 컬럼 순서), D-04 (Format hex), SHEET-01~08, TECH-03/06, OUT-01~03.
  </action>
  <verify>
    <automated>uv run python -c "
from stocksig.output.writer import make_workbook
from stocksig.output.sheet_per_ticker import write_sheet_for_ticker, build_column_layout
from stocksig.compute.ema import add_ema_columns
from stocksig.compute.stats import add_expanding_stats, cumulative_scalars
from stocksig.compute.indicators import stoch_slow, rsi_wilder
import pandas as pd, numpy as np
rng = np.random.default_rng(42)
n = 250
df = pd.DataFrame({
    'Open': 100 + np.cumsum(rng.normal(0, 1, n)),
    'High': 0, 'Low': 0, 'Close': 0, 'Volume': rng.integers(1e6, 1e7, n).astype(float),
}, index=pd.date_range('2025-01-01', periods=n))
df['Close'] = df['Open'] + rng.normal(0, 0.5, n); df['High']=df['Close']*1.02; df['Low']=df['Close']*0.98
df = add_ema_columns(df)
layout = build_column_layout(df)
data_cols = [c for c in layout if c not in ('Date','Stoch_%K','Stoch_%D','RSI') and not c.endswith(('_median','_std'))]
df = add_expanding_stats(df, data_cols)
scalars = cumulative_scalars(df, data_cols)
st = stoch_slow(df); df['Stoch_%K']=st['Stoch_%K']; df['Stoch_%D']=st['Stoch_%D']; df['RSI']=rsi_wilder(df)
wb, fmt = make_workbook('output/_smoke_writer.xlsx')
write_sheet_for_ticker(wb, fmt, 'TEST', df, scalars)
wb.close()
import os; assert os.path.exists('output/_smoke_writer.xlsx'); print('ok size', os.path.getsize('output/_smoke_writer.xlsx'))
"
    </automated>
  </verify>
  <done>
    위 인라인 smoke가 `ok size <bytes>` 출력 (파일 > 5KB). 워크북이 Excel에서 열림. `.planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md` 존재 + 124 행 표 포함.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compute→output | enriched DataFrame + scalars dict 신뢰 (같은 프로세스 in-memory). NaN/0 핸들링은 color_rules 책임 (Wave 2에서 잠금) |
| Python→filesystem (`output/*.xlsx`) | 출력 파일에 보유 종목 정보 — `.gitignore`로 보호 (Wave 0 Task 1에서 잠금) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FMT | Tampering (Format 폭증으로 styles.xml 손상) | XlsxWriter add_format | mitigate | Format 캐싱 — workbook당 9회만 add_format 호출 (Pattern 8). Lookup으로 셀당 0 추가 |
| T-01-INFO | Information Disclosure | `output/portfolio_*.xlsx` 보유 종목 노출 | mitigate | `.gitignore`에 `output/`, `*.xlsx` (Wave 0 잠금) |
| T-01-CALC2 | Tampering | 색 베이킹 잘못된 셀 적용 (예: Stoch 셀에 sigma bucket 적용) | mitigate | write_sheet_for_ticker의 분기 (col_name == 'Stoch_%K' 등) + 단위 테스트는 Wave 4 smoke가 종합 검증 |
</threat_model>

<verification>
- `uv run python -c "..."` (verify 블록의 smoke) 성공
- `output/_smoke_writer.xlsx`를 Excel에서 열기 → 시트 이름 TEST, A1=TEST, 5행 한국어 헤더 bold, 6행+ 데이터 일부에 초록/빨강 글자 (250행 중 일부는 σ 발화)
- Excel "조건부 서식 → 규칙 관리" → 0개 (정적 베이킹 검증)
- `01-04-COLUMN-MAP.md` 존재, 124행 표
</verification>

<success_criteria>
- Format이 워크북당 9개 (셀당 새 객체 미생성)
- D-03 124 컬럼 순서가 코드 + reference 문서에서 일치
- 정적 색 베이킹 — 동적 CF 규칙 0개 (Excel "규칙 관리" 빈 상태)
- 모든 데이터 셀에 SigmaBucket, Stoch/RSI 셀에 TechBucket, _median/_std 셀에 색 없음
</success_criteria>

<output>
Create `.planning/phases/01-foundation-single-ticker/01-04-SUMMARY.md` when done. SUMMARY에 output 레이어 공개 시그니처 + 124 컬럼 layout 첫 10개 + 끝 5개 + Format 캐시 키 9개 목록.
</output>
