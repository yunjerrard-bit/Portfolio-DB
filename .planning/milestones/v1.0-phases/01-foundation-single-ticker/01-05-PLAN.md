---
phase: 01-foundation-single-ticker
plan: 05
type: execute
wave: 4
depends_on: [01-02, 01-03, 01-04]
files_modified:
  - main.py
  - src/stocksig/main_run.py
autonomous: false
requirements:
  - EXEC-01
  - EXEC-02
user_setup: []
tags: [orchestration, logging, walking-skeleton, smoke-test, manual-verify]

must_haves:
  truths:
    - "`uv run python main.py` 한 줄로 Windows 로컬에서 워크북이 생성된다 (EXEC-01)"
    - "main 흐름: load_env → read_tickers → 각 ticker별 fetch_ohlcv → add_ema_columns → add_expanding_stats → cumulative_scalars → stoch_slow + rsi_wilder → write_sheet_for_ticker → wb.close()"
    - "한국어 로깅이 stdout으로 출력된다 (D-05 포맷, Windows console UTF-8 보장)"
    - "smoke test (test_single_ticker_workbook + test_color_at_three_rows)가 GREEN"
    - "수기 검증 6 포인트 (VALIDATION.md §Manual-Only Verifications) 모두 통과"
  artifacts:
    - path: "main.py"
      provides: "엔트리포인트 — argparse, sys.stdout.reconfigure(UTF-8), logging.basicConfig, main_run.run() 호출"
      min_lines: 30
    - path: "src/stocksig/main_run.py"
      provides: "run(tickers_path, env_path, output_dir) 오케스트레이션 함수 (test에서 호출 가능)"
      min_lines: 60
  key_links:
    - from: "main.py"
      to: "stocksig.main_run.run"
      via: "argparse → run() 호출 → exit code 0 (성공) 또는 ≠0 (실패)"
      pattern: "from stocksig\\.main_run import run|main_run\\.run"
    - from: "main_run.run"
      to: "io.input.read_tickers + io.market.fetch_ohlcv + compute.* + output.*"
      via: "Wave 1~3의 모든 공개 시그니처를 orchestrate"
      pattern: "fetch_ohlcv|add_ema_columns|write_sheet_for_ticker"
---

<objective>
Wave 4: 통합 + 수기 검증 (Walking Skeleton 완성). `main.py` 엔트리포인트 + `main_run.run()` 오케스트레이션 함수가 Wave 1~3의 모든 공개 시그니처를 연결한다. 본 wave 종료 시 `uv run python main.py`가 실제 yfinance에서 AAPL OHLCV를 받아 `output/portfolio_YYYYMMDD.xlsx`를 생성한다.

Purpose: Walking Skeleton의 "마지막 한 줄" — Core Value(중앙값±σ 색 신호)가 실제 .xlsx 셀에 살아있음이 사용자에게 가시화되는 지점. smoke test 2개가 자동 검증을 닫고, 수기 검증 6 포인트가 시각·UI 사실(동적 CF 부재, Excel 렌더링)을 잠근다.

Output: smoke test 2/2 GREEN, 수기 6 포인트 완료, 사용자가 `uv run python main.py` 실행 가능.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-single-ticker/01-CONTEXT.md
@.planning/phases/01-foundation-single-ticker/01-RESEARCH.md
@.planning/phases/01-foundation-single-ticker/01-VALIDATION.md
@.planning/phases/01-foundation-single-ticker/01-SKELETON.md
@.planning/phases/01-foundation-single-ticker/01-04-COLUMN-MAP.md
@.planning/phases/01-foundation-single-ticker/01-01-SUMMARY.md
@.planning/phases/01-foundation-single-ticker/01-02-SUMMARY.md
@.planning/phases/01-foundation-single-ticker/01-03-SUMMARY.md
@.planning/phases/01-foundation-single-ticker/01-04-SUMMARY.md
@tests/test_smoke_end_to_end.py
@tests/conftest.py
@src/stocksig/io/input.py
@src/stocksig/io/market.py
@src/stocksig/config.py
@src/stocksig/compute/ema.py
@src/stocksig/compute/stats.py
@src/stocksig/compute/indicators.py
@src/stocksig/compute/color_rules.py
@src/stocksig/output/writer.py
@src/stocksig/output/sheet_per_ticker.py

<interfaces>
src/stocksig/main_run.py:
  def run(
      tickers_path: str | Path = "tickers.txt",
      env_path: str | Path | None = None,
      output_dir: str | Path = "output",
  ) -> Path
      """오케스트레이션:
        1. env = load_env(env_path)  # INPUT-05 fail-fast
        2. tickers = read_tickers(tickers_path)  # INPUT-01/02/03
        3. output_path = output_dir / f"portfolio_{date.today():%Y%m%d}.xlsx"
        4. wb, formats = make_workbook(output_path)
        5. for ticker in tickers:
             try:
               df = fetch_ohlcv(ticker)  # MKTD-01~03
               df = add_ema_columns(df)
               data_cols = [...]  # 40개 (OHLCV+EMA+DIFF+dailychg)
               df = add_expanding_stats(df, data_cols)
               scalars = cumulative_scalars(df, data_cols)
               stoch = stoch_slow(df); df['Stoch_%K']=stoch['Stoch_%K']; df['Stoch_%D']=stoch['Stoch_%D']
               df['RSI'] = rsi_wilder(df)
               write_sheet_for_ticker(wb, formats, ticker, df, scalars)
               logger.info(f"... | {ticker} | 시트 작성 완료")
             except Exception as e:
               # Phase 1은 단일 티커 fail-fast (MKTD-04 deferred to Phase 2)
               wb.close()
               raise
        6. wb.close()
        7. return output_path"""

main.py:
  if __name__ == "__main__":
      # 1. sys.stdout/stderr reconfigure UTF-8 (Windows 콘솔 한국어)
      # 2. argparse: --tickers (default tickers.txt), --env (default .env), --output-dir (default output/)
      # 3. logging.basicConfig with D-05 포맷, encoding='utf-8'
      # 4. try: path = run(...); print(f"완료: {path}"); sys.exit(0)
      #    except SystemExit: raise
      #    except Exception: logger.exception("실행 실패"); sys.exit(1)
</interfaces>

<data_cols_list>
<!-- main_run.run() 내부 add_expanding_stats가 사용할 40개 컬럼 -->
4 OHLCV: Close, High, Low, Volume
12 EMA:  EMA_{Close,High,Low}_{11,22,96,192}
12 DIFF: DIFF_{Close,High,Low}_{11,22,96,192}
12 dailychg: EMA_{Close,High,Low}_{11,22,96,192}_dailychg
총 40 컬럼
(Stoch_%K, Stoch_%D, RSI는 expanding stats 대상 아님 — 임계값 색)
</data_cols_list>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: main_run.run() + main.py + smoke test GREEN 전환</name>
  <files>main.py, src/stocksig/main_run.py</files>
  <behavior>
    smoke test (`tests/test_smoke_end_to_end.py`):

    `test_single_ticker_workbook`:
    - `mocker.patch('stocksig.io.market.yf.Ticker').return_value.history.return_value = mock_ohlcv_df`
    - `tmp_tickers = tmp_tickers_file("AAPL")`, `tmp_env = tmp_env_file("EDGAR_USER_AGENT_EMAIL=x\nOPENDART_API_KEY=y")`
    - `output_path = run(tmp_tickers, tmp_env, tmp_path / "output")`
    - `assert output_path.exists() and output_path.suffix == '.xlsx'`
    - openpyxl로 열기: `wb = openpyxl.load_workbook(output_path); ws = wb["AAPL"]`
    - `assert ws["A1"].value == "AAPL"`
    - 3행/4행 (row=3, 4 in openpyxl 1-indexed): Close 위치(col=2 in 1-indexed)에 scalars median/std 값
    - 5행 (row=5): 한국어 헤더 ('날짜', '종가', '종가 일별 중앙값', ...)
    - 6행 (row=6): 가장 최신 날짜 (sorted descending)
    - 시트 폭: 124 컬럼

    `test_color_at_three_rows` (Success Criteria #2/#3):
    - mock_ohlcv_df 사용 (250+ 행)
    - run() 실행 후 워크북 read
    - 가장 최근 / 중간 / 가장 오래된 3 행 선택
    - 해당 행의 Close 셀에 대해 expected SigmaBucket을 외부 계산 (mock df에서 직접 expanding median/std 계산 → decide_sigma_bucket 호출)
    - openpyxl cell.font.color.rgb 또는 cell.fill.fgColor.rgb가 expected hex와 일치 (D-04)
    - HARD_GREEN 케이스: font=`#1B5E20`, fill=`#C8E6C9`
    - HARD_RED 케이스: font=`#B71C1C`, fill=`#FFCDD2`
    - DEFAULT 케이스: font color None 또는 black, fill None
  </behavior>
  <action>
    `src/stocksig/main_run.py` Write — `<interfaces>` 블록의 `run()` 구현. 명시적으로:
    - `data_cols`를 모듈 상수 또는 함수 내 list로 명시 (`<data_cols_list>` 블록 그대로 40개)
    - stats 적용 순서: `add_ema_columns(df)` → EMA/DIFF/dailychg 36 컬럼 생성 → 그 다음 `add_expanding_stats(df, data_cols)` 호출 — 즉 stats가 EMA 컬럼들도 cover. 단, EMA192의 처음 ~200행은 seed bias로 expanding std 좁음 (Pitfall C) — D-02가 흡수.
    - `stoch_slow(df)` 및 `rsi_wilder(df)` 호출 후 결과 컬럼을 df에 merge.
    - `output_dir`이 Path object일 수도 string일 수도 — `Path(output_dir).mkdir(parents=True, exist_ok=True)` (writer가 이미 mkdir하지만 안전망).
    - 같은 날 재실행 시 overwrite (OUT-03은 "매 실행마다 새 파일" — 같은 날 두 번 실행이면 같은 이름 overwrite, RESEARCH.md Open Question #2의 권장).
    - 로깅: D-05 포맷, 한국어. 각 단계마다 (`OHLCV 수신 시작`, `OHLCV {N} 거래일 수신 완료`, `EMA 계산 완료`, `시트 작성 완료`, `워크북 저장: {path}`).

    `main.py` Write — `<interfaces>` 블록의 entrypoint 구현. 명시적으로:
    ```python
    import sys, logging, argparse
    from pathlib import Path

    if sys.platform == "win32":
        try:
            sys.stdout.reconfigure(encoding="utf-8")
            sys.stderr.reconfigure(encoding="utf-8")
        except Exception:
            pass

    logging.basicConfig(
        level=logging.INFO,
        format="[%(levelname)s] %(asctime)s | %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S",
        encoding="utf-8",
    )

    def main():
        parser = argparse.ArgumentParser(description="표준편차 기반 주식 매매신호 워크북 생성")
        parser.add_argument("--tickers", default="tickers.txt")
        parser.add_argument("--env", default=None)
        parser.add_argument("--output-dir", default="output")
        args = parser.parse_args()
        from stocksig.main_run import run
        path = run(args.tickers, args.env, args.output_dir)
        print(f"완료: {path}")

    if __name__ == "__main__":
        main()
    ```

    Tests 활성화: `tests/test_smoke_end_to_end.py`의 2 함수 xfail 제거 + behavior 블록의 assertion. `test_color_at_three_rows`에서 openpyxl 색 검증은 다음 패턴:
    - `cell.font.color`가 None이거나 `cell.font.color.rgb` 값을 `#RRGGBB`로 정규화 (openpyxl은 'FF1B5E20' 같은 ARGB 반환 — 앞 2글자 alpha 제거)
    - 정규화 후 D-04 hex와 일치 검증

    Decision references: D-01 (모든 import 경로), D-05 (한국어 로깅 + UTF-8 reconfigure), D-06 (fetch_ohlcv 호출), EXEC-01/02, Pitfall C (EMA192 seed bias — D-02 자연 흡수, 별도 처리 없음).
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke_end_to_end.py -x -q --timeout=30</automated>
  </verify>
  <done>
    `tests/test_smoke_end_to_end.py` 2/2 GREEN. 전체 `uv run pytest -x -q`가 19/19 GREEN (xfail 0). `uv run python -c "from stocksig.main_run import run; print(run.__doc__[:50])"` 동작.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Walking Skeleton 수기 검증 6 포인트 (VALIDATION.md §Manual-Only Verifications)</name>
  <what-built>
    Wave 0~4 통합 결과물:
    - `uv run python main.py`로 실제 yfinance(curl_cffi + tenacity) AAPL OHLCV 수신 → EMA·차이·일변동·expanding median/std·Stoch·RSI 계산 → 정적 색 베이킹된 단일 시트 `output/portfolio_YYYYMMDD.xlsx` 생성
    - 19개 자동 테스트 GREEN
    - Walking Skeleton 완성 — Core Value(중앙값±σ 색 신호)가 실제 .xlsx 셀에 살아있음
  </what-built>
  <how-to-verify>
    PowerShell에서 다음 6 포인트를 순서대로 수행:

    1. **파일 생성 (OUT-01/02):**
       ```powershell
       cd "C:\Users\kimyunjae\Documents\Claude 앱 개발\example"
       uv run python main.py
       ```
       `.env` 파일에 `EDGAR_USER_AGENT_EMAIL`/`OPENDART_API_KEY` 채워져 있어야 함 (`.env.example` 참조).
       기대 결과: 콘솔에 한국어 진행 로그 (`[INFO] ... | AAPL | OHLCV ... 거래일 수신 완료`, `[INFO] ... | AAPL | 시트 작성 완료`), 마지막에 `완료: output\portfolio_YYYYMMDD.xlsx`. `output/portfolio_{오늘날짜}.xlsx` 파일 존재.

    2. **시트 구조 (SHEET-01~06):**
       Excel에서 위 .xlsx 열기 → 시트 이름 `AAPL` → A1=`AAPL` → 3행 숫자 (각 데이터 컬럼의 누적 중앙값) → 4행 숫자 (누적 표준편차) → 5행 한국어 헤더 (`날짜`, `종가`, `종가 일별 중앙값`, ...) → 6행부터 날짜가 내림차순 (최상단=오늘 또는 어제).

    3. **색 정합성 (COLOR-01~07, Success Criteria #3):**
       3개 행 선정 (예: 6행=가장 최신, 1500행쯤=중간, 마지막 데이터 행=가장 오래됨).
       각 행의 `종가` 셀에 대해 수기 계산: 그 날짜의 expanding median 및 std는 3행/4행이 아니라 그 행의 `종가 일별 중앙값`/`종가 일별 표준편차` 셀에 표시되어 있다 (sigma 신호는 expanding이고, 3·4행은 누적 스칼라). `(종가 - 중앙값) / 표준편차`가 (-2, -1), (-1, +1), (+1, +2), (-∞,-2), (+2,+∞) 어느 구간에 속하는지 확인 → D-04 표의 색과 일치하는지 시각 확인. 적어도 1개 SOFT_GREEN/SOFT_RED 케이스 + 가능하면 1개 HARD_GREEN/HARD_RED 케이스 (250일 이상이면 outlier 1~2 행 존재 가능성 ↑).

    4. **동적 CF 부재 (Success Criteria #3 후반):**
       Excel 메뉴: 홈 → 조건부 서식 → 규칙 관리. "이 워크시트" 선택. 항목 **0개**여야 함. 색은 모두 정적 `Format`으로 베이킹되었으므로 규칙이 없는 것이 정상.

    5. **Stoch/RSI 색 (TECH-04/05/06):**
       시트 우측 끝 3개 컬럼 (`Stoch %K`, `Stoch %D`, `RSI`). 데이터 전체에서 `Stoch %K ≤ 20`인 행이 1개 이상 존재하면 해당 셀이 부드러운 초록 (#2E7D32) 글자색 — 시각 확인. `Stoch %K ≥ 80`인 셀이 부드러운 빨강 — 시각 확인. RSI 동일 (30/70 임계값).

    6. **에러 경로 (INPUT-03, Success Criteria #4):**
       ```powershell
       Copy-Item tickers.txt tickers.txt.bak
       Set-Content tickers.txt ""
       uv run python main.py
       echo "exit code: $LASTEXITCODE"
       Move-Item -Force tickers.txt.bak tickers.txt
       ```
       기대 결과: 한국어 에러 메시지 stderr 출력 (예: `[ERROR] ... | tickers.txt 파일이 비어있습니다.`) + `exit code: 1`.

    **추가 (Success Criteria #5):** `.env`에서 한 줄 비우고 `uv run python main.py` → 한국어 fail-fast 메시지 + exit ≠ 0.

    각 포인트 결과를 한 줄씩 보고 (PASS/FAIL + 한 줄 코멘트). FAIL이 있으면 어느 wave plan으로 돌아가야 할지 명시.
  </how-to-verify>
  <resume-signal>6 포인트 모두 PASS면 "approved", 일부 FAIL이면 FAIL 항목 + 원인 가설 (예: "4번 FAIL — 규칙 관리에 1개 항목 발견, 정적 베이킹 가정 위반").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user→main.py (argparse, env file) | 사용자 입력 — 본 task는 Wave 1 io 레이어가 이미 잠금 |
| local→Yahoo Finance (실제 호출) | Wave 1 market.py가 이미 잠금 (tenacity, curl_cffi) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-EXC | Denial of Service | 단일 티커 fetch 실패 시 워크북 깨짐 | mitigate | `wb.close()`를 try-finally 또는 except에서 호출하여 부분 워크북이라도 디스크에 저장 — 단, Phase 1은 단일 티커 fail-fast가 명시 (MKTD-04 Phase 2). 본 plan은 fail-fast (raise) + finally wb.close() 패턴 |
| T-01-LOG | Information Disclosure | 한국어 로깅이 .env 값 노출 | mitigate | Wave 1 config.py가 잠금 (값 자체 미로깅) |
| T-01-UTF | Tampering (Windows 콘솔 한국어 깨짐) | sys.stdout encoding | mitigate | sys.stdout.reconfigure(encoding="utf-8") + logging.basicConfig(encoding="utf-8") + README 안내 (PYTHONIOENCODING=utf-8 또는 chcp 65001) |
</threat_model>

<verification>
- `uv run pytest -x -q` 19/19 GREEN
- 수기 검증 6 포인트 모두 PASS
- Phase 1 Success Criteria 6개 모두 충족:
  1. ✓ `uv run python main.py` → `output/portfolio_YYYYMMDD.xlsx` 생성
  2. ✓ Excel 열기 → AAPL 시트 + A1/3행/4행/5행/6행+ 구조
  3. ✓ 정적 색 베이킹 + 동적 CF 0개 + 3행 색 일치
  4. ✓ 빈 tickers.txt → 한국어 에러 + 비0 exit
  5. ✓ 빈 .env 키 → 한국어 fail-fast
  6. ✓ Stoch/RSI 색 (≤20/30 초록, ≥80/70 빨강)
</verification>

<success_criteria>
- Walking Skeleton 완성: Core Value(중앙값±σ 색 신호)가 실제 .xlsx 셀에 살아있음을 자동 + 수기로 입증
- 19/19 자동 테스트 GREEN
- 사용자가 `uv run python main.py` 한 줄로 실행 가능 (EXEC-01/02 충족)
- 후속 Phase 2(N 티커 스케일링)가 본 wave의 `main_run.run()` 시그니처를 확장만 하면 동작 (loop max_workers=4 + cache 모듈 추가)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-single-ticker/01-05-SUMMARY.md` when done. SUMMARY에 entrypoint 흐름 다이어그램 + 수기 검증 6 포인트 결과 + Phase 2 진입 시 변경 지점 (main_run.run의 for loop를 ThreadPoolExecutor로 + cache 호출 삽입 위치) 기록.
</output>
