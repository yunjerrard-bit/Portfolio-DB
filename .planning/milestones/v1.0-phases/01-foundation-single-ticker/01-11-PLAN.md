---
phase: 01-foundation-single-ticker
plan: 11
type: execute
wave: 5
gap_closure: true
depends_on: [01-07, 01-08, 01-09, 01-10]
files_modified:
  - src/stocksig/compute/ema.py
  - src/stocksig/compute/color_rules.py
  - src/stocksig/output/sheet_per_ticker.py
  - tests/test_ema.py
  - tests/test_color_rules.py
  - tests/test_smoke_end_to_end.py
autonomous: true
requirements:
  - SHEET-07
user_setup: []
tags: [compute, ema-trend, color-rules, layout, gap-fix]

must_haves:
  truths:
    - "신규 컬럼 4개: EMA_Close_{11,22,96,192}_trend = EMA_Close_N.pct_change() (비율, 양수=상승)"
    - "trend 컬럼은 EMA_Close_N 값 컬럼 바로 다음 위치에 삽입 — 같은 period 그룹 내에서 [EMA val, EMA_median, EMA_std, EMA_trend] 순"
    - "EMA_Close_N 값 컬럼도 hidden 처리 (기존 _median/_std hidden 룰에 추가)"
    - "trend 컬럼은 가시 상태 유지 (suffix _trend는 hidden 룰에 포함하지 않음)"
    - "trend num_format = percent_ratio ('0.00%' — Excel 자동 ×100 표시)"
    - "trend 색 규칙: value > 0 → TechBucket.SOFT_GREEN, value < 0 → TechBucket.SOFT_RED, else DEFAULT (Stoch/RSI와 동일 클래스 재사용)"
    - "총 데이터 컬럼: 76 → 80 (+4 trend)"
  artifacts:
    - path: "src/stocksig/compute/ema.py"
      provides: "add_ema_columns가 EMA_Close_N_trend 4개 추가"
      contains: "_trend"
    - path: "src/stocksig/compute/color_rules.py"
      provides: "decide_trend_bucket(value) → TechBucket"
      contains: "decide_trend_bucket"
    - path: "src/stocksig/output/sheet_per_ticker.py"
      provides: "build_column_layout 80 cols + trend hidden 룰 제외 + EMA val 추가 hide + korean_header 'ema11 추세' 등"
      contains: "_trend"
---

<objective>
사용자 요청:
1. EMA_Close_{11,22,96,192} 값 컬럼(N, Q, T, W) 숨김
2. 대신 4개 추세(전일 대비 등락률) 컬럼 추가
3. 양수=초록, 음수=빨강 색 베이킹

목적: EMA 절댓값보다 추세 방향 가독성이 더 유용함.
</objective>

<tasks>

<task id="1">
<title>compute/ema.py — EMA_Close_N_trend 컬럼 4개 추가</title>

<behavior>
add_ema_columns 내부 EMA_Close 루프에서 각 N에 대해:

```
ema = compute_ema(out["Close"], n)
out[f"EMA_Close_{n}"] = ema
out[f"EMA_Close_{n}_dailychg"] = ema.diff()
out[f"EMA_Close_{n}_trend"] = ema.pct_change()
```

DIFF 12개는 그대로. 총 신규 컬럼: 4 EMA + 12 DIFF + 4 dailychg + 4 trend = 24.
</behavior>

<files>
- src/stocksig/compute/ema.py
</files>

<automated>
uv run python -c "from stocksig.compute.ema import add_ema_columns; import pandas as pd; df = pd.DataFrame({'Close': [100,101,102,103,104,105,106,107,108,109,110,111,112], 'High': [101,102,103,104,105,106,107,108,109,110,111,112,113], 'Low': [99,100,101,102,103,104,105,106,107,108,109,110,111], 'Volume': [1000]*13}); out = add_ema_columns(df); print('trend_cols:', [c for c in out.columns if c.endswith('_trend')]); print('non-null trend 11:', out['EMA_Close_11_trend'].notna().sum())"
</automated>

<verify>
출력에 `EMA_Close_11_trend`, `EMA_Close_22_trend`, `EMA_Close_96_trend`, `EMA_Close_192_trend` 4개 + 0이 아닌 값 존재.
</verify>
</task>

<task id="2">
<title>compute/color_rules.py — decide_trend_bucket 추가</title>

<behavior>
```python
def decide_trend_bucket(value) -> TechBucket:
    """전일 대비 EMA 등락률 색 결정.
    
    value > 0 → SOFT_GREEN
    value < 0 → SOFT_RED
    else (0, NaN) → DEFAULT
    """
    import math
    if value is None or (isinstance(value, float) and math.isnan(value)):
        return TechBucket.DEFAULT
    if value > 0:
        return TechBucket.SOFT_GREEN
    if value < 0:
        return TechBucket.SOFT_RED
    return TechBucket.DEFAULT
```

tests/test_color_rules.py에 신규 케이스 `test_trend_bucket` 추가 — 양수/음수/0/NaN 모두 검증.
</behavior>

<files>
- src/stocksig/compute/color_rules.py
- tests/test_color_rules.py
</files>

<automated>
uv run pytest tests/test_color_rules.py -x -q
</automated>

<verify>
전체 color_rules 테스트 GREEN.
</verify>
</task>

<task id="3">
<title>sheet_per_ticker.py — layout/hide/header/color 통합</title>

<behavior>
변경 사항:

(a) `build_column_layout`의 EMA_Close 그룹을 다음으로 갱신 (4 periods × 4 cols = 16):
```python
for n in _EMA_PERIODS:
    base = f"EMA_Close_{n}"
    layout += [base, f"{base}_median", f"{base}_std", f"{base}_trend"]
```
다른 그룹은 변경 없음. 총 76 → 80.

(b) `korean_header` 추가 매핑:
- `EMA_Close_11_trend` → "ema11 추세"
- 동일 패턴 22/96/192

(c) hidden 룰 확장: write_sheet_for_ticker 말미의 hidden 루프에 `EMA_Close_N` 값 컬럼도 포함:
```python
for col_idx, col_name in enumerate(layout):
    if (col_name.endswith(("_median", "_std"))
        or (col_name.startswith("EMA_Close_") 
            and not col_name.endswith(("_dailychg", "_trend")) 
            and "_" in col_name[len("EMA_Close_"):] is False)):  
        ws.set_column(col_idx, col_idx, None, None, {"hidden": True})
```
주의: 위 조건은 정확히 `EMA_Close_{N}` (suffix 없는 값 컬럼)에만 매치되어야 함. 가장 명료한 구현:
```python
import re
EMA_VALUE_RE = re.compile(r"^EMA_Close_\d+$")
...
if col_name.endswith(("_median", "_std")) or EMA_VALUE_RE.match(col_name):
    ws.set_column(col_idx, col_idx, None, None, {"hidden": True})
```

(d) write 루프의 색 결정 분기에 trend 케이스 추가:
```python
from stocksig.compute.color_rules import decide_trend_bucket
...
elif col_name.endswith("_trend"):
    bucket = decide_trend_bucket(value)
    fmt_type = "percent_ratio"
    fmt = formats[(bucket, fmt_type)]
```

(e) `column_num_format` 헬퍼에 trend → "percent_ratio" 분기 추가 (DIFF와 동일).
</behavior>

<files>
- src/stocksig/output/sheet_per_ticker.py
</files>

<automated>
uv run python -c "from stocksig.output.sheet_per_ticker import build_column_layout, korean_header; cols = build_column_layout(); print('count:', len(cols)); trend = [c for c in cols if c.endswith('_trend')]; print('trends:', trend); print('kr:', [korean_header(t) for t in trend]); assert len(cols) == 80; assert trend == ['EMA_Close_11_trend','EMA_Close_22_trend','EMA_Close_96_trend','EMA_Close_192_trend']; print('OK')"
</automated>

<verify>
출력에 `count: 80`, 4개 trend 컬럼, 한국어 `["ema11 추세","ema22 추세","ema96 추세","ema192 추세"]`, `OK`.
</verify>
</task>

<task id="4">
<title>smoke test — trend 컬럼/색/숨김 검증 + 회귀</title>

<behavior>
tests/test_smoke_end_to_end.py에 또는 적절한 단위 테스트 파일에 추가:

(a) `test_ema_value_columns_hidden`: openpyxl로 EMA_Close_{11,22,96,192} 위치가 hidden=True 확인 (range-aware lookup 활용 — 기존 hidden 테스트와 동일 헬퍼)

(b) `test_trend_columns_visible_and_formatted`: EMA_Close_11_trend 위치 hidden=False, num_format == '0.00%'

(c) `test_trend_color_baking`: 시드 데이터에서 trend > 0 행과 trend < 0 행을 찾아 font color가 GREEN_800 / RED_800 인지 검증 + font.b == True (bold from 01-10)

기존 RSI/DIFF/freeze/bold 테스트는 동적 인덱스 계산을 쓰므로 회귀 없음. 컬럼 순서 검증 테스트(test_diff_columns_ordered_by_period)도 회귀 없음.

전체 회귀: `uv run pytest -x -q`
</behavior>

<files>
- tests/test_smoke_end_to_end.py
</files>

<automated>
uv run pytest -x -q
</automated>

<verify>
전체 35+ GREEN.
</verify>
</task>

</tasks>

<acceptance>
- 전체 테스트 GREEN
- `uv run python main.py` 재실행 → Excel에서:
  - 기존 N, Q, T, W (EMA 값 컬럼) 숨김
  - 같은 위치 그룹 끝에 "ema11 추세" 등 4개 % 컬럼 가시
  - 양수→초록 굵게, 음수→빨강 굵게
- 다른 데이터/색/서식 회귀 없음
</acceptance>
