---
phase: 01-foundation-single-ticker
plan: 12
type: execute
wave: 5
gap_closure: true
depends_on: [01-07, 01-11]
files_modified:
  - src/stocksig/compute/ema.py
  - src/stocksig/output/sheet_per_ticker.py
  - src/stocksig/main_run.py
  - tests/test_ema.py
  - tests/test_smoke_end_to_end.py
autonomous: true
requirements:
  - SHEET-07
user_setup: []
tags: [compute, remove-dailychg, layout-shrink, gap-fix]

must_haves:
  truths:
    - "EMA_Close_N_dailychg 4개 컬럼 + 8개 (_median, _std) 모두 제거 — 총 12 컬럼 감소"
    - "trend 컬럼은 그대로 유지 (이번 변경 영향 없음)"
    - "총 데이터 컬럼: 80 → 68"
    - "main_run.py DATA_COLS에서 dailychg 4개 엔트리 제거"
    - "한국어 헤더에서 '일변동' 라벨 분기 제거 (또는 lookup에 더 이상 호출 없음)"
  artifacts:
    - path: "src/stocksig/compute/ema.py"
      provides: "add_ema_columns에서 dailychg 라인 제거"
    - path: "src/stocksig/output/sheet_per_ticker.py"
      provides: "build_column_layout에서 dailychg 그룹 제거"
---

<objective>
trend(%) 컬럼이 EMA 변화량의 더 직관적인 표현이라는 사용자 결정에 따라 가격 단위 dailychg 컬럼을 완전 제거. 레이아웃을 더 깔끔하게.
</objective>

<tasks>

<task id="1">
<title>compute/ema.py — dailychg 라인 제거</title>

<behavior>
add_ema_columns에서 다음 라인 제거:
```python
out[f"EMA_Close_{n}_dailychg"] = ema.diff()
```
trend 라인 (`out[f"EMA_Close_{n}_trend"] = ema.pct_change()`)은 유지.
</behavior>

<files>
- src/stocksig/compute/ema.py
</files>

<automated>
uv run python -c "from stocksig.compute.ema import add_ema_columns; import pandas as pd; df = pd.DataFrame({'Close': list(range(100, 120)), 'High': list(range(101, 121)), 'Low': list(range(99, 119)), 'Volume': [1000]*20}); out = add_ema_columns(df); dailychg = [c for c in out.columns if c.endswith('_dailychg')]; print('dailychg:', dailychg); assert dailychg == [], 'dailychg should be empty'; print('OK')"
</automated>

<verify>
출력 `dailychg: []` + `OK`.
</verify>
</task>

<task id="2">
<title>sheet_per_ticker.py — layout에서 dailychg 그룹 제거</title>

<behavior>
build_column_layout에서 `_EMA_PERIODS` 루프로 dailychg 추가하는 블록 전체 삭제. EMA_Close 그룹은 [val, median, std, trend] 4-tuple 그대로 유지.

korean_header에서 `_dailychg` suffix 처리 분기는 dead code이므로 제거(또는 그대로 두어도 무방하나 cleanliness를 위해 제거 권장).

column_num_format에서 dailychg 분기 제거. EMA_Close_N_trend는 percent_ratio (기존 그대로).

write 루프의 색 결정 분기에서 dailychg 관련 처리가 별도로 있다면 제거 (현재는 color_rules에서 sigma_bucket으로 처리됐을 수도 있음 — 코드 확인 후 적절히 정리).
</behavior>

<files>
- src/stocksig/output/sheet_per_ticker.py
</files>

<automated>
uv run python -c "from stocksig.output.sheet_per_ticker import build_column_layout; cols = build_column_layout(); print('count:', len(cols)); dailychg = [c for c in cols if 'dailychg' in c]; print('dailychg in layout:', dailychg); assert len(cols) == 68; assert dailychg == []; print('OK')"
</automated>

<verify>
출력 `count: 68`, `dailychg in layout: []`, `OK`.
</verify>
</task>

<task id="3">
<title>main_run.py — DATA_COLS에서 dailychg 엔트리 제거</title>

<behavior>
main_run.py의 `_build_data_cols()` (또는 DATA_COLS 상수)에서 EMA_Close_{N}_dailychg 4개 엔트리 제거. 결과 컬럼 수: 24 → 20 (4 OHLCV + 4 EMA_Close + 12 DIFF — dailychg 4 제거; trend는 stats 대상이 아닐 수 있음 — 현재 trend가 DATA_COLS에 포함되어 있는지 확인 후 일관성 유지).
</behavior>

<files>
- src/stocksig/main_run.py
</files>

<automated>
uv run python -c "from stocksig.main_run import _build_data_cols; cols = _build_data_cols(); print('count:', len(cols)); dailychg = [c for c in cols if 'dailychg' in c]; print('dailychg in DATA_COLS:', dailychg); assert dailychg == []; print('OK')"
</automated>

<verify>
출력 `dailychg in DATA_COLS: []` + `OK`.
</verify>
</task>

<task id="4">
<title>테스트 갱신 + 전체 회귀</title>

<behavior>
- tests/test_ema.py: 기존 `test_daily_change`를 다음으로 갱신:
  ```python
  def test_no_dailychg_columns():
      ...
      assert not any(c.endswith("_dailychg") for c in out.columns)
  ```
  (테스트 함수 이름 변경 OK, 또는 단순히 dailychg 부재 검증으로 의미 전환)

- tests/test_smoke_end_to_end.py: 컬럼 수 hard-coded 80 → 68로 갱신. dailychg 셀 주소 참조가 있으면 제거.

- 전체 회귀: `uv run pytest -x -q`
</behavior>

<files>
- tests/test_ema.py
- tests/test_smoke_end_to_end.py
</files>

<automated>
uv run pytest -x -q
</automated>

<verify>
전체 GREEN (이전 37개 - 일부 변경된 케이스 + 1 신규 검증).
</verify>
</task>

</tasks>

<acceptance>
- 전체 테스트 GREEN
- `uv run python main.py` 재실행 → Excel에서 68 컬럼, dailychg 흔적 없음, trend 4개 가시 유지
- 다른 데이터/색/서식 회귀 없음
</acceptance>
