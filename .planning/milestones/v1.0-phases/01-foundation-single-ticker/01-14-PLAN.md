---
phase: 01-foundation-single-ticker
plan: 14
type: execute
wave: 5
gap_closure: true
depends_on: [01-07, 01-11, 01-12, 01-13]
files_modified:
  - src/stocksig/compute/stats.py
  - src/stocksig/compute/indicators.py
  - src/stocksig/compute/color_rules.py
  - src/stocksig/compute/weekly.py
  - src/stocksig/compute/impulse.py
  - src/stocksig/output/writer.py
  - src/stocksig/output/sheet_per_ticker.py
  - src/stocksig/main_run.py
  - tests/test_stats.py
  - tests/test_indicators.py
  - tests/test_color_rules.py
  - tests/test_weekly.py
  - tests/test_impulse.py
  - tests/test_smoke_end_to_end.py
autonomous: true
requirements:
  - COMP-02
  - COMP-04
  - COMP-05
  - COMP-06
  - TECH-01
  - TECH-02
  - SHEET-07
  - COLOR-06
user_setup: []
tags: [big-update, rolling-window, weekly, macd, impulse, header-bg, layout]

must_haves:
  truths:
    - "일봉 OHLC(Close/High/Low) median/std는 rolling(200, min_periods=200) 기준"
    - "그 외 모든 sigma 컬럼(Close_pct_change, Volume_pct_change, DIFF 12개)은 expanding window 유지"
    - "Close_pct_change 색 = decide_sigma_bucket (이전 trend → σ), Close_pct_change_median/_std 신규 (hidden, %)"
    - "A1 ticker 셀: bold + 20pt Format"
    - "MACD-OSC(12, 26, 9) 일봉/주봉 — Line=EMA12(Close)-EMA26(Close), Signal=EMA9(Line), OSC=Line-Signal. 색=OSC.diff() 부호 (decide_trend_bucket 재사용)"
    - "주봉 데이터: pandas resample('W-FRI'){Close:last, High:max, Low:min, Volume:sum} 후 reindex(daily_index, method='ffill')로 5영업일 broadcast"
    - "주봉 컬럼: OHLC(med/std hidden, expanding), Close_pct_change(σ + med/std hidden), Volume(색=Volume_pct_change_week 부호), Volume_pct_change(σ + med/std hidden), MACD-OSC, Stoch %K/%D, RSI"
    - "임펄스: (일)EMA_Close_11_trend 부호 AND (일)MACD_OSC.diff() 부호 → 녹색/적색/청색. 주봉은 주봉 EMA11(내부 계산) + 주봉 MACD-OSC.diff()"
    - "임펄스 셀 출력 텍스트: '녹색', '적색', '청색' + 각각 글자색 #2E7D32/#C62828/#1565C0 + bg #C8E6C9/#FFCDD2/#BBDEFB + bold"
    - "헤더 배경색: DIFF EMA11 그룹 → #BDD7EE (바다색 60% 밝게), EMA22 → #F8CBAD (주황 60%), EMA96 → #E2EFDA (황록 60%), EMA192 → #E1BEE7 (자주 60%). 헤더 row 5만, data rows 영향 없음"
    - "(일)/(주) 접두사: OHLC, 종가 등락률, Volume, Volume 등락률, MACD-OSC, Stoch %K/%D, RSI, 임펄스 모두 적용. EMA·DIFF는 일봉 only이므로 접두사 없음 유지"
    - "B~J(일봉 OHLC) 헤더에 '(200일)' 접미사 추가"
    - "총 데이터 컬럼: 70 → 95"

artifacts_summary:
  - 신규 모듈: compute/weekly.py, compute/impulse.py
  - 신규 헬퍼: indicators.compute_macd_oscillator
  - color_rules: BLUE_100/BLUE_800 상수 + ImpulseBucket enum(GREEN/RED/BLUE) + decide_impulse(ema_trend, osc_diff)
  - sheet_per_ticker: 새 95-col layout, 헤더 bg 4구간 매핑, 임펄스 셀 분기, A1 폰트
  - writer.py: Format 캐시 확장 — header_bg(4 variants) + impulse(3 variants) + 'a1_title'
---

<objective>
사용자 26-05-22 일괄 수정/추가 사항 반영. 컨트랙트 변경(200일 rolling, σ 신호 재정의), 컬럼 +25개(주봉/MACD/임펄스), 헤더 배경색, A1 폰트, 임펄스 시스템.
</objective>

<tasks>

<task id="1">
<title>compute/stats.py — 200-day rolling 분기 + Close_pct_change σ 지원</title>

<behavior>
add_expanding_stats를 다음으로 분리/확장:
- 기존 expanding 동작은 유지 (DIFF, Volume_pct_change 용)
- 신규 `add_rolling_stats(df, cols, window=200)`: rolling(window, min_periods=window).median()/std() — daily OHLC 전용
- 또는 add_expanding_stats에 `mode='rolling'` 옵션 추가

main_run.py에서 OHLC(Close/High/Low)는 rolling(200) 호출, Close_pct_change/Volume_pct_change/DIFF는 기존 expanding 호출.

Close_pct_change_median, Close_pct_change_std 컬럼이 expanding stats로 생성되도록 DATA_COLS에 Close_pct_change 포함.
</behavior>

<files>
- src/stocksig/compute/stats.py
- src/stocksig/main_run.py
</files>

<automated>
uv run python -c "
import pandas as pd, numpy as np
from stocksig.compute.stats import add_rolling_stats, add_expanding_stats, add_pct_change_columns
df = pd.DataFrame({'Close': np.arange(250.0), 'High': np.arange(250.0)+1, 'Low': np.arange(250.0)-1, 'Volume': np.arange(250.0)*1000+1000})
df = add_pct_change_columns(df)
df = add_rolling_stats(df, ['Close','High','Low'], window=200)
df = add_expanding_stats(df, ['Close_pct_change','Volume_pct_change'])
print('Close_median NaN rows:', df['Close_median'].isna().sum())
assert df['Close_median'].isna().sum() == 199, 'rolling(200) should leave first 199 NaN'
assert 'Close_pct_change_median' in df.columns
print('OK')
"
</automated>

<verify>
출력에 `Close_median NaN rows: 199` + `OK`. rolling(200) 정확 검증.
</verify>
</task>

<task id="2">
<title>compute/indicators.py — MACD-OSC(12,26,9) 계산</title>

<behavior>
```python
def compute_macd_oscillator(close: pd.Series, fast: int = 12, slow: int = 26, signal: int = 9) -> pd.Series:
    ema_fast = close.ewm(span=fast, adjust=False).mean()
    ema_slow = close.ewm(span=slow, adjust=False).mean()
    macd_line = ema_fast - ema_slow
    signal_line = macd_line.ewm(span=signal, adjust=False).mean()
    return macd_line - signal_line
```

tests/test_indicators.py에 `test_macd_oscillator` 추가 — 알려진 입력에 대한 sanity check (값 자체보다 정상 finite + 길이 일치).
</behavior>

<files>
- src/stocksig/compute/indicators.py
- tests/test_indicators.py
</files>

<automated>
uv run pytest tests/test_indicators.py -x -q
</automated>

<verify>
신규 케이스 포함 GREEN.
</verify>
</task>

<task id="3">
<title>compute/weekly.py 신규 — W-FRI resample + forward fill</title>

<behavior>
```python
def compute_weekly(daily_df: pd.DataFrame) -> pd.DataFrame:
    """W-FRI 주봉 집계 후 daily_df.index로 forward fill (broadcast).
    
    Returns DataFrame with columns: Close_week, High_week, Low_week, Volume_week
    indexed by daily_df.index.
    """
    weekly = pd.DataFrame({
        'Close_week': daily_df['Close'].resample('W-FRI').last(),
        'High_week': daily_df['High'].resample('W-FRI').max(),
        'Low_week': daily_df['Low'].resample('W-FRI').min(),
        'Volume_week': daily_df['Volume'].resample('W-FRI').sum(),
    })
    # reindex to daily index with forward fill
    return weekly.reindex(daily_df.index, method='ffill')
```

신규 `tests/test_weekly.py`:
- 5영업일 입력 → 같은 주의 모든 daily row가 동일 주봉 값
- Volume_week = 5일 합산 검증
</behavior>

<files>
- src/stocksig/compute/weekly.py
- tests/test_weekly.py
</files>

<automated>
uv run pytest tests/test_weekly.py -x -q
</automated>

<verify>
신규 케이스 GREEN.
</verify>
</task>

<task id="4">
<title>compute/color_rules.py — BLUE 상수 + ImpulseBucket + decide_impulse</title>

<behavior>
```python
BLUE_100 = "#BBDEFB"
BLUE_800 = "#1565C0"
BLUE_900 = "#0D47A1"

class ImpulseBucket(str, Enum):
    GREEN = "녹색"
    RED = "적색"
    BLUE = "청색"
    DEFAULT = "DEFAULT"

def decide_impulse(ema11_trend: float | None, macd_osc_diff: float | None) -> ImpulseBucket:
    import math
    if any(v is None or (isinstance(v, float) and math.isnan(v)) for v in (ema11_trend, macd_osc_diff)):
        return ImpulseBucket.DEFAULT
    up_ema = ema11_trend > 0
    up_osc = macd_osc_diff > 0
    if up_ema and up_osc: return ImpulseBucket.GREEN
    if (not up_ema) and (not up_osc): return ImpulseBucket.RED
    return ImpulseBucket.BLUE
```

tests/test_color_rules.py에 `test_decide_impulse` 4 케이스 + NaN 케이스.
</behavior>

<files>
- src/stocksig/compute/color_rules.py
- tests/test_color_rules.py
</files>

<automated>
uv run pytest tests/test_color_rules.py -x -q
</automated>

<verify>
신규 케이스 5개 모두 GREEN.
</verify>
</task>

<task id="5">
<title>compute/impulse.py 신규 — 일봉/주봉 임펄스 컬럼 계산</title>

<behavior>
```python
def add_impulse_columns(df: pd.DataFrame) -> pd.DataFrame:
    """(일)임펄스, (주)임펄스 컬럼 추가.
    
    의존 컬럼: EMA_Close_11_trend (일봉), MACD_OSC, MACD_OSC_week,
              EMA_Close_11_week_trend (내부 계산), 
    """
    out = df.copy()
    # 일봉 임펄스
    osc_diff = out['MACD_OSC'].diff()
    out['Impulse_daily'] = [
        decide_impulse(t, d).value for t, d in zip(out['EMA_Close_11_trend'], osc_diff)
    ]
    # 주봉 EMA11 trend 내부 계산
    ema11_week = out['Close_week'].ewm(span=11, adjust=False).mean()
    ema11_week_trend = ema11_week.pct_change()
    osc_week_diff = out['MACD_OSC_week'].diff()
    out['Impulse_weekly'] = [
        decide_impulse(t, d).value for t, d in zip(ema11_week_trend, osc_week_diff)
    ]
    return out
```

신규 `tests/test_impulse.py`:
- 알려진 입력 → 4가지 결과 모두 검증
- NaN 처리
</behavior>

<files>
- src/stocksig/compute/impulse.py
- tests/test_impulse.py
</files>

<automated>
uv run pytest tests/test_impulse.py -x -q
</automated>

<verify>
GREEN.
</verify>
</task>

<task id="6">
<title>output/writer.py — Format 캐시 확장 (헤더 bg + 임펄스 + a1_title)</title>

<behavior>
make_workbook에 추가:

(a) **a1_title**: `{'bold': True, 'font_size': 20}` (1키)

(b) **header_bg 4 variants**: 
- `header_bg_ema11`: header_fmt + `bg_color='#BDD7EE'`
- `header_bg_ema22`: + `bg_color='#F8CBAD'`
- `header_bg_ema96`: + `bg_color='#E2EFDA'`
- `header_bg_ema192`: + `bg_color='#E1BEE7'`

(c) **impulse 3 variants**:
- `impulse_green`: `{'bold': True, 'font_color': '#2E7D32', 'bg_color': '#C8E6C9', 'align': 'center'}`
- `impulse_red`: `{'bold': True, 'font_color': '#C62828', 'bg_color': '#FFCDD2', 'align': 'center'}`
- `impulse_blue`: `{'bold': True, 'font_color': '#1565C0', 'bg_color': '#BBDEFB', 'align': 'center'}`

(d) **임펄스 default**: 텍스트 없을 때 일반 셀 — formats[ImpulseBucket.DEFAULT] = wb.add_format({'align':'center'})

기존 33키 + 1(a1_title) + 4(header_bg) + 4(impulse 3+default) = 42키.

writer 모듈에서 BLUE_100/800/900 import.
</behavior>

<files>
- src/stocksig/output/writer.py
</files>

<automated>
uv run python -c "
from stocksig.output.writer import make_workbook
import tempfile, os
p = os.path.join(tempfile.gettempdir(), '_t.xlsx')
wb, f = make_workbook(p)
print('keys:', len(f))
assert 'a1_title' in f
assert 'header_bg_ema11' in f
assert 'impulse_green' in f
wb.close(); os.remove(p)
print('OK')
"
</automated>

<verify>
출력 `keys: 42` + `OK`.
</verify>
</task>

<task id="7">
<title>output/sheet_per_ticker.py — 95-col layout + 헤더 + num_format + hidden 룰</title>

<behavior>
(a) `build_column_layout()` 새 순서 (총 95):
```
Date,
# 일봉 OHLC (200일 rolling)
Close, Close_median, Close_std,
High, High_median, High_std,
Low, Low_median, Low_std,
# 주봉 OHLC (expanding, forward-filled)
Close_week, Close_week_median, Close_week_std,
High_week, High_week_median, High_week_std,
Low_week, Low_week_median, Low_week_std,
# 일봉 종가 등락률 + 주봉
Close_pct_change, Close_pct_change_median, Close_pct_change_std,
Close_pct_change_week, Close_pct_change_week_median, Close_pct_change_week_std,
# Volume
Volume, Volume_week,
# Volume 등락률
Volume_pct_change, Volume_pct_change_median, Volume_pct_change_std,
Volume_pct_change_week, Volume_pct_change_week_median, Volume_pct_change_week_std,
# EMA_Close groups
for n in [11,22,96,192]:
    EMA_Close_{n}, EMA_Close_{n}_median, EMA_Close_{n}_std, EMA_Close_{n}_trend,
# DIFF blocks
for n in [11,22,96,192]:
    for price in [Close, High, Low]:
        DIFF_{price}_{n}, DIFF_{price}_{n}_median, DIFF_{price}_{n}_std,
# Stoch
Stoch_%K, Stoch_%D, Stoch_%K_week, Stoch_%D_week,
# RSI
RSI, RSI_week,
# MACD-OSC
MACD_OSC, MACD_OSC_week,
# Impulse
Impulse_daily, Impulse_weekly,
```

(b) `korean_header` 갱신 매핑 (선택 예시):
- Close → "(일)종가 (200일)"
- Close_median → "(일)종가 일별 중앙값 (200일)"
- Close_week → "(주)종가"
- Close_week_median → "(주)종가 일별 중앙값"
- Close_pct_change → "(일)종가 등락률"
- Close_pct_change_week → "(주)종가 등락률"
- Volume → "(일)거래량"
- Volume_week → "(주)거래량"
- Volume_pct_change → "(일)거래량 등락률"
- Volume_pct_change_week → "(주)거래량 등락률"
- Stoch_%K → "(일)Stoch %K", Stoch_%K_week → "(주)Stoch %K" (동일 패턴 %D)
- RSI → "(일)RSI", RSI_week → "(주)RSI"
- MACD_OSC → "(일)MACD-OSC", MACD_OSC_week → "(주)MACD-OSC"
- Impulse_daily → "(일)임펄스", Impulse_weekly → "(주)임펄스"
- EMA·DIFF·trend 기존 매핑 유지

(c) `column_num_format` 갱신:
- Volume, Volume_week → "volume"
- Close_pct_change*, Volume_pct_change*, EMA_Close_*_trend → "percent_ratio"
- Stoch_*, RSI*, MACD_OSC* → "percent_literal" (0~100 스케일 가정. MACD-OSC는 실제로 작은 가격 단위지만 % 표시는 명시 없음 — 기본 'price'로 두는 게 안전. 다시 결정: MACD_OSC → "price" 사용)
- Impulse_* → 'a1_title' 아닌 별도 fmt_type 없이 impulse Format 직접 사용 (column_num_format 분기 'impulse' 추가, 단 write 루프에서 impulse는 별도 처리)
- 그 외 가격 계열 → "price"

(d) `build_column_layout` Excel 헤더 bg 적용: write_sheet_for_ticker에서 row 5 헤더 작성 시 컬럼별로 적절한 header Format 선택:
- DIFF_*_11 블록 → header_bg_ema11
- DIFF_*_22 블록 → header_bg_ema22
- DIFF_*_96 블록 → header_bg_ema96
- DIFF_*_192 블록 → header_bg_ema192
- 그 외 → 기본 "header" (bold + center, bg 없음)

(e) hidden 룰: 기존 (`_median/_std` suffix + `^EMA_Close_\d+$`) 유지. 추가로 모든 `*_week_median`, `*_week_std`는 _median/_std suffix 룰에 자동 매칭. OK.

(f) A1 셀: `ws.write(0, 0, ticker, formats['a1_title'])` (bold 20pt).

(g) write 루프 색 분기 갱신:
- Close_pct_change*: σ 분기로 변경 (DIFF처럼 col_median/col_std 사용)
- Close_pct_change_week*: σ
- Volume → trend bucket (Volume_pct_change 부호로 변경)
- Volume_week → trend bucket (Volume_pct_change_week 부호)
- Volume_pct_change*: σ (기존 유지)
- Volume_pct_change_week*: σ
- MACD_OSC, MACD_OSC_week: trend bucket (자기 .diff() 부호) — diff는 row-level 접근 필요. write 루프에서 prev_row state 유지 또는 precomputed `MACD_OSC_diff` 컬럼 추가. 후자 선택: compute/indicators.py에서 osc_diff 컬럼도 같이 반환하거나 main_run.py에서 추가. 단순화 위해 main_run.py에서 `df['MACD_OSC_diff'] = df['MACD_OSC'].diff()` 추가 후 write 루프에서 참조.
- Stoch %K/%D, Stoch %K_week/%D_week, RSI, RSI_week → 기존 분기 적용. 주봉도 동일 임계.
- Impulse_daily, Impulse_weekly: 셀 값이 "녹색"/"적색"/"청색" 텍스트. fmt = formats['impulse_green'] etc.
- EMA trend는 기존 유지.

기존 trend 분기와 σ 분기는 새 컬럼명에 맞게 확장.
</behavior>

<files>
- src/stocksig/output/sheet_per_ticker.py
</files>

<automated>
uv run python -c "
from stocksig.output.sheet_per_ticker import build_column_layout, korean_header
cols = build_column_layout()
print('count:', len(cols))
assert len(cols) == 95
assert 'Impulse_daily' in cols
assert 'Impulse_weekly' in cols
assert 'MACD_OSC' in cols
assert 'RSI_week' in cols
assert 'Close_week' in cols
print('header Close:', korean_header('Close'))
print('header Close_week:', korean_header('Close_week'))
print('header MACD_OSC:', korean_header('MACD_OSC'))
print('header Impulse_daily:', korean_header('Impulse_daily'))
print('OK')
"
</automated>

<verify>
출력 `count: 95` + 한국어 헤더 정상 + `OK`.
</verify>
</task>

<task id="8">
<title>main_run.py — 신규 pipeline 통합</title>

<behavior>
```python
def run(...):
    ...
    raw = fetch_ohlcv(ticker)
    daily = add_pct_change_columns(raw)
    weekly_cols = compute_weekly(raw)
    daily = pd.concat([daily, weekly_cols], axis=1)
    # 주봉 pct_change
    daily['Close_pct_change_week'] = daily['Close_week'].pct_change()
    daily['Volume_pct_change_week'] = daily['Volume_week'].pct_change()
    # EMA + DIFF + trend (일봉 only)
    daily = add_ema_columns(daily)
    # MACD-OSC
    daily['MACD_OSC'] = compute_macd_oscillator(daily['Close'])
    daily['MACD_OSC_week'] = compute_macd_oscillator(daily['Close_week'])
    daily['MACD_OSC_diff'] = daily['MACD_OSC'].diff()
    daily['MACD_OSC_week_diff'] = daily['MACD_OSC_week'].diff()
    # Stoch + RSI 일/주
    daily['Stoch_%K'], daily['Stoch_%D'] = stoch_slow(...일봉 High/Low/Close)
    daily['Stoch_%K_week'], daily['Stoch_%D_week'] = stoch_slow(...주봉 High/Low/Close)
    daily['RSI'] = rsi_wilder(daily['Close'])
    daily['RSI_week'] = rsi_wilder(daily['Close_week'])
    # rolling vs expanding stats
    daily = add_rolling_stats(daily, ['Close','High','Low'], window=200)
    expanding_cols = [...Close_week, High_week, Low_week, Close_pct_change, Close_pct_change_week, 
                      Volume_pct_change, Volume_pct_change_week, ...DIFF 12개...]
    daily = add_expanding_stats(daily, expanding_cols)
    # Impulse
    daily = add_impulse_columns(daily)
    
    write_sheet_for_ticker(wb, formats, ticker, daily, scalars)
```

DATA_COLS는 더 이상 단일 리스트로 부족. 함수가 받는 인자를 명시적 cols list로 받게 분리.

scalars 계산: 3행 누적 median, 4행 누적 std. 일봉 OHLC는 200일 rolling이므로 누적이 아님 — 마지막 값(가장 최근의 200일 rolling median/std) 사용. 다른 컬럼들은 expanding이므로 .iloc[-1] 그대로.
</behavior>

<files>
- src/stocksig/main_run.py
</files>

<automated>
uv run python -c "from stocksig.main_run import _build_data_cols; print(len(_build_data_cols()))"
</automated>

<verify>
출력이 합리적 (>20).
</verify>
</task>

<task id="9">
<title>smoke test 전체 갱신 + 신규 검증</title>

<behavior>
tests/test_smoke_end_to_end.py 갱신:
- 컬럼 수 70 → 95
- 셀 주소가 하드코딩된 곳: `build_column_layout()` + `get_column_letter` 동적 계산으로 통일
- 기존 테스트 회귀 확인 (DIFF / EMA trend / Stoch / RSI / freeze / bold / hidden)

신규 테스트:
- `test_a1_bold_20pt`: A1 폰트 size=20 + bold
- `test_ohlc_rolling_200`: B 컬럼(Close)에 대해 6행 (가장 최근) Close_median이 그 시점 기준 직전 200일 rolling median과 일치 (mock 데이터 250 rows)
- `test_close_pct_change_sigma_colored`: 양수 행이 sigma bucket 색을 받음 (이전 trend bucket이 아닌 σ bucket)
- `test_weekly_columns_present_and_ffilled`: Close_week 5영업일 동일 값 검증
- `test_macd_osc_columns`: MACD_OSC와 MACD_OSC_week 존재 + finite
- `test_macd_osc_color_by_diff_sign`: MACD_OSC > 직전 행이면 green 색
- `test_impulse_cells`: Impulse_daily 셀이 "녹색"/"적색"/"청색" 중 하나 + 글자 bold + bg 일치
- `test_diff_block_header_bg`: DIFF_Close_11 헤더 위치의 fill color가 #BDD7EE 같은 4가지 중 하나에 매치
- 일/주 Stoch/RSI 컬럼 존재 검증

전체 회귀: `uv run pytest -x -q`
</behavior>

<files>
- tests/test_smoke_end_to_end.py
</files>

<automated>
uv run pytest -x -q
</automated>

<verify>
전체 GREEN. 신규 8+ 케이스 포함.
</verify>
</task>

</tasks>

<acceptance>
- 전체 테스트 GREEN
- `uv run python main.py` 재실행 → Excel에서:
  - 95 컬럼 (가시: 약 35+)
  - A1 = 티커 (20pt bold)
  - B~J 헤더 "(200일)" 접미사
  - 주봉 OHLC, Close 등락률, Volume, Volume 등락률, MACD-OSC, Stoch, RSI 컬럼 가시
  - DIFF 4그룹 헤더 행 배경색 4가지
  - 임펄스 2컬럼에 "녹색"/"적색"/"청색" 텍스트 + 일치 색 + bold
  - MACD-OSC 색: 전일 대비 상승=초록, 하락=빨강
- 기존 EMA trend, Stoch, RSI, freeze panes, hidden median/std 회귀 없음
</acceptance>
