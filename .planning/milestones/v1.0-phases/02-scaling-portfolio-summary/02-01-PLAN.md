---
phase: 02-scaling-portfolio-summary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - src/stocksig/io/cache.py
  - src/stocksig/io/throttle.py
  - src/stocksig/io/market_kind.py
  - tests/test_cache.py
  - tests/test_throttle.py
  - tests/test_market_kind.py
autonomous: true
requirements:
  - MKTD-05
  - EXEC-03
user_setup: []
tags:
  - python
  - sqlite
  - rate-limit
  - caching

must_haves:
  truths:
    - "`uv add diskcache pyrate-limiter` succeeds and lockfile updated"
    - ".cache/ is gitignored so OHLCV pickles never enter the repo"
    - "`io.cache.get_ohlcv(t)` returns None on miss and the same DataFrame on hit within 24h"
    - "`io.throttle.throttled_yahoo` blocks calls so that effective rate ≤ 2 req/s"
    - "`io.market_kind.classify_market` returns 'KR' for `.KS`/`.KQ` suffixes, 'US' otherwise"
  artifacts:
    - path: src/stocksig/io/cache.py
      provides: "OHLCV disk cache with 24h TTL, key=(ticker, today_iso)"
      exports: ["get_ohlcv", "put_ohlcv", "make_key"]
    - path: src/stocksig/io/throttle.py
      provides: "Yahoo token bucket (2 req/s) decorator"
      exports: ["throttled_yahoo"]
    - path: src/stocksig/io/market_kind.py
      provides: "Suffix-based US/KR classifier"
      exports: ["classify_market", "KR_SUFFIXES"]
  key_links:
    - from: pyproject.toml
      to: diskcache, pyrate-limiter
      via: "[project.dependencies]"
      pattern: "diskcache.*pyrate-limiter|pyrate-limiter.*diskcache"
    - from: .gitignore
      to: .cache/
      via: "ignore pattern"
      pattern: "^\\.cache/?"
---

<objective>
Phase 2 Wave 1 — install infrastructure dependencies and create three small pure modules
(`io/cache.py`, `io/throttle.py`, `io/market_kind.py`) that the runner and the extended
`io/market.py` will compose in Wave 2. No business logic wiring yet. All three modules
must be import-clean and unit-tested in isolation so that downstream waves can rely on
them without surprises.

Purpose: De-risk new deps (diskcache, pyrate-limiter) early, lock the cache key format and
Yahoo rate (2 RPS per D-04), establish `.cache/` as gitignored.

Output: Updated `pyproject.toml` + `uv.lock`, `.gitignore`, 3 new modules, 3 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scaling-portfolio-summary/RESEARCH.md
@.planning/phases/02-scaling-portfolio-summary/02-CONTEXT.md
@.planning/phases/02-scaling-portfolio-summary/02-VALIDATION.md
@src/stocksig/io/market.py
@pyproject.toml
@.gitignore

<interfaces>
<!-- All three modules are NEW. Contracts to honor: -->

io/cache.py exports (consumed by io/market.py in Wave 2):
  make_key(ticker: str, today: date | None = None) -> str       # "AAPL|2026-05-26"
  get_ohlcv(ticker: str) -> pandas.DataFrame | None             # None on miss; logs HIT/MISS in Korean
  put_ohlcv(ticker: str, df: pandas.DataFrame) -> None          # expire=86400 (24h)

io/throttle.py exports (consumed by io/market.py in Wave 2):
  throttled_yahoo(fn) -> fn                                     # decorator; blocks via pyrate_limiter

io/market_kind.py exports (consumed by sheet_portfolio.py + runner.py in later waves):
  KR_SUFFIXES: tuple[str, ...] = (".KS", ".KQ", ".KOSDAQ", ".KOSPI")
  classify_market(symbol: str) -> Literal["US", "KR"]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deps + gitignore + cache module</name>
  <files>pyproject.toml, .gitignore, src/stocksig/io/cache.py, tests/test_cache.py</files>
  <behavior>
    - test_cache_dir_created: importing `io.cache` and calling `_get_cache()` creates `.cache/ohlcv/`
    - test_put_then_get_roundtrip: `put_ohlcv("AAPL", df)` then `get_ohlcv("AAPL")` returns equal DataFrame
    - test_miss_returns_none: `get_ohlcv("DOES_NOT_EXIST")` returns None
    - test_hit_within_ttl: with freezegun freezing 23h59m forward, value still retrievable
    - test_miss_after_24h: at 24h01m forward, get returns None (expired)
    - test_make_key_format: `make_key("AAPL", date(2026,5,26)) == "AAPL|2026-05-26"`
    - test_cache_logs_hit_miss_korean: caplog at INFO contains "cache HIT" / "cache MISS" with ticker
  </behavior>
  <action>
    1. Add deps via `uv add diskcache>=5.6 pyrate-limiter>=3` (per D-07). Verify both appear in `pyproject.toml` `[project] dependencies` and `uv.lock`.
    2. Append to `.gitignore`: `.cache/` (single line, ensure trailing newline). Per D-05.
    3. Create `src/stocksig/io/cache.py` per RESEARCH Pattern 2:
       - Module logger `logging.getLogger(__name__)`
       - Constants: `_DEFAULT_DIR = Path(".cache/ohlcv")`, `_TTL_SECONDS = 24 * 60 * 60`
       - Module-level `_cache: Cache | None = None` with lazy `_get_cache()` initializer that calls `_DEFAULT_DIR.mkdir(parents=True, exist_ok=True)` before constructing `diskcache.Cache(str(_DEFAULT_DIR))`
       - `make_key(ticker, today=None)`: returns `f"{ticker}|{(today or date.today()).isoformat()}"`
       - `get_ohlcv(ticker)`: returns `_get_cache().get(make_key(ticker))`; logs `logger.info("%s | cache HIT (key=%s)", ticker, key)` on non-None, else `cache MISS` (Korean phrasing optional but the literal substrings "cache HIT" / "cache MISS" are required by validation map MKTD-05). Add Korean prefix: `"%s | 캐시 HIT (cache HIT, key=%s)"` so both Korean and English substrings appear.
       - `put_ohlcv(ticker, df)`: `_get_cache().set(make_key(ticker), df, expire=_TTL_SECONDS)`
    4. Create `tests/test_cache.py`. Use `tmp_path` + monkeypatch on `cache._DEFAULT_DIR` to redirect to `tmp_path / "ohlcv"`; reset `cache._cache = None` between tests via fixture. Use `freezegun.freeze_time` (add `freezegun` to dev deps if not present — `uv add --dev freezegun`).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -x -q</automated>
  </verify>
  <done>All 7 test cases pass. `uv pip show diskcache` and `uv pip show pyrate-limiter` both return non-empty. `.gitignore` contains `.cache/`. `grep -n "^\.cache" .gitignore` returns one line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Throttle module + market_kind classifier</name>
  <files>src/stocksig/io/throttle.py, src/stocksig/io/market_kind.py, tests/test_throttle.py, tests/test_market_kind.py</files>
  <behavior>
    throttle:
      - test_decorator_preserves_return: wrapped fn returns original value
      - test_decorator_preserves_signature: `functools.wraps` applied (`wrapper.__name__ == "fetch_ohlcv"`)
      - test_yahoo_2rps: 10 sequential calls to a `throttled_yahoo`-wrapped no-op take ≥ 4.5s (10 calls @ 2 RPS ≈ 4.5–5.5s wall clock); assert `elapsed >= 4.5`. Use `time.monotonic()` and tolerate jitter. Per D-04.
    market_kind:
      - test_us_default: `classify_market("AAPL") == "US"`
      - test_kr_ks: `classify_market("005930.KS") == "KR"`
      - test_kr_kq: `classify_market("035720.KQ") == "KR"`
      - test_kr_aliases: `.KOSPI`, `.KOSDAQ` suffixes → "KR"
      - test_case_insensitive: `"aapl"` → "US", `"005930.ks"` → "KR"
      - test_no_match_unknown_suffix: `"7203.T"` → "US" (Japanese suffix not in KR list — per L-14 only `.KS`/`.KQ` family supported; everything else falls through to US default. Document this limitation in a module docstring.)
  </behavior>
  <action>
    1. Create `src/stocksig/io/throttle.py` per RESEARCH Pattern 3:
       - Imports: `from pyrate_limiter import Duration, Rate, Limiter`, `from functools import wraps`
       - `_YAHOO_RATE = Rate(2, Duration.SECOND)` per D-04
       - `_yahoo_limiter = Limiter(_YAHOO_RATE)` (module-level singleton)
       - `def throttled_yahoo(fn)`: decorator using `@wraps(fn)`; inside wrapper call `_yahoo_limiter.try_acquire("yahoo")` (this blocks until a token is available) then return `fn(*args, **kwargs)`.
       - Add module docstring noting EDGAR/DART limiters will be added in Phase 3.
    2. Create `src/stocksig/io/market_kind.py` per RESEARCH Pattern 6:
       - `KR_SUFFIXES: tuple[str, ...] = (".KS", ".KQ", ".KOSDAQ", ".KOSPI")` per L-14
       - `classify_market(symbol: str) -> str`: uppercase the input, iterate `KR_SUFFIXES`, return "KR" on match, else "US". Type annotate return as `Literal["US","KR"]` (import from `typing`).
    3. Tests use real wall clock — keep test count minimal so suite stays under 6 seconds. If the 2 RPS test is flaky on slow CI, allow `>= 4.0` floor.
  </action>
  <verify>
    <automated>uv run pytest tests/test_throttle.py tests/test_market_kind.py -x -q</automated>
  </verify>
  <done>All tests pass. Both modules importable: `python -c "from stocksig.io.throttle import throttled_yahoo; from stocksig.io.market_kind import classify_market; print(classify_market('005930.KS'))"` prints `KR`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local FS → cache | `.cache/ohlcv` pickle store; only this process writes |
| pypi → local | new packages `diskcache`, `pyrate-limiter` installed via `uv add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | diskcache pickle deserialization | accept | Cache file lives under user-owned `.cache/`, no shared access; pickle from self only |
| T-02-02 | Information Disclosure | OHLCV cached on disk | accept | Public market data; no PII |
| T-02-SC | Tampering | npm/pip installs (diskcache, pyrate-limiter) | mitigate | T-02-SC handled by Wave-0 blocking checkpoint (see Plan 02-01 install step). Both packages [ASSUMED] per RESEARCH audit; verify on PyPI before install |
</threat_model>

<verification>
- `uv pip show diskcache pyrate-limiter` both non-empty
- `grep -c "^\.cache" .gitignore` == 1
- `uv run pytest tests/test_cache.py tests/test_throttle.py tests/test_market_kind.py -q` → all green
- `uv run python -c "from stocksig.io import cache, throttle, market_kind"` exits 0
</verification>

<success_criteria>
- Two new packages locked into `pyproject.toml` / `uv.lock`
- `.cache/` gitignored
- 3 new modules + 3 new test files, all tests green
- No edits to `io/market.py`, `main_run.py`, `sheet_per_ticker.py`, `writer.py` (Wave 2+ work)
</success_criteria>

<output>
Create `.planning/phases/02-scaling-portfolio-summary/02-01-SUMMARY.md` when done.
</output>
