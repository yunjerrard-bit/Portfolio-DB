---
phase: 02-scaling-portfolio-summary
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/stocksig/main_run.py
  - tests/test_smoke_n_tickers.py
  - tests/test_smoke_end_to_end.py
autonomous: true
requirements:
  - INPUT-04
  - MKTD-04
  - MKTD-05
  - MKTD-06
  - PORT-01
  - PORT-02
  - PORT-08
  - EXEC-03
  - EXEC-05
user_setup: []
tags:
  - python
  - orchestration
  - integration

must_haves:
  truths:
    - "main_run.run() is 2-pass: PASS 1 = parallel fetch+compute (via runner.run_all), PASS 2 = write 시트1 FIRST then per-ticker sheets in input order"
    - "Generated workbook has 시트1 (포트폴리오) as `sheetnames[0]` (PORT-01)"
    - "Each successful ticker also gets its own sheet (Phase 1 `write_sheet_for_ticker` unchanged)"
    - "Failed tickers do NOT get per-ticker sheets but DO get a row in 시트1 (D-03)"
    - "Same-day re-run hits cache (cache HIT log lines for every ticker)"
    - "100-ticker smoke (mocked pipeline) completes without per-thread races"
  artifacts:
    - path: src/stocksig/main_run.py
      provides: "2-pass orchestrator using read_tickers_extended + run_all + write_portfolio_sheet"
      exports: ["run"]
    - path: tests/test_smoke_n_tickers.py
      provides: "10+ ticker end-to-end smoke with mocked yfinance"
  key_links:
    - from: src/stocksig/main_run.py
      to: src/stocksig/runner.py
      via: "run_all(specs, classify_market, pipeline)"
      pattern: "run_all\\("
    - from: src/stocksig/main_run.py
      to: src/stocksig/output/sheet_portfolio.py
      via: "write_portfolio_sheet(wb, formats, results, failures, input_order)"
      pattern: "write_portfolio_sheet"
    - from: src/stocksig/main_run.py
      to: src/stocksig/io/market.py
      via: "fetch_ohlcv_cached"
      pattern: "fetch_ohlcv_cached"
---

<objective>
Wave 4 — refactor `main_run.run()` into a 2-pass orchestrator. The Phase 1 sequential
fetch-compute-write loop is replaced by:

PASS 1: build a `pipeline(symbol) -> enriched_df` callable that combines
`fetch_ohlcv_cached` + the existing Phase 1 compute chain, then hand it to
`runner.run_all(specs, classify_market, pipeline)` for parallel execution with isolation.

PASS 2: open the workbook, add 시트1 FIRST via `write_portfolio_sheet(...)`, then iterate
results in `input_order` calling Phase 1 `write_sheet_for_ticker` to produce per-ticker
sheets. This guarantees `sheetnames[0] == "시트1"` (PORT-01, RESEARCH L-05).

Purpose: First moment Phase 2 is end-to-end runnable. Smoke tests prove
sheet ordering, failure routing, cache hits, and parallel safety.

Output: Refactored `main_run.py`, new smoke test, updated existing smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-scaling-portfolio-summary/RESEARCH.md
@.planning/phases/02-scaling-portfolio-summary/02-CONTEXT.md
@.planning/phases/02-scaling-portfolio-summary/02-VALIDATION.md
@src/stocksig/main_run.py
@src/stocksig/runner.py
@src/stocksig/io/market.py
@src/stocksig/io/input.py
@src/stocksig/io/market_kind.py
@src/stocksig/output/sheet_portfolio.py
@src/stocksig/output/sheet_per_ticker.py
@src/stocksig/output/writer.py
@tests/test_smoke_end_to_end.py

<interfaces>
<!-- All needed exports already exist from prior plans + Phase 1: -->
read_tickers_extended(path) -> list[TickerSpec]
classify_market(symbol) -> "US" | "KR"
fetch_ohlcv_cached(ticker) -> pd.DataFrame
run_all(specs, classify_market, pipeline) -> (results, failures)
make_workbook(path) -> (wb, formats)
write_portfolio_sheet(wb, formats, results, failures, input_order, now=None) -> None
write_sheet_for_ticker(wb, formats, ticker, df, scalars) -> None   # Phase 1, unchanged

<!-- Phase 1 compute chain (must be wrapped into `pipeline`): -->
See main_run.py current run() body lines 110-186 (steps 1-10 building enriched_df + scalars).
This becomes a helper `_compute_enriched(raw_df) -> (enriched_df, scalars)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor main_run.py to 2-pass</name>
  <files>src/stocksig/main_run.py</files>
  <action>
    Rewrite `run()` in `src/stocksig/main_run.py` per RESEARCH Pattern 7. Keep all existing Phase 1 imports.

    1. New imports (top):
       ```
       from stocksig.io.input import read_tickers_extended, TickerSpec
       from stocksig.io.market import fetch_ohlcv_cached
       from stocksig.io.market_kind import classify_market
       from stocksig.runner import run_all, TickerResult, TickerFailure
       from stocksig.output.sheet_portfolio import write_portfolio_sheet
       ```
       Remove the now-unused direct `fetch_ohlcv` import (still used via market module if smoke tests import it; check and adjust).

    2. Extract the Phase 1 per-ticker compute chain into a module-private helper:
       ```
       def _compute_enriched(raw: pd.DataFrame) -> tuple[pd.DataFrame, dict[str, dict[str, float]]]:
           # ... move current body of the for-loop from lines 113-183 here,
           # operating on `raw` (input) and returning (df, scalars)
       ```
       This helper contains: add_pct_change_columns, compute_weekly, week pct_change, add_ema_columns,
       compute_macd_oscillator (daily+weekly+diffs), stoch_slow (daily+weekly), rsi_wilder (daily+weekly),
       add_rolling_stats (DAILY_OHLC, 200), add_expanding_stats (_EXPANDING_COLS), add_impulse_columns,
       and the scalars dict building (current lines 165-182).

    3. New `_make_pipeline()`:
       ```
       def _make_pipeline() -> Callable[[str], pd.DataFrame]:
           def pipeline(symbol: str) -> pd.DataFrame:
               raw = fetch_ohlcv_cached(symbol)
               enriched, _scalars = _compute_enriched(raw)
               # Stash scalars on the DataFrame so PASS 2 can retrieve without recomputing.
               enriched.attrs["scalars"] = _scalars
               return enriched
           return pipeline
       ```

    4. New `run()` body:
       ```
       def run(tickers_path="tickers.txt", env_path=None, output_dir="output") -> Path:
           load_env(env_path)
           specs = read_tickers_extended(tickers_path)
           logger.info("main | 티커 %d개 로드 완료", len(specs))

           # PASS 1: fan-out
           pipeline = _make_pipeline()
           results, failures = run_all(specs, classify_market, pipeline)

           # PASS 2: write
           out_dir = Path(output_dir); out_dir.mkdir(parents=True, exist_ok=True)
           output_path = out_dir / f"portfolio_{date.today():%Y%m%d}.xlsx"
           wb, formats = make_workbook(output_path)
           input_order = [s.symbol for s in specs]
           try:
               # ★ 시트1 먼저 (PORT-01 / RESEARCH L-05)
               write_portfolio_sheet(wb, formats, results, failures, input_order)
               # 그 후 각 티커 시트 (입력 순서)
               by_symbol = {r.spec.symbol: r for r in results}
               for sym in input_order:
                   res = by_symbol.get(sym)
                   if res is None:
                       continue
                   scalars = res.enriched_df.attrs.get("scalars", {})
                   write_sheet_for_ticker(wb, formats, res.spec.symbol, res.enriched_df, scalars)
           finally:
               wb.close()

           logger.info("main | 워크북 저장: %s", output_path)
           if failures:
               logger.warning("실패 %d개 — 시트1에 표시됨", len(failures))
           return output_path
       ```

    5. Update logger format usage to keep `[k/N] OK` lines coming from `runner.run_all` (no duplication).

    6. Add `from typing import Callable` import if missing.
  </action>
  <verify>
    <automated>uv run python -c "from stocksig.main_run import run; print('import ok')"</automated>
  </verify>
  <done>main_run.py imports cleanly. Function signatures unchanged externally (still `run(tickers_path, env_path, output_dir)`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Smoke test — 10-ticker end-to-end with mocked yfinance</name>
  <files>tests/test_smoke_n_tickers.py, tests/test_smoke_end_to_end.py</files>
  <behavior>
    All tests use `tmp_path` + monkeypatch on `stocksig.io.market.fetch_ohlcv` to return a synthetic 2500-row DataFrame. Also monkeypatch `stocksig.io.cache._DEFAULT_DIR` to `tmp_path / ".cache" / "ohlcv"` and reset `cache._cache = None`.

    test_smoke_n_tickers.py:
    - test_portfolio_is_first_sheet: tickers.txt with 3 US + 2 KR tickers → run() → `openpyxl.load_workbook(path).sheetnames[0] == "시트1"` (PORT-01)
    - test_all_tickers_have_sheets: workbook has 1 + 5 = 6 sheets in expected order (시트1, AAPL, MSFT, GOOG, 005930.KS, 035720.KQ)
    - test_failed_ticker_in_sheet1_only: tickers.txt with 2 US + 1 invalid (pipeline raises for `BAD`) → workbook has 시트1 + 2 ticker sheets (no `BAD` sheet); 시트1 contains a failure row for `BAD` with `"실패:"` prefix
    - test_partial_data_marked_failure: pipeline returns 1000-row DataFrame for one ticker → that ticker ends up in 시트1 failure row with `"부분 데이터:"` (D-06)
    - test_cache_hit_on_second_run: call `run()` twice; assert 2nd run's caplog contains `"cache HIT"` for every ticker AND that `fetch_ohlcv` (stub) was called only N times total (not 2N)
    - test_10_tickers_completes: 10 tickers → run completes, log line `"총 10 티커 중 성공 10 / 실패 0"` present
    - test_input_order_preserved: tickers in file [C,A,B] → 시트 순서 = [시트1, C, A, B]

    test_smoke_end_to_end.py: existing Phase 1 single-ticker smoke must still pass. Update only if the smoke uses `read_tickers` legacy signature — it should keep working via back-compat shim.
  </behavior>
  <action>
    Create `tests/test_smoke_n_tickers.py` per behavior block. Make a fixture `mock_pipeline_env` that:
    - monkeypatches `stocksig.io.market.fetch_ohlcv` to return a synthetic DataFrame (use a helper that builds a date-indexed OHLCV with `pd.date_range(end=date.today(), periods=rows, freq='B')` and random-ish Close/High/Low/Volume from a fixed seed)
    - patches `cache._DEFAULT_DIR` to `tmp_path / ".cache/ohlcv"` and `cache._cache = None`
    - writes `tickers.txt` to `tmp_path / "tickers.txt"` per test

    For tests asserting on log content, use `caplog.set_level(logging.INFO)`.

    Update `tests/test_smoke_end_to_end.py` only if it directly imports `read_tickers` and asserts on its return type (should now be `list[str]` — same as Phase 1, no change needed). Run the existing smoke once to confirm no regression.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke_n_tickers.py tests/test_smoke_end_to_end.py -x -q</automated>
  </verify>
  <done>All 7 new smoke tests + Phase 1 smoke still green. Full suite: `uv run pytest -q` → green, count = prior 59 + new tests from 02-01/02/03/04 (≥ 90 expected).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline thread → main thread | results crossed back via Future; exceptions caught in runner |
| compute chain → xlsxwriter | enriched_df may carry NaN; writer must `pd.isna` guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | shared curl_cffi `_SESSION` under 4 threads | accept | yfinance docs + curl_cffi share single session per Phase 1 design; PITFALLS only flags thread session DUPLICATION as risk |
| T-02-11 | DoS | one slow ticker holds the whole run | mitigate | `as_completed` returns as each future finishes; logging keeps user informed of progress |
| T-02-12 | Tampering | `enriched.attrs["scalars"]` survives DataFrame copy? | mitigate | pandas `attrs` is preserved through indexing in 2.x; smoke test asserts scalars round-trip on a copied df |
</threat_model>

<verification>
- Full suite green: `uv run pytest -q`
- Mock-based 10-ticker smoke completes < 30s wall time
- Generated workbook (during smoke) has 시트1 first
</verification>

<success_criteria>
- `main_run.run()` is 2-pass; PORT-01 verified
- Failed tickers visible only on 시트1 (D-03)
- Cache hit on same-day re-run (MKTD-05)
- Partial-data tickers routed to failure (D-06)
</success_criteria>

<output>
Create `.planning/phases/02-scaling-portfolio-summary/02-04-SUMMARY.md` when done.
</output>
