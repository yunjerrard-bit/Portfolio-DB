---
phase: 02-scaling-portfolio-summary
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified: []
autonomous: false
requirements:
  - PORT-01
  - PORT-02
  - PORT-07
  - PORT-08
  - MKTD-05
  - MKTD-04
  - INPUT-04
  - EXEC-03
  - EXEC-05
user_setup: []
tags:
  - manual-verify
  - smoke

must_haves:
  truths:
    - "Real (non-mocked) end-to-end run with 10 mixed US/KR tickers produces a workbook the user accepts"
    - "User confirms 시트1 displays correctly: ordering, hyperlinks, colors, timestamps, failure rows"
    - "Same-day re-run shows console `cache HIT` lines"
    - "100-ticker stress (if user has the list) completes without 429/403"
  artifacts: []
  key_links: []
---

<objective>
Wave 5 — manual user verification of Phase 2 end-to-end behavior against the 8 manual
checkpoints in `02-VALIDATION.md` §"Manual-Only". This is a checkpoint plan: Claude has
no autonomous work to do; it presents the verification protocol and waits for the user.

Purpose: Catch issues that automated tests cannot — color tone, hyperlink click feel,
real Yahoo Finance rate-limit behavior at 100-ticker scale, cache hit observability in
real console output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-scaling-portfolio-summary/02-VALIDATION.md
@.planning/phases/02-scaling-portfolio-summary/02-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual verification — 8 points from 02-VALIDATION.md</name>
  <what-built>
    Phase 2 code complete (plans 02-01 ~ 02-04):
    - New deps: diskcache, pyrate-limiter
    - New modules: io/cache.py, io/throttle.py, io/market_kind.py, runner.py, output/sheet_portfolio.py
    - Extended: io/input.py (TickerSpec + tab/whitespace + back-compat), io/market.py (fetch_ohlcv_cached), main_run.py (2-pass)
    - Format cache: 42 → 44 keys
    - Test suite: 59 + new ≥ 90 passing
  </what-built>
  <how-to-verify>
    Perform the 8 checks from `02-VALIDATION.md` §"Manual-Only" in order:

    1. **Mixed 10-ticker run.** Edit `tickers.txt` with 5 US + 5 KR tickers (one per line; optionally add tier/industry separated by tab or space):
       ```
       # Phase 2 manual verify
       AAPL\t1\tTechnology
       MSFT\t1\tTechnology
       GOOG\t2\tTechnology
       AMZN\t2\tConsumer
       NVDA\t1\tSemiconductor
       005930.KS\t1\t반도체
       035720.KQ\t2\tInternet
       000660.KS\t1\t반도체
       051910.KS\t2\t화학
       005380.KS\t2\t자동차
       ```
       Then run `uv run python main.py`.
       Expected: `output/portfolio_YYYYMMDD.xlsx` is created. Console shows `[k/10] OK <ticker>` lines.

    2. **시트1 first + timestamp.** Open the .xlsx in Excel. The leftmost sheet tab is "시트1" and cell A1 reads `실행 시각: YYYY-MM-DD HH:MM:SS` matching the run time.

    3. **Hyperlinks.** Click each ticker cell in 시트1 column A (rows 6+). Each click should jump to the per-ticker sheet's A1 cell.

    4. **DIFF color consistency (3-ticker sample).** Pick 3 tickers. For each, compare 시트1's DIFF EMA11/22/96/192 cell colors (columns G..J) against the per-ticker sheet's DIFF_Close_11/22/96/192 cells in the latest data row. Colors must match.

    5. **Impulse color consistency.** For the same 3 tickers, compare 시트1 columns N/O (일/주 임펄스) against the per-ticker sheet's Impulse_daily/weekly latest-row cells. Text + bg + font color must match.

    6. **Cache hit.** Immediately re-run `uv run python main.py` (same day). Console output should contain `cache HIT` or `캐시 HIT` lines for every ticker. Wall-clock time should be significantly shorter than run 1.

    7. **Invalid ticker isolation.** Append `ZZZZZZ` (no real Yahoo listing) to `tickers.txt`. Run. Console shows a Korean WARNING line `[k/N] FAIL ZZZZZZ | ...`. Open the xlsx: 시트1 has a final row with `ZZZZZZ | ? | ... | 실패: ...` in pastel red italic. The other tickers were unaffected.

    8. **100-ticker stress (only if user has the list).** Run on a 100-ticker tickers.txt. Expected: completes within ~5 minutes (cached run faster), no `429` or `403` errors in console, final summary `총 100 티커 중 성공 X / 실패 Y` matches your expectation.

    For each point above, type `PASS-{n}` or `FAIL-{n}: <description>`.
  </how-to-verify>
  <resume-signal>Reply `approved` (all 8 PASS) or list failing point numbers with descriptions, e.g. `FAIL-4: AAPL DIFF EMA11 color mismatch — 시트1 green, per-ticker red`</resume-signal>
</task>

</tasks>

<verification>
- User signs off all 8 manual points
- Any FAIL routes to a `--gaps` plan-phase invocation
</verification>

<success_criteria>
- Phase 2 marked complete in ROADMAP.md
- All 14 phase 2 requirement IDs marked Done in REQUIREMENTS.md traceability table
</success_criteria>

<output>
Create `.planning/phases/02-scaling-portfolio-summary/02-05-SUMMARY.md` recording the user's PASS/FAIL responses.
</output>
