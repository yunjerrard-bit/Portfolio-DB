---
phase: 03-edgar-dart-yfinance-naver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/stocksig/io/throttle.py
  - src/stocksig/io/cache.py
  - tests/test_throttle.py
  - tests/test_cache.py
autonomous: true
requirements: [FUND-04, FUND-06]
must_haves:
  truths:
    - "EDGAR 호출은 초당 8회, DART 호출은 초당 2회를 넘지 않도록 토큰버킷이 강제한다"
    - "같은 분기 내 두 번째 펀더멘털 조회는 7일 TTL sqlite 캐시에서 HIT한다"
    - "펀더멘털 캐시 디렉터리(.cache/fundamentals)는 기존 OHLCV 캐시(.cache/ohlcv)와 분리된다"
  artifacts:
    - path: "src/stocksig/io/throttle.py"
      provides: "throttled_edgar (8 RPS), throttled_dart (2 RPS) 데코레이터"
      contains: "def throttled_edgar"
    - path: "src/stocksig/io/cache.py"
      provides: "get_fund/put_fund/make_fund_key — 7d TTL 펀더멘털 캐시"
      contains: "def make_fund_key"
  key_links:
    - from: "src/stocksig/io/cache.py"
      to: ".cache/fundamentals"
      via: "diskcache.Cache(expire=7d)"
      pattern: "_FUND_TTL_SECONDS"
---

<objective>
Phase 3 cross-cutting 인프라 확장 — 신규 의존성 4종 설치(pyproject 핀 갱신 포함), `io/throttle.py`에 EDGAR(8 RPS)/DART(2 RPS) 토큰버킷 데코레이터 추가, `io/cache.py`에 7일 TTL 펀더멘털 캐시(`_FUND_CACHE`, 키 `(source,ticker,quarter)`) 추가.

Purpose: 후속 4개 펀더멘털 클라이언트(edgar/dart/naver/yf)가 공통으로 의존하는 rate-limit·캐시 토대를 먼저 깐다. FUND-06(throttle)·FUND-04(7d 캐시) 충족.
Output: 핀 갱신된 pyproject.toml, 확장된 throttle.py/cache.py, 통과하는 test_throttle.py/test_cache.py 신규 케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md

<interfaces>
<!-- 기존 throttle.py 패턴 (그대로 2회 복제, Rate 값·키 문자열만 변경) -->
From src/stocksig/io/throttle.py:
```python
from pyrate_limiter import Duration, Limiter, Rate
from functools import wraps
_YAHOO_RATE = Rate(2, Duration.SECOND)
_yahoo_limiter = Limiter(_YAHOO_RATE)
def throttled_yahoo(fn):
    @wraps(fn)
    def wrapper(*args, **kwargs):
        _yahoo_limiter.try_acquire("yahoo")
        return fn(*args, **kwargs)
    return wrapper
```

<!-- 기존 cache.py 패턴 (별도 인스턴스 + 7d TTL + 3-part 키로 복제) -->
From src/stocksig/io/cache.py:
```python
_DEFAULT_DIR = Path(".cache/ohlcv")
_TTL_SECONDS = 24 * 60 * 60
_cache: Optional[Cache] = None
def _get_cache() -> Cache:
    global _cache
    if _cache is None:
        _DEFAULT_DIR.mkdir(parents=True, exist_ok=True)
        _cache = Cache(str(_DEFAULT_DIR))
    return _cache
def make_key(ticker, today=None) -> str:
    return f"{ticker}|{d.isoformat()}"
def put_ohlcv(ticker, df) -> None:
    _get_cache().set(key, df, expire=_TTL_SECONDS)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 신규 의존성 4종 설치 + pyproject 핀 갱신</name>
  <read_first>
    - pyproject.toml (수정 대상 — L6-16 dependencies 배열)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"Standard Stack", §"Package Legitimacy Audit"(4종 전부 [OK]), §"State of the Art"(edgartools 핀 4.x→5.x 정정 근거)
    - CLAUDE.md §Technology Stack (edgartools CHOSEN / OpenDartReader CHOSEN primary)
  </read_first>
  <action>
    `uv add "edgartools>=5,<6" "OpenDartReader>=0.3" "beautifulsoup4>=4.12" "lxml>=5"` 실행 — RESEARCH가 CONTEXT의 stale 핀 `edgartools>=4`를 정정했으므로 핀은 `>=5,<6` 사용(import 이름은 `edgar`, 패키지명 `edgartools`와 다름에 유의). 설치 직후 `uv run python -c "import edgar; print(edgar.__version__)"` + `import OpenDartReader` + `import bs4` + `import lxml` 4건이 ImportError 없이 통과하는지 확인(RESEARCH Pitfall 1 — import 이름 혼동 방지). pyproject.toml dependencies 배열에 4 항목이 추가되고 `uv lock` 갱신됨을 확인. httpx는 edgartools가 끌어오므로 별도 추가 금지(RESEARCH Standard Stack — Supporting). Package Legitimacy Audit가 4종 전부 [OK]·[SLOP]/[SUS] 0건이므로 별도 checkpoint 불필요.
  </action>
  <verify>
    <automated>uv run python -c "import edgar, OpenDartReader, bs4, lxml; print(edgar.__version__)"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import edgar"` 가 ImportError 없이 종료코드 0
    - pyproject.toml dependencies 배열에 `edgartools`, `OpenDartReader`, `beautifulsoup4`, `lxml` 4 항목 존재 (grep으로 4건 확인)
    - edgartools 핀이 `>=5,<6` (4.x 아님)
  </acceptance_criteria>
  <done>4 신규 의존성 설치 완료, import 검증 통과, pyproject 핀 5.x로 갱신.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: throttle.py EDGAR/DART limiter 추가 + cache.py 7d 펀더멘털 캐시 추가</name>
  <read_first>
    - src/stocksig/io/throttle.py (수정 대상 — 전체. L13-27 throttled_yahoo가 in-file analog. L7 주석이 이 확장을 예고)
    - src/stocksig/io/cache.py (수정 대상 — 전체. L19-57 get_ohlcv/put_ohlcv/make_key/_get_cache가 in-file analog)
    - tests/test_throttle.py, tests/test_cache.py (수정 대상 — 기존 케이스 옆에 추가)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §"throttle.py 수정", §"cache.py 수정" (복제 패턴 명세)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"throttle.py 확장", §"cache.py 확장" (Rate 값·키 포맷·TTL 코드 시드)
  </read_first>
  <behavior>
    - throttled_edgar로 감싼 함수는 `_edgar_limiter`(Rate 8/sec, 키 "edgar")의 try_acquire를 호출 후 원함수 결과를 그대로 반환한다
    - throttled_dart로 감싼 함수는 `_dart_limiter`(Rate 2/sec, 키 "dart")의 try_acquire를 호출 후 원함수 결과를 반환한다
    - make_fund_key("EDGAR", "AAPL", "2026Q3") == "EDGAR|AAPL|2026Q3"
    - put_fund 후 get_fund는 같은 (source,ticker,quarter) 키로 저장값을 반환; 다른 quarter 키는 None
    - 7일 TTL 경과 후(freezegun 시간 전진) get_fund는 None (만료) — `_FUND_TTL_SECONDS == 7*24*60*60`
    - 펀더멘털 캐시 디렉터리는 `.cache/fundamentals` (OHLCV `.cache/ohlcv`와 분리)
  </behavior>
  <action>
    throttle.py: 기존 `throttled_yahoo`(L19-27) 블록을 그대로 2회 복제하여 `_EDGAR_RATE = Rate(8, Duration.SECOND)` + `_edgar_limiter` + `def throttled_edgar(fn)`(try_acquire 키 `"edgar"`), `_DART_RATE = Rate(2, Duration.SECOND)` + `_dart_limiter` + `def throttled_dart(fn)`(키 `"dart"`)을 추가. `@wraps(fn)` 유지(Phase 1 `_no_retry_wait` fixture 호환 — STATE 02-02 참조). 8 RPS는 FUND-06 locked 값(PITFALLS 10 RPS 하드캡보다 보수적).
    cache.py: 기존 OHLCV 캐시(`_cache`/`_get_cache`/`make_key`/`get_ohlcv`/`put_ohlcv`) 무손상 유지. 별도 `_FUND_DIR = Path(".cache/fundamentals")`, `_FUND_TTL_SECONDS = 7*24*60*60`, `_fund_cache: Optional[Cache] = None`, `_get_fund_cache()`(동일 lazy-init), `make_fund_key(source, ticker, quarter_label) -> f"{source}|{ticker}|{quarter_label}"`, `get_fund(source,ticker,quarter_label)`(get + 한국어 HIT/MISS 로그 — `get_ohlcv` L46-50 형식 차용), `put_fund(source,ticker,quarter_label,value)`(set expire=`_FUND_TTL_SECONDS`) 추가.
    test_throttle.py: throttled_edgar/throttled_dart가 try_acquire를 호출하고 원함수 반환값을 통과시키는지 검증(mocker로 limiter.try_acquire patch 또는 호출 후 반환값 단언). test_cache.py: 기존 `_isolated_cache_dir`/`freeze_time` 패턴(L15-66 analog) 재사용해 `_FUND_CACHE` monkeypatch + make_fund_key 포맷 + put/get HIT + 다른 quarter MISS + 7d 만료 검증. 테스트 함수명에 `test_fund_cache` 포함(VALIDATION per-req map FUND-04 명령과 일치).
  </action>
  <verify>
    <automated>uv run pytest tests/test_throttle.py tests/test_cache.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_cache.py::test_fund_cache -x` GREEN (FUND-04 VALIDATION 명령)
    - `uv run pytest tests/test_throttle.py -x` GREEN (FUND-06 VALIDATION 명령)
    - 소스 단언: throttle.py에 `Rate(8` 와 `Rate(2` 2개 limiter, `def throttled_edgar`·`def throttled_dart` 존재
    - 소스 단언: cache.py에 `_FUND_TTL_SECONDS = 7 * 24 * 60 * 60` 와 `def make_fund_key` 존재, 기존 `def get_ohlcv` 무손상
    - 기존 전체 스위트 회귀 없음 (`uv run pytest` GREEN — Phase 1/2 130+ 테스트)
  </acceptance_criteria>
  <done>EDGAR 8/DART 2 RPS limiter + 7d 펀더멘털 캐시 추가, 신규·기존 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 API(EDGAR/DART) → 본 프로세스 | rate-limit 정책 경계. 위반 시 403/쿼터차단(신뢰 불가 측은 정책). |
| 디스크 캐시 → 메모리 | pickle 역직렬화 경계 — 본인 생성 캐시만(.cache/), 신뢰. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | EDGAR/DART 호출 빈도 | mitigate | `throttled_edgar`(8 RPS)/`throttled_dart`(2 RPS) 토큰버킷 — FUND-06 locked, PITFALLS #2/#3 403·쿼터차단 회피 |
| T-03-02 | Tampering | diskcache pickle | accept | 본인 프로세스가 생성한 .cache/fundamentals 만 역직렬화. 외부 주입 경로 없음. 개인 로컬 도구. |
| T-03-SC | Tampering | uv add 4 패키지 | accept | RESEARCH Package Legitimacy Audit — slopcheck 0.6.1 4종 전부 [OK], [SLOP]/[SUS] 0건. checkpoint 불필요. |
</threat_model>

<verification>
- `uv run pytest tests/test_throttle.py tests/test_cache.py -x` GREEN
- `uv run python -c "import edgar, OpenDartReader, bs4, lxml"` 종료코드 0
- 기존 전체 스위트 회귀 없음
</verification>

<success_criteria>
- FUND-06: EDGAR 8 RPS / DART 2 RPS 토큰버킷 데코레이터 존재·동작
- FUND-04: 7d TTL 펀더멘털 캐시(`(source,ticker,quarter)` 키) HIT/MISS/만료 동작
- 4 신규 의존성 설치 + 핀 5.x 갱신
- 기존 OHLCV 캐시·Yahoo throttle 무손상
</success_criteria>

<output>
Create `.planning/phases/03-edgar-dart-yfinance-naver/03-01-SUMMARY.md` when done
</output>
