---
phase: 03-edgar-dart-yfinance-naver
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/fixtures/edgar_aapl_facts.py
  - tests/fixtures/dart_005930_finstate.py
  - tests/fixtures/naver_005930.html
  - tests/fixtures/yf_info_sample.py
  - src/stocksig/io/dart_account_map.py
  - .planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md
autonomous: false
requirements: [FUND-01, FUND-03]
user_setup:
  - service: SEC EDGAR
    why: "edgartools 라이브 facts 호출로 5.x 메서드 시그니처·concept tag 확정 (A1/A2)"
    env_vars:
      - name: EDGAR_USER_AGENT_EMAIL
        source: ".env (이미 yunjerrard@gmail.com 설정 — STATE Session Continuity 확인됨)"
  - service: OpenDART
    why: "DART finstate_all 라이브 호출로 account_nm 한글 문자열·sj_div·fs_div 확정 (A3/A6)"
    env_vars:
      - name: OPENDART_API_KEY
        source: ".env (사용자 보유 — 실제 값 채워졌는지 실행 전 확인)"
must_haves:
  truths:
    - "edgartools 5.x의 PER/PEG/GPM/OPM raw facts 취득 경로(메서드 시그니처·concept tag)가 실데이터로 확정된다"
    - "DART account_nm 정확 한글 문자열·sj_div·fs_div 조합이 005930 실호출로 확정된다"
    - "Naver finance 셀렉터·EUC-KR 인코딩이 005930 실페이지로 확정된다"
    - "확정된 상수로 작성된 mock fixture가 후속 클라이언트 단위테스트의 입력이 된다"
  artifacts:
    - path: "src/stocksig/io/dart_account_map.py"
      provides: "DART account_nm → 논리 지표명 상수 dict (실데이터 확정)"
      contains: "DART_ACCOUNT_MAP"
    - path: ".planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md"
      provides: "A1~A7 확정 결과 기록 (메서드명·concept tag·account_nm·셀렉터·키)"
    - path: "tests/fixtures/edgar_aapl_facts.py"
      provides: "AAPL EDGAR 응답 mock fixture (실데이터 기반)"
  key_links:
    - from: "tests/fixtures"
      to: "tests/test_edgar_client.py / test_dart_client.py / test_naver_scraper.py"
      via: "import fixture in Wave-3/4 unit tests"
      pattern: "from tests.fixtures"
---

<objective>
A1~A7 실데이터 검증 스파이크 — edgartools 5.x·OpenDartReader 0.3.2·Naver·yfinance를 AAPL/MSFT/GOOGL(EDGAR) + 005930(DART, Naver) 로 각 1회 실호출하여 RESEARCH의 `[ASSUMED]` 상수(메서드 시그니처, XBRL concept tag, DART account_nm 한글 문자열, Naver 셀렉터, EUC-KR, yfinance .info 키, quarter_label 산출)를 확정한다. 확정 결과로 mock fixture 4종 + `dart_account_map.py` 상수를 작성하고, 후속 Wave 3/4 클라이언트가 이를 입력으로 삼는다.

Purpose: RESEARCH가 본 sandbox에서 라이브 호출 불가하여 미검증으로 남긴 A1~A7(상수 값 문제)를 코딩 시작 전에 실데이터로 못 박는다. 잘못된 상수는 US/KR 펀더멘털 전량 결손으로 이어지므로 가장 먼저 de-risk.
Output: 03-SPIKE-FINDINGS.md(확정 기록), 4 fixture, dart_account_map.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md

<interfaces>
<!-- RESEARCH가 시드한 DART account_nm 후보 (executor가 실데이터로 확정) -->
From 03-PATTERNS.md §dart_account_map.py:
```python
DART_ACCOUNT_MAP: dict[str, tuple[str, ...]] = {
    "revenue":      ("매출액", "수익(매출액)", "영업수익"),
    "gross_profit": ("매출총이익",),
    "op_income":    ("영업이익", "영업이익(손실)"),
    "net_income":   ("당기순이익", "당기순이익(손실)"),
    "eps":          ("기본주당이익", "기본주당순이익"),
}
```
<!-- edgartools 5.x 후보 경로 (A1 — 실호출로 메서드명 확정) -->
From 03-RESEARCH.md Pattern 1:
  from edgar import Company, set_identity
  company.get_facts().to_pandas("us-gaap:EarningsPerShareDiluted")  # (B) facts 경로
  company.get_financials().income_statement().to_dataframe()        # (A) financials 경로
<!-- Naver 후보 셀렉터 (A5 — 실페이지로 확정) -->
  url = "https://finance.naver.com/item/main.naver?code=005930"; r.encoding = "euc-kr"
  soup.select_one("#_per")  # EUC-KR 디코딩
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0 (checkpoint): .env + 네트워크 사전 확인</name>
  <action>실데이터 스파이크(Task 1) 진행 전, 사용자가 `.env` 키와 외부 API 접근 가능 여부를 확인하고 "approved"를 입력할 때까지 대기한다. 차단 시 RESEARCH best-guess 상수 경로로 폴백(Task 1 action 말미 참조).</action>
  <what-built>
    Wave 1에서 edgartools/OpenDartReader/bs4/lxml 설치 완료. 본 태스크는 라이브 외부 API 1회 호출이 필요하다.
  </what-built>
  <how-to-verify>
    1. `.env`에 `EDGAR_USER_AGENT_EMAIL=yunjerrard@gmail.com` 과 `OPENDART_API_KEY=<실제값>` 이 채워졌는지 확인.
    2. 네트워크 연결 + SEC EDGAR / OpenDART / finance.naver.com 접근 가능 확인.
    3. "approved" 입력 시 executor가 실호출 스파이크(다음 auto 태스크)를 진행.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 차단 사유 기술 (예: "OPENDART_API_KEY 미설정")</resume-signal>
</task>

<task type="auto">
  <name>Task 1: 실데이터 스파이크 — A1~A7 상수 확정 + dart_account_map.py + fixture 4종</name>
  <files>src/stocksig/io/dart_account_map.py, tests/fixtures/edgar_aapl_facts.py, tests/fixtures/dart_005930_finstate.py, tests/fixtures/naver_005930.html, tests/fixtures/yf_info_sample.py, .planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"Assumptions Log"(A1~A7), §"Open Questions", Pattern 1/4/5/6
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §dart_account_map.py, §naver_scraper.py
    - src/stocksig/config.py (load_env — EDGAR_USER_AGENT_EMAIL/OPENDART_API_KEY 로드)
    - src/stocksig/io/market_kind.py (classify_market — US/KR 라우팅)
  </read_first>
  <action>
    임시 스크립트(또는 `uv run python` REPL)로 다음 실호출을 1회씩 수행하고 결과를 03-SPIKE-FINDINGS.md에 기록:
    (A1/A2) `from edgar import Company, set_identity; set_identity("Yunjae Kim yunjerrard@gmail.com")` 후 AAPL/MSFT/GOOGL에 대해 RESEARCH Pattern 1 (A)/(B)/(C) 세 경로를 호출 — 5.33.0에서 실제 존재하는 메서드명(`get_financials` vs `financials` 속성, `get_facts` vs `facts`)과 PER/PEG/GPM/OPM 4지표를 가장 안정적으로 채우는 경로를 확정. EPS=`EarningsPerShareDiluted` 우선순위·GrossProfit·OperatingIncomeLoss·Revenues concept tag가 3종목에 실제 존재하는지 교차검증(A2). period_of_report로 quarter_label("2026Q3") 산출 방식 확정(A7).
    (A3/A6) `OpenDartReader(api_key).finstate_all("005930", <최신연도>, reprt_code="11011", fs_div="CFS")` 호출 — 반환 status 필드("000" 정상) 확인, 매출액/매출총이익/영업이익/기본주당이익의 정확 account_nm 한글 문자열·sj_div·fs_div 조합 확정. 6자리 stock_code 직접 수용·corp_code 내부 해석 동작 확인(A6). `thstrm_amount` 쉼표 포함 문자열 포맷 확인.
    (A5) `httpx.get("https://finance.naver.com/item/main.naver?code=005930")` 후 `r.encoding="euc-kr"` → `BeautifulSoup(r.text, "lxml")` → `#_per`/`#_pbr`/`#_eps` select_one 텍스트 확정. Naver가 GPM/OPM 미노출임을 확인(PER만 폴백 가능). 셀렉터 변경 시 실제 id 기록.
    (A4) `yf.Ticker("AAPL", session=market._SESSION).info` 에서 `trailingPE`/`pegRatio`(또는 `trailingPegRatio`)/`grossMargins`/`operatingMargins` 키 존재 확인. KR(005930.KS) .info 부분지원 여부 기록.
    확정 후: `src/stocksig/io/dart_account_map.py`에 `DART_ACCOUNT_MAP` 상수(실데이터 확정 문자열, writer.py `_NUM_FORMAT_MAP` 모듈 상수 스타일)를 작성. `tests/fixtures/` 디렉터리에 실응답을 축약한 mock fixture 4종 작성: `edgar_aapl_facts.py`(EDGAR 응답 dict/DataFrame 샘플), `dart_005930_finstate.py`(finstate_all 반환 DataFrame 샘플), `naver_005930.html`(EUC-KR 페이지 발췌 — PER 포함), `yf_info_sample.py`(.info dict 샘플). 03-SPIKE-FINDINGS.md에 A1~A7 각각의 확정 값 + 출처(어느 종목 호출)를 표로 기록.
    차단/실패 시(API 키 미설정·네트워크·쿼터): RESEARCH best-guess 상수(Assumptions Log·매핑표)로 fixture를 작성하되 SPIKE-FINDINGS.md에 `[UNVERIFIED — 실데이터 미검증, follow-up 필요]` 명시하고 후속 plan에서 fixture를 실데이터로 교체하는 follow-up 항목을 STATE Todos에 기록. 이 경우에도 Wave 3/4 코딩은 best-guess 상수로 진행 가능.
  </action>
  <verify>
    <automated>uv run python -c "from stocksig.io.dart_account_map import DART_ACCOUNT_MAP; assert 'revenue' in DART_ACCOUNT_MAP and 'eps' in DART_ACCOUNT_MAP; print(len(DART_ACCOUNT_MAP))"</automated>
  </verify>
  <acceptance_criteria>
    - `src/stocksig/io/dart_account_map.py` 에 `DART_ACCOUNT_MAP` (revenue/gross_profit/op_income/net_income/eps 키 포함) 존재
    - `tests/fixtures/` 에 edgar/dart/naver/yf 4 fixture 파일 존재
    - `03-SPIKE-FINDINGS.md` 에 A1~A7 확정 값(또는 [UNVERIFIED] 플래그) 표 기록
    - import 검증: `uv run python -c "from stocksig.io.dart_account_map import DART_ACCOUNT_MAP"` 종료코드 0
  </acceptance_criteria>
  <done>A1~A7 실데이터 확정(또는 best-guess+UNVERIFIED 플래그), dart_account_map.py + fixture 4종 작성, SPIKE-FINDINGS 기록.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` 비밀 → 스파이크 스크립트 | OPENDART_API_KEY·UA email 로그/커밋 유출 금지 경계. |
| Naver HTML → 파서 | 신뢰 불가 외부 입력, EUC-KR 디코딩·float 파싱 경계. |
| SEC EDGAR UA → SEC 로그 | 실이메일 노출(정책상 필수) 경계. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | OPENDART_API_KEY / UA email | mitigate | `.env`+python-dotenv(기존 config.py). SPIKE-FINDINGS.md·fixture·로그에 API 키 평문 기록 금지. .env gitignore 유지. |
| T-03-04 | Tampering | Naver HTML 파싱 | mitigate | bs4 텍스트만 추출, `float(text.replace(",",""))` try/except 가드, eval 금지(ASVS V5). |
| T-03-05 | Information Disclosure | EDGAR UA 실이메일 | accept | SEC 정책상 필수(PROJECT.md 명시 yunjerrard@gmail.com). 공개 repo에 .env 미커밋으로 통제. |
</threat_model>

<verification>
- dart_account_map.py import 성공 + 5 키 존재
- 4 fixture 파일 존재
- 03-SPIKE-FINDINGS.md A1~A7 기록 완결
</verification>

<success_criteria>
- A1~A7 상수가 실데이터로 확정(또는 best-guess + UNVERIFIED follow-up 명시)
- dart_account_map.py + mock fixture 4종이 Wave 3/4 단위테스트 입력으로 준비됨
- API 키·실이메일 평문 유출 0건
</success_criteria>

<output>
Create `.planning/phases/03-edgar-dart-yfinance-naver/03-02-SUMMARY.md` when done
</output>
