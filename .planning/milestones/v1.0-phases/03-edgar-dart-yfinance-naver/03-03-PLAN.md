---
phase: 03-edgar-dart-yfinance-naver
plan: 03
type: tdd
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/stocksig/io/edgar_client.py
  - src/stocksig/io/yf_fundamentals.py
  - src/stocksig/io/fundamentals.py
  - tests/test_edgar_client.py
  - tests/test_yf_fundamentals.py
  - tests/test_fundamentals.py
autonomous: true
requirements: [FUND-01, FUND-02, FUND-05]
must_haves:
  truths:
    - "미국 티커는 EDGAR에서 EPS/Revenues/GrossProfit/OperatingIncome raw facts를 받아 PER/PEG/GPM/OPM를 산출한다"
    - "EDGAR 호출 전 set_identity가 'Yunjae Kim yunjerrard@gmail.com' 형식 UA로 1회 설정된다"
    - "EDGAR에서 결손된 개별 지표만 yfinance.info로 보완되고 각 지표에 사용 소스(EDGAR/yf)가 기록된다"
    - "PEG는 PER/((EPS_TTM/EPS_prior−1)×100)로 산출되며 성장률≤0·0분모·전년EPS없음·PER없음 시 빈값+한국어 사유"
  artifacts:
    - path: "src/stocksig/io/fundamentals.py"
      provides: "MetricCell/FundamentalsResult dataclass + fetch_fundamentals 라우팅 + PEG 산식 + provenance"
      contains: "def fetch_fundamentals"
    - path: "src/stocksig/io/edgar_client.py"
      provides: "fetch_edgar_raw/fetch_edgar_cached — throttle+cache EDGAR facts"
      contains: "set_identity"
    - path: "src/stocksig/io/yf_fundamentals.py"
      provides: "fetch_yf_info — .info 폴백 (_SESSION 재사용)"
      contains: "trailingPE"
  key_links:
    - from: "src/stocksig/io/edgar_client.py"
      to: "io/throttle.throttled_edgar + io/cache.get_fund/put_fund"
      via: "decorator + cache-first fetch"
      pattern: "throttled_edgar"
    - from: "src/stocksig/io/fundamentals.py"
      to: "edgar_client + yf_fundamentals"
      via: "per-metric fallback (US: EDGAR→yf)"
      pattern: "market == .US."
---

<objective>
US 펀더멘털 수직 슬라이스 (첫 end-to-end 경로) — `edgar_client.py`(EDGAR facts + set_identity + throttle + 7d cache), `yf_fundamentals.py`(.info 폴백, `_SESSION` 재사용), `fundamentals.py`(MetricCell/FundamentalsResult 데이터모델 + `fetch_fundamentals(ticker, market, last_close)` 라우팅 + PEG 산식 + per-metric provenance). 본 plan은 US 경로(D-03: EDGAR→yf)만 완성한다 — KR 경로는 03-04에서 같은 오케스트레이터에 확장.

Purpose: FUND-01(EDGAR raw facts)·FUND-02(set_identity UA)·FUND-05(폴백+provenance) + PEG 산식(Claude 재량 확정식)을 구현. PER 분자(주가)는 EDGAR에 없으므로 호출부가 주입하는 `last_close`(시트1 col 4 = enriched_df Close 최신값) 재사용.
Output: edgar_client.py, yf_fundamentals.py, fundamentals.py + 3 단위테스트(mock 기반).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md

<interfaces>
<!-- mother analog: io/market.py 의 module-singleton + throttle + cache-first 구조 -->
From src/stocksig/io/market.py:
  _SESSION = curl_requests.Session(impersonate="chrome")   # 재사용 — 신규 세션 금지
  @throttled_yahoo
  def fetch_ohlcv(ticker): ...                              # 빈응답 ValueError fail-fast
  def fetch_ohlcv_cached(ticker):                           # get → miss시 fetch → put
      df = cache.get_ohlcv(ticker)
      if df is not None: return df
      df = fetch_ohlcv(ticker); cache.put_ohlcv(ticker, df); return df

<!-- Wave 1에서 추가된 신규 인프라 (03-01) -->
From src/stocksig/io/throttle.py:  def throttled_edgar(fn): ...   # 8 RPS, 키 "edgar"
From src/stocksig/io/cache.py:     def get_fund(source, ticker, quarter_label); def put_fund(source, ticker, quarter_label, value)

<!-- 03-02에서 확정된 edgar 메서드·concept tag — SPIKE-FINDINGS.md 참조 -->
<!-- RESEARCH 권고 데이터 모델 (PATTERNS §fundamentals.py) -->
  @dataclass
  class MetricCell: value: float | None; source: str | None; note: str | None
  @dataclass
  class FundamentalsResult: per: MetricCell; peg: MetricCell; gpm: MetricCell; opm: MetricCell

<!-- PEG 산식 (RESEARCH Pattern 3 — 확정식 + 엣지케이스) -->
  growth_pct = (eps_ttm/eps_prior - 1)*100  if eps_prior not in (None,0) else None
  peg = per/growth_pct  if growth_pct and growth_pct > 0  else None
  # eps_prior None → "조회 실패: 전년 EPS 미존재" / ==0 → "전년 EPS 0" /
  # growth<=0 → "EPS 성장률 ≤ 0" / per None → "PER 없음"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fundamentals.py 데이터모델 + PEG 산식 + US 라우팅 (RED→GREEN)</name>
  <read_first>
    - src/stocksig/runner.py (L34-49 — @dataclass TickerResult/TickerFailure 스타일 analog)
    - src/stocksig/io/market_kind.py (classify_market — US/KR 라우팅 재사용)
    - tests/test_runner.py (callable 주입 + 폴백 분기 analog), tests/conftest.py
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"Code Examples"(FundamentalsResult), Pattern 2(EPS_TTM/PER), Pattern 3(PEG 엣지케이스)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §fundamentals.py
  </read_first>
  <behavior>
    - MetricCell(value, source, note) / FundamentalsResult(per, peg, gpm, opm) dataclass 정의
    - PER = last_close / eps_ttm; eps_ttm None 또는 ≤0 시 PER 빈값 + 사유 "조회 실패: EPS TTM 미존재" (또는 "EPS ≤ 0")
    - PEG: growth_pct = (eps_ttm/eps_prior−1)×100. eps_prior None → "조회 실패: 전년 EPS 미존재"; eps_prior==0 → "조회 실패: 전년 EPS 0"; growth_pct≤0 → "조회 실패: EPS 성장률 ≤ 0"; per None → "조회 실패: PER 없음". 정상 시 peg=per/growth_pct, source 1차 소스와 동일
    - GPM = gross_profit/revenue (0~1 비율); OPM = op_income/revenue. 분모 0/None 시 빈값+사유
    - US 라우팅: market=="US" → EDGAR 1차, EDGAR에서 None인 *개별 지표만* yf로 보완(per-metric — 1차에서 채운 지표는 덮어쓰지 않음). 각 MetricCell.source는 실제 사용 소스("EDGAR"|"yf"). note는 "EDGAR · <quarter>" 또는 결손 사유
    - fetch_fundamentals는 raise하지 않거나 내부 try/except로 흡수 (펀더멘털 결손 ≠ 티커 실패, D-disc-10)
  </behavior>
  <action>
    `src/stocksig/io/fundamentals.py` 생성. `runner.py` L34-49 dataclass 스타일로 `MetricCell`/`FundamentalsResult` 정의. 순수 산식 헬퍼 `_compute_per(last_close, eps_ttm)`, `_compute_peg(per, eps_ttm, eps_prior)`(RESEARCH Pattern 3 엣지케이스 4종 한국어 사유), `_compute_margin(numer, denom)`(GPM/OPM 공용) 작성 — fenced code 금지, 식별자·사유 문자열은 위 behavior·interfaces 참조. `fetch_fundamentals(ticker, market, last_close) -> FundamentalsResult`: `classify_market` 결과로 분기(본 plan은 US 분기만 — KR 분기는 `raise NotImplementedError` 또는 빈 결과 + "KR 미구현" placeholder로 두고 03-04에서 채움; placeholder는 SCOPE 축소 아님 — 03-04가 같은 함수 확장). US 분기: `edgar_client.fetch_edgar_cached`로 raw dict 취득 → 산식 → 결손 지표만 `yf_fundamentals.fetch_yf_info`로 보완 → per-metric source 라벨. Korean 로그 `[k/N] fund OK <t> (EDGAR)` / `(EDGAR→yf)` 형식(runner L114 차용). 전 경로 try/except 흡수.
    RED: test_fundamentals.py에 in-memory stub(edgar_fn/yf_fn 주입 또는 mocker.patch)으로 ① EDGAR 전지표 채움(source 전부 EDGAR) ② EDGAR GPM 결손→yf 보완(GPM source=yf, 나머지 EDGAR) ③ PEG 성장률≤0 빈값+사유 ④ PEG 0분모 ⑤ PEG 전년EPS None ⑥ PER eps≤0 빈값 — 6 케이스 작성, 먼저 실패 확인. GREEN: 구현. 테스트 함수에 `test_fallback_chain` 포함(VALIDATION FUND-05 명령 일치).
  </action>
  <verify>
    <automated>uv run pytest tests/test_fundamentals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_fundamentals.py::test_fallback_chain -x` GREEN (FUND-05)
    - PEG 엣지케이스 4종(성장률≤0/0분모/전년없음/PER없음) 각각 빈값(value=None)+한국어 note 단언
    - per-metric 폴백: EDGAR 결손 GPM만 yf로 채워지고 source="yf", 나머지 source="EDGAR" 단언
    - fetch_fundamentals가 stub 예외 주입 시 raise 없이 빈 FundamentalsResult 반환(흡수 단언)
  </acceptance_criteria>
  <done>fundamentals.py 데이터모델+산식+US 라우팅+provenance, 6 테스트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: edgar_client.py (EDGAR facts + set_identity + throttle + cache) + yf_fundamentals.py</name>
  <read_first>
    - src/stocksig/io/market.py (전체 — module-singleton `_SESSION` L37-38, `@throttled_yahoo` L44, cache-first `fetch_ohlcv_cached` L89-102 analog)
    - src/stocksig/io/throttle.py (throttled_edgar — 03-01 추가)
    - src/stocksig/io/cache.py (get_fund/put_fund/make_fund_key — 03-01 추가)
    - src/stocksig/config.py (load_env — EDGAR_USER_AGENT_EMAIL)
    - tests/test_market.py (외부 클라이언트 mocker.patch analog L31-51), tests/fixtures/edgar_aapl_facts.py + yf_info_sample.py (03-02)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md (A1/A2/A4/A7 확정 메서드명·concept tag·yf 키·quarter_label)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md Pattern 1/2/5, §"Anti-Patterns"
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §edgar_client.py, §yf_fundamentals.py
  </read_first>
  <behavior>
    - 모듈 import 시 set_identity가 "Yunjae Kim yunjerrard@gmail.com" 형식(이름+이메일)으로 1회 호출 (per-call 금지)
    - fetch_edgar_raw(ticker)는 @throttled_edgar로 감싸지고 SPIKE-FINDINGS 확정 경로로 EPS_TTM/EPS_prior/Revenue/GrossProfit/OpIncome raw dict 반환 + quarter_label
    - fetch_edgar_cached(ticker, quarter_label): get_fund("EDGAR",..) → miss시 fetch_edgar_raw → put_fund
    - fetch_yf_info(ticker)는 @throttled_yahoo + market._SESSION 재사용, .info에서 trailingPE/pegRatio(또는 trailingPegRatio)/grossMargins/operatingMargins None-safe .get()
  </behavior>
  <action>
    `src/stocksig/io/edgar_client.py` 생성 — market.py 구조 복제: `from edgar import Company, set_identity`(import 이름 `edgar`, A1 주의), import-time `set_identity(...)` 1회(config.load_env로 email 취득 권장, RESEARCH Anti-Pattern per-call 금지). `@throttled_edgar def fetch_edgar_raw(ticker)`: SPIKE-FINDINGS 확정 경로(get_financials/get_facts 중 채택)로 raw dict 산출 + period_of_report→quarter_label. retry는 생략(edgartools 내부 처리). 빈응답/None-safe 가드. `fetch_edgar_cached(ticker, quarter_label)`: cache-first(market.py L89-102 패턴). 한국어 로그 "%s | EDGAR facts 수신 완료".
    `src/stocksig/io/yf_fundamentals.py` 생성 — `from stocksig.io.market import _SESSION`(신규 세션 금지, RESEARCH Anti-Pattern), `from stocksig.io.throttle import throttled_yahoo`(기존 2 RPS 재사용 — 신규 limiter 불필요). `@throttled_yahoo def fetch_yf_info(ticker) -> dict`: `yf.Ticker(ticker, session=_SESSION).info`에서 PER/PEG/GPM/OPM None-safe `.get()`(pegRatio→trailingPegRatio 키 변동 가드, A4).
    RED→GREEN: test_edgar_client.py — `mocker.patch("stocksig.io.edgar_client.Company")` + edgar_aapl_facts fixture로 fetch_edgar_raw 산출 dict 단언, set_identity 호출 형식 단언(test_set_identity — FUND-02 VALIDATION 명령), cache HIT시 재호출 0 단언. test_yf_fundamentals.py — `.info` mock으로 키 추출·None-safe 단언. EDGAR/DART를 ThreadPool 고동시성 호출 금지(throttle은 RPS 상한)·set_identity per-call 금지 anti-pattern 준수.
  </action>
  <verify>
    <automated>uv run pytest tests/test_edgar_client.py tests/test_yf_fundamentals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_edgar_client.py::test_set_identity -x` GREEN — set_identity가 "<이름> <이메일>" 형식 인자로 호출됨 단언 (FUND-02)
    - `uv run pytest tests/test_edgar_client.py -x` GREEN — fetch_edgar_raw가 EPS/Revenue/GrossProfit/OpIncome 키 raw dict 반환 (FUND-01)
    - 소스 단언: edgar_client.py에 `from edgar import` (NOT `import edgartools`), `@throttled_edgar`, `set_identity(` 존재
    - 소스 단언: yf_fundamentals.py에 `from stocksig.io.market import _SESSION` (신규 세션 생성 없음)
    - cache HIT 경로: 같은 (EDGAR,ticker,quarter) 2회 호출 시 fetch_edgar_raw 1회만 (mock call_count==1)
  </acceptance_criteria>
  <done>edgar_client.py(facts+set_identity+throttle+cache) + yf_fundamentals.py(_SESSION 재사용), 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EDGAR/yfinance 응답 → 산식 | 결손/형식이상 값이 PER/PEG/평균을 오염시키지 않게 방어. |
| UA email → SEC 로그 | set_identity 실이메일 노출(정책 필수). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering (도메인 무결성) | EDGAR/yf 결손값 | mitigate | None-safe `.get()`, 결손 시 MetricCell.value=None(0/-999999 금지, D-05). PEG 엣지케이스 4종 빈값+사유. Core Value 보호. |
| T-03-07 | Information Disclosure | set_identity UA 이메일 | accept | SEC 정책 필수(PROJECT.md). 코드 하드코딩 대신 config.load_env 권장, .env gitignore. |
| T-03-08 | Denial of Service | EDGAR 403/15분차단 | mitigate | set_identity 1회(per-call 금지)·@throttled_edgar 8 RPS·ThreadPool 고동시성 금지(RESEARCH Anti-Pattern). |
</threat_model>

<verification>
- `uv run pytest tests/test_fundamentals.py tests/test_edgar_client.py tests/test_yf_fundamentals.py -x` GREEN
- 기존 전체 스위트 회귀 없음
</verification>

<success_criteria>
- FUND-01: EDGAR raw facts(EPS/Revenue/GrossProfit/OpIncome) → PER/PEG/GPM/OPM 산출
- FUND-02: set_identity UA 형식 1회 호출
- FUND-05: US 폴백(EDGAR→yf) per-metric + provenance 라벨
- PEG 산식 + 4 엣지케이스 한국어 사유
</success_criteria>

<output>
Create `.planning/phases/03-edgar-dart-yfinance-naver/03-03-SUMMARY.md` when done
</output>
