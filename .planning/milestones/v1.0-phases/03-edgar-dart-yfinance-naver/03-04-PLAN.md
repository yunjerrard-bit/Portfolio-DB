---
phase: 03-edgar-dart-yfinance-naver
plan: 04
type: tdd
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - src/stocksig/io/dart_client.py
  - src/stocksig/io/naver_scraper.py
  - src/stocksig/io/fundamentals.py
  - tests/test_dart_client.py
  - tests/test_naver_scraper.py
  - tests/test_fundamentals.py
autonomous: true
requirements: [FUND-03, FUND-05]
must_haves:
  truths:
    - "한국 티커는 DART finstate_all로 재무를 받아 account_nm 매핑으로 PER/PEG/GPM/OPM를 산출한다 (stock_code 직접 수용, corp_code 내부 해석)"
    - "DART 결손 시 PER만 Naver finance(EUC-KR)로 폴백하고, GPM/OPM은 DART→yf로 폴백한다 (metric별 차등 체인)"
    - "네이버 폴백은 run당 NAVER_FALLBACK_CAP(기본 20)건으로 캡되고, 초과분은 네이버를 건너뛰고 yf로 직행하며 사유 'Naver 대량 사용 제한(폴백 상한 초과)'이 기록된다 (D-07 — 소수 폴백 전용)"
    - "DART/Naver/yf 모두 결손 시 빈값 + 한국어 사유, 각 채워진 지표에 사용 소스(DART/Naver/yf)가 기록된다"
  artifacts:
    - path: "src/stocksig/io/dart_client.py"
      provides: "fetch_dart_raw/fetch_dart_cached — throttle+cache, account_nm 매핑, status 가드"
      contains: "throttled_dart"
    - path: "src/stocksig/io/naver_scraper.py"
      provides: "fetch_naver_per — EUC-KR 스크래핑 (PER만)"
      contains: "euc-kr"
  key_links:
    - from: "src/stocksig/io/dart_client.py"
      to: "io/dart_account_map.DART_ACCOUNT_MAP + io/throttle.throttled_dart + io/cache.get_fund"
      via: "account_nm 매핑 + throttle + cache-first"
      pattern: "DART_ACCOUNT_MAP"
    - from: "src/stocksig/io/fundamentals.py"
      to: "dart_client + naver_scraper + yf_fundamentals"
      via: "KR per-metric fallback (PER: DART→Naver→yf; GPM/OPM: DART→yf)"
      pattern: "market == .KR."
---

<objective>
KR 펀더멘털 확장 슬라이스 — `dart_client.py`(OpenDartReader finstate_all + account_nm 매핑 + throttle + 7d cache + status 가드), `naver_scraper.py`(finance.naver.com EUC-KR 스크래핑, PER만), 그리고 03-03의 `fetch_fundamentals` KR 분기 구현(D-04: DART→Naver→yf, metric별 차등 체인). 동일 `FundamentalsResult` 반환 형태·provenance 라벨 재사용.

Purpose: FUND-03(DART 재무, stock_code 직접·corp_code 내부해석)·FUND-05(KR 폴백+provenance) 충족. RESEARCH Open Q4 명시 — Naver는 GPM/OPM 미노출이므로 PER만 Naver 폴백, GPM/OPM은 DART→yf로 직행(Naver 건너뜀).
Output: dart_client.py, naver_scraper.py, fundamentals.py KR 분기 + 단위테스트 확장.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md

<interfaces>
<!-- 03-03에서 만든 오케스트레이터 (본 plan이 KR 분기 채움) -->
From src/stocksig/io/fundamentals.py:
  def fetch_fundamentals(ticker, market, last_close) -> FundamentalsResult   # US 분기 완성, KR placeholder
  @dataclass class MetricCell: value; source; note
  @dataclass class FundamentalsResult: per; peg; gpm; opm
  _compute_per / _compute_peg / _compute_margin   # 산식 헬퍼 재사용 (US와 동일)

<!-- 03-02 확정 상수 -->
From src/stocksig/io/dart_account_map.py:  DART_ACCOUNT_MAP  # revenue/gross_profit/op_income/net_income/eps → account_nm 후보 tuple

<!-- 03-01 인프라 -->
From src/stocksig/io/throttle.py:  def throttled_dart(fn): ...   # 2 RPS, 키 "dart"; throttled_yahoo (Naver 보수적 재사용)
From src/stocksig/io/cache.py:     get_fund/put_fund   # 키 "DART|<ticker>|<quarter>"

<!-- RESEARCH Pattern 4 — DART 호출 (stock_code 직접 수용) -->
  dart = OpenDartReader(api_key)
  df = dart.finstate_all("005930", year, reprt_code="11011", fs_div="CFS")
  # status: "000"=정상 "013"=데이터없음 "020"=쿼터초과
  # thstrm_amount 쉼표 문자열 → int(s.replace(",",""))
<!-- RESEARCH Pattern 6 — Naver (PER만, EUC-KR) -->
  r.encoding = "euc-kr"; soup.select_one("#_per")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dart_client.py (finstate_all + account_nm 매핑 + throttle + cache + status 가드) + naver_scraper.py</name>
  <read_first>
    - src/stocksig/io/market.py (module-singleton + throttle + cache-first analog)
    - src/stocksig/io/market_kind.py (L12 KR_SUFFIXES — .split(".")[0] stock_code 파싱)
    - src/stocksig/io/dart_account_map.py (DART_ACCOUNT_MAP — 03-02 확정)
    - src/stocksig/io/throttle.py (throttled_dart — 03-01), src/stocksig/io/cache.py (get_fund/put_fund)
    - tests/test_market.py (mocker.patch analog), tests/fixtures/dart_005930_finstate.py + naver_005930.html (03-02)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-SPIKE-FINDINGS.md (A3/A5/A6 — account_nm·sj_div·fs_div·Naver 셀렉터·EUC-KR 확정)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md Pattern 4/6, Pitfall 3(corp_code/쿼터)/5(Naver 인코딩)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §dart_client.py, §naver_scraper.py
  </read_first>
  <behavior>
    - fetch_dart_raw(ticker, year)는 @throttled_dart로 감싸지고 `.split(".")[0]`로 6자리 stock_code 추출 후 finstate_all(stock_code, year, reprt_code="11011", fs_div="CFS") 호출
    - 응답 status 가드: "000" 정상 산출; "013" → 사유 "DART 데이터 미존재"; "020" → 사유 "DART 쿼터 초과"; 그 외 → "DART corp_code 매핑 실패"
    - 계정 매핑(03-02 A3 [VERIFIED] — **account_id 1차 / account_nm 2차**): 먼저 `DART_ACCOUNT_ID_MAP`(표준 태그, 예 revenue=ifrs-full_Revenue·op_income=dart_OperatingIncomeLoss·eps=ifrs-full_BasicEarningsLossPerShare)로 `account_id` 정확매칭, 실패 시 `DART_ACCOUNT_MAP`(한글 account_nm) 후보 tuple 폴백. 손익 행만(`sj_div` ∈ SJ_DIV_INCOME_STATEMENT = IS/CIS). 매칭 행의 thstrm_amount(쉼표 문자열) → int 파싱(try/except 가드, ASVS V5). revenue/gross_profit/op_income/net_income/eps raw dict 반환
    - fetch_dart_cached(ticker, quarter_label): cache-first (키 "DART|..")
    - fetch_naver_per(ticker): @throttled_yahoo(보수적 재사용), httpx GET finance.naver.com, **인코딩=UTF-8**(03-02 스파이크 A5 [VERIFIED] — RESEARCH의 euc-kr 가정 반증, charset=utf-8이라 euc-kr/cp949 디코드 시 UnicodeDecodeError. httpx 기본 charset 신뢰 또는 `r.content.decode("utf-8")`), select_one("#_per") None 가드 → float(text.replace(",","")) try/except. GPM/OPM 미노출 — PER만 반환
    - D-07 호출 상한: naver_scraper에 모듈 레벨 카운터 + 상수 `NAVER_FALLBACK_CAP = 20`(env로 override 가능) + `reset_naver_count()`. fetch_naver_per는 카운터 ≥ CAP이면 즉시 None + 사유 신호 반환(스크래핑 미수행). runner는 run 시작 시 reset 호출. 429/403·빈 응답(소프트 블록)은 None으로 안전 처리(시세·실행 흐름 전파 금지)
  </behavior>
  <action>
    `src/stocksig/io/dart_client.py` 생성 — market.py 구조 복제. `@throttled_dart def fetch_dart_raw(ticker, year)`: stock_code 파싱(market_kind KR_SUFFIXES 발상, `.split(".")[0]`), `OpenDartReader(api_key)`(config.load_env OPENDART_API_KEY), finstate_all(reprt_code/fs_div는 SPIKE-FINDINGS 확정값), status 필드 4분기 한국어 사유(Pitfall 3), 계정 매핑 account_id 1차(`DART_ACCOUNT_ID_MAP`)→account_nm 2차(`DART_ACCOUNT_MAP`) 폴백 + 손익행만(`sj_div` ∈ SJ_DIV_INCOME_STATEMENT), thstrm_amount int 파싱 가드. `fetch_dart_cached(ticker, quarter_label)` cache-first.
    `src/stocksig/io/naver_scraper.py` 생성 — `import httpx; from bs4 import BeautifulSoup; from stocksig.io.throttle import throttled_yahoo`. 모듈 레벨 `NAVER_FALLBACK_CAP = int(os.getenv("NAVER_FALLBACK_CAP", "20"))` + `_naver_calls = 0` + `def reset_naver_count()`. `@throttled_yahoo def fetch_naver_per(ticker) -> float | None`: **먼저 `_naver_calls >= NAVER_FALLBACK_CAP`이면 스크래핑 없이 None 반환**(D-07 상한); 아니면 `_naver_calls += 1` 후 code 파싱, httpx.get(timeout=10, UA 헤더), **UTF-8 디코드**(03-02 A5 [VERIFIED] — euc-kr 아님; httpx 기본 charset 또는 `r.content.decode("utf-8")`), BeautifulSoup(.., "lxml"), select_one("#_per") (03-02 확정: `#_per`=28.94, `#_eps`=12,372) None 가드, float 파싱 try/except, 429/403·빈 응답 시 None. GPM/OPM 미노출 명시 주석(RESEARCH Open Q4).
    RED→GREEN: test_dart_client.py — `mocker.patch("stocksig.io.dart_client.OpenDartReader")` + dart_005930_finstate fixture로 account_nm 매핑·int 파싱·status 가드(000/013/020) 단언, stock_code 직접 수용(.KS 제거) 단언, cache HIT 재호출 0. test_naver_scraper.py — naver_005930.html fixture(UTF-8) mock으로 #_per 추출·None 가드(셀렉터 없음→None)·CAP 초과 시 httpx.get 미호출 단언. EDGAR/DART ThreadPool 고동시성 금지·corp_code 수동 매핑 재구현 금지(라이브러리 흡수) anti-pattern 준수.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dart_client.py tests/test_naver_scraper.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dart_client.py -x` GREEN — finstate_all stock_code 직접 수용(".KS" 제거), account_nm 매핑 raw dict 반환 (FUND-03)
    - status 가드 단언: "013"/"020"/기타 각각 한국어 사유 반환
    - thstrm_amount 쉼표 문자열 int 파싱 단언 (예: "1,234,567" → 1234567)
    - 소스 단언: dart_client.py에 `throttled_dart`, `DART_ACCOUNT_ID_MAP`/`DART_ACCOUNT_MAP`, `finstate_all` 존재; naver_scraper.py에 `utf-8`(euc-kr 아님 — A5), `select_one`, None 가드, `NAVER_FALLBACK_CAP`, `reset_naver_count` 존재
    - D-07 상한 단언: `reset_naver_count()` 후 fetch_naver_per를 CAP+5회 호출 시 실제 httpx.get은 CAP회만(초과분은 스크래핑 없이 None) — mocker로 httpx.get call_count == NAVER_FALLBACK_CAP 단언
    - cache HIT: 같은 (DART,ticker,quarter) 2회 호출 시 finstate_all 1회만
  </acceptance_criteria>
  <done>dart_client.py(account_nm 매핑+status 가드+throttle+cache) + naver_scraper.py(EUC-KR PER), 테스트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fundamentals.py KR 분기 구현 (DART→Naver→yf, metric별 차등 체인)</name>
  <read_first>
    - src/stocksig/io/fundamentals.py (수정 대상 — 03-03이 만든 fetch_fundamentals US 분기 + 산식 헬퍼. KR placeholder 분기를 실구현으로 교체)
    - src/stocksig/io/dart_client.py + naver_scraper.py + yf_fundamentals.py (Task 1 + 03-03)
    - tests/test_fundamentals.py (03-03 작성 — KR 케이스 추가)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"Open Questions" Q4(Naver GPM/OPM 미노출 — metric별 차등), Pattern 4 KR 산출, §"Architecture Patterns" 다이어그램(KR 체인)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §fundamentals.py 시장 라우팅
  </read_first>
  <behavior>
    - market=="KR" 분기: DART 1차로 PER/PEG/GPM/OPM 산출(KRW last_close ÷ 기본주당이익=PER; 매출총이익/매출액=GPM; 영업이익/매출액=OPM; PEG는 03-03 _compute_peg 재사용, EPS_prior=전년 사업보고서 기본주당이익)
    - PER 결손 시: Naver fetch_naver_per 폴백(source="Naver", D-07 상한 내) → Naver가 None(결손/상한 초과) 시 yf(source="yf"). 상한 초과로 건너뛴 경우 yf도 결손이면 사유 "Naver 대량 사용 제한(폴백 상한 초과)"
    - GPM/OPM 결손 시: Naver 건너뜀(미노출) → yf 직행(source="yf") → 결손 시 빈값+사유
    - per-metric: 1차(DART)에서 채운 지표는 폴백으로 덮어쓰지 않음. 각 MetricCell.source 정확 라벨
    - 전 경로 try/except 흡수 (펀더멘털 결손 ≠ 티커 실패). Korean 로그 "(DART)" / "(DART→Naver)" / "(DART→yf)" / "fund MISS"
  </behavior>
  <action>
    `fundamentals.py`의 KR placeholder 분기를 실구현으로 교체(03-03이 같은 함수에 US 분기·산식 헬퍼·dataclass를 이미 둠 — KR은 헬퍼 재사용). KR 산식은 RESEARCH Pattern 4(GPM=매출총이익/매출액, OPM=영업이익/매출액, PER=last_close/기본주당이익(KRW)). metric별 차등 폴백 체인 구현: PER → DART→Naver→yf; GPM/OPM → DART→yf(Naver 건너뜀, Open Q4); PEG → DART(EPS_prior 전년 보고서)→(PER 의존). per-metric — 1차 채운 지표 보존. source 라벨("DART"/"Naver"/"yf"). 전 경로 try/except.
    RED→GREEN: test_fundamentals.py에 KR 케이스 추가 ① DART 전지표 채움(source 전부 DART) ② DART PER 결손→Naver 보완(PER source=Naver) ③ DART+Naver PER 결손→yf(PER source=yf) ④ DART GPM 결손→yf 직행(Naver 안 거침 — naver_fn mock call 0 단언) ⑤ 전 소스 결손→빈값+사유. test 함수에 KR 폴백 명시. mocker로 dart/naver/yf stub 주입.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fundamentals.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_fundamentals.py -x` GREEN (US 03-03 케이스 + KR 신규 케이스 전부)
    - KR PER 차등 체인 단언: DART 결손 시 Naver 호출, GPM/OPM 결손 시 Naver 미호출(call_count==0)·yf 직행
    - per-metric source 라벨 정확("DART"/"Naver"/"yf")
    - 전 소스 결손 시 value=None + 한국어 사유, raise 없음
    - 기존 전체 스위트 회귀 없음 (`uv run pytest` GREEN)
  </acceptance_criteria>
  <done>fundamentals.py KR 분기(metric별 차등 폴백+provenance), US+KR 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Naver HTML → 파서 | 신뢰 불가 외부 입력, EUC-KR 디코딩·float 파싱 경계. |
| DART 응답 status → 산식 | 쿼터/결손 응답이 잘못된 값으로 해석되지 않게 가드. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Naver HTML 파싱 | mitigate | bs4 텍스트만, `float(text.replace(",",""))` try/except, select_one None 가드, eval 금지(ASVS V5). UTF-8 디코딩(03-02 A5 [VERIFIED] — euc-kr 아님). |
| T-03-10 | Tampering (도메인 무결성) | DART status/thstrm_amount | mitigate | status 4분기 한국어 사유, int 파싱 try/except, 결손 시 value=None(0 금지 D-05). |
| T-03-11 | Denial of Service | DART 쿼터 소진 | mitigate | @throttled_dart 2 RPS·7d 캐시·status "020" 시 Naver 폴백(2번째 키 발급 금지, Pitfall 3). |
| T-03-12 | Denial of Service / 탐지 차단 | Naver 스크래핑 볼륨 (ToS·IP 차단) | mitigate | D-07 — run당 `NAVER_FALLBACK_CAP=20` 호출 상한, PER 단일 지표 폴백 전용, @throttled_yahoo 2 RPS 보수적, 429/403·소프트블록은 None 안전 처리. 상한 도달은 DART 1차 점검 신호로 간주(스크래핑 확대 금지). |
</threat_model>

<verification>
- `uv run pytest tests/test_dart_client.py tests/test_naver_scraper.py tests/test_fundamentals.py -x` GREEN
- 기존 전체 스위트 회귀 없음
</verification>

<success_criteria>
- FUND-03: DART finstate_all(stock_code 직접) + account_nm 매핑 → 재무 산출
- FUND-05: KR 폴백(DART→Naver→yf, metric별 차등) + provenance 라벨
- Naver는 PER만 폴백, GPM/OPM은 DART→yf
- D-07: 네이버는 소수 폴백 전용 — run당 NAVER_FALLBACK_CAP(기본 20) 호출 상한, 초과 시 yf 직행 + 사유
</success_criteria>

<output>
Create `.planning/phases/03-edgar-dart-yfinance-naver/03-04-SUMMARY.md` when done
</output>
