---
phase: 03-edgar-dart-yfinance-naver
plan: 05
type: execute
wave: 5
depends_on: ["03-03", "03-04"]
files_modified:
  - src/stocksig/runner.py
  - src/stocksig/main_run.py
  - src/stocksig/output/sheet_portfolio.py
  - tests/test_runner.py
  - tests/test_sheet_portfolio.py
autonomous: true
requirements: [PORT-05, FUND-05]
must_haves:
  truths:
    - "시트1의 각 티커 행 R/S/T/U(col 17~20)에 PER/PEG/GPM/OPM 값이 채워지고 PER/PEG는 #,##0.00, GPM/OPM은 0.00% 포맷이다"
    - "각 펀더멘털 셀에 데이터 소스(EDGAR/DART/yf/Naver) 또는 결손 사유가 Excel 셀 주석으로 표시된다"
    - "결손 셀은 빈 칸 + 주석에 한국어 사유만 (0/-999999 미사용)"
    - "펀더멘털 fetch 예외는 티커 실패가 아니며 시세 정상 티커는 시트가 생성되고 펀더멘털만 빈 셀이 된다"
  artifacts:
    - path: "src/stocksig/output/sheet_portfolio.py"
      provides: "PORTFOLIO_COLUMNS 21열 + _write_fund_cell + write_comment"
      contains: "PER"
    - path: "src/stocksig/runner.py"
      provides: "TickerResult.fundamentals 필드 + process_ticker PASS 1b try/except 흡수"
      contains: "fundamentals"
  key_links:
    - from: "src/stocksig/runner.py"
      to: "io/fundamentals.fetch_fundamentals"
      via: "fundamentals_fn 클로저 주입 (PASS 1b, try/except 흡수)"
      pattern: "fundamentals_fn"
    - from: "src/stocksig/output/sheet_portfolio.py"
      to: "TickerResult.fundamentals"
      via: "col 17~20 write_number + write_comment"
      pattern: "write_comment"
---

<objective>
배선(wiring) — `runner.py`에 `TickerResult.fundamentals` 필드(기본 None, 하위호환) + `process_ticker` PASS 1b(시세 fetch 직후 `fundamentals_fn` try/except 흡수, last_close 주입), `main_run.py`가 `fetch_fundamentals` 클로저를 `run_all`에 전달, `sheet_portfolio.py` PORTFOLIO_COLUMNS 17→21열 + `_write_fund_cell`(값 write_number + 출처 write_comment, 결손 시 write_blank + 사유 주석). 신규 Format 0개 — 기존 캐시 재사용.

Purpose: 03-03/03-04가 만든 `fetch_fundamentals`를 per-ticker 파이프라인에 연결하고 시트1 R/S/T/U(D-01)에 값+주석(D-02)으로 렌더. PORT-05(값+출처)·FUND-05(provenance 시각화)·D-05(빈셀+사유)·D-06(시트1만, 종목별 시트 불변)·D-disc-10(펀더멘털 결손≠티커 실패) 충족.
Output: 확장된 runner.py/main_run.py/sheet_portfolio.py + 테스트 확장.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md
@.planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md

<interfaces>
<!-- 03-03/03-04 완성 오케스트레이터 -->
From src/stocksig/io/fundamentals.py:
  def fetch_fundamentals(ticker, market, last_close) -> FundamentalsResult
  @dataclass class MetricCell: value: float|None; source: str|None; note: str|None
  @dataclass class FundamentalsResult: per; peg; gpm; opm

<!-- runner.py 수정 대상 (in-file analog) -->
From src/stocksig/runner.py:
  @dataclass class TickerResult: spec; enriched_df; market   # + fundamentals: FundamentalsResult|None = None
  def process_ticker(spec, classify_market, pipeline):       # + fundamentals_fn=None 인자
      market = classify_market(spec.symbol); df = pipeline(spec.symbol)
      reason = _validate_row_count(...); if reason: raise ValueError(reason)  # 시세 결손=티커 실패 (유지)
      return TickerResult(spec=spec, enriched_df=df, market=market)
  def run_all(specs, classify_market, pipeline):             # + fundamentals_fn=None, submit에 전달

<!-- main_run.py 수정 대상 -->
From src/stocksig/main_run.py:
  def _make_pipeline() -> Callable[[str], pd.DataFrame]   # 기존
  results, failures = run_all(specs, classify_market, pipeline)   # + fundamentals_fn 전달
  # last_close = enriched_df.iloc[-1]["Close"] = 시트1 col 4 (PER 분자 재사용)

<!-- sheet_portfolio.py 수정 대상 (in-file analog) -->
From src/stocksig/output/sheet_portfolio.py:
  PORTFOLIO_COLUMNS = [..., "(주)임펄스"]   # 현재 index 16 끝 (17열)  → +PER/PEG/GPM/OPM = 21열
  ws.write_number(row, 4, float(close), formats[(SigmaBucket.DEFAULT, "price")])   # col 4 analog
  def _nan(value) -> bool: ...
  ws.set_column(0, len(PORTFOLIO_COLUMNS)-1, 14)   # 21열 자동 적용
  ws.freeze_panes(5, 1)   # 유지 (Phase 4 OUT-04 — 본 plan 건드리지 않음)
  # Format 키: (SigmaBucket.DEFAULT, "price")=#,##0.00 / (SigmaBucket.DEFAULT, "percent_ratio")=0.00%
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runner.py PASS 1b + main_run.py 클로저 주입</name>
  <read_first>
    - src/stocksig/runner.py (수정 대상 — L34-41 TickerResult, L63-79 process_ticker, L82-130 run_all)
    - src/stocksig/main_run.py (수정 대상 — L198-211 _make_pipeline 클로저, L214-271 run / L234 run_all 호출)
    - src/stocksig/io/fundamentals.py (fetch_fundamentals — 03-04 완성)
    - tests/test_runner.py (callable 주입 analog), tests/conftest.py
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"runner.py 통합"(D-disc-10), §"Anti-Patterns"(펀더멘털 결손≠티커 실패)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §runner.py 수정, §"의존성 주입 클로저"
  </read_first>
  <behavior>
    - TickerResult에 `fundamentals: "FundamentalsResult | None" = None` 필드 추가 (기본 None → Phase 1/2 3-인자 호출 무회귀)
    - process_ticker에 `fundamentals_fn=None` 인자 추가. 시세 검증(_validate_row_count) 통과 후, fundamentals_fn 있으면 last_close=df.iloc[-1].get("Close") 주입해 호출, 예외는 try/except로 흡수(logger.warning "펀더멘털 fetch 예외 흡수")하고 fundamentals=None 유지 → 시세 정상이면 TickerResult 반환(티커 실패 아님)
    - run_all에 `fundamentals_fn=None` 인자 추가, executor.submit에 전달
    - main_run.run이 fetch_fundamentals를 클로저로 run_all에 전달
  </behavior>
  <action>
    runner.py: `TickerResult` dataclass에 `fundamentals` 필드 `= None` 추가. `process_ticker` 시그니처에 `fundamentals_fn: Callable | None = None` 추가, 시세 검증 직후 RESEARCH §runner.py 통합 패턴대로 try/except 1블록(last_close 주입, 예외 흡수 → fund=None). `run_all` 시그니처에 `fundamentals_fn=None` + submit 인자 전달. 기존 D-06 부분데이터=티커실패 경로 무손상.
    main_run.py: `run`에서 `from stocksig.io.fundamentals import fetch_fundamentals` import 후 `run_all(specs, classify_market, pipeline, fundamentals_fn=fetch_fundamentals)` (또는 _make_pipeline과 동일 클로저 스타일 주입). run 시작 시 `naver_scraper.reset_naver_count()` 1회 호출(D-07 — run마다 네이버 폴백 카운터 초기화, 다회 실행/장수 프로세스 안전). load_env가 이미 EDGAR_USER_AGENT_EMAIL/OPENDART_API_KEY 검증(fail-fast) — 추가 검증 불필요.
    RED→GREEN: test_runner.py — fundamentals_fn stub 주입 시 TickerResult.fundamentals 채워짐, fundamentals_fn 예외 시 fund=None이고 TickerResult 정상 반환(티커 실패 아님) 단언. 3-인자(fundamentals_fn 없음) 호출 시 fundamentals=None(하위호환) 단언. 기존 test_runner.py 케이스 무회귀.
  </action>
  <verify>
    <automated>uv run pytest tests/test_runner.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_runner.py -x` GREEN (기존 + 신규 케이스)
    - 소스 단언: runner.py TickerResult에 `fundamentals` 필드 `= None`, process_ticker에 `fundamentals_fn` 인자
    - 펀더멘털 예외 흡수 단언: fundamentals_fn이 raise해도 TickerResult 반환(failures에 안 들어감), fundamentals=None
    - 하위호환 단언: fundamentals_fn 미전달 시 fundamentals=None, Phase 1/2 smoke 무회귀
    - main_run.run이 fetch_fundamentals를 run_all에 전달 (소스 단언)
  </acceptance_criteria>
  <done>runner PASS 1b(예외 흡수) + main_run 클로저 주입, 하위호환 유지, 테스트 GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sheet_portfolio.py 21열 + _write_fund_cell + write_comment</name>
  <read_first>
    - src/stocksig/output/sheet_portfolio.py (수정 대상 — L34-52 PORTFOLIO_COLUMNS, L73-81 _nan, L97-188 _write_success_row, L118-122 col 4 write_number analog, L222-252 write_portfolio_sheet)
    - src/stocksig/output/writer.py (Format 캐시 — (SigmaBucket.DEFAULT,"price")/(.,"percent_ratio") 재사용, 신규 Format 0개)
    - src/stocksig/io/fundamentals.py (MetricCell/FundamentalsResult — 03-03)
    - tests/test_sheet_portfolio.py (수정 대상 — L88-89 openpyxl _open readback, L95-96 test_column_count 17→21, ws.cell().comment)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-RESEARCH.md §"sheet_portfolio.py — col 17~20 + 주석", Pitfall 6(write_comment 성능)
    - .planning/phases/03-edgar-dart-yfinance-naver/03-PATTERNS.md §sheet_portfolio.py 수정, §"Format 캐시 재사용"
  </read_first>
  <behavior>
    - PORTFOLIO_COLUMNS 끝에 "PER","PEG","GPM","OPM" 추가 → 길이 21 (index 17,18,19,20)
    - _write_fund_cell(ws,row,col,cell,num_fmt,formats): cell.value not None & not nan → write_number(formats[(DEFAULT,num_fmt)]) + cell.source 있으면 write_comment(note or source); 결손 → write_blank + write_comment(note or "조회 실패")
    - _write_success_row 말미(col 16 다음): res.fundamentals None 가드(하위호환 — None이면 4셀 빈칸+주석 "펀더멘털 미수집"); 있으면 PER/PEG=col17/18 "price"(#,##0.00), GPM/OPM=col19/20 "percent_ratio"(0.00%)
    - write_comment 후 num_format 무손상 (값/포맷과 독립, D-02)
    - freeze_panes(5,1)·set_column 기존 라인 유지, 신규 Format 0개
  </behavior>
  <action>
    sheet_portfolio.py: `PORTFOLIO_COLUMNS`에 "PER","PEG","GPM","OPM" 4개 append(D-01 — index 17~20, 총 21열). 신규 헬퍼 `_write_fund_cell(ws, row, col, cell, num_fmt, formats)` — col 4 write_number 패턴(L118-122) + write_comment 추가, 결손 시 write_blank + 주석(D-05 — 0/-999999 금지). `_write_success_row` 말미(L188 이후)에 `if res.fundamentals is not None:` 가드 후 4셀 작성(PER/PEG="price", GPM/OPM="percent_ratio" — RESEARCH 포맷 매핑), None이면 4셀 빈칸+주석 "펀더멘털 미수집"(하위호환). `ws.set_column(0, len(PORTFOLIO_COLUMNS)-1, 14)`은 21열 자동 적용(변경 불필요), `freeze_panes(5,1)` 유지(Phase 4 OUT-04 — 건드리지 않음). 신규 Format 0개(make_workbook 44키 불변 — writer.py 미변경). 실패행 `_write_failure_row`는 col 16 사유 그대로 — 펀더멘털 셀 안 씀.
    RED→GREEN: test_sheet_portfolio.py — `test_column_count` 상수 17→21 갱신. `test_fund_cols`(VALIDATION PORT-05 명령): openpyxl readback으로 col 17~20 값 + `ws.cell(row,col).comment` 주석 텍스트 단언(채워진 셀 source 주석 / 결손 셀 빈값+사유 주석), GPM 셀 num_format이 0.00%·PER 셀 #,##0.00 단언(write_comment 후 포맷 무손상), res.fundamentals=None 시 4셀 빈칸+"펀더멘털 미수집" 주석 단언. MetricCell stub로 결손/정상 혼합 행 구성.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sheet_portfolio.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sheet_portfolio.py::test_fund_cols -x` GREEN (PORT-05)
    - openpyxl readback: col 17~20 값 + ws.cell().comment 주석 존재 단언; 결손 셀 value None + 사유 주석 단언
    - num_format 무손상: PER/PEG 셀 `#,##0.00`, GPM/OPM 셀 `0.00%` (write_comment 후에도)
    - 컬럼 수 21 단언 (test_column_count 갱신)
    - 하위호환: res.fundamentals=None 시 4셀 빈칸 + "펀더멘털 미수집" 주석, raise 없음
    - 신규 Format 0개: make_workbook Format 캐시 44키 불변 (기존 test_add_format_count 무회귀)
    - 기존 전체 스위트 회귀 없음 (`uv run pytest` GREEN — 종목별 시트 97열·D-06 불변)
  </acceptance_criteria>
  <done>시트1 21열 + PER/PEG/GPM/OPM 값+출처 주석, 결손 빈셀+사유, 하위호환·포맷 무손상, 테스트 GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| FundamentalsResult → 시트1 셀 | 결손값이 0/특수값으로 시트·평균 오염되지 않게(D-05). |
| 펀더멘털 fetch 예외 → 파이프라인 | 펀더멘털 결손이 시세 처리(티커 시트 생성) 흐름을 깨지 않게. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Tampering (도메인 무결성) | 결손 펀더멘털 셀 | mitigate | D-05 — write_blank(빈셀) + write_comment(한국어 사유). 0/-999999 미사용 → 평균/정렬 오염 방지. Core Value 보호. |
| T-03-13 | Denial of Service (가용성) | 펀더멘털 fetch 예외 | mitigate | process_ticker try/except 흡수 — 시세 정상 티커는 시트 생성, 펀더멘털만 빈 셀(D-disc-10). 한 티커 펀더멘털 실패가 전체 실행 미중단. |
| T-03-14 | (회귀) | 종목별 시트 97열 / Format 캐시 | mitigate | D-06 — 펀더멘털은 시트1만. 신규 Format 0개·freeze_panes 유지. 기존 전체 스위트 회귀 게이트. |
</threat_model>

<verification>
- `uv run pytest tests/test_runner.py tests/test_sheet_portfolio.py -x` GREEN
- 전체 스위트 `uv run pytest` GREEN (Phase 1/2 130+ 무회귀)
- (수기, Phase gate) `python main.py` 1회 실행 후 시트1 R/S/T/U 4값 + 출처 주석 육안 확인 — VALIDATION Manual-Only
</verification>

<success_criteria>
- PORT-05: 시트1 col 17~20 PER/PEG/GPM/OPM 값 + 출처 셀 주석
- D-05: 결손 빈셀 + 한국어 사유 주석 (0/특수값 미사용)
- D-disc-10: 펀더멘털 결손 ≠ 티커 실패 (시세 정상 시 시트 생성)
- D-06: 종목별 시트 불변, 신규 Format 0개
</success_criteria>

<output>
Create `.planning/phases/03-edgar-dart-yfinance-naver/03-05-SUMMARY.md` when done
</output>
