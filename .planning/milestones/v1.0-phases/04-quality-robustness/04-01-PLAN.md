---
phase: 04-quality-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stocksig/io/cache.py
  - tests/test_cache.py
  - src/stocksig/main_run.py
  - tests/test_smoke_n_tickers.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [EXEC-04]
must_haves:
  truths:
    - "200 티커 실행 종료 시 콘솔에 캐시 hit/miss 집계 통계 한 줄이 출력된다"
    - "실행 종료부에 한국어 최종 요약 블록(총/성공/실패 + 캐시 통계 + 실패 티커 목록)이 출력된다"
    - "기존 per-call 캐시 HIT/MISS 로그와 [k/N] 진행 로그는 변경 없이 유지된다"
    - "REQUIREMENTS.md EXEC-04와 ROADMAP.md Phase 4 성공기준 2번이 D-01(콘솔 요약 대체)에 맞게 갱신된다"
  artifacts:
    - path: "src/stocksig/io/cache.py"
      provides: "모듈 레벨 hit/miss 카운터 + reset_cache_stats/get_cache_stats (lock 보호)"
      contains: "reset_cache_stats"
    - path: "src/stocksig/main_run.py"
      provides: "종료부 한국어 요약 블록 + 시작부 reset_cache_stats 호출"
      contains: "실행 요약"
  key_links:
    - from: "src/stocksig/main_run.py"
      to: "src/stocksig/io/cache.py"
      via: "reset_cache_stats() 시작부 + get_cache_stats() 종료부"
      pattern: "get_cache_stats\\(\\)"
    - from: "src/stocksig/io/cache.py get_ohlcv/get_fund"
      to: "_stats counter"
      via: "HIT/MISS 분기에서 lock-guarded += 1"
      pattern: "_stats\\["
---

<objective>
콘솔 최종 요약 블록 수직 슬라이스 — 캐시 hit/miss 집계 카운터를 `cache.py`에 추가하고, `main_run.run()` 종료부에 한국어 실행 요약 블록(티커 총/성공/실패 + 캐시 통계 + 실패 티커 목록)을 출력한다. D-01 결정에 따라 "데이터 품질 시트"를 만들지 않으므로, EXEC-04의 "이슈를 한곳에서 확인" 취지를 이 콘솔 요약 블록 + 기존 시트1 실패행 + 셀 주석으로 충족한 것으로 REQUIREMENTS.md/ROADMAP.md 텍스트를 갱신한다.

Purpose: 로드맵 SC3(캐시 통계 + 최종 실패 요약)을 만족시키고, D-01에 따른 문서 정합을 맞춘다. 인증 상태 줄은 Plan 03에서 같은 블록에 추가되므로, 이 plan은 블록 골격 + 캐시/실패 줄만 만든다.

Output: lock-보호 캐시 카운터, 종료부 요약 블록, 갱신된 REQUIREMENTS.md/ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quality-robustness/04-CONTEXT.md
@.planning/phases/04-quality-robustness/04-RESEARCH.md
@.planning/phases/04-quality-robustness/04-PATTERNS.md

<interfaces>
<!-- 그라운딩된 기존 시그니처/패턴 — executor는 코드 탐색 없이 사용. -->

cache.py 현행 (확장 대상):
- `get_ohlcv(ticker) -> value` — HIT 분기 `cache.py:51` logger.info("%s | 캐시 HIT (cache HIT, key=%s)", ...), MISS 분기 `:53`
- `get_fund(source, ticker, quarter_label) -> value` — HIT `:92`, MISS `:94`
- 모듈 상단 import: `from __future__ import annotations`, `logging`, `from diskcache import Cache`

naver_scraper.py 카운터 패턴 (차용 원본):
- `_naver_calls: int = 0` 모듈 변수 + `def reset_naver_count() -> None: global _naver_calls; _naver_calls = 0`

main_run.py 현행 run() (확장 대상):
- 시작부 `:232` `naver_scraper.reset_naver_count()` → 이 줄 옆에 `cache.reset_cache_stats()` 추가
- 종료부 `:272-277` `logger.info("main | 워크북 저장: %s", output_path)` + 실패 경고 → 이 뒤에 요약 블록 추가
- `specs`(list[TickerSpec]), `results`(list[TickerResult]), `failures`(list[TickerFailure], 각 `.spec.symbol` 보유) 변수가 종료부에 모두 in-scope
- cache 모듈은 이미 import됨? — `read_first`로 확인 후 미import면 `from stocksig.io import cache` 추가

runner.py 한국어 로그 (변경 금지 — grep 의존):
- `[%d/%d] OK %s` / `총 %d 티커 중 성공 %d / 실패 %d` (`runner.py:147,156-157`)

test_smoke_n_tickers.py 기존 assert (변경 금지):
- `"cache HIT" in r.getMessage()` (per-call 로그 의존), `"총 N 티커 중 성공 X / 실패 Y"` grep
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cache.py hit/miss 집계 카운터 (lock 보호) + reset/get 함수</name>
  <files>src/stocksig/io/cache.py, tests/test_cache.py</files>
  <read_first>
    - src/stocksig/io/cache.py (현행 get_ohlcv/get_fund HIT/MISS 분기 — 카운터 삽입 지점 확인)
    - src/stocksig/io/naver_scraper.py (L34-44 `_naver_calls` + `reset_naver_count` 카운터/리셋 패턴 차용 원본)
    - tests/test_cache.py (기존 테스트 스타일 + Windows 캐시 파일 핸들 close 패턴 확인)
  </read_first>
  <behavior>
    - Test: reset_cache_stats() 호출 후 get_cache_stats()는 {"ohlcv_hit":0,"ohlcv_miss":0,"fund_hit":0,"fund_miss":0} 반환
    - Test: get_ohlcv HIT 1회 → get_cache_stats()["ohlcv_hit"] == 1; MISS 1회 → ["ohlcv_miss"] == 1
    - Test: get_fund HIT/MISS → fund_hit/fund_miss 각각 증가
    - Test: get_cache_stats()는 내부 dict의 복사본을 반환(반환값 변형이 _stats를 오염시키지 않음)
    - Test (race): ThreadPoolExecutor(max_workers=4)로 같은 키 get_ohlcv를 다수 호출해도 hit+miss 합계가 호출 횟수와 정확히 일치(lock 보호 증명)
  </behavior>
  <action>
    cache.py 모듈 레벨에 `import threading`, `_stats = {"ohlcv_hit": 0, "ohlcv_miss": 0, "fund_hit": 0, "fund_miss": 0}`, `_stats_lock = threading.Lock()`을 추가한다. `reset_cache_stats() -> None`(lock 안에서 모든 키 0으로) 과 `get_cache_stats() -> dict[str, int]`(lock 안에서 `dict(_stats)` 반환 — 복사본)을 naver_scraper의 reset_naver_count 패턴대로 추가한다. `get_ohlcv`의 HIT 분기(`cache.py:51` logger.info 줄)에서 `with _stats_lock: _stats["ohlcv_hit"] += 1`, MISS 분기(`:53`)에서 `_stats["ohlcv_miss"] += 1`을 증가시킨다. `get_fund`의 HIT 분기(`:92`)에서 `_stats["fund_hit"] += 1`, MISS(`:94`)에서 `_stats["fund_miss"] += 1`. 기존 per-call `logger.info(... cache HIT/MISS ...)` 로그는 그대로 유지(삭제·강등 금지 — test_smoke가 grep). Pitfall 3: read-modify-write `+=`는 lock으로 보호.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "reset_cache_stats" src/stocksig/io/cache.py` ≥ 1 이고 `get_cache_stats` 존재
    - `grep "_stats_lock" src/stocksig/io/cache.py` 매치(lock 존재)
    - 기존 per-call 로그 문자열 `cache HIT`/`cache MISS`가 cache.py에 여전히 존재 (`grep -c "cache HIT" src/stocksig/io/cache.py` ≥ 2)
    - `uv run pytest tests/test_cache.py -x -q` 전부 green (race 테스트 포함)
  </acceptance_criteria>
  <done>캐시 모듈이 hit/miss를 lock-safe하게 집계하며 reset/get 함수가 노출되고, 기존 per-call 로그는 무손상이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: main_run 종료부 한국어 요약 블록 + 시작부 reset_cache_stats</name>
  <files>src/stocksig/main_run.py, tests/test_smoke_n_tickers.py</files>
  <read_first>
    - src/stocksig/main_run.py (L226-278 run() 본문 — 시작부 reset 삽입 지점 :232, 종료부 요약 삽입 지점 :272-277, cache import 여부)
    - src/stocksig/io/cache.py (Task 1 결과 — reset_cache_stats/get_cache_stats 시그니처)
    - tests/test_smoke_n_tickers.py (기존 caplog assert 스타일 + mock_pipeline_env/env_file fixture)
    - src/stocksig/runner.py (TickerResult/TickerFailure dataclass — failures[].spec.symbol 접근)
  </read_first>
  <behavior>
    - Test (test_summary_block_emitted): run() 실행 후 caplog INFO 레코드에 "실행 요약" 포함 메시지 존재
    - Test: caplog에 "캐시:" + "HIT" 동시 포함 메시지 존재
    - Test: caplog에 "티커: 총" + "성공" + "실패" 패턴의 요약 줄 존재
    - Test (실패 티커 목록): failures가 1건 이상일 때 요약 블록 안에 "실패 티커:" + 각 실패 심볼이 포함된 줄이 출력됨(ROADMAP SC3 "최종 실패 요약 블록")
    - Test: failures가 0건일 때 "실패 티커:" 줄은 생략됨
    - Test (회귀): 기존 "cache HIT" per-call 로그와 "총 N 티커 중 성공 X / 실패 Y" 패턴이 여전히 출력됨(요약 블록이 대체가 아니라 추가임을 증명)
  </behavior>
  <action>
    main_run.run() 시작부 `naver_scraper.reset_naver_count()`(`:232`) 바로 다음 줄에 `cache.reset_cache_stats()`를 추가한다(cache 미import면 `from stocksig.io import cache` 추가). 종료부 `logger.info("main | 워크북 저장: %s", output_path)` 및 실패 경고(`:272-277`) 이후에 요약 블록을 추가한다: `stats = cache.get_cache_stats()` 후 `logger.info("════════ 실행 요약 ════════")`, `logger.info("티커: 총 %d / 성공 %d / 실패 %d", len(specs), len(results), len(failures))`, `logger.info("캐시: OHLCV HIT %d/MISS %d · 펀더멘털 HIT %d/MISS %d", stats["ohlcv_hit"], stats["ohlcv_miss"], stats["fund_hit"], stats["fund_miss"])`. 이어서 실패 티커 목록 줄을 같은 요약 블록 안에 명시한다(ROADMAP SC3 "최종 실패 요약 블록" 취지): `if failures: logger.info("실패 티커: %s", ", ".join(f.spec.symbol for f in failures))` — failures 0건이면 이 줄을 생략한다. 이는 기존 실패 경고(`실패 %d개 — 시트1에 표시됨`)를 대체하지 않고 요약 블록 안에 실패 심볼 나열을 추가하는 것이다(기존 경고 줄은 변경 금지). 인증 상태 줄은 Plan 03에서 같은 블록에 삽입되므로 이 plan에서는 자리만 비워둔다(주석 `# TODO(04-03): 인증 상태 줄`로 명시 가능하나 필수 아님). 기존 runner의 `[k/N]`/`총 N 티커 중` 로그는 변경 금지. test_smoke_n_tickers.py에 test_summary_block_emitted(요약 블록 + 실패 티커 목록 줄 포함) 테스트를 RESEARCH Code Examples 형식대로 추가한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke_n_tickers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "실행 요약" src/stocksig/main_run.py` ≥ 1
    - `grep "실패 티커:" src/stocksig/main_run.py` 매치(요약 블록 내 실패 티커 목록 줄)
    - `grep "reset_cache_stats" src/stocksig/main_run.py` 매치(시작부 호출)
    - `grep "get_cache_stats" src/stocksig/main_run.py` 매치(종료부 사용)
    - `uv run pytest tests/test_smoke_n_tickers.py -x -q` 전부 green (요약 블록 + 실패 티커 목록 + 기존 cache HIT/진행로그 회귀 모두 통과)
  </acceptance_criteria>
  <done>매 실행 종료 시 한국어 요약 블록이 콘솔에 출력되고 캐시 통계와 실패 티커 목록이 정확히 표시되며, 기존 진행/캐시 로그는 무손상이다.</done>
</task>

<task type="auto">
  <name>Task 3: D-01 문서 정합 — REQUIREMENTS.md EXEC-04 + ROADMAP.md Phase 4 SC2 갱신</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (L97 EXEC-04 항목 + Traceability 표)
    - .planning/ROADMAP.md (L99-110 Phase 4 Goal + Success Criteria 2번)
    - .planning/phases/04-quality-robustness/04-CONTEXT.md (D-01 결정 전문)
  </read_first>
  <action>
    REQUIREMENTS.md의 EXEC-04(L97) 텍스트를 D-01에 맞게 갱신한다: 기존 "워크북 마지막 부분에 '데이터 품질' 시트를 두어..."를 "데이터 누락·부분수신·인증실패 등 종목별 이슈를 (a) 콘솔 최종 실행 요약 블록, (b) 시트1 실패행, (c) 펀더멘털 셀 주석으로 한곳에서 확인 가능하게 한다 (D-01: 별도 '데이터 품질' 시트 미생성, 콘솔 요약으로 대체)"로 수정한다. ROADMAP.md Phase 4 Success Criteria 2번(L108 "워크북의 마지막 시트가 '데이터 품질'이며...")을 "잘못된 형식 티커·부분 데이터 수신(<50%)·인증 실패·corp_code 매핑 실패 등이 (a) 콘솔 최종 실패 요약 블록 (b) 시트1 실패행 (c) 셀 주석으로 종목별 사유와 함께 확인된다 (D-01: 별도 시트 미생성)"로 수정한다. 다른 요구사항·SC·Traceability 행은 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -c "데이터 품질' 시트 미생성\|콘솔 요약으로 대체\|D-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md EXEC-04에 "D-01" 및 "콘솔" 언급이 존재 (`grep "D-01" .planning/REQUIREMENTS.md` 매치)
    - ROADMAP.md Phase 4 SC2에 "콘솔" 및 "D-01" 언급이 존재 (`grep -c "D-01" .planning/ROADMAP.md` ≥ 1)
    - "데이터 품질" 별도 시트를 만들라는 명령형 문구가 EXEC-04/SC2에 더 이상 남아있지 않음(콘솔 대체로 재정의됨)
  </acceptance_criteria>
  <done>요구사항·로드맵 문서가 D-01(콘솔 요약 대체) 결정과 정합한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 콘솔 로그 출력 → 사용자/로그파일 | 요약 블록·캐시 통계가 민감정보(API 키)를 노출하면 안 됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | main_run 요약 블록 캐시/티커 통계 로그 | mitigate | 요약 블록은 카운트(정수)·티커 심볼만 출력. API 키·UA·예외 원문 미포함. 인증 상태 줄(Plan 03)에서만 인증 결과를 다루며 키 미포함 사유 사용 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 0개 (RESEARCH §Package Legitimacy Audit "신규 설치 없음") — 설치 task 없음, slopcheck 대상 없음 |
</threat_model>

<verification>
- `uv run pytest tests/test_cache.py tests/test_smoke_n_tickers.py -x -q` 전부 green
- `uv run pytest` 전체 회귀 green (기존 200 passed 무회귀)
- REQUIREMENTS.md / ROADMAP.md D-01 정합 grep 통과
</verification>

<success_criteria>
- 캐시 hit/miss가 lock-safe 집계되고 reset/get 함수가 노출된다
- 매 실행 종료 시 한국어 요약 블록(티커 + 캐시 통계 + 실패 티커 목록)이 출력된다
- 기존 per-call 캐시 로그·[k/N] 진행 로그가 무손상이다
- EXEC-04/SC2 문서가 D-01과 정합한다
</success_criteria>

<output>
Create `.planning/phases/04-quality-robustness/04-01-SUMMARY.md` when done
</output>
</content>
</invoke>
