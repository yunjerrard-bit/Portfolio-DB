---
phase: 04-quality-robustness
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_freeze_panes.py
  - tests/test_color_tone.py
  - src/stocksig/compute/color_rules.py
autonomous: false
requirements: [OUT-04]
must_haves:
  truths:
    - "모든 시트의 1~5행이 frozen 처리됨이 openpyxl 읽기로 자동 회귀 검증된다"
    - "시트1은 행 1~5 + A열까지 고정(freeze_panes == 'B6'), 종목 시트는 행 1~5 고정(== 'A6')"
    - "HARD 버킷(±2σ) 배경색이 그레이스케일 휘도차로 식별 가능함이 자동 테스트로 증명된다"
    - "현행 파스텔/소프트 색 톤이 '강렬하지 않은 톤' 기준을 충족함이 수기 시각 검증된다"
  artifacts:
    - path: "tests/test_freeze_panes.py"
      provides: "OUT-04 frozen panes openpyxl 회귀 테스트"
      contains: "freeze_panes"
    - path: "tests/test_color_tone.py"
      provides: "WCAG 휘도 그레이스케일 구분 테스트"
      contains: "luminance"
  key_links:
    - from: "tests/test_freeze_panes.py"
      to: "src/stocksig/output/sheet_per_ticker.py / sheet_portfolio.py"
      via: "run() end-to-end → openpyxl load_workbook → ws.freeze_panes 단언"
      pattern: "freeze_panes"
    - from: "tests/test_color_tone.py"
      to: "src/stocksig/compute/color_rules.py"
      via: "GREEN_100/RED_100 상수 import + 휘도 계산"
      pattern: "from stocksig.compute.color_rules import"
---

<objective>
검증 수직 슬라이스 — OUT-04 frozen panes(이미 구현됨)에 대한 openpyxl 읽기 회귀 테스트와, COLOR-07/SC4 색 톤 그레이스케일 구분에 대한 WCAG 휘도 자동 테스트를 추가한다. 신규 프로덕션 코드는 없으며, 휘도 테스트가 SOFT/HARD 버킷의 그레이스케일 구분 부족을 드러내면 단일 진원지 `color_rules.py` 상수만 조정한다. 마지막에 수기 시각 검증 체크포인트로 파스텔 톤·흑백 구분을 사람이 확정한다.

Purpose: 후속 phase가 frozen panes/색 톤을 깨뜨리지 못하게 자동 회귀로 못박고, SC4(그레이스케일 ±1σ/±2σ 구분)를 측정 가능하게 만든다.

의존성: Task 1이 `run()`을 end-to-end로 호출하므로, `run()`이 `cache.reset_cache_stats()`/요약 블록을 참조하는 04-01(cache.py·main_run.py 수정) 완료 후에 실행한다(AttributeError 회피). 따라서 wave:2, depends_on:["04-01"].

Output: test_freeze_panes.py, test_color_tone.py, (필요 시) color_rules.py 상수 조정.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quality-robustness/04-RESEARCH.md
@.planning/phases/04-quality-robustness/04-PATTERNS.md

<interfaces>
<!-- 그라운딩된 기존 시그니처 — executor는 코드 탐색 없이 사용. -->

freeze_panes (이미 구현됨 — 검증 대상):
- sheet_per_ticker.py:438 `ws.freeze_panes(5, 0)`  → openpyxl 읽기 시 "A6"
- sheet_portfolio.py:303 `ws.freeze_panes(5, 1)`    → openpyxl 읽기 시 "B6"
- XlsxWriter freeze_panes(row, col) = "첫 비고정 셀" 규약 [CITED: xlsxwriter.readthedocs.io/worksheet.html]

color_rules.py 상수 (color_rules.py:16-26):
- GREEN_800="#2E7D32" GREEN_900="#1B5E20" GREEN_100="#C8E6C9"
- RED_800="#C62828" RED_900="#B71C1C" RED_100="#FFCDD2"
- DEFAULT_BLACK="#000000" BLUE_100/800/900
- 색 단일 진원지(D-02 from Phase 2): 조정 시 이 상수만 변경, writer/시트 무변경

main_run.run() 시그니처 (end-to-end 테스트용):
- `run(tickers_path, env_path, output_dir, summary_only=False) -> Path`
- 04-01 적용 후: 시작부 `cache.reset_cache_stats()` + 종료부 "실행 요약" 블록 포함 (이 plan은 04-01 완료를 전제)
- 시트1 시트명 literal "시트1"

테스트 fixture (차용):
- tests/test_smoke_n_tickers.py — mock_pipeline_env / env_file fixture + openpyxl load_workbook 패턴
- tests/test_cache.py:25-33 — Windows 캐시 파일 핸들 close 패턴(필요 시)
- tests/test_color_rules.py — color_rules 상수 import + 단언 스타일
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_freeze_panes.py — OUT-04 frozen panes openpyxl 회귀</name>
  <files>tests/test_freeze_panes.py</files>
  <read_first>
    - tests/test_smoke_n_tickers.py (mock_pipeline_env/env_file fixture + run() end-to-end + openpyxl load_workbook 패턴)
    - src/stocksig/output/sheet_per_ticker.py (L438 freeze_panes(5,0) — 검증 대상 확인)
    - src/stocksig/output/sheet_portfolio.py (L303 freeze_panes(5,1) — 검증 대상 확인)
    - tests/test_cache.py (L25-33 Windows 캐시 파일 핸들 close 패턴, 필요 시)
  </read_first>
  <behavior>
    - Test (test_all_sheets_freeze_rows_1_to_5): run()으로 AAPL+MSFT 워크북 생성 후 openpyxl로 로드, 모든 시트의 freeze_panes가 None 아니고 "6"으로 끝남(행 6이 첫 비고정 = 행 1~5 고정)
    - Test: wb["시트1"].freeze_panes == "B6" (행 1~5 + A열 고정)
    - Test: 종목 시트(예: "AAPL")의 freeze_panes == "A6" (행 1~5 고정, 열 미고정)
  </behavior>
  <action>
    test_freeze_panes.py를 신규 생성한다. test_smoke_n_tickers.py의 mock_pipeline_env/env_file fixture를 차용해 tmp_path tickers.txt("AAPL\nMSFT\n")로 `out = run(tickers, env_file, tmp_path / "output")`를 호출하고 `wb = openpyxl.load_workbook(out)`로 읽는다. run()은 04-01에서 추가된 `cache.reset_cache_stats()`/요약 블록을 포함하므로, 본 plan은 04-01 완료 후 실행됨을 전제(depends_on 04-01)한다. 모든 `wb.sheetnames`에 대해 `fp = wb[name].freeze_panes`가 None 아니고 `fp.endswith("6")`임을 단언한다. 추가로 `wb["시트1"].freeze_panes == "B6"`, 종목 시트(예 "AAPL")는 `== "A6"`을 단언한다. RESEARCH Code Examples의 frozen panes 회귀 테스트 형식을 따른다. Windows에서 caches 핸들이 잠겨 tmp_path 정리 실패하면 test_cache.py:25-33 close 패턴 적용.
  </action>
  <verify>
    <automated>uv run pytest tests/test_freeze_panes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_freeze_panes.py 존재, `grep -c "freeze_panes" tests/test_freeze_panes.py` ≥ 2
    - `grep "B6" tests/test_freeze_panes.py` 및 `grep "A6" tests/test_freeze_panes.py` 매치
    - `uv run pytest tests/test_freeze_panes.py -x -q` 전부 green
  </acceptance_criteria>
  <done>OUT-04 frozen panes가 openpyxl 읽기 회귀 테스트로 자동 검증되며, 후속 변경이 깨뜨리면 즉시 적색이 된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_color_tone.py — WCAG 휘도 그레이스케일 구분 + (필요 시) 상수 조정</name>
  <files>tests/test_color_tone.py, src/stocksig/compute/color_rules.py</files>
  <read_first>
    - src/stocksig/compute/color_rules.py (L16-26 GREEN/RED 800·900·100 상수 — import 대상 + 조정 시 유일 수정 지점)
    - tests/test_color_rules.py (color_rules 상수 import + 단언 스타일 차용)
    - .planning/phases/04-quality-robustness/04-RESEARCH.md (§색 톤 휘도 검증 — WCAG _rel_luminance 공식 + Pitfall 5)
  </read_first>
  <behavior>
    - Test (test_hard_buckets_distinguishable_grayscale): _rel_luminance(GREEN_100) 와 _rel_luminance(RED_100)의 차가 임계값(실측 후 확정, 초기 0.05) 초과 — HARD 배경색은 흑백에서 구분 가능
    - Test: _rel_luminance가 WCAG relative luminance 공식(0.2126·R + 0.7152·G + 0.0722·B, sRGB 선형화 포함)을 정확히 구현(알려진 흑/백 입력으로 0.0/1.0 근사 단언)
    - Test (SOFT 분리 판정): SOFT 글자색(GREEN_800/RED_800) 휘도차를 측정해 별도 단언 — Pitfall 5대로 SOFT는 본질적으로 약하므로, 휘도차가 임계 미만이면 bold 의존을 문서화하거나 상수 조정으로 휘도차 확보
  </behavior>
  <action>
    test_color_tone.py를 신규 생성한다. RESEARCH §색 톤 휘도 검증의 `_rel_luminance(hex)` 헬퍼(`h.lstrip("#")` → r,g,b 0~1 → sRGB 선형화 `c/12.92 if c<=0.03928 else ((c+0.055)/1.055)**2.4` → `0.2126*r+0.7152*g+0.0722*b`)를 테스트 파일 안에 둔다. `from stocksig.compute.color_rules import GREEN_100, RED_100, GREEN_800, RED_800`. HARD: `abs(_rel_luminance(GREEN_100)-_rel_luminance(RED_100)) > THRESHOLD`(실측 후 확정; 초기 0.05). 실제 두 상수 휘도를 한 번 계산해 임계값을 데이터에 맞게 설정한다(임계값을 통과시키려 억지로 낮추지 말 것 — 측정값 기준 합리적 마진). SOFT(GREEN_800/RED_800)는 별도 테스트로 휘도차를 측정·단언하되, Pitfall 5대로 SOFT가 흑백에서 약하면 (a) bold가 이미 적용됨을 근거로 SOFT 휘도차 임계를 낮게 두거나 (b) SC4 충족을 위해 color_rules.py의 SOFT 글자색 상수를 휘도차가 벌어지도록 조정한다. 상수 조정 시 color_rules.py만 수정(D-02 단일 진원지), writer/시트 코드 무변경. 조정했다면 기존 test_color_rules.py가 깨지지 않는지 확인.
  </action>
  <verify>
    <automated>uv run pytest tests/test_color_tone.py tests/test_color_rules.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_color_tone.py 존재, `grep -ci "luminance" tests/test_color_tone.py` ≥ 1
    - `grep "0.2126" tests/test_color_tone.py` 매치(WCAG 공식 구현)
    - HARD 배경색 휘도차 단언 통과 + SOFT 휘도차 측정 단언 존재
    - color_rules.py 상수를 조정한 경우 tests/test_color_rules.py 무회귀
    - `uv run pytest tests/test_color_tone.py tests/test_color_rules.py -x -q` 전부 green
  </acceptance_criteria>
  <done>그레이스케일 ±1σ/±2σ 구분이 휘도 자동 테스트로 측정 가능하며, HARD 버킷 구분이 통과한다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 수기 시각 검증 — frozen panes 육안 + 파스텔 톤 + 흑백 구분</name>
  <what-built>frozen panes 회귀 테스트, 색 톤 휘도 자동 테스트, (필요 시) 조정된 파스텔 팔레트. 실제 `uv run python main.py` 실행 시 생성되는 `output/portfolio_YYYYMMDD.xlsx`로 미적/시각 판정을 사람이 확정.</what-built>
  <how-to-verify>
    1. 실제 tickers.txt(US+KR 혼합 몇 개)로 `uv run python main.py` 실행 → `output/portfolio_YYYYMMDD.xlsx` 생성 확인
    2. Excel로 열어 임의 시트에서 6행 이하 데이터를 아래로 스크롤 → 1~5행(헤더·중앙값·표준편차)이 항상 보이는지 확인(OUT-04 / SC1)
    3. 시트1과 종목 시트의 ±1σ/±2σ 색이 "강렬하지 않은 파스텔/소프트 톤"인지 육안 확인(COLOR-07 / SC4)
    4. 파일 → 인쇄 → 흑백/회색조 미리보기로 전환 → ±1σ(글자색)와 ±2σ(글자색+배경) 신호가 흑백에서도 방향(매수/과열) 구분 가능한지 확인
    5. 구분이 부족하면 어떤 셀/버킷이 약한지 기록 → 04-02 재계획으로 color_rules.py 상수 조정
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 부족한 색/버킷을 구체적으로 기술</resume-signal>
  <action>자동 검증(Task 1·2) 완료 후, 사람이 실제 워크북을 열어 frozen panes 육안·파스텔 톤·흑백 구분을 위 how-to-verify 절차대로 확정한다. 자동화 불가한 미적/시각 판정만 담당하며, 구분 부족 시 color_rules.py 상수 조정으로 재계획한다.</action>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음) | 이 plan은 테스트 + 색 상수만 다룸. 외부 입력·네트워크·자격증명 경계 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | (n/a) | 테스트/색 상수 | accept | 외부 입력·민감정보 미관여. 색 상수는 정적 hex, 사용자 데이터 무관 |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 0개 (openpyxl은 기존 dev 의존성) — 설치 task 없음 |
</threat_model>

<verification>
- `uv run pytest tests/test_freeze_panes.py tests/test_color_tone.py tests/test_color_rules.py -x -q` green
- `uv run pytest` 전체 회귀 green
- 수기 체크포인트 approved (frozen panes 육안 + 흑백 색 구분)
</verification>

<success_criteria>
- OUT-04 frozen panes가 openpyxl 회귀로 자동 검증된다
- HARD 버킷 그레이스케일 휘도 구분이 자동 테스트로 통과한다
- 사람이 파스텔 톤·흑백 구분을 실파일에서 확정한다
</success_criteria>

<output>
Create `.planning/phases/04-quality-robustness/04-02-SUMMARY.md` when done
</output>
</content>
