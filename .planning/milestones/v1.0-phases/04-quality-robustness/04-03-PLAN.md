---
phase: 04-quality-robustness
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/stocksig/io/auth_check.py
  - tests/test_auth_check.py
  - src/stocksig/io/fundamentals.py
  - src/stocksig/main_run.py
  - tests/test_smoke_n_tickers.py
autonomous: true
requirements: [EXEC-04]
user_setup:
  - service: EDGAR (SEC)
    why: "EDGAR ping이 UA 이메일 유효성/403을 검증 — 기존 .env 키 재사용"
    env_vars:
      - name: EDGAR_USER_AGENT_EMAIL
        source: ".env (Phase 1에서 이미 설정됨, yunjerrard@gmail.com)"
  - service: DART (OpenDART)
    why: "DART ping이 API 키 유효성 검증 — 기존 .env 키 재사용"
    env_vars:
      - name: OPENDART_API_KEY
        source: ".env (사용자 보유, opendart.fss.or.kr)"
must_haves:
  truths:
    - "매 실행 시작 시 tickers.txt에 US 티커가 있으면 EDGAR ping, KR 티커가 있으면 DART ping이 1회씩 실행된다"
    - "해당 시장 티커가 없으면 그 ping은 생략된다(조건부, D-04)"
    - "ping 실패 시 raise하지 않고 한국어 경고만 출력하며 실행은 계속된다(D-02, fail-fast 아님)"
    - "ping 실패한 소스의 1차 펀더멘털 호출은 스킵되되, 독립 소스인 yf 폴백은 살아있다(A4 확정)"
    - "ping 예외 사유에 OPENDART_API_KEY/UA 원문이 EDGAR·DART 양쪽 모두에서 절대 포함되지 않는다(보안)"
    - "종료부 요약 블록에 인증 상태(EDGAR/DART) 한 줄이 추가된다"
  artifacts:
    - path: "src/stocksig/io/auth_check.py"
      provides: "ping_edgar()/ping_dart() raise-금지 순수 함수 + AuthStatus dataclass"
      contains: "AuthStatus"
    - path: "src/stocksig/io/fundamentals.py"
      provides: "fetch_fundamentals에 skip_edgar/skip_dart 인자(1차만 스킵, yf 폴백 유지)"
      contains: "skip_edgar"
    - path: "src/stocksig/main_run.py"
      provides: "조건부 ping 호출(시작) + skip 클로저 주입 + 요약 인증 줄"
      contains: "ping_edgar"
  key_links:
    - from: "src/stocksig/main_run.py"
      to: "src/stocksig/io/auth_check.py"
      via: "markets 집합 판단 후 조건부 ping_edgar/ping_dart 호출"
      pattern: "ping_edgar\\(\\)|ping_dart\\(\\)"
    - from: "src/stocksig/main_run.py _fundamentals_with_auth 클로저"
      to: "src/stocksig/io/fundamentals.py fetch_fundamentals"
      via: "skip_edgar/skip_dart 인자 바인딩 후 run_all fundamentals_fn 전달"
      pattern: "skip_edgar|skip_dart"
---

<objective>
인증 사전검증 수직 슬라이스 (이 phase 유일한 신규 기능) — `io/auth_check.py`에 raise하지 않는 `ping_edgar()`/`ping_dart()` 순수 함수와 `AuthStatus` dataclass를 만들고, `main_run.run()`이 티커 로드 직후 조건부로(US/KR 티커 존재 시) ping한 뒤 (a) 한국어 경고, (b) 실패 소스 1차 펀더멘털 스킵 클로저, (c) 종료부 요약 블록의 인증 줄로 결과를 흘려보낸다. EDGAR 1차 실패 시에도 독립 소스 yf 폴백은 살리고(A4 확정), ping 예외 사유는 EDGAR·DART 양쪽 모두 키/UA 미포함 일반 메시지로 sanitize한다(보안).

Purpose: 200티커 실행 전에 인증 상태를 한 번에 검증해 불필요한 per-call 인증실패 재시도·throttle 대기를 사전 차단(D-02~04). EXEC-04 충족의 인증 부분.

Output: auth_check.py, fetch_fundamentals skip 인자, main_run ping 배선 + 인증 요약 줄.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quality-robustness/04-CONTEXT.md
@.planning/phases/04-quality-robustness/04-RESEARCH.md
@.planning/phases/04-quality-robustness/04-PATTERNS.md

<interfaces>
<!-- 그라운딩된 기존 시그니처/패턴 — executor는 코드 탐색 없이 사용. -->

edgar_client.py (ping 재사용 대상):
- `_resolve_identity() -> str` (L37) — `"<이름> <이메일>"` UA, .env 이메일 우선
- import-time 1회 `set_identity(_resolve_identity())` (L44-45) — import 부수효과로 UA 등록
- `@throttled_edgar` 데코레이터 (throttle.py:35-43, 8 RPS)
- `fetch_edgar_raw(ticker)` (L63, @throttled_edgar) / `fetch_edgar_cached` (L96, 캐시 쓰기 — ping에 사용 금지, Pitfall 1)

dart_client.py (ping 재사용 대상):
- `_resolve_api_key() -> str` (L50)
- `@throttled_dart` (throttle.py:50-58, 2 RPS)
- `_STATUS_NOTES` (L43): {"013":"DART 데이터 미존재"(키 유효), "020":"DART 쿼터 초과"(키 유효)} + _STATUS_OTHER_NOTE
- `fetch_dart_raw(ticker, year)` (L114, @throttled_dart, status 가드 L135-137) / `fetch_dart_cached` (L164 — ping에 사용 금지)
- Pitfall 4: status "013"/"020"은 키 유효 → OK 판정. 키 무효 코드만 실패

fundamentals.py (skip 인자 추가 대상):
- `fetch_fundamentals(ticker, market, last_close, edgar_fn=None, yf_fn=None, dart_fn=None, naver_fn=None) -> FundamentalsResult` (L261) — raise 금지(전 경로 흡수)
- `_fill_us(ticker, last_close, edgar_fn, yf_fn)` (L113) — EDGAR 1차 raw = `edgar_fn(ticker)`, 결손 지표만 yf 보완(per-metric)
- `_empty_result(note=None) -> FundamentalsResult` (L59) — 전 지표 결손 + note
- `MetricCell(value, source, note)` (L32)

market_kind.py:
- `classify_market(symbol) -> "US"|"KR"` (이미 main_run.py:49에 import됨)

main_run.py run() (Plan 04-01이 이미 수정함 — 충돌 주의):
- 시작부: `naver_scraper.reset_naver_count()` + `cache.reset_cache_stats()`(04-01 추가) 직후, `read_tickers_extended`(:234) + `logger.info("티커 N개 로드 완료")`(:235) 다음이 ping 삽입 지점
- PASS 1 호출: `run_all(specs, classify_market, pipeline, fundamentals_fn=fetch_fundamentals)` (:239) — 여기 fundamentals_fn을 클로저로 교체
- 종료부: 04-01이 만든 "실행 요약" 블록 — 티커 줄과 캐시 줄 사이에 인증 줄 삽입

runner.py:
- `run_all(specs, classify_market, pipeline, fundamentals_fn) -> (results, failures)` (L109), fundamentals_fn 시그니처 `(ticker, market, last_close) -> FundamentalsResult` (L73,113)

auth_check.py 목표 계약 (RESEARCH Pattern 1):
- `@dataclass class AuthStatus: edgar_ok: bool|None=None; dart_ok: bool|None=None; edgar_note: str|None=None; dart_note: str|None=None` (None=ping 미실행)
- `ping_edgar() -> tuple[bool, str|None]` — raise 금지, (ok, 키미포함 사유)
- `ping_dart() -> tuple[bool, str|None]` — raise 금지

테스트 fixture:
- tests/test_edgar_client.py (Company/mocker.patch 패턴), tests/test_naver_scraper.py (httpx graceful-None)
- tests/test_smoke_n_tickers.py (caplog + mock_pipeline_env/env_file)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: io/auth_check.py — ping_edgar/ping_dart (raise 금지, 키 미노출) + AuthStatus</name>
  <files>src/stocksig/io/auth_check.py, tests/test_auth_check.py</files>
  <read_first>
    - src/stocksig/io/edgar_client.py (L37-45 _resolve_identity + set_identity import 부수효과, @throttled_edgar 사용, fetch_edgar_raw)
    - src/stocksig/io/dart_client.py (L42-57 _STATUS_NOTES + _resolve_api_key, L114-143 fetch_dart_raw status 가드, @throttled_dart)
    - src/stocksig/io/fundamentals.py (L309-313 except Exception 흡수 계약 — ping이 차용)
    - src/stocksig/io/naver_scraper.py (HTTP graceful-None 패턴)
    - tests/test_edgar_client.py (mocker.patch 패턴 + 소스 단언 스타일)
    - .planning/phases/04-quality-robustness/04-RESEARCH.md (Pattern 1, Pitfall 1·2·4, §Security Domain — 키 누설 방지)
  </read_first>
  <behavior>
    - Test: ping_edgar() 성공 경로 → (True, None), raise 없음
    - Test: ping_edgar() 내부 호출이 403 예외 → (False, "EDGAR 403 (UA 확인)"), raise 없음
    - Test: ping_edgar() 내부 호출이 일반 예외 → (False, 키/UA 미포함 일반 사유), raise 없음
    - Test (보안 EDGAR): ping_edgar 내부 호출이 던지는 예외에 EDGAR UA/이메일 값(예: "yunjerrard@gmail.com")을 주입해도 반환 note에 그 UA/이메일 값이 포함되지 않음 — EDGAR note는 키/UA 미포함 고정 사유여야 함
    - Test: ping_dart() status "013"/"020"(키 유효) → (True, None) — Pitfall 4 false-negative 방지
    - Test: ping_dart() 키 무효 status/예외 → (False, "DART 인증 실패"류 키 미포함 사유), raise 없음
    - Test (보안 DART): ping_dart 예외에 OPENDART_API_KEY 문자열을 주입해도 반환 note에 그 키 값이 포함되지 않음
    - Test (소스 단언): auth_check.py 소스에 "except Exception" 존재(raise 금지 계약), throttle 경유 또는 캐시 미경유 호출
    - Test (AuthStatus): 기본 인스턴스의 4필드가 모두 None
  </behavior>
  <action>
    io/auth_check.py를 신규 생성한다. `@dataclass class AuthStatus(edgar_ok: bool|None=None, dart_ok: bool|None=None, edgar_note: str|None=None, dart_note: str|None=None)` (None=ping 미실행). `ping_edgar() -> tuple[bool, str|None]`: try 안에서 캐시 미경유 가벼운 식별 호출(Pitfall 1 — `fetch_edgar_cached` 금지). **EDGAR ping 경로는 httpx 직접 GET 단일 경로로 확정한다(Pitfall 2 혼합 금지 — edgartools 부수효과 경로는 사용하지 않음)**: `edgar_client._resolve_identity()`로 User-Agent 헤더를 구성하고 SEC 식별/소형 엔드포인트(예: `https://www.sec.gov/cgi-bin/browse-edgar` 또는 company-facts 소형 GET)에 httpx로 1회 GET, `@throttled_edgar` 경유, `resp.raise_for_status()`로 403/4xx만 검출한다. 성공 → logger.info("auth | EDGAR 인증 OK") + return (True, None). `except Exception as e`(D-02 흡수): "403" in str(e)면 note="EDGAR 403 (UA 확인)", 아니면 note="EDGAR 인증 실패"로 **고정 한국어 사유**를 쓴다 — 예외 원문 e를 note에 보간하지 말 것(`f"...: {e}"`/`{e}` in note 금지, 보안: UA/이메일 누설 가능). logger.warning("auth | ⚠ %s — 미국 펀더멘털 결손 처리됩니다", note) + return (False, note). `ping_dart() -> tuple[bool, str|None]`: 캐시 미경유 가벼운 호출(opendartreader list 1건 또는 `dart_client._STATUS_NOTES` 가드 경유, `@throttled_dart`). status "013"/"020"은 키 유효 → (True, None)(Pitfall 4). 키 무효/예외 → note="DART 인증 실패"(키 미포함 고정 한국어 사유, `f"...: {e}"`/`{e}` in note 금지 — opendartreader가 키를 URL에 넣으므로 §Security) + logger.warning + return (False, note). test_auth_check.py를 mocker.patch로 외부 호출(httpx GET·opendartreader)을 차단해 성공/403/일반예외/키유효status/보안(EDGAR UA·DART 키 양쪽 미노출)/조건부 골격을 단언한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - io/auth_check.py 존재, `grep -c "AuthStatus" src/stocksig/io/auth_check.py` ≥ 1, `grep "except Exception" src/stocksig/io/auth_check.py` 매치
    - DART note에 예외 원문 보간 없음 — `grep -n "DART 인증 실패" src/stocksig/io/auth_check.py` 매치되고 그 줄에 `{e}` 미포함
    - EDGAR note에 예외 원문 보간 없음 — `grep -n "EDGAR 인증 실패\|EDGAR 403" src/stocksig/io/auth_check.py` 매치되고 그 줄들에 `{e}` 미포함
    - 보안 테스트(EDGAR UA·DART 키 양쪽 미노출) 포함 `uv run pytest tests/test_auth_check.py -x -q` 전부 green
    - ping 함수가 캐시 쓰기 경로(`fetch_edgar_cached`/`fetch_dart_cached`)를 호출하지 않음 (`grep "fetch_edgar_cached\|fetch_dart_cached" src/stocksig/io/auth_check.py` 무매치)
    - EDGAR ping이 httpx 직접 GET 단일 경로로 구현됨 (`grep "httpx" src/stocksig/io/auth_check.py` 매치, edgartools 부수효과 분기 부재)
  </acceptance_criteria>
  <done>ping 함수가 절대 raise하지 않고 (ok, 키/UA 미포함 사유)를 EDGAR·DART 양쪽 모두 반환하며, 키 유효 status를 OK로 올바르게 판정하고 캐시를 오염시키지 않는다. EDGAR ping은 httpx 단일 경로다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fetch_fundamentals skip_edgar/skip_dart 인자 (1차만 스킵, yf 폴백 유지)</name>
  <files>src/stocksig/io/fundamentals.py, tests/test_fundamentals.py, tests/test_auth_check.py</files>
  <read_first>
    - src/stocksig/io/fundamentals.py (L113-156 _fill_us EDGAR 1차+yf 보완, L261 fetch_fundamentals 시그니처, L59 _empty_result, KR 경로 _fill_kr)
    - .planning/phases/04-quality-robustness/04-RESEARCH.md (Pattern 3 + Open Q2/A4 — yf 폴백 유지 권장)
    - tests/test_fundamentals.py (기존 fetch_fundamentals 테스트 스타일, edgar_fn/yf_fn mock)
  </read_first>
  <behavior>
    - Test: fetch_fundamentals(market="US", skip_edgar=True) → EDGAR 1차 호출(edgar_fn)을 건너뛰고, yf_fn 폴백으로 채울 수 있는 지표는 채워짐(독립 소스 유지, A4)
    - Test: skip_edgar=True 시 결손 지표 note에 "EDGAR 인증 실패"류 사유가 표시되고 EDGAR source는 없음
    - Test: fetch_fundamentals(market="KR", skip_dart=True) → DART 1차 건너뛰고 Naver→yf 폴백 체인은 작동
    - Test: skip 인자 미전달(기본 False) 시 기존 동작과 완전히 동일(무회귀)
  </behavior>
  <action>
    fetch_fundamentals 시그니처에 `skip_edgar: bool = False`, `skip_dart: bool = False`를 추가한다(기본 False = 기존 동작 무회귀). US 경로(_fill_us 호출 지점): skip_edgar=True면 EDGAR 1차 `edgar_fn(ticker)` 호출을 건너뛰고 모든 1차 지표를 결손(note="EDGAR 인증 실패")으로 둔 뒤, 기존 yf 보완 로직(결손 지표만 yf_fn으로 채움)은 그대로 실행 — yf는 EDGAR와 독립 소스이므로 살린다(A4 확정). 구현은 _fill_us에 skip_edgar를 전달하거나, fetch_fundamentals에서 EDGAR raw를 빈 dict로 대체하는 방식 중 기존 per-metric provenance를 깨지 않는 쪽 선택. KR 경로(_fill_kr): skip_dart=True면 DART 1차를 건너뛰고 Naver→yf 폴백 체인만 작동(D-07 네이버 캡 NAVER_FALLBACK_CAP=20 존중 — 기존 카운터 그대로). raise 금지 계약 유지. test_fundamentals.py(또는 test_auth_check.py)에 skip 경로 테스트를 추가한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fundamentals.py tests/test_auth_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "skip_edgar" src/stocksig/io/fundamentals.py` ≥ 1 이고 `skip_dart` 존재
    - 기본값이 False여서 인자 미전달 호출이 기존과 동일(기존 test_fundamentals.py 무회귀)
    - skip_edgar=True 시 yf_fn 폴백이 여전히 호출됨을 검증하는 테스트 green
    - `uv run pytest tests/test_fundamentals.py tests/test_auth_check.py -x -q` 전부 green
  </acceptance_criteria>
  <done>인증 실패 소스의 1차 호출만 스킵되고 독립 소스 yf/Naver 폴백은 살아있으며, skip 미전달 시 무회귀다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: main_run 조건부 ping 배선 + skip 클로저 + 요약 인증 줄</name>
  <files>src/stocksig/main_run.py, tests/test_smoke_n_tickers.py</files>
  <read_first>
    - src/stocksig/main_run.py (Plan 04-01 적용 후 현행 — :232 reset 영역, :234-235 티커 로드, :239 run_all 호출, 종료부 04-01 "실행 요약" 블록)
    - src/stocksig/io/auth_check.py (Task 1 결과 — AuthStatus, ping_edgar, ping_dart)
    - src/stocksig/io/fundamentals.py (Task 2 결과 — skip_edgar/skip_dart 인자)
    - tests/test_smoke_n_tickers.py (caplog + mock_pipeline_env/env_file fixture, ping mock 지점)
    - .planning/phases/04-quality-robustness/04-RESEARCH.md (Pattern 2·3·5)
  </read_first>
  <behavior>
    - Test: tickers에 US 티커 포함 시 ping_edgar 호출됨, KR 포함 시 ping_dart 호출됨(mocker.patch로 호출 단언)
    - Test (조건부 D-04): US 티커만 있으면 ping_dart 미호출; KR만 있으면 ping_edgar 미호출
    - Test: ping_edgar가 (False, note) 반환해도 run()이 예외 없이 완료되고 워크북 생성됨(D-02)
    - Test: ping 실패 시 US 종목 펀더멘털 fetch에 skip_edgar=True가 전달됨(클로저 경유)
    - Test: 종료부 요약 블록에 "인증:" + "EDGAR" + "DART" 줄이 출력됨
    - Test (보안): 인증 줄에 OPENDART_API_KEY 값/EDGAR UA 값이 포함되지 않음
  </behavior>
  <action>
    main_run.run()에서 `from stocksig.io.auth_check import AuthStatus, ping_edgar, ping_dart` import 추가. 티커 로드(`logger.info("티커 N개 로드 완료")`, :235) 직후에 조건부 ping(Pattern 2)을 삽입: `markets = {classify_market(s.symbol) for s in specs}`, `auth = AuthStatus()`, `if "US" in markets: auth.edgar_ok, auth.edgar_note = ping_edgar()`, `if "KR" in markets: auth.dart_ok, auth.dart_note = ping_dart()`. run_all 호출(:239)의 `fundamentals_fn=fetch_fundamentals`를 skip 클로저로 교체(Pattern 3): `def _fundamentals_with_auth(ticker, market, last_close): return fetch_fundamentals(ticker, market, last_close, skip_edgar=(market=="US" and auth.edgar_ok is False), skip_dart=(market=="KR" and auth.dart_ok is False))` 후 `fundamentals_fn=_fundamentals_with_auth` 전달. 종료부 04-01 요약 블록의 티커 줄과 캐시 줄 사이에 인증 줄 삽입(Pattern 5): `_auth_label(ok, note)` 헬퍼(`None`→"해당없음", `True`→"OK", `False`→f"실패({note})")를 main_run에 정의하고 `logger.info("인증: EDGAR %s | DART %s", _auth_label(auth.edgar_ok, auth.edgar_note), _auth_label(auth.dart_ok, auth.dart_note))`. 보안: note는 Task 1에서 이미 EDGAR·DART 양쪽 모두 키/UA 미포함 고정 사유이므로 _auth_label이 그대로 사용해도 안전(키/UA 미포함 재확인). test_smoke_n_tickers.py에 조건부 ping 호출/skip 전파/인증 요약 줄/D-02 계속 실행/보안(키·UA 미노출) 테스트를 추가한다.
  </action>
  <verify>
    <automated>uv run pytest tests/test_smoke_n_tickers.py tests/test_auth_check.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ping_edgar\|ping_dart" src/stocksig/main_run.py` ≥ 2 (조건부 호출)
    - `grep "skip_edgar\|skip_dart" src/stocksig/main_run.py` 매치(클로저 전파)
    - `grep "인증:" src/stocksig/main_run.py` 매치(요약 인증 줄)
    - 조건부 ping(US만/KR만) + ping 실패 시 계속 실행(D-02) + skip 전파 + 인증 줄 + 보안(키·UA 미노출) 테스트 green
    - `uv run pytest tests/test_smoke_n_tickers.py tests/test_auth_check.py -x -q` 전부 green
  </acceptance_criteria>
  <done>매 실행 시작 시 조건부 인증 ping이 돌고, 실패해도 경고 후 계속하며 실패 소스 1차만 스킵하고, 종료 요약에 키/UA 미포함 인증 상태가 표시된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env 자격증명(EDGAR UA, OPENDART_API_KEY) → ping 호출 | 키가 로그/요약/예외 사유로 새어나가면 안 됨 |
| 외부 API 응답(SEC EDGAR, OpenDART) → ping 판정 | 외부 응답을 신뢰하지 않고 status/예외로만 판정 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | ping_dart/ping_edgar 예외 사유 `f"...: {e}"` (opendartreader가 키를 URL에 포함, EDGAR 예외에 UA/이메일 포함 가능) | mitigate | DART·EDGAR ping note 양쪽 모두 키/UA 미포함 고정 한국어 사유("DART 인증 실패"/"EDGAR 인증 실패"/"EDGAR 403 (UA 확인)")로 치환. 예외 원문 e를 note에 보간 금지. _auth_label도 키/UA 미포함 (Task 1·3 acceptance에서 EDGAR·DART 양쪽 grep + 보안 테스트 검증) |
| T-04-04 | Information Disclosure | 요약 블록 인증 줄 | mitigate | _auth_label은 ok 상태 + 키/UA 미포함 사유만 출력 |
| T-04-05 | DoS (타인 서비스) | ping이 EDGAR/DART에 요청 | accept | D-03 매 실행 1회 + @throttled_edgar/@throttled_dart 경유 — 무시할 부하 |
| T-04-06 | Tampering | ping이 캐시를 오염(Pitfall 1) | mitigate | ping은 캐시 쓰기 경로(fetch_*_cached) 미사용, 캐시 미경유 가벼운 호출 (Task 1 acceptance grep 검증) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 0개 (RESEARCH §Package Legitimacy Audit) — 기존 edgartools/opendartreader/httpx 재사용 |
</threat_model>

<verification>
- `uv run pytest tests/test_auth_check.py tests/test_fundamentals.py tests/test_smoke_n_tickers.py -x -q` green
- `uv run pytest` 전체 회귀 green (기존 200 passed 무회귀)
- 보안: ping note(EDGAR·DART 양쪽)/요약 인증 줄에 API 키·UA 원문 미포함 (grep + 테스트 검증)
- (수기, VALIDATION Manual-Only) 실제 `.env`로 `uv run python main.py` 1회 — EDGAR/DART ping 콘솔 출력 + 인증 요약 줄 육안 확인
</verification>

<success_criteria>
- 조건부 인증 ping(D-04)이 시작 시 1회씩 돈다
- ping 실패는 경고 후 계속(D-02), 실패 소스 1차만 스킵하고 yf 폴백은 유지(A4)
- ping 예외/요약에 API 키·UA 원문이 EDGAR·DART 양쪽 모두 노출되지 않는다(보안)
- 종료 요약 블록에 인증 상태 줄이 추가된다
</success_criteria>

<output>
Create `.planning/phases/04-quality-robustness/04-03-SUMMARY.md` when done
</output>
</content>
