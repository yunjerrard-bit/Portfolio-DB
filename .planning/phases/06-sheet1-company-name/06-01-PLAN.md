---
phase: 06-sheet1-company-name
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stocksig/io/company.py
  - src/stocksig/io/cache.py
  - src/stocksig/runner.py
  - src/stocksig/main_run.py
  - src/stocksig/output/sheet_portfolio.py
  - tests/test_company_name.py
  - tests/test_sheet_portfolio.py
  - tests/test_freeze_panes.py
  - tests/test_smoke_end_to_end.py
autonomous: false
requirements: [COMPANY-01, COMPANY-02, COMPANY-03, COMPANY-04]

must_haves:
  truths:
    - "시트1 B열에 각 성공 티커의 영문 기업명이 표시된다 (한국 종목도 영문)."
    - "기업명은 yfinance .info(longName→shortName→티커 폴백)에서만 조회되고 DART는 호출되지 않는다."
    - "기업명 조회 실패/결손 시 티커로 폴백되어 행이 깨지지 않는다."
    - "컬럼 1칸 시프트 후 시장·티어·산업·가격·σ·임펄스·펀더멘털 PER/PEG/GPM/OPM 셀, 티커 하이퍼링크, 조건부 색, freeze, 실패행 마커가 회귀 없이 동작한다."
    - "재실행 시 기업명 캐시 HIT으로 yfinance 무호출 (30일 TTL)."
  artifacts:
    - path: "src/stocksig/io/company.py"
      provides: "fetch_company_name(ticker) — 캐시 우선 + @throttled_yahoo + @retry + 공유 _SESSION .info + longName/shortName/티커 폴백"
      contains: "def fetch_company_name"
    - path: "src/stocksig/io/cache.py"
      provides: "company 네임스페이스 (30일 TTL) get/put + name_hit/name_miss 통계"
      contains: "def get_company_name"
    - path: "src/stocksig/runner.py"
      provides: "TickerResult.company_name: str | None = None 필드 + process_ticker 채움"
      contains: "company_name"
    - path: "src/stocksig/output/sheet_portfolio.py"
      provides: "헤더 기반 _COL 명명 인덱스 + 기업명 index 1 삽입 (22열)"
      contains: "기업명"
    - path: "tests/test_company_name.py"
      provides: "longName/shortName/티커 폴백 + 캐시 HIT/MISS (.info mock, 네트워크 없음)"
      contains: "def test_"
  key_links:
    - from: "src/stocksig/main_run.py:process_ticker(fundamentals_fn)"
      to: "src/stocksig/io/company.py:fetch_company_name"
      via: "company_name_fn 클로저 주입 → process_ticker가 TickerResult.company_name 채움"
      pattern: "fetch_company_name"
    - from: "src/stocksig/output/sheet_portfolio.py:_write_success_row"
      to: "TickerResult.company_name"
      via: "ws.write_string(row, _COL['기업명'], res.company_name or spec.symbol)"
      pattern: "company_name"
    - from: "src/stocksig/io/company.py:fetch_company_name"
      to: "src/stocksig/io/cache.py:get_company_name/put_company_name"
      via: "캐시 우선 조회 → MISS 시 .info 호출 후 put"
      pattern: "get_company_name|put_company_name"
---

<objective>
시트1(통합 포트폴리오)의 티커 열(A)과 시장 열 사이에 영문 기업명(Company) B열을 추가한다. 기업명은 yfinance `.info`에서만 조회(DART 미사용, 영문명, 한국 종목 포함)하고, 기존 throttle(2 RPS)/retry/캐시 정책을 따른다. 가장 큰 위험인 컬럼 1칸 시프트는 `sheet_portfolio.py`의 하드코딩 정수 인덱스를 헤더 기반 명명 인덱스(`_COL`)로 리팩터한 뒤 "기업명"을 index 1에 삽입해 구조적으로 차단한다.

Purpose: 사용자가 시트1에서 티커만 보고도 종목을 한눈에 식별. Core Value(색 신호)는 불변 — 표시 열 추가일 뿐.
Output: 신규 `io/company.py`, `cache.py` company 네임스페이스, `TickerResult.company_name`, 시트1 22열(기업명 index 1), 신규/회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sheet1-company-name/06-CONTEXT.md
@.planning/phases/06-sheet1-company-name/06-RESEARCH.md

@src/stocksig/io/yf_fundamentals.py
@src/stocksig/io/cache.py
@src/stocksig/io/market.py
@src/stocksig/io/throttle.py
@src/stocksig/runner.py
@src/stocksig/main_run.py
@src/stocksig/output/sheet_portfolio.py
@tests/test_sheet_portfolio.py
@tests/test_freeze_panes.py
@tests/test_smoke_end_to_end.py
@tests/conftest.py

<interfaces>
<!-- 실행자가 코드베이스를 재탐색하지 않도록 핵심 계약을 여기에 박아둔다. 위 @파일에서 추출. -->

기존 yfinance .info 호출 패턴 (io/yf_fundamentals.py) — 재사용 대상:
  from stocksig.io.market import _SESSION          # curl_cffi Chrome 단일 인스턴스 — 신규 세션 금지
  from stocksig.io.throttle import throttled_yahoo  # 2 RPS limiter
  info = yf.Ticker(ticker, session=_SESSION).info or {}   # .info 1회 왕복

기존 retry 데코레이터 스택 (io/market.py:fetch_ohlcv) — 복제 대상:
  @throttled_yahoo
  @retry(
      wait=wait_exponential(multiplier=1, min=2, max=30) + wait_random(0, 1),
      stop=stop_after_attempt(5),
      retry=retry_if_exception_type(YFRateLimitError),
      reraise=True,
  )
  # 데코레이터 순서 = throttled_yahoo(outer) → retry(inner): 재시도 attempt마다 토큰 획득
  from yfinance.exceptions import YFRateLimitError

기존 캐시 패턴 (io/cache.py) — company 네임스페이스 복제 대상:
  _FUND_DIR = Path(".cache/fundamentals"); _FUND_TTL_SECONDS = 7*24*60*60
  _fund_cache: Optional[Cache] = None
  def _get_fund_cache() -> Cache: ...   # lazy mkdir + Cache(...)
  _stats: dict[str,int] = {"ohlcv_hit":0,"ohlcv_miss":0,"fund_hit":0,"fund_miss":0}  # _stats_lock 보호
  def get_fund(...): ...   # HIT/MISS 로그는 "cache HIT"/"cache MISS" 영문 substring 포함 + _stats[...] += 1
  def reset_cache_stats(): for k in _stats: _stats[k]=0   # ★ 신규 키도 여기서 0 초기화돼야 함

기존 결과 운반 (runner.py):
  @dataclass
  class TickerResult:
      spec: TickerSpec
      enriched_df: pd.DataFrame
      market: str                       # "US" | "KR"
      fundamentals: "object | None" = None
      # ← company_name: str | None = None  추가 (기본값 → 기존 3/4인자 호출·테스트 무회귀)

  def process_ticker(spec, classify_market, pipeline, fundamentals_fn=None) -> TickerResult: ...
  def run_all(specs, classify_market, pipeline, fundamentals_fn=None) -> tuple[list, list]: ...
  # ★ 시그니처에 company_name_fn 추가 시 기본값 None 유지 (하위호환). process_ticker가 펀더멘털처럼 try/except로 흡수 — 기업명 결손 ≠ 티커 실패.

main_run.py wiring 지점:
  results, failures = run_all(specs, classify_market, pipeline, fundamentals_fn=_fundamentals_with_auth)
  # ← company_name_fn 클로저 추가 주입. cache.reset_cache_stats() 는 run 시작부에서 이미 호출됨.
  # 요약 로그 "캐시: OHLCV HIT %d/MISS %d · 펀더멘털 HIT %d/MISS %d" 에 기업명 HIT/MISS 추가 권장.

sheet_portfolio.py 현행 하드코딩 col (전수, 모두 _COL["헤더명"]으로 치환):
  PORTFOLIO_COLUMNS = [티커,시장,티어,산업,최신 종가,전일 등락률,DIFF Close vs EMA11,
    DIFF Close vs EMA22,DIFF Close vs EMA96,DIFF Close vs EMA192,거래량,(일)Stoch %K,
    (일)RSI,(주)Stoch %K,(주)RSI,(일)임펄스,(주)임펄스,PER,PEG,GPM,OPM]   # 현 21열
  _write_success_row 정수: 0=티커(write_url 하이퍼링크),1=시장,2=티어,3=산업,4=최신 종가,
    5=전일 등락률,6+i=DIFF EMA{11,22,96,192}(i=0..3),10=거래량,11=(일)Stoch,12=(일)RSI,
    13=(주)Stoch,14=(주)RSI,15=(일)임펄스,16=(주)임펄스
  _FUND_COL_PER=17,_FUND_COL_PEG=18,_FUND_COL_GPM=19,_FUND_COL_OPM=20
  _write_failure_row 정수 (현행): 0=티커(marker),1="?"(marker, 현재 시장 칸),16=f"실패: {reason}"(marker)
  ws.freeze_panes(5, 1)   # ★ 유지 — A열(티커)만 고정. 기업명(B) 비고정 → openpyxl "B6" 불변 → test_freeze_panes 무수정.

★ HIGH-1 실패행 단일 규칙 (확정): 시프트 = 현행 동작을 한 칸 우측 이동.
  시프트 후 `_write_failure_row` 0-base 좌표:
    index0 = 티커 (marker)
    index1 = 기업명 칸 → **빈칸**(write 안 함)
    index2 = 시장 칸 → "?"(marker)  ← 현행 index1 "?"가 한 칸 시프트
    _COL["(주)임펄스"] = f"실패: {reason}"(marker)  ← 현행 index16 reason이 시프트
  openpyxl(1-base) 대응 좌표: 티커=column1, 기업명=column2(빈칸), "?"=column3, reason=column18.

연구 확정 폴백 체인 (Q1/Q3):
  longName → shortName → 티커.  shortName 단독 신뢰 불가(KR 쓰레기값)지만 longName 결손 시 차선책. 최종 None → 티커.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — 기업명 fetch/캐시 실패 테스트 + 시트1 회귀 좌표 갱신 (의도적 RED)</name>
  <files>tests/test_company_name.py, tests/test_sheet_portfolio.py</files>
  <read_first>tests/test_sheet_portfolio.py, tests/test_freeze_panes.py, src/stocksig/io/cache.py, src/stocksig/io/yf_fundamentals.py</read_first>
  <behavior>
    tests/test_company_name.py (신규, .info mock — 네트워크 없음). monkeypatch로 `stocksig.io.company.yf.Ticker` 를 패치해 `instance.info` 를 고정 dict 반환하게 한다. 캐시는 conftest autouse 격리에 더해 company 네임스페이스도 tmp로 격리(`_NAME_DIR`/`_name_cache` monkeypatch):
      - longName 존재 → fetch_company_name("AAPL")=="Apple Inc."
      - longName None, shortName 정상 → shortName 반환
      - longName/shortName 모두 None/빈값 → 티커 폴백 (fetch_company_name("X")=="X")
      - shortName이 KR 쓰레기값이어도 longName 있으면 longName 우선 (longName 1순위 검증)
      - 캐시 HIT: put_company_name 후 yf.Ticker 호출 횟수 0 (mock call_count==0) — COMPANY-04
      - 캐시 MISS: 첫 호출 시 .info 1회 호출 + put 발생
      - .info가 YFRateLimitError를 5회 raise → fetch_company_name이 예외를 흡수하고 티커 폴백 반환 (기업명 결손 ≠ 실패; Pitfall 4)
    tests/test_sheet_portfolio.py (갱신):
      - test_column_count_is_21 → test_column_count_is_22: len(PORTFOLIO_COLUMNS)==22, [1]=="기업명", [-4:]==["PER","PEG","GPM","OPM"]
      - 모든 기존 성공행 `column=N` 좌표 +1 (시장 2→3, 산업 3→4, 최신 종가 5→6, 전일 등락률 6→7, DIFF G열 7→8, 거래량 11→12, Stoch 12→13, RSI 13→14, 임펄스 16→17/17→18, 펀더멘털 18→19·19→20·20→21·21→22)
      - ★ HIGH-1 실패행 단일 규칙 (시프트 = 현행 동작을 한 칸 우측 이동). test_failed_row_in_sheet1 좌표를 다음으로 갱신:
          · `column=1`(티커, 1-base) == "XYZ"  (유지)
          · `column=2`(기업명 칸, 1-base) == 빈칸  →  `in (None, "")` 단언 추가
          · `column=2`("?") → **`column=3`**(시장 칸, 1-base = 0-base index2) == "?"  ← "?" 좌표 +1 시프트
          · reason `column=17` → **`column=18`**(1-base, = _COL["(주)임펄스"] 0-base index17) startswith("실패: ")
          · italic 단언 셀도 `column=17` → `column=18`로 동기화
      - test_input_order_preserved_with_mixed_success_fail 의 실패행 `column=2`("?") 도 동일하게 **`column=3`**로 갱신 (단일 규칙 일관)
      - 신규 test_company_name_column: TickerResult(company_name="Apple Inc.") → ws.cell(row=6, column=2).value=="Apple Inc."; company_name=None → 티커 폴백("AAPL")
      - _result/_make_enriched_df 헬퍼는 company_name 인자 무전달 시 None 기본 (TickerResult 기본값)
  </behavior>
  <action>
    신규 tests/test_company_name.py를 위 behavior대로 작성. mock은 test_smoke_end_to_end._setup_mock_yfinance 패턴 차용하되 대상 모듈은 `stocksig.io.company.yf.Ticker`, 반환값은 `instance.info`(dict)로 설정. 캐시 격리는 conftest._isolated_disk_cache가 ohlcv/fund만 잡으므로 company 네임스페이스(`_NAME_DIR`/`_name_cache`)도 각 테스트에서 monkeypatch tmp 격리 + close.
    test_sheet_portfolio.py는 위 좌표 +1 매핑을 전수 적용. 실패행은 HIGH-1 단일 규칙 좌표(티커 col1, 기업명 col2=빈칸, "?" col3, reason col18)로 갱신. 헤더 카운트 테스트 이름·단언을 22로 갱신. 이 단계에서는 sheet_portfolio.py 본체를 아직 안 고쳤으므로 좌표 테스트도 RED.
    ★ 이 태스크는 의도적 RED 단계다. 구현(`fetch_company_name`/캐시/22열 시프트)이 아직 없으므로 테스트는 반드시 실패해야 정상이다. 코드 인라인 금지 — 위 시그니처/좌표 표를 따라 작성.
  </action>
  <verify>
    <automated>cd "C:/Users/kimyunjae/Documents/Claude 앱 개발/example"; uv run pytest tests/test_company_name.py tests/test_sheet_portfolio.py -x 2>&1 | Select-String -Pattern "FAILED|ImportError|ModuleNotFoundError|AssertionError|passed|failed"</automated>
    <!-- 이 단계 테스트는 의도적 RED: ImportError/ModuleNotFoundError(미구현 fetch_company_name) 또는 AssertionError(22열 미반영)가 나타나면 예상된 RED로 간주. GREEN 전환은 Task 2·3에서 발생. -->
  </verify>
  <acceptance_criteria>
    - `Select-String -Path tests/test_company_name.py -Pattern "def test_"` 가 ≥6건
    - `Select-String -Path tests/test_company_name.py -Pattern "longName|shortName|call_count|YFRateLimitError"` 매치 존재
    - `Select-String -Path tests/test_sheet_portfolio.py -Pattern "is_22|len\(PORTFOLIO_COLUMNS\) == 22"` 매치 존재
    - `Select-String -Path tests/test_sheet_portfolio.py -Pattern "column=3"` 매치 존재 (실패행 "?" 시프트 좌표) + `column=18` 매치 존재 (실패행 reason 시프트 좌표)
    - ★ RED 판별: pytest 출력에 ImportError/ModuleNotFoundError/AssertionError 중 하나가 나타나면 의도된 RED(미구현)로 간주하고 통과로 처리. 두 파일이 PASS로 나오면 오히려 이상 — 구현 전 단계이므로 RED여야 함. 다음 GREEN 태스크(2·3)에서 통과로 전환된다.
  </acceptance_criteria>
  <done>tests/test_company_name.py 작성 완료, test_sheet_portfolio.py 좌표/카운트 +1 갱신 + 실패행 HIGH-1 단일 규칙(col3="?", col18=reason) 갱신 완료, 두 파일이 (구현 전이라) 의도대로 RED 실패(ImportError 또는 AssertionError). 커밋: test(06): 기업명 fetch/캐시 + 시트1 +1 시프트 회귀 좌표 테스트 (RED)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: io/company.py + cache.py company 네임스페이스 (GREEN)</name>
  <files>src/stocksig/io/company.py, src/stocksig/io/cache.py</files>
  <read_first>src/stocksig/io/yf_fundamentals.py, src/stocksig/io/market.py, src/stocksig/io/cache.py, src/stocksig/io/throttle.py</read_first>
  <behavior>
    Task 1의 tests/test_company_name.py가 전부 GREEN으로 전환되어야 한다 (폴백 체인, 캐시 HIT 무호출, MISS 후 put, rate-limit 흡수).
  </behavior>
  <action>
    cache.py에 company 네임스페이스를 펀더멘털 캐시와 동일 구조로 추가 (COMPANY-04, RESEARCH Pattern 2):
      - `_NAME_DIR = Path(".cache/company")`, `_NAME_TTL_SECONDS = 30*24*60*60` (30일 — 기업명 안정적)
      - `_name_cache: Optional[Cache] = None`, `_get_name_cache()` (lazy mkdir + Cache, _get_fund_cache 복제)
      - `make_name_key(ticker) -> str: return ticker` (날짜 무관 — 기업명 불변성)
      - `get_company_name(ticker)` / `put_company_name(ticker, name)` — get_fund/put_fund 복제. get은 "cache HIT"/"cache MISS" 영문 substring 로그 + `_stats["name_hit"]/["name_miss"] += 1` (_stats_lock 보호)
      - `_stats` dict에 `"name_hit":0, "name_miss":0` 추가 → reset_cache_stats가 자동 초기화 (for k in _stats)
    신규 io/company.py 작성 (RESEARCH Pattern 1(b) — 펀더멘털과 독립, 책임 명확):
      - market._SESSION 재사용 (신규 세션 금지), throttle import (Anti-Pattern 준수)
      - `_pick_name(info: dict, ticker: str) -> str`: `info.get("longName") or info.get("shortName")` → falsy면 ticker (longName 우선; D2 영문명; Q3 티커 폴백)
      - `_fetch_info_with_retry(ticker) -> dict`: market.fetch_ohlcv와 동일한 `@throttled_yahoo` + `@retry(YFRateLimitError 5회, wait_exponential 2~30 + jitter, reraise=True)` 스택을 얹어 `yf.Ticker(ticker, session=_SESSION).info or {}` 반환 (Pitfall 4 — .info에 retry 추가)
      - `fetch_company_name(ticker: str) -> str`: 캐시 우선(`cache.get_company_name`) HIT 시 즉시 반환(왕복 0); MISS 시 _fetch_info_with_retry 호출 → _pick_name → `cache.put_company_name(ticker, name)` 후 반환. 모든 fetch 예외(YFRateLimitError 포함)는 try/except로 흡수하고 티커 폴백 반환 + `logger.warning("%s | 기업명 조회 예외 흡수: %s", ticker, type(e).__name__)` (T-04 보안 — 예외 원문/키 미노출, runner.py:100 패턴). 폴백값은 캐시에 put하지 않는다(다음 실행 재시도 허용).
    코드 인라인 금지 — interfaces 블록 시그니처를 따라 구현.
  </action>
  <verify>
    <automated>cd "C:/Users/kimyunjae/Documents/Claude 앱 개발/example"; uv run pytest tests/test_company_name.py -x 2>&1 | Select-String -Pattern "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path src/stocksig/io/company.py -Pattern "_SESSION"` 매치 존재 (공유 세션 재사용)
    - `Select-String -Path src/stocksig/io/company.py -Pattern "throttled_yahoo"` 매치 존재 (2 RPS limiter 적용)
    - `Select-String -Path src/stocksig/io/company.py -Pattern "YFRateLimitError"` 매치 존재 (retry 대상 예외)
    - `Select-String -Path src/stocksig/io/company.py -Pattern "def fetch_company_name"` 매치 존재
    - `Select-String -Path src/stocksig/io/company.py -Pattern "def _pick_name"` 매치 존재
    - `Select-String -Path src/stocksig/io/company.py -Pattern "Session\(impersonate|curl_cffi"` 매치 0 (신규 세션 미생성 — _SESSION 재사용만)
    - `Select-String -Path src/stocksig/io/cache.py -Pattern "_NAME_TTL_SECONDS = 30|def get_company_name|def put_company_name|name_hit"` 모두 매치
    - tests/test_company_name.py 전부 PASS
  </acceptance_criteria>
  <done>io/company.py + cache.py company 네임스페이스 구현, tests/test_company_name.py GREEN. 커밋: feat(06): yfinance 기업명 조회 + 30일 캐시 (longName 폴백·retry·_SESSION 재사용)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 시트1 헤더 기반 명명 인덱스 리팩터 + 기업명 열 삽입 + 파이프라인 배선</name>
  <files>src/stocksig/output/sheet_portfolio.py, src/stocksig/runner.py, src/stocksig/main_run.py</files>
  <read_first>src/stocksig/output/sheet_portfolio.py, src/stocksig/runner.py, src/stocksig/main_run.py, tests/test_sheet_portfolio.py</read_first>
  <behavior>
    Task 1의 test_sheet_portfolio.py 좌표 +1 테스트 + 신규 test_company_name_column이 전부 GREEN. 기존 모든 시트1 테스트(하이퍼링크·색·임펄스·펀더멘털·실패행)가 +1 좌표로 통과.
  </behavior>
  <action>
    sheet_portfolio.py 리팩터 (RESEARCH Pattern 3 — 수기 +1 금지, 구조적 시프트 차단; Pitfall 1):
      - PORTFOLIO_COLUMNS에 index 1로 "기업명" 삽입 → 22열 (D3: 시트1만, 종목시트 불변). 주석의 "21 컬럼"도 22로 갱신.
      - `_COL = {name: i for i, name in enumerate(PORTFOLIO_COLUMNS)}` 단일 진실 출처 추가.
      - `_write_success_row` 전 정수 col을 `_COL["헤더명"]`으로 치환: 티커=_COL["티커"], 시장=_COL["시장"], 티어/산업, 최신 종가=_COL["최신 종가"], 전일 등락률, DIFF는 `_COL["DIFF Close vs EMA11"]+i` (연속 4열 가정 유지), 거래량, (일/주)Stoch·RSI, (일/주)임펄스.
      - col 1(기업명) write 추가: `ws.write_string(row, _COL["기업명"], res.company_name or spec.symbol, formats[(SigmaBucket.DEFAULT, "price")])` 또는 일반 텍스트 포맷. company_name None/빈값 → spec.symbol 폴백 (COMPANY-03).
      - `_FUND_COL_*` 상수를 `_COL["PER"]` 등으로 도출 (17→18 등 자동).
      - ★ HIGH-1 `_write_failure_row` 단일 규칙 (현행 동작을 _COL 기반으로 한 칸 우측 시프트):
          · `ws.write_string(row, _COL["티커"], fail.spec.symbol, marker)`  (0-base index0)
          · 기업명 칸 `_COL["기업명"]`(index1)에는 **아무 것도 write 하지 않는다** — 빈칸 유지.
          · `ws.write_string(row, _COL["시장"], "?", marker)`  (0-base index2 — 현행 "?"가 한 칸 시프트)
          · `ws.write_string(row, _COL["(주)임펄스"], f"실패: {fail.reason}", marker)`  (현행 index16 reason 시프트)
          · 결과 좌표는 test_sheet_portfolio.py 기대값과 정확히 일치: 1-base 티커=col1, 기업명=col2(빈칸), "?"=col3, reason=col18.
      - `ws.freeze_panes(5, 1)` 유지 — 변경 금지 (RESEARCH Q2 — A열만 고정, B6 불변, test_freeze_panes 무수정).
      - `ws.set_column(0, len(PORTFOLIO_COLUMNS)-1, 14)` 는 길이 자동 추종 — 22열 폭 자동.
    runner.py:
      - TickerResult에 `company_name: str | None = None` 필드 추가 (fundamentals 다음, 기본값 → 무회귀).
      - process_ticker / run_all 시그니처에 `company_name_fn: Callable[[str], str] | None = None` 추가(기본 None). process_ticker는 펀더멘털과 동일하게 try/except로 호출해 company_name 채움 — 결손 ≠ 티커 실패(D-disc-10 패턴). 예외 흡수 시 type(e).__name__만 로그.
      - ★ HIGH-3 배선: `run_all` 내부 `executor.submit(...)` 호출에 `company_name_fn` 인자를 **실제로 전달**해야 한다 — 시그니처 추가만으로 끝내지 말 것. 즉 `executor.submit(process_ticker, spec, classify_market, pipeline, fundamentals_fn, company_name_fn)`(또는 키워드 `company_name_fn=company_name_fn`)로 변경해, run_all이 받은 company_name_fn이 각 worker의 process_ticker까지 흘러가도록 한다.
    main_run.py:
      - `from stocksig.io.company import fetch_company_name` import.
      - run_all 호출에 `company_name_fn=fetch_company_name` 추가 주입.
      - 요약 로그 "캐시: ..." 줄에 기업명 HIT/MISS 추가 (get_cache_stats의 name_hit/name_miss) — 선택이나 권장.
    코드 인라인 금지 — interfaces 좌표표/시그니처를 따를 것.
  </action>
  <verify>
    <automated>cd "C:/Users/kimyunjae/Documents/Claude 앱 개발/example"; uv run pytest tests/test_sheet_portfolio.py tests/test_freeze_panes.py -x 2>&1 | Select-String -Pattern "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path src/stocksig/output/sheet_portfolio.py -Pattern "_COL\["` 가 다수 매치 (정수 하드코딩 대체)
    - `Select-String -Path src/stocksig/output/sheet_portfolio.py -Pattern "\"기업명\""` 매치 존재
    - `Select-String -Path src/stocksig/output/sheet_portfolio.py -Pattern "freeze_panes\(5, 1\)"` 매치 1 (freeze 불변)
    - `Select-String -Path src/stocksig/output/sheet_portfolio.py -Pattern '_COL\["시장"\], "\?"'` 매치 존재 (HIGH-1 실패행 "?"가 시장 칸 0-base index2에 위치 — 기업명 칸 미기록)
    - `Select-String -Path src/stocksig/runner.py -Pattern "company_name"` 매치 ≥2 (필드 + process_ticker)
    - ★ HIGH-3: `Select-String -Path src/stocksig/runner.py -Pattern "executor\.submit"` 의 매치 라인(또는 그 직후 인자 라인)에 `company_name_fn` 이 포함됨을 확인 — `Select-String -Path src/stocksig/runner.py -Pattern "submit\(.*company_name_fn|company_name_fn=company_name_fn"` 매치 존재 (시그니처만이 아니라 submit 배선까지 전달)
    - `Select-String -Path src/stocksig/main_run.py -Pattern "fetch_company_name|company_name_fn"` 매치 존재
    - test_sheet_portfolio.py + test_freeze_panes.py 전부 PASS (freeze B6 무수정 확인)
  </acceptance_criteria>
  <done>시트1 22열 명명 인덱스 리팩터·기업명 삽입·실패행 HIGH-1 단일 규칙·freeze B6 유지·파이프라인 배선(run_all→executor.submit company_name_fn 실제 전달) 완료, test_sheet_portfolio.py + test_freeze_panes.py GREEN. 커밋: feat(06): 시트1 기업명 B열 + 헤더 기반 명명 인덱스 시프트 + 파이프라인 배선</done>
</task>

<task type="auto">
  <name>Task 4: 회귀 — smoke 테스트 .info stub 추가 + 시트1 기업명 측정 단언 + 전체 스위트 그린</name>
  <files>tests/test_smoke_end_to_end.py, tests/test_freeze_panes.py</files>
  <read_first>tests/test_smoke_end_to_end.py, tests/test_freeze_panes.py, src/stocksig/io/company.py</read_first>
  <action>
    네트워크 격리 확보 (Pitfall 5): Task 3로 run() 경로에 fetch_company_name이 추가되면, smoke/freeze 테스트가 실제 yfinance `.info`를 칠 수 있다 — company.py는 자체 `yf.Ticker` import를 쓰므로 기존 `_setup_mock_yfinance`(stocksig.io.market.yf.Ticker 패치)로 커버되지 않는다.
      - test_smoke_end_to_end.py: `_setup_mock_yfinance`가 `stocksig.io.company.yf.Ticker`도 패치하도록 확장하거나, fetch_company_name 자체를 monkeypatch(고정 이름 반환)하는 stub을 추가. 가장 단순한 결정론: `mocker.patch("stocksig.io.company.fetch_company_name", side_effect=lambda t: t)` (티커=이름) 또는 `.info` 반환 mock.
      - ★ HIGH-2 측정 가능한 회귀 단언 (smoke가 생성한 .xlsx를 openpyxl로 다시 열어 검증). 다음 두 단언을 test_smoke_end_to_end.py(또는 시트1을 직접 검증하는 test_sheet_portfolio.py의 신규/기존 테스트)에 명시적으로 추가:
          1. 시트1 헤더 단언: `ws.cell(row=5, column=2).value == "기업명"` (B5 헤더 셀 == "기업명"). "company" 단순 문자열 매치는 제거 — 헤더 좌표 단언으로 대체.
          2. 데이터행 B열 비어있지 않음 단언: 첫 성공 데이터 행(예: row=6)의 B열 `ws.cell(row=6, column=2).value` 가 None/"" 가 아님 (기업명 또는 티커 폴백이 반드시 채워짐). pandas로 읽는 경우 `df.iloc[0]["기업명"]` 또는 해당 좌표로 not-empty 단언.
      - test_freeze_panes.py: `mock_pipeline_env` fixture에 company fetch stub 추가 (예: monkeypatch.setattr로 fetch_company_name → 티커 반환). freeze 단언 "B6"는 그대로 통과해야 함(불변).
    어떤 production 코드도 변경하지 않는다 — 테스트 격리 + 단언만.
  </action>
  <verify>
    <automated>cd "C:/Users/kimyunjae/Documents/Claude 앱 개발/example"; uv run pytest -q 2>&1 | Select-String -Pattern "passed|failed|error"</automated>
  </verify>
  <acceptance_criteria>
    - ★ HIGH-2 헤더 단언 존재: `Select-String -Path tests/test_smoke_end_to_end.py,tests/test_sheet_portfolio.py -Pattern '기업명'` 가 매치하며, 그 중 **B5 헤더 좌표 단언 라인**(`cell(row=5, column=2)` 또는 동등 표현 == "기업명")이 존재함을 grep로 확인. 단순 "company" 문자열 매치 기준은 제거됨.
    - ★ HIGH-2 데이터행 B열 not-empty 단언 존재: `Select-String -Pattern 'column=2|iloc\[0\]\["기업명"\]'` 매치 라인 중 데이터 행 B열이 비어있지 않음(`not in (None, "")` 또는 truthy)을 단언하는 라인이 test_smoke_end_to_end.py 또는 test_sheet_portfolio.py에 존재.
    - `Select-String -Path tests/test_smoke_end_to_end.py -Pattern "fetch_company_name|stocksig.io.company"` 매치 존재 (.info stub 또는 fetch_company_name patch — 네트워크 격리)
    - `Select-String -Path tests/test_freeze_panes.py -Pattern "company|fetch_company_name"` 매치 존재
    - `uv run pytest -q` 전체 스위트 PASS (0 failed) — 네트워크 호출 없음
    - test_freeze_panes 3건 모두 PASS (B6 불변 회귀 확인)
  </acceptance_criteria>
  <done>smoke/freeze 테스트에 기업명 fetch stub 추가 + 시트1 B5="기업명" 헤더 단언 + 데이터행 B열 not-empty 단언(HIGH-2) 추가, 전체 pytest 스위트 그린(네트워크 없음). 커밋: test(06): smoke·freeze 기업명 fetch stub + B5 헤더/데이터행 측정 단언 + 전 스위트 회귀 그린</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude가 실제 tickers.txt(US+KR 혼합 포함)로 워크북을 생성한다 (네트워크 호출 발생 — 실제 yfinance .info 기업명 조회). 생성 명령:
      cd "C:/Users/kimyunjae/Documents/Claude 앱 개발/example"; uv run python main.py
    또는 main_run.run() 진입점. 출력 .xlsx 경로(output/portfolio_YYYYMMDD.xlsx)를 사용자에게 알린다. 첫 실행은 기업명 캐시 MISS(.info 왕복), 재실행은 캐시 HIT(무호출) — 요약 로그의 기업명 HIT/MISS로 확인 가능.
  </what-built>
  <how-to-verify>
    1. 생성된 output/portfolio_YYYYMMDD.xlsx 를 Excel로 열어 "시트1" 선택.
    2. B5 헤더가 "기업명"인지, A열(티커)과 C열(시장) 사이에 있는지 확인.
    3. 각 성공 티커 행 B열에 영문 기업명이 표시되는지 (예: AAPL→"Apple Inc.", 005930.KS→"Samsung Electronics Co., Ltd." 영문). 한국 종목도 영문인지.
    4. 기업명 결손 종목이 있다면 B열에 티커가 폴백 표시되는지 (빈칸이 아닌지).
    5. 컬럼 시프트 후 기존 열이 정상인지: 시장(C)·티어·산업·최신 종가·전일 등락률·DIFF EMA 색·거래량·Stoch/RSI·(일)/(주)임펄스 색·펀더멘털 PER/PEG/GPM/OPM 값·주석이 올바른 열에 있고 색이 깨지지 않았는지.
    6. A열 티커 하이퍼링크 클릭 → 해당 종목 시트로 이동하는지.
    7. 스크롤 시 1~5행 + A열(티커)이 고정되는지 (freeze B6 — 기업명은 함께 스크롤).
    8. 실패 티커가 있다면 실패 행 마커가 올바른 열에 있는지: A열=티커, B열(기업명 칸)=빈칸, C열(시장 칸)="?", 마지막 임펄스 열="실패: ...".
    9. (선택) 두 번째 실행 후 콘솔 요약의 기업명 캐시 HIT가 증가하는지 (재실행 무호출 = COMPANY-04).
  </how-to-verify>
  <resume-signal>시트1 기업명 표시·영문명·컬럼 시프트 후 기존 열/색/하이퍼링크/freeze 정상 확인 시 "approved" 입력. 문제 발견 시 어느 열/색/행이 어떻게 어긋났는지 기술.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| yfinance `.info` → 시트 셀 | yahoo가 반환한 longName/shortName 문자열이 시트1 B열에 write_string으로 기록 (외부 비신뢰 문자열) |

신규 trust boundary 없음 — 기존 yfinance 의존성에 `.info` metadata 한 호출 추가일 뿐(OHLCV/펀더멘털과 동일 경로·세션·throttle).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | yfinance longName 응답 → 시트1 B열 | mitigate | write_string으로만 기록(표시 전용). 정렬·필터·수식 미사용(D3). xlsxwriter write_string은 수식 인젝션 미실행 → 셀 안전 |
| T-06-02 | Information Disclosure | company.py fetch 예외 로그 | mitigate | 예외 흡수 시 `type(e).__name__`만 로그(runner.py:100 패턴). URL·API 키·UA 원문 미노출. 기업명 fetch는 무인증(yfinance)이라 누설 자격증명 없음 |
| T-06-03 | Denial of Service | yahoo rate-limit (200종목 .info 추가 왕복) | mitigate | 기존 `@throttled_yahoo`(2 RPS) 통과 + `@retry`(YFRateLimitError 5회) + 30일 캐시(재실행 무호출, COMPANY-04). 신규 limiter/세션 없음 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 설치 0건 — 전 의존성(yfinance/curl_cffi/diskcache/tenacity/xlsxwriter)이 v1.0~v1.1에서 검증·사용 중. RESEARCH Package Legitimacy Audit: "신규 없음". 슬롭체크 불필요 |
</threat_model>

<verification>
- `uv run pytest -q` 전체 스위트 PASS (네트워크 없음 — 모든 yfinance .info/.history mock/stub).
- 시트1 헤더 22열, index 1=="기업명", index -4:==["PER","PEG","GPM","OPM"].
- 시트1 B5 헤더 셀 == "기업명" + 데이터행 B열 not-empty 단언 존재 (HIGH-2 측정 가능 회귀).
- 실패행 단일 규칙: A=티커, B(기업명)=빈칸, C(시장)="?", 마지막 임펄스칸=실패 reason (HIGH-1).
- freeze_panes "B6" 불변 (test_freeze_panes 3건 무수정 통과).
- company.py가 market._SESSION 재사용 + @throttled_yahoo + @retry 스택 보유, 신규 세션 미생성.
- run_all → executor.submit 에 company_name_fn 실제 전달 (HIGH-3 배선).
- cache.py company 네임스페이스 30일 TTL + name_hit/name_miss 통계 + reset_cache_stats 자동 초기화.
- 실제 워크북 생성 후 사용자 시각 검증 통과 (checkpoint).
</verification>

<success_criteria>
1. (COMPANY-01) 시트1 A(티커)와 C(시장) 사이 B열="기업명", 각 성공 행에 영문 기업명 표시 (한국 종목 영문).
2. (COMPANY-02) 기업명은 yfinance `.info`(longName→shortName→티커)에서만 조회, DART 미호출.
3. (COMPANY-03) 조회 실패/결손 시 티커 폴백 — 행 무손상. 컬럼 1칸 시프트 후 시장·티어·산업·가격·σ·임펄스·펀더멘털 PER/PEG/GPM/OPM·티커 하이퍼링크·조건부 색·freeze(B6)·실패행 마커 회귀 없음 (회귀 스위트 그린).
4. (COMPANY-04) 기업명 조회가 기존 throttle(2 RPS)/retry/캐시(30일) 정책 준수 — 재실행 무호출, 200종목 비현실적 지연 없음.
</success_criteria>

<output>
Create `.planning/phases/06-sheet1-company-name/06-01-SUMMARY.md` when done
</output>
