---
phase: 07-sqlite
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/stocksig/io/dart_account_map.py
  - src/stocksig/io/edgar_client.py
  - src/stocksig/io/dart_client.py
  - tests/fixtures/edgar_aapl_facts.py
  - tests/fixtures/dart_005930_finstate.py
  - tests/test_edgar_quarterly.py
  - tests/test_dart_quarterly.py
autonomous: true
requirements: [FUND-07]
must_haves:
  truths:
    - "D-04: fetch_edgar_quarterly_raw가 get_facts() 1회로 분기별 손익(duration)·BS(StockholdersEquity·Liabilities·총자산, instant)·현금흐름·발행주식수를 accession·period_end와 함께 리스트로 반환한다(기존 TTM 손익 4종 경로 밖의 신규 추출 경로)"
    - "D-01: fetch_edgar_quarterly_raw는 EntityFacts에 딸려온 과거 분기를 전부(별도 호출 없이) 추출하고, fetch_dart_quarterly_raw는 최근 3년(years=3, ~12분기) finstate_all만 루프 backfill한다(소스별 차등)"
    - "fetch_dart_quarterly_raw가 IS/CIS/BS/CF 행을 as-reported로 추출한다(D-05)"
    - "신규 raw 필드(total_equity·total_liabilities·total_assets·operating_cash_flow·shares_outstanding)가 D-03 슈퍼셋대로 추출된다"
    - "D-08: 각 행의 quarter 키는 period 종료일 기준 캘린더 분기로 정규화된 'YYYYQn' 문자열이다(EDGAR get_display_period_key·DART bsns_year+reprt_code 매핑)"
    - "결손 필드는 None으로 반환된다(0/-999999 금지, D-05)"
    - "기존 fetch_edgar_raw / fetch_dart_raw(시트1 TTM 경로)는 불변이다(D-06)"
  artifacts:
    - path: "src/stocksig/io/edgar_client.py"
      provides: "fetch_edgar_quarterly_raw(ticker) per-quarter 추출 (additive)"
      contains: "def fetch_edgar_quarterly_raw"
    - path: "src/stocksig/io/dart_client.py"
      provides: "fetch_dart_quarterly_raw(ticker, years) 분기 백필 (additive)"
      contains: "def fetch_dart_quarterly_raw"
    - path: "src/stocksig/io/dart_account_map.py"
      provides: "BS/CF/shares account 매핑 + SJ_DIV_BALANCE_SHEET/SJ_DIV_CASHFLOW 상수"
      contains: "SJ_DIV_BALANCE_SHEET"
  key_links:
    - from: "fetch_edgar_quarterly_raw"
      to: "facts.query().by_period_type"
      via: "D-04: EntityFacts query 빌더 (duration 손익/instant BS·발행주식수 신규 추출)"
      pattern: "by_period_type"
    - from: "fetch_dart_quarterly_raw"
      to: "dart_account_map BS/CF 매핑"
      via: "D-04: _match_amount + SJ_DIV_BALANCE_SHEET/SJ_DIV_CASHFLOW (신규 BS/CF 추출 경로)"
      pattern: "SJ_DIV_BALANCE_SHEET"
---

<objective>
Phase 3 fetch 층에 **per-quarter raw 추출 경로**를 additive로 추가한다 — EDGAR `fetch_edgar_quarterly_raw`(get_facts 1회로 분기별 손익·BS·현금흐름·발행주식수 추출, D-01 "공짜") 와 DART `fetch_dart_quarterly_raw`(최근 3년 finstate_all 분기 백필, D-01), 그리고 `dart_account_map.py`에 D-03 슈퍼셋 신규 필드(자본총계·부채총계·총자산·영업현금흐름·발행주식수) 매핑·sj_div 상수를 추가한다. 기존 시트1 TTM 경로(`fetch_edgar_raw`/`fetch_dart_raw`)는 절대 건드리지 않는다(D-06).

D-01 (소스별 차등 backfill): EDGAR는 `EntityFacts` fetch에 딸려오는 과거 분기를 별도 호출 없이 전부 추출(사실상 공짜), DART는 비용·쿼터를 고려해 최근 3년(`years=3`, ~12분기)만 `finstate_all` 루프로 backfill한다.
D-04 (신규 추출 경로): 현재 EDGAR 클라이언트는 손익 4종 TTM만 가져오므로, BS 항목(StockholdersEquity·Liabilities·발행주식수)·현금흐름·총자산의 분기 추출 경로를 신규로 만든다.
D-08 (캘린더 분기 정규화): 모든 행의 quarter 키는 period 종료일 기준 캘린더 분기(2026Q2 = 4~6월)로 매핑된 "YYYYQn"이다 — fiscal≠calendar 기업도 일관 정렬.

Purpose: store(Plan 01)에 넣을 분기 행의 원천 추출기. D-05대로 as-reported 그대로 추출(분기 분해·Q4 보정은 Phase 8). 슈퍼셋 필드 덕에 Phase 8에서 ROE/PBR/부채비율 등 신규 지표를 외부 재호출 0으로 계산할 수 있다.
Output: edgar_client.py·dart_client.py·dart_account_map.py(수정·additive), fixture 확장 2종, 테스트 2종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sqlite/07-CONTEXT.md
@.planning/phases/07-sqlite/07-RESEARCH.md
@.planning/phases/07-sqlite/07-PATTERNS.md

<interfaces>
<!-- 기존 코드 계약 — executor는 아래를 직접 재사용, 탐색 불필요. -->

edgar_client.py (기존 — 불변, 신규 함수만 추가):
- `from edgar import Company, set_identity` (import-time set_identity 1회 — 추가 호출 금지)
- `@throttled_edgar` 데코레이터 (8 RPS)
- `def _ttm_value(metric) -> float | None`  # None-safe 추출
- 기존 `fetch_edgar_raw(ticker) -> dict` 불변

EDGAR query 빌더 (RESEARCH Pattern 1 [VERIFIED 5.35.0]):
- `facts = Company(ticker).get_facts()`  # EntityFacts, 과거 분기 전부 포함 (D-01)
- `facts.query().by_concept("Revenue").by_period_type("duration").by_period_length(3).execute()` → list[FinancialFact]
- BS/shares: `.by_period_type("instant")`
- FinancialFact 속성: `.numeric_value`(None-safe), `.period_start`, `.period_end`, `.period_type`, `.fiscal_period`, `.accession`, `.unit`, `.get_display_period_key()`("Q2 2026" 캘린더 정규화 D-08)
- [A5] concept 빈 결과 시 헬퍼 fallback: `facts.get_total_assets()`/`facts.get_shareholders_equity()`

dart_client.py (기존 — 불변, 신규 함수만 추가):
- `@throttled_dart` (2 RPS), `from opendartreader import OpenDartReader`
- `def _parse_amount(raw) -> int | None`  # 쉼표 문자열 → int, None-safe
- `def _match_amount(rows, id_candidates, nm_candidates, column="thstrm_amount") -> int | None`  # id 1차/nm 2차
- `_STATUS_NOTES`, status 가드 `isinstance(resp, dict)`, 빈 df 가드 패턴
- `stock_code = ticker.split(".")[0]`
- 기존 `fetch_dart_raw(ticker, year) -> dict` 불변
- `dart.finstate_all(stock_code, year, reprt_code=..., fs_div="CFS")` → DataFrame; 매 행 `rcept_no` 동일

dart_account_map.py (기존 — additive 확장):
- `DART_ACCOUNT_ID_MAP: dict[str, tuple[str,...]]` (account_id 1차, IFRS 표준 태그)
- `DART_ACCOUNT_MAP: dict[str, tuple[str,...]]` (account_nm 2차, 한글)
- `SJ_DIV_INCOME_STATEMENT = ("IS", "CIS")`

DART finstate_all 컬럼 (fixture COLUMNS [VERIFIED]):
rcept_no, reprt_code, bsns_year, corp_code, sj_div, sj_nm, account_id, account_nm, account_detail, thstrm_nm, thstrm_amount, frmtrm_nm, frmtrm_amount, bfefrmtrm_nm, bfefrmtrm_amount, ord, currency, thstrm_add_amount
- reprt_code: 11013=1Q, 11012=반기, 11014=3Q, 11011=연간
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: dart_account_map.py — BS/CF/shares 매핑 + sj_div 상수 + 005930 BS/CF account_id 확정</name>
  <files>src/stocksig/io/dart_account_map.py</files>
  <read_first>
    - src/stocksig/io/dart_account_map.py (기존 DART_ACCOUNT_ID_MAP L31-51 / DART_ACCOUNT_MAP L55-65 / SJ_DIV_INCOME_STATEMENT L69 — 확장 대상)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Open Questions" Q2(L469-472 — BS/CF/shares 미검증 account_id 후보)
    - .planning/phases/07-sqlite/07-PATTERNS.md §"dart_account_map.py"(L157-175)
  </read_first>
  <action>
    D-04 (신규 추출 경로 매핑 기반): `DART_ACCOUNT_ID_MAP`와 `DART_ACCOUNT_MAP`에 D-03 슈퍼셋 신규 키 추가(기존 5종 revenue/gross_profit/op_income/net_income/eps 불변): `total_equity`(자본총계, id 후보 `ifrs-full_Equity`, `ifrs-full_EquityAttributableToOwnersOfParent`; nm 후보 "자본총계","자본총계(지배+비지배)"), `total_liabilities`(부채총계, id `ifrs-full_Liabilities`; nm "부채총계"), `total_assets`(총자산, id `ifrs-full_Assets`; nm "자산총계"), `operating_cash_flow`(영업현금흐름, id `ifrs-full_CashFlowsFromUsedInOperatingActivities`; nm "영업활동현금흐름","영업활동으로 인한 현금흐름").
    신규 상수 추가: `SJ_DIV_BALANCE_SHEET: tuple[str, ...] = ("BS",)`, `SJ_DIV_CASHFLOW: tuple[str, ...] = ("CF",)`.
    발행주식수(shares_outstanding)는 finstate_all에 통상 부재일 수 있음(Open Q2) — 매핑 키만 placeholder로 추가하되 주석으로 "finstate_all 부재 시 Phase 8/yf 위임" 명시(이 plan에서 별도 API 호출 추가 금지).
    각 신규 account_id는 005930 실응답 미검증이므로 `# [Open Q2 — 005930 BS/CF 실응답 1회 확정 후 VERIFIED]` 주석을 단다. executor가 가능하면 `dart.finstate_all("005930", 2025, reprt_code="11011", fs_div="CFS")` 실호출로 sj_div='BS'/'CF' 행의 account_id를 확인해 매핑 정확도를 올린다(불가 시 후보 tuple 유지).
  </action>
  <verify>
    <automated>python -c "from stocksig.io.dart_account_map import DART_ACCOUNT_ID_MAP, SJ_DIV_BALANCE_SHEET, SJ_DIV_CASHFLOW; print('total_assets' in DART_ACCOUNT_ID_MAP, 'operating_cash_flow' in DART_ACCOUNT_ID_MAP, SJ_DIV_BALANCE_SHEET, SJ_DIV_CASHFLOW)"</automated>
  </verify>
  <acceptance_criteria>
    - dart_account_map.py의 DART_ACCOUNT_ID_MAP에 키 `total_equity`, `total_liabilities`, `total_assets`, `operating_cash_flow` 존재 (D-04 신규 BS/CF 추출 기반)
    - 모듈에 `SJ_DIV_BALANCE_SHEET = ("BS",)` 와 `SJ_DIV_CASHFLOW = ("CF",)` 정의 존재
    - 기존 5종 키(revenue/gross_profit/op_income/net_income/eps)와 SJ_DIV_INCOME_STATEMENT 불변
    - 위 verify 명령이 `True True ('BS',) ('CF',)` 출력
    - shares_outstanding 분기 결정(Open Q2 RESOLVED) 반영: `shares_outstanding` 매핑은 placeholder로만 추가하고 finstate_all 실응답에 없으면 본 plan에서 별도 API 호출을 추가하지 않는다(주석으로 "finstate_all 부재 시 Phase 8 또는 yf 보완으로 위임" 명시). 본 phase는 finstate_all에서 가능한 필드만 D-03 슈퍼셋으로 저장(결손은 None).
  </acceptance_criteria>
  <done>신규 BS/CF/현금흐름 매핑·sj_div 상수가 추가되고 기존 매핑은 불변(D-04 추출 경로 기반).</done>
</task>

<task type="auto">
  <name>Task 2: EDGAR per-quarter 추출 — fetch_edgar_quarterly_raw + fixture/테스트</name>
  <files>src/stocksig/io/edgar_client.py, tests/fixtures/edgar_aapl_facts.py, tests/test_edgar_quarterly.py</files>
  <read_first>
    - src/stocksig/io/edgar_client.py (fetch_edgar_raw L63-93 패턴 — throttle·None-safe·한국어 로그·set_identity 불변)
    - tests/fixtures/edgar_aapl_facts.py (기존 mock 표면 — 분기 BS/CF/shares 행 확장 대상)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Pattern 1"(L179-214) + Pitfall 3(L290-294) + Assumptions A1/A5(L456-460)
    - .planning/phases/07-sqlite/07-PATTERNS.md §"edgar_client.py"(L106-129)
  </read_first>
  <action>
    `edgar_client.py`에 `fetch_edgar_quarterly_raw(ticker: str) -> list[dict]` 신규 추가(`@throttled_edgar`). 기존 `fetch_edgar_raw`·`fetch_edgar_cached`·import-time set_identity 불변. `facts = Company(ticker).get_facts()` 1회 호출(D-01: EntityFacts에 과거 분기가 전부 딸려오므로 별도 호출 없이 공짜) 후 query 빌더로 D-04 신규 필드 추출:
    - 유량(손익·현금흐름): `facts.query().by_concept(C).by_period_type("duration").by_period_length(3).execute()` — concept C ∈ {Revenue→"revenue", GrossProfit→"gross_profit", OperatingIncomeLoss→"op_income", NetIncomeLoss→"net_income", EarningsPerShareDiluted→"eps", NetCashProvidedByUsedInOperatingActivities→"operating_cash_flow"}.
    - 저량(BS·발행주식수, D-04 신규 경로): `.by_period_type("instant")` — StockholdersEquity→"total_equity", Liabilities→"total_liabilities", Assets→"total_assets"; 발행주식수는 `facts.shares_outstanding_fact`(있으면 accession 포함 dict 행, 없으면 skip).
    각 FinancialFact → dict 행 생성: ticker, source="EDGAR", quarter=`f.get_display_period_key()`를 "YYYYQn"로 정규화(D-08: period 종료일 기준 캘린더 분기, 예 "Q2 2026"→"2026Q2"), field(논리명), value=`f.numeric_value`(None-safe, D-05), unit=`f.unit`, accession=`f.accession`, period_start/period_end(ISO str), period_type, reprt_code=None. concept 빈 결과 시 [A5] `facts.get_total_assets()`/`get_shareholders_equity()` 헬퍼 fallback. 완료 로그 한국어("%s | EDGAR 분기 raw 추출 완료 (%d행)"). as-reported만 — Q4 보정·분기 분해 금지(Phase 8, D-05).
    `tests/fixtures/edgar_aapl_facts.py`에 분기별 BS(instant)·CF(duration)·shares 행을 mock 표면에 추가(기존 행 보존). `tests/test_edgar_quarterly.py` 신규 — facts를 mock(네트워크 0)하고 fetch_edgar_quarterly_raw가 분기 행 리스트(각 행에 D-08 quarter "YYYYQn"·accession·field·value)를 반환하는지, instant/duration 필드 구분(Pitfall 3, D-04 BS vs 손익), 결손 None 단언.
  </action>
  <verify>
    <automated>python -m pytest tests/test_edgar_quarterly.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - edgar_client.py contains `def fetch_edgar_quarterly_raw(`
    - 함수가 `Company(ticker).get_facts()` 를 1회만 호출(D-01 공짜 backfill, 추가 set_identity 호출 없음)
    - D-04 신규 필드(total_equity/total_liabilities/total_assets/operating_cash_flow/발행주식수) instant/duration 구분 추출
    - quarter 키가 "YYYYQn" 형식(D-08, 예 "2026Q2"), 결손 value=None (0/-999999 아님)
    - 기존 `fetch_edgar_raw` 시그니처·동작 불변 (`python -m pytest tests/ -k edgar -q` 기존 테스트 그린)
    - `python -m pytest tests/test_edgar_quarterly.py -x` exits 0, 네트워크 호출 0(mock)
  </acceptance_criteria>
  <done>EDGAR per-quarter 추출기가 get_facts 1회로 손익·BS·현금흐름·발행주식수 분기 행을 as-reported로 반환하고(D-01·D-04) D-08 캘린더 분기키를 부여하며 fixture 기반 테스트 통과.</done>
</task>

<task type="auto">
  <name>Task 3: DART 분기 백필 — fetch_dart_quarterly_raw + fixture/테스트</name>
  <files>src/stocksig/io/dart_client.py, tests/fixtures/dart_005930_finstate.py, tests/test_dart_quarterly.py</files>
  <read_first>
    - src/stocksig/io/dart_client.py (fetch_dart_raw L114-161 — _parse_amount·_match_amount·_income_rows·status 가드 재사용, 기존 불변)
    - src/stocksig/io/dart_account_map.py (Task 1 추가한 BS/CF 매핑·SJ_DIV_BALANCE_SHEET/SJ_DIV_CASHFLOW)
    - tests/fixtures/dart_005930_finstate.py (COLUMNS·IS_CIS_ROWS·EXPECTED_VALUES — BS/CF 행 확장 대상)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"DART 분기 백필"(L417-438) + Pitfall 4(L296-300) + A3(L458)
    - .planning/phases/07-sqlite/07-PATTERNS.md §"dart_client.py"(L132-154)
  </read_first>
  <action>
    `dart_client.py`에 `fetch_dart_quarterly_raw(ticker: str, years: int = 3) -> list[dict]` 신규 추가(`@throttled_dart`). 기존 `fetch_dart_raw`·`fetch_dart_cached` 불변. D-01 (최근 3년 backfill): `stock_code = ticker.split(".")[0]`, `QUARTER_CODES = ["11013", "11012", "11014", "11011"]`(1Q/반기/3Q/연간). `for year in range(this_year - years, this_year + 1): for code in QUARTER_CODES:` 루프로 `dart.finstate_all(stock_code, year, reprt_code=code, fs_div="CFS")` 호출 — `years=3` 기본으로 ~12분기만 backfill(EDGAR와 차등). 각 호출에 기존 status 가드(`isinstance(resp, dict)` → skip)·빈 df 가드 재사용. 응답에서 `rcept = resp.iloc[0]["rcept_no"]`(매 행 동일, 델타 키). 행 추출 헬퍼(D-04 신규 BS/CF 경로): 손익은 기존 `_income_rows`(SJ_DIV_INCOME_STATEMENT) + `_match_amount`로 5종, BS는 `SJ_DIV_BALANCE_SHEET` 필터 + total_equity/total_liabilities/total_assets, CF는 `SJ_DIV_CASHFLOW` 필터 + operating_cash_flow. 각 필드 → dict 행: ticker, source="DART", quarter=캘린더 정규화(D-08: `bsns_year`+`reprt_code`→"YYYYQn": 11013→Q1, 11012→Q2(반기), 11014→Q3, 11011→Q4), field, value=`_match_amount(...)`(None-safe), unit="KRW", accession=rcept, period_start/period_end=None, period_type(손익=duration/BS=instant), reprt_code=code. DART YTD 누적은 as-reported 그대로 저장(Pitfall 4, 분기 분해는 Phase 8). `thstrm_add_amount`도 별도 행 또는 메타로 보존 권고(Phase 8 입력). 한국어 완료 로그. DART 인스턴스는 매 호출마다 OpenDartReader를 새로 만들지 말 것 — 07-03 fundamentals_delta._get_dart() 싱글톤을 재사용하거나, 그 전에 실행되는 본 plan에서는 dart_client 모듈 레벨 싱글톤(_dart_singleton, double-checked locking)을 도입해 corp_codes 이중 다운로드(Pitfall 1)를 막고 07-03이 이를 공유하도록 한다.
    `tests/fixtures/dart_005930_finstate.py`에 sj_div='BS'(자본총계/부채총계/총자산)·sj_div='CF'(영업현금흐름) 행을 추가(기존 IS_CIS_ROWS 보존). `tests/test_dart_quarterly.py` 신규 — finstate_all을 mock(네트워크 0)하고 fetch_dart_quarterly_raw가 분기 백필 행을 반환, BS/CF 필드 추출·rcept_no accession·결손 None·YTD as-reported·D-08 "YYYYQn" 분기키 단언.
  </action>
  <verify>
    <automated>python -m pytest tests/test_dart_quarterly.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - dart_client.py contains `def fetch_dart_quarterly_raw(` + `QUARTER_CODES = ["11013", "11012", "11014", "11011"]`
    - 기본 `years=3`로 최근 3년만 루프 backfill(D-01 DART 차등)
    - BS 행은 SJ_DIV_BALANCE_SHEET, CF 행은 SJ_DIV_CASHFLOW로 필터됨 (D-04 신규 경로, 손익행 오염 없음)
    - quarter 키가 캘린더 분기 "YYYYQn"(D-08: bsns_year+reprt_code 매핑)
    - 각 행 accession == 응답 rcept_no, value 결손 시 None (0/-999999 아님)
    - DART 값이 YTD as-reported 그대로 저장(분기 분해·뺄셈 없음 — Phase 8 위임)
    - 기존 `fetch_dart_raw` 불변 (`python -m pytest tests/ -k dart -q` 기존 테스트 그린)
    - `python -m pytest tests/test_dart_quarterly.py -x` exits 0, 네트워크 0(mock)
  </acceptance_criteria>
  <done>DART 분기 백필기가 최근 3년(D-01) finstate_all을 루프하며 IS/CIS/BS/CF 행을 as-reported·캘린더 분기키(D-08)로 반환하고 fixture 테스트 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EDGAR/DART JSON 응답 → 파싱 | 외부 신뢰 불가 응답이 추출기로 유입(thstrm_amount 문자열·numeric_value) |
| API 키(OPENDART_API_KEY) → 호출/예외 | crtfc_key가 URL에 포함 — 예외 메시지 누설 위험 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | DART thstrm_amount 파싱 | mitigate | 기존 `_parse_amount`(쉼표 제거 + int try/except, T-03-10) 재사용. 파싱 실패=None(ASVS V5). |
| T-07-05 | Information Disclosure | DART probe/fetch 예외 메시지 | mitigate | 예외 원문·crtfc_key 보간 금지(T-04-03). 타입명만 로그. |
| T-07-06 | Tampering | 신뢰 불가 값으로 raw 오염 | mitigate | 결손=None(D-05), 0/-999999 금지 → Phase 8 산식 오염 차단. |
| T-07-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0(edgartools/opendartreader 기존 의존, RESEARCH Audit [OK]). |
</threat_model>

<verification>
- `python -m pytest tests/test_edgar_quarterly.py tests/test_dart_quarterly.py -x -q` 통과 (네트워크 0)
- `python -m pytest -q` 전 스위트 그린 (기존 fetch_edgar_raw/fetch_dart_raw 시트1 경로 회귀 무손상)
</verification>

<success_criteria>
- per-quarter 추출기가 D-03 슈퍼셋 필드(손익 5종 + BS 3종 + 영업현금흐름)를 as-reported·캘린더 분기키(D-08)·accession과 함께 반환 (SC1)
- D-01: EDGAR는 get_facts 1회(과거 분기 전부), DART는 3년 백필 루프 — 기존 시트1 TTM 경로 불변 (D-06)
- 결손=None, YTD/Q4 분해 없음(Phase 8 위임)
</success_criteria>

<output>
Create `.planning/phases/07-sqlite/07-02-SUMMARY.md` when done
</output>
</content>
