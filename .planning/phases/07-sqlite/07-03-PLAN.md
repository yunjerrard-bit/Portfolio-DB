---
phase: 07-sqlite
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/stocksig/io/fundamentals_delta.py
  - tests/test_fundamentals_delta.py
autonomous: true
requirements: [FUND-08]
must_haves:
  truths:
    - "probe_edgar_accession / probe_dart_rcept가 가벼운 메타 호출로 최신 접수번호만 반환한다"
    - "D-02: 저장된 last_accession과 최신 접수번호가 같으면(델타 없음) full-fetch가 생략되고 신규 저장이 발생하지 않는다(delta_hit, full_fetch 호출 0 — backfill 이후 forward 누적은 변경 시에만)"
    - "D-02: 접수번호가 다르면(새 분기/정정공시 또는 state 없음) full-fetch → upsert → set_last_accession이 실행되어 forward 누적된다"
    - "probe 실패 시 갱신 생략 + 기존 DB 유지(보수적 재추출 안 함)"
    - "OpenDartReader 인스턴스가 종목마다 생성되지 않고 모듈 싱글톤으로 재사용된다"
  artifacts:
    - path: "src/stocksig/io/fundamentals_delta.py"
      provides: "접수번호 probe + delta_state 비교 + skip/refetch 오케스트레이션 + DART 싱글톤"
      contains: "def sync_ticker_history"
      min_lines: 60
    - path: "tests/test_fundamentals_delta.py"
      provides: "FUND-08 + SC3 probe/skip/refetch/probe실패/≈0 호출 spy 테스트"
      contains: "def test_steady_state_zero_full_calls"
  key_links:
    - from: "fundamentals_delta.sync_ticker_history"
      to: "fundamentals_store.get_last_accession / set_last_accession"
      via: "D-02: SELECT 1회 비교 — 같으면 신규 저장 없음, 다르면 forward 누적"
      pattern: "get_last_accession"
    - from: "fundamentals_delta"
      to: "edgar_client.fetch_edgar_quarterly_raw / dart_client.fetch_dart_quarterly_raw"
      via: "D-02: 델타(변경) 있을 때만 full-fetch 호출"
      pattern: "fetch_(edgar|dart)_quarterly_raw"
---

<objective>
접수번호 델타 모듈 `fundamentals_delta.py`를 신규 생성한다 — 가벼운 메타 probe(`probe_edgar_accession`: `Company(ticker).latest("10-Q").accession_number` / `probe_dart_rcept`: `dart.list(corp, kind="A").iloc[0]["rcept_no"]`)로 최신 접수번호만 얻고, store(Plan 01)의 `delta_state`와 비교해 같으면 full-fetch를 생략(평소 ≈0)·다르면 추출기(Plan 02)로 재추출·누적·`last_accession` 갱신한다. probe 실패 시 "갱신 생략, 기존 DB 유지"(Pitfall 2 권고). OpenDartReader는 모듈 싱글톤(Pitfall 1).

D-02 (backfill 이후 forward 누적): 첫 backfill 이후에는 매 실행 forward 누적 모델을 따른다 — 접수번호 델타가 없으면(저장된 last_accession == 최신 접수번호) 신규 저장이 전혀 발생하지 않고(full-fetch 생략), 델타가 있을 때만 재추출·upsert·last_accession 갱신으로 누적한다.

Purpose: FUND-08의 핵심 — "새 분기/정정공시 없으면 외부 펀더멘털 전체 호출 ≈0". probe는 인정하되 비용 낮은 메타 호출, full-fetch는 변경 시에만(D-02). SC3(≈0)·SC4(변경 시만 재추출)를 spy 테스트로 입증.
Output: `src/stocksig/io/fundamentals_delta.py`(신규), `tests/test_fundamentals_delta.py`(신규).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sqlite/07-CONTEXT.md
@.planning/phases/07-sqlite/07-RESEARCH.md
@.planning/phases/07-sqlite/07-PATTERNS.md
@.planning/phases/07-sqlite/07-01-SUMMARY.md
@.planning/phases/07-sqlite/07-02-SUMMARY.md

<interfaces>
<!-- Plan 01·02가 만든 계약 — executor는 직접 소비, 탐색 불필요. -->

fundamentals_store (Plan 01):
- `def get_store() -> sqlite3.Connection`
- `def upsert_quarters(rows: list[tuple]) -> None`  # 12-tuple 순서: ticker, source, quarter, field, value, unit, accession, period_start, period_end, period_type, reprt_code, fetched_at
- `def get_last_accession(ticker, source) -> str | None`
- `def set_last_accession(ticker, source, accession) -> None`
- `def count_rows(ticker=None) -> int`
- `def mark_delta_hit() / mark_delta_miss() / mark_full_fetch()`  # 카운터
- `def reset_delta_stats() / get_delta_stats() -> dict[str,int]`

추출기 (Plan 02):
- `edgar_client.fetch_edgar_quarterly_raw(ticker) -> list[dict]`  # dict 키 = store 컬럼명
- `dart_client.fetch_dart_quarterly_raw(ticker, years=3) -> list[dict]`

probe API (RESEARCH Pattern 2 [VERIFIED]):
- EDGAR: `Company(ticker).latest("10-Q")` → Filing; `.accession_number` [A4 — 속성명 1회 introspect]
- DART: `_get_dart().list(stock_code, kind="A")` → DataFrame; `.iloc[0]["rcept_no"]` (rcept_dt desc), 빈 df → None
- `from stocksig.io.throttle import throttled_edgar, throttled_dart`

cache.py 싱글톤 패턴 (DART 인스턴스 싱글톤 복제 원본):
- double-checked locking: `if _x is None: with _x_lock: if _x is None: _x = <생성>`

market 분류 (main_run에서 사용 — delta는 source 인자로 받음):
- US → source="EDGAR", KR → source="DART"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — fundamentals_delta 테스트 스캐폴드 (probe/skip/refetch/probe실패/≈0 spy)</name>
  <files>tests/test_fundamentals_delta.py</files>
  <read_first>
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Validation Architecture" 테스트 맵(L508-522 — FUND-08/SC3 행) + Pitfall 2(L279-288)
    - tests/test_fundamentals_store.py (Plan 01이 만든 store 테스트 패턴·격리 fixture 사용법)
    - tests/conftest.py (_isolated_fundamentals_db fixture)
  </read_first>
  <behavior>
    - test_same_accession_skips_fetch (D-02): delta_state에 ("AAPL","EDGAR","ACC1") 시드 후 probe가 "ACC1" 반환하도록 mock → sync 시 fetch_edgar_quarterly_raw 호출 카운트 == 0(신규 저장 없음), mark_delta_hit 1회
    - test_changed_accession_refetches (D-02): 시드 "ACC1", probe "ACC2" → fetch 1회, upsert 호출, set_last_accession이 "ACC2"로 갱신(forward 누적), mark_delta_miss + mark_full_fetch
    - test_no_state_triggers_first_fetch: delta_state 비어있음 + probe "ACC1" → full-fetch(첫 backfill 경로)
    - test_probe_failure_keeps_db: probe가 예외/None → fetch 호출 0, count_rows 불변(기존 DB 유지), last_accession 미갱신
    - test_steady_state_zero_full_calls (D-02/SC3): 여러 종목 전부 동일 accession → fetch 총 호출 0, get_delta_stats()["full_fetch"]==0, delta_hit==종목수
  </behavior>
  <action>
    `tests/test_fundamentals_delta.py` 신규 작성 — 위 behavior 5종. D-02 검증: 같은 접수번호 시 신규 저장/fetch 0(test_same_accession_skips_fetch, test_steady_state_zero_full_calls), 다른 접수번호 시에만 forward 누적(test_changed_accession_refetches). `from stocksig.io import fundamentals_delta as fd, fundamentals_store as fs`. probe는 `mocker.patch`로 `fd.probe_edgar_accession`/`fd.probe_dart_rcept` 반환값 지정. full-fetch spy는 `mocker.spy(edgar_client, "fetch_edgar_quarterly_raw")` 또는 monkeypatch counter로 `.call_count`/카운터 단언. delta_state는 `fs.set_last_accession(...)`로 시드. ≈0 검증은 `fs.reset_delta_stats()` → sync 루프 → `fs.get_delta_stats()` 단언(기존 smoke/freeze 네트워크 0 stub 발상 재사용, quick-260617-k34 패턴). 운영 DB는 conftest `_isolated_fundamentals_db`로 격리됨. RED: delta 모듈 미존재로 실패 — Task 2 GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_fundamentals_delta.py -q 2>&1 | grep -E "error|failed|collected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_fundamentals_delta.py에 `def test_same_accession_skips_fetch`, `def test_changed_accession_refetches`, `def test_probe_failure_keeps_db`, `def test_steady_state_zero_full_calls` 정의 존재
    - full-fetch 호출 카운트를 spy/counter로 단언하는 어서션 존재(`.call_count == 0` 또는 카운터 == 0, D-02 델타 없으면 신규 저장 없음)
    - RED: delta 모듈 부재로 실패(Task 2에서 green)
  </acceptance_criteria>
  <done>FUND-08/SC3 검증 테스트 5종이 존재하고 RED 상태(델타 skip·refetch·probe실패·≈0 계약 확정, D-02 forward 누적).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fundamentals_delta.py — probe + delta_state 비교 + skip/refetch 오케스트레이션</name>
  <files>src/stocksig/io/fundamentals_delta.py</files>
  <read_first>
    - src/stocksig/io/edgar_client.py + dart_client.py (throttle·싱글톤·예외 가드 패턴; Plan 02 신규 추출기 시그니처)
    - src/stocksig/io/cache.py (double-checked locking — DART 인스턴스 싱글톤 복제 원본)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Pattern 2"(L216-236) + Pitfall 1(L273-277) + Pitfall 2(L279-288) + §"Architecture Patterns" 다이어그램(L123-155)
    - .planning/phases/07-sqlite/07-PATTERNS.md §"fundamentals_delta.py"(L73-103)
    - tests/test_fundamentals_delta.py (Task 1 계약)
  </read_first>
  <action>
    `src/stocksig/io/fundamentals_delta.py` 신규 작성. 한국어 docstring + `from __future__ import annotations`. DART 인스턴스 모듈 싱글톤 `_get_dart()` — double-checked locking(`_dart_lock`)으로 `OpenDartReader(_resolve_api_key())` 1회 생성(Pitfall 1; 종목마다 생성 금지). API 키 해석은 dart_client `_resolve_api_key` 재사용(import). 단, 07-02 Task 3가 dart_client에 모듈 싱글톤(_dart_singleton)을 도입했다면 본 모듈의 _get_dart()는 그 싱글톤을 import해 공유하고 별도 OpenDartReader 인스턴스를 새로 만들지 말 것(corp_codes 이중 다운로드 방지, Pitfall 1).
    probe 함수 2종:
    - `@throttled_edgar def probe_edgar_accession(ticker) -> str | None:` → `latest = Company(ticker).latest("10-Q"); return latest.accession_number if latest else None` ([A4] 속성명 introspect 1회로 확정).
    - `@throttled_dart def probe_dart_rcept(ticker) -> str | None:` → `df = _get_dart().list(ticker.split(".")[0], kind="A"); return df.iloc[0]["rcept_no"] if not df.empty else None`.
    오케스트레이터 `sync_ticker_history(ticker: str, source: str, years: int = 3) -> None` (D-02 forward 누적):
    1) probe를 try/except로 호출 — 실패(예외/None) 시 "갱신 생략, 기존 DB 유지"(Pitfall 2 권고; 보수적 재추출 금지), 로그 한국어 + 타입명만(API 키/원문 보간 금지, T-04-03), return.
    2) `last = store.get_last_accession(ticker, source)`; D-02: `if last is not None and last == latest: store.mark_delta_hit(); return`(델타 없음 → SKIP, full-fetch·신규 저장 0).
    3) else (D-02 델타 있음): `store.mark_delta_miss()` → source별 full-fetch(`fetch_edgar_quarterly_raw(ticker)` 또는 `fetch_dart_quarterly_raw(ticker, years)`) → dict 행을 12-tuple로 변환 → `store.upsert_quarters(rows)` → `store.mark_full_fetch()` → `store.set_last_accession(ticker, source, latest)`(forward 누적).
    probe/fetch 실패의 예외 가드는 기존 dart_client status 가드 발상 재사용. 전 외부 호출 함수에 throttle 데코레이터 적용. SQL은 store가 `?` 바인딩으로 처리(여기서 SQL 직접 작성 없음).
  </action>
  <verify>
    <automated>python -m pytest tests/test_fundamentals_delta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - fundamentals_delta.py contains `def sync_ticker_history(`, `def probe_edgar_accession(`, `def probe_dart_rcept(`, `def _get_dart(`
    - D-02: 접수번호 동일 시 full-fetch 호출 카운터 == 0, 신규 저장 없음 (test_same_accession_skips_fetch green)
    - D-02: 접수번호 변경 시에만 재추출 + set_last_accession 갱신(forward 누적) (test_changed_accession_refetches green)
    - probe 실패 시 count_rows 불변·last_accession 미갱신 (test_probe_failure_keeps_db green)
    - 변경 없는 평소 실행 full_fetch == 0 (test_steady_state_zero_full_calls green, D-02)
    - OpenDartReader 인스턴스가 `_get_dart()` 싱글톤으로만 생성(종목 루프 내 재생성 없음)
    - 예외 메시지에 API 키/원문 보간 없음 (타입명만 로그)
    - `python -m pytest tests/test_fundamentals_delta.py -x` exits 0
  </acceptance_criteria>
  <done>델타 모듈이 probe·skip·refetch·probe실패 처리를 구현하고(D-02 델타 시에만 forward 누적) FUND-08/SC3 테스트 5종 전부 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EDGAR/DART 메타 응답 → probe | 외부 신뢰 불가 응답에서 accession/rcept_no 추출 |
| OPENDART_API_KEY → list/finstate 호출·예외 | crtfc_key가 URL 포함 — 예외 누설 위험 |
| ThreadPool/순차 루프 → delta_state write | 동시 write lost-update (store _store_lock에 위임) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of Service | probe 실패 시 보수적 재추출 폭주 | mitigate | probe 실패 → SKIP + 기존 DB 유지(Pitfall 2). DART 쿼터 초과 연쇄 차단. test_probe_failure_keeps_db. |
| T-07-08 | Information Disclosure | probe/fetch 예외 메시지(crtfc_key URL) | mitigate | 예외 원문·키 보간 금지(T-04-03). 타입명만 로그. acceptance_criteria 검증. |
| T-07-09 | DoS / 비용 | OpenDartReader 인스턴스 반복 생성(corp_codes zip) | mitigate | `_get_dart()` 모듈 싱글톤(Pitfall 1). 종목 루프 재생성 금지. |
| T-07-10 | Tampering | delta_state 동시 write | mitigate | store `_store_lock` 직렬화(Plan 01). delta는 store API만 사용. |
| T-07-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0(기존 의존, RESEARCH Audit [OK]). |
</threat_model>

<verification>
- `python -m pytest tests/test_fundamentals_delta.py -x -q` 통과 (네트워크 0, probe/fetch mock)
- `python -m pytest -q` 전 스위트 그린
</verification>

<success_criteria>
- D-02: 접수번호 동일 → full-fetch·신규 저장 생략(평소 ≈0, SC3), 변경 → 재추출·last_accession 갱신·forward 누적(SC4)
- state 테이블 last_accession 비교 동작(SC2)
- probe 실패 안전 폴백(기존 DB 유지), DART 싱글톤
</success_criteria>

<output>
Create `.planning/phases/07-sqlite/07-03-SUMMARY.md` when done
</output>
</content>
