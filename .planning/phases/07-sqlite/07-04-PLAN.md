---
phase: 07-sqlite
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/stocksig/main_run.py
  - .gitignore
  - tests/test_history_integration.py
autonomous: true
requirements: [FUND-07, FUND-08]
must_haves:
  truths:
    - "D-07: run() 종료부 근처에서 종목별로 히스토리 경로(sync_ticker_history)가 시트1 경로와 완전 분리된 별도 순차 루프로 호출된다 — 분기 경계(새 공시/정정공시)에서 히스토리 probe/fetch와 시트1 fetch의 이중 외부 호출을 허용한다(드문 이벤트·시트1 회귀 위험 0)"
    - "시트1 PASS1/PASS2(portfolio_*.xlsx)·.cache/fundamentals 7일 캐시는 불변이다(D-06)"
    - "실행 요약 블록에 델타 HIT/MISS·full-fetch 카운트가 추가된다(API 키·예외 원문 미포함)"
    - "data/fundamentals.db가 .gitignore되어 커밋되지 않는다"
    - "변경 없는 평소 실행에서 히스토리 경로 full-fetch 호출이 0이다(probe만)"
  artifacts:
    - path: "src/stocksig/main_run.py"
      provides: "run() 종료부 히스토리 경로 오케스트레이션 + 델타 카운터 reset/요약"
      contains: "sync_ticker_history"
    - path: ".gitignore"
      provides: "data/fundamentals.db 미커밋"
      contains: "data/"
    - path: "tests/test_history_integration.py"
      provides: "히스토리 경로 통합 테스트 (≈0 호출·시트1 불변·DB 생성)"
      contains: "def test_steady_state_history_zero_full_fetch"
  key_links:
    - from: "main_run.run"
      to: "fundamentals_delta.sync_ticker_history"
      via: "D-07: 별도 순차 루프 (PASS1/PASS2 이후, 요약 직전) — 분기 경계 이중 외부 호출 허용"
      pattern: "sync_ticker_history"
    - from: "main_run.run"
      to: "fundamentals_store.reset_delta_stats / get_delta_stats"
      via: "run 시작 reset + 요약 블록 출력"
      pattern: "(reset|get)_delta_stats"
---

<objective>
히스토리 경로를 main 실행 흐름에 배선한다 — `main_run.run()`의 PASS1/PASS2 시트1 경로(불변, D-06) 이후·요약 블록 직전에 **별도 순차 루프**로 종목별 `sync_ticker_history(ticker, source)`를 호출하고, run 시작부에 `store.reset_delta_stats()`를 추가하며 요약 블록에 델타 HIT/MISS·full-fetch 줄을 추가한다(카운트·심볼만, T-04-01). `.gitignore`에 `data/`를 추가해 DB 미커밋(SC5). 통합 테스트로 "평소 실행 ≈0 호출"·"시트1 불변"·"DB 생성"을 입증한다.

D-07 (분기 경계 이중 외부 호출 허용): 히스토리 경로는 시트1 경로와 완전히 분리된 additive 경로다. 분기 경계(새 공시/정정공시)에서는 시트1 fundamentals fetch와 히스토리 probe/full-fetch가 같은 종목에 대해 이중으로 외부 호출될 수 있으나, 드문 이벤트이고 구현이 단순하며 시트1 회귀 위험이 0이므로 이를 허용한다(단일 원천 통합은 Phase 10에서 자연 해소).

Purpose: FUND-07/08을 실행 진입점에 연결해 사용자가 `uv run python main.py` 시 분기 raw가 누적되고 평소 외부 펀더멘털 호출이 ≈0이 되게 한다. 시트1 회귀 위험 0이 최우선 — 히스토리는 완전 분리된 additive 경로(D-07).
Output: `main_run.py`(수정), `.gitignore`(수정), `tests/test_history_integration.py`(신규).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sqlite/07-CONTEXT.md
@.planning/phases/07-sqlite/07-RESEARCH.md
@.planning/phases/07-sqlite/07-PATTERNS.md
@.planning/phases/07-sqlite/07-01-SUMMARY.md
@.planning/phases/07-sqlite/07-03-SUMMARY.md

<interfaces>
<!-- 기존 run() 구조 + Plan 01/03 계약. executor는 직접 소비. -->

main_run.run() (기존 — L234-351, 불변 유지할 골격):
- `cache.reset_cache_stats()` (L252, run 시작 카운터 reset 패턴 — 옆에 store.reset_delta_stats() 추가)
- `specs = read_tickers_extended(...)`; `classify_market(s.symbol)` → "US"/"KR"
- PASS1 `run_all(...)` (L277-285) · PASS2 write_portfolio_sheet/write_sheet_for_ticker (L295-314) — **불변**
- 요약 블록(L323-349): `logger.info("════════ 실행 요약 ════════")` + 캐시 줄 — 옆에 히스토리 델타 줄 추가
- T-04-01: 카운트(정수)·티커 심볼만 출력 (API 키·예외 원문 미포함)

fundamentals_delta (Plan 03):
- `def sync_ticker_history(ticker: str, source: str, years: int = 3) -> None`  # D-07: 시트1과 분리된 히스토리 전용 경로

fundamentals_store (Plan 01):
- `def reset_delta_stats() -> None` / `def get_delta_stats() -> dict[str,int]`  # {"delta_hit","delta_miss","full_fetch"}
- `def count_rows(ticker=None) -> int`

market 분류: US → source="EDGAR", KR → source="DART" (yf/Naver 폴백은 접수번호 개념 없음 — 이 plan에서는 EDGAR/DART 종목만 히스토리 경로 대상, 폴백 분기라벨 보완은 deferred)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: .gitignore — data/ 추가 (DB 미커밋, SC5)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (기존 — `.cache/`·`docs_cache/` 라인 존재, `data/` 미등재)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Runtime State Inventory"(L257-269 — data/fundamentals.db 신규·미커밋)
  </read_first>
  <action>
    `.gitignore`에 기존 `.cache/` 라인 근처에 `data/` 라인을 추가한다(SC5 — `data/fundamentals.db` 미커밋). `data/` 디렉토리 전체를 무시(현재 미등재). 기존 라인(`.planning/`, `/output/`, `.env`, `*.xlsx`, `__pycache__/`, `.venv/`, `*.pyc`, `.pytest_cache/`, `.cache/`, `docs_cache/`)은 보존.
  </action>
  <verify>
    <automated>git check-ignore data/fundamentals.db && echo IGNORED</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore에 `data/` 라인 존재
    - `git check-ignore data/fundamentals.db` 가 경로를 반환(IGNORED 출력) — DB가 커밋 대상에서 제외됨
    - 기존 .gitignore 라인 전부 보존
  </acceptance_criteria>
  <done>data/fundamentals.db가 .gitignore되어 git 추적 대상에서 제외된다.</done>
</task>

<task type="auto">
  <name>Task 2: main_run.run() 히스토리 경로 배선 + 델타 카운터 reset/요약</name>
  <files>src/stocksig/main_run.py</files>
  <read_first>
    - src/stocksig/main_run.py (run() L234-351 — reset 위치 L252, 요약 블록 L323-349, PASS1/PASS2 L277-314 불변)
    - src/stocksig/io/fundamentals_delta.py (sync_ticker_history 시그니처 — Plan 03)
    - src/stocksig/io/fundamentals_store.py (reset_delta_stats/get_delta_stats/count_rows — Plan 01)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"Open Questions" Q3(L474-477 — 별도 순차 루프 권고) + §Architecture 다이어그램(L123-155)
    - .planning/phases/07-sqlite/07-PATTERNS.md §"main_run.py"(L179-194)
  </read_first>
  <action>
    `main_run.py` run()에 히스토리 경로를 additive로 배선(D-07: 시트1과 완전 분리, 분기 경계 이중 외부 호출 허용). import 추가: `from stocksig.io import fundamentals_store as fund_store, fundamentals_delta`.
    (a) run 시작부 카운터 reset(L252 `cache.reset_cache_stats()` 옆)에 `fund_store.reset_delta_stats()` 추가.
    (b) PASS2 write(L314 `wb.close()` finally) 이후·요약 블록(L323) 직전에 **별도 순차 루프** 삽입(D-07: 시트1 경로와 독립된 히스토리 전용 루프): `for s in specs:` 종목별 market 분류 → US면 `fundamentals_delta.sync_ticker_history(s.symbol, "EDGAR")`, KR면 `sync_ticker_history(s.symbol, "DART")`. yf/Naver 전용 폴백 종목·인증 실패 소스는 skip(접수번호 개념 없음 — deferred). 각 종목 호출을 try/except로 감싸 히스토리 경로 실패가 시트1 산출물(이미 저장됨)을 깨지 않게 한다(예외는 타입명만 로그, T-04-03). PASS1/PASS2 시트1 경로(L277-314)는 한 줄도 수정 금지(D-06). 분기 경계에서 시트1 fetch와 히스토리 probe/fetch가 같은 종목에 이중 호출될 수 있으나 허용(D-07).
    (c) 요약 블록(L337-345 캐시 줄 옆)에 델타 줄 추가: `stats = fund_store.get_delta_stats()` → `logger.info("히스토리: 델타 HIT %d/MISS %d · full-fetch %d", stats["delta_hit"], stats["delta_miss"], stats["full_fetch"])` (T-04-01: 정수 카운트만, API 키/예외 원문 미포함).
  </action>
  <verify>
    <automated>python -m pytest tests/test_history_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - main_run.py에 `fundamentals_delta.sync_ticker_history` 호출과 `fund_store.reset_delta_stats()` 호출 존재
    - D-07: 히스토리 루프가 PASS2 write(wb.close) 이후에 위치(시트1 작성 전 아님), 시트1 경로와 분리된 별도 루프
    - PASS1 `run_all` 호출부·PASS2 `write_portfolio_sheet`/`write_sheet_for_ticker` 블록 무수정 (시트1 회귀 테스트 그린)
    - 요약 블록에 `"히스토리: 델타 HIT %d/MISS %d · full-fetch %d"` 로그 존재 (API 키·예외 원문 미포함)
    - 히스토리 경로 종목 호출이 try/except로 시트1 산출물 보호
  </acceptance_criteria>
  <done>run()이 시트1 경로 불변인 채 히스토리 경로를 별도 순차 루프로 호출하고(D-07 이중 호출 허용) 델타 카운터를 reset/요약한다.</done>
</task>

<task type="auto">
  <name>Task 3: 히스토리 경로 통합 테스트 (≈0 호출 · 시트1 불변 · DB 생성)</name>
  <files>tests/test_history_integration.py</files>
  <read_first>
    - tests/test_fundamentals_delta.py (Plan 03 spy 패턴) + tests/conftest.py (_isolated_fundamentals_db·_isolated_disk_cache)
    - tests/ 기존 run() 통합/smoke 테스트(네트워크 0 stub 패턴 — quick-260617-k34)
    - .planning/phases/07-sqlite/07-RESEARCH.md §"외부 호출 ≈0 검증 방법"(L519-526) + §"과거 분기 보존 검증"(L524-526)
  </read_first>
  <behavior>
    - test_run_creates_history_db: run()을 네트워크 0 mock(probe/fetch stub)으로 1회 실행 → 격리 data/fundamentals.db에 raw_facts 행 누적(count_rows > 0)
    - test_steady_state_history_zero_full_fetch: delta_state 사전 시드(전 종목 동일 accession) + probe가 같은 accession 반환 mock → run() 후 fetch_*_quarterly_raw 호출 0, get_delta_stats()["full_fetch"]==0 (SC3)
    - test_sheet1_unchanged_by_history (D-07): 히스토리 경로 추가 전후 write_portfolio_sheet 결과를 명시적으로 스냅샷 비교해 시트1 산출물이 바이트/셀 단위로 불변임을 직접 단언(D-07 시트1 회귀 위험 0 입증). 히스토리 경로 mock on/off 두 실행의 portfolio_*.xlsx 결과(또는 write_portfolio_sheet에 전달된 셀/색 신호 인자)가 동일함을 assert — 막연한 "기존 회귀 그린"에 의존하지 않는다.
    - test_cache_dir_unchanged_by_history: 히스토리 실행 전후 `.cache/` 디렉토리의 파일 목록(이름·개수)이 불변임을 직접 assert(SC-5 ".cache/와 별개" — 히스토리 경로는 data/fundamentals.db만 쓰고 .cache/fundamentals 7일 캐시는 건드리지 않음). data/ 와 .cache/ 가 분리된 저장소임을 입증.
    - test_history_failure_does_not_break_sheet1: sync_ticker_history가 예외를 던져도 run()이 portfolio_*.xlsx를 정상 저장(시트1 보호, D-07 분리 보장)
  </behavior>
  <action>
    `tests/test_history_integration.py` 신규 — 위 behavior 5종. run()의 외부 호출은 전부 mock(네트워크 0): OHLCV/시트1 펀더멘털은 기존 통합 테스트 stub 재사용, 히스토리 경로는 `probe_edgar_accession`/`probe_dart_rcept`·`fetch_edgar_quarterly_raw`/`fetch_dart_quarterly_raw`를 mock. ≈0 검증은 fetch spy `.call_count == 0` + `fund_store.get_delta_stats()` 단언. D-07 시트1 불변은 히스토리 경로 mock on/off 두 실행의 write_portfolio_sheet 결과(셀/색 신호)를 스냅샷 비교해 직접 단언(막연한 회귀 통과 의존 금지). `.cache/` 분리는 히스토리 실행 전후 `.cache/` 파일 목록을 os.listdir 등으로 캡처해 불변 assert(SC-5). conftest `_isolated_fundamentals_db`로 운영 DB 격리. tmp output_dir 사용.
  </action>
  <verify>
    <automated>python -m pytest tests/test_history_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_history_integration.py에 `def test_steady_state_history_zero_full_fetch`, `def test_sheet1_unchanged_by_history`, `def test_cache_dir_unchanged_by_history`, `def test_history_failure_does_not_break_sheet1` 정의 존재
    - test_sheet1_unchanged_by_history가 히스토리 on/off 두 실행의 write_portfolio_sheet 결과(셀/색 신호 또는 xlsx)를 스냅샷 비교해 직접 단언(D-07 시트1 회귀 위험 0)
    - test_cache_dir_unchanged_by_history가 히스토리 실행 전후 `.cache/` 파일 목록 불변을 직접 assert(SC-5, data/와 .cache/ 분리)
    - 변경 없는 평소 실행 fetch_*_quarterly_raw 호출 카운트 == 0 단언 (SC3)
    - 히스토리 경로 예외 시에도 portfolio_*.xlsx 생성 단언 (시트1 보호)
    - 네트워크 호출 0 (전 외부 호출 mock)
    - `python -m pytest tests/test_history_integration.py -x` exits 0
  </acceptance_criteria>
  <done>통합 테스트가 ≈0 호출·시트1 불변(D-07)·DB 생성·실패 격리를 입증하고 전부 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 히스토리 경로 예외 → 시트1 산출물 | 히스토리 실패가 Core Value(시트1)를 깨면 안 됨 |
| 요약 로그 → 콘솔/파일 | API 키·예외 원문 누설 위험 |
| 생성 DB → git | data/fundamentals.db 커밋 시 데이터/경로 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Denial of Service | 히스토리 경로 예외가 시트1 산출 중단 | mitigate | 종목별 try/except + PASS2 write 이후 호출(시트1 이미 저장). test_history_failure_does_not_break_sheet1. |
| T-07-12 | Information Disclosure | 요약/예외 로그(API 키·원문) | mitigate | 카운트·심볼만 출력(T-04-01), 예외 타입명만(T-04-03). |
| T-07-13 | Information Disclosure | data/fundamentals.db git 커밋 | mitigate | `.gitignore` `data/`(Task 1). `git check-ignore` 검증. |
| T-07-SC | Tampering | 패키지 설치 | accept | 신규 패키지 0(기존 의존). |
</threat_model>

<verification>
- `python -m pytest tests/test_history_integration.py -x -q` 통과 (네트워크 0)
- `python -m pytest -q` 전 스위트 그린 (시트1·캐시·임펄스 전 회귀 무손상 — D-06 additive 입증)
- `git check-ignore data/fundamentals.db` 가 경로 반환(미커밋)
</verification>

<success_criteria>
- run()이 분기 raw를 누적하고(SC1) 변경 없는 실행에서 히스토리 full-fetch ≈0(SC3)
- 시트1 portfolio_*.xlsx·색 신호·.cache/fundamentals 불변(D-06, Core Value 보호), 히스토리는 분리된 additive 경로(D-07)
- data/fundamentals.db 미커밋(SC5), 기존 .cache/와 별개 동작
</success_criteria>

<output>
Create `.planning/phases/07-sqlite/07-04-SUMMARY.md` when done
</output>
</content>
