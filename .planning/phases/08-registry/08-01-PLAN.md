---
phase: 08-registry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/stocksig/io/fundamentals_store.py
  - tests/test_metrics_engine.py
  - tests/test_raw_semantics_spike.py
  - tests/fixtures/raw_quarters.py
autonomous: true
requirements: [FUND-09]

must_haves:
  truths:
    - "DART thstrm_amount(손익)가 분기 단독값인지 누적값인지 실데이터/공식가이드 근거로 확정되고, 그 결정이 코드 주석·테스트에 박제된다"
    - "EDGAR raw에 Q4(캘린더 4분기) 손익 단독값과 FY duration이 부재함이 검증되고, Q4 처리 방침(빈값+사유 = 기본 채택)이 박제된다"
    - "fundamentals_store.fetch_raw_quarters(ticker)가 raw_facts를 ?-바인딩 SELECT로 분기 정렬 반환한다"
    - "tests/test_metrics_engine.py가 -k 마커별 RED 스텁(skeleton)으로 존재해 Wave 2 엔진이 즉시 채울 수 있다"
  artifacts:
    - path: "src/stocksig/io/fundamentals_store.py"
      provides: "fetch_raw_quarters SELECT 헬퍼 (raw 조회 진입점)"
      contains: "def fetch_raw_quarters"
    - path: "tests/test_raw_semantics_spike.py"
      provides: "DART 분기/누적 의미 + EDGAR Q4 갭 진실 단언 spike"
      contains: "def test_dart_quarter_semantics"
    - path: "tests/fixtures/raw_quarters.py"
      provides: "분기 raw 행 builder (EDGAR 3개월·DART 분기·BS instant·결손 분기)"
      contains: "def raw_row"
    - path: "tests/test_metrics_engine.py"
      provides: "엔진 테스트 RED 스캐폴드 (-k 마커별)"
      contains: "def test"
  key_links:
    - from: "tests/test_raw_semantics_spike.py"
      to: "tests/fixtures/dart_005930_finstate.py"
      via: "기존 DART 실응답 fixture 재사용"
      pattern: "dart_005930"
    - from: "src/stocksig/io/fundamentals_store.py::fetch_raw_quarters"
      to: "raw_facts 테이블"
      via: "get_store().execute(?-바인딩)"
      pattern: "WHERE ticker=\\?"
---

<objective>
Phase 8 산식 구현의 **선행 진실 확정 + 입력 경로 + 테스트 스캐폴드**를 만든다.

연구가 보고한 2개 🔴 raw-data 의미 갭(DART `thstrm_amount` 분기/누적, EDGAR Q4 부재)은
코드 패턴 문제가 아니라 데이터 의미 문제로, TTM 합산·Q4 보정 산식의 옳고 그름을 좌우한다.
이 plan은 그 진실을 spike로 확정해 Wave 2 엔진(08-03)이 올바른 산식을 구현하도록 한다.
동시에 엔진이 읽을 store 조회 헬퍼와 엔진 테스트 RED 스캐폴드를 제공한다.

Purpose: 잘못된 분기값 해석으로 DART TTM이 2~4배 오계산되거나 US Q4 유량이 영구 결손되는
사고를 산식 구현 *전에* 차단한다. (RESEARCH Pitfall 1·2, Assumptions A1·A2)
Output: `fetch_raw_quarters` 헬퍼 · spike 테스트(진실 박제) · raw 행 builder fixture ·
엔진 테스트 RED 스캐폴드.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-registry/08-CONTEXT.md
@.planning/phases/08-registry/08-RESEARCH.md
@.planning/phases/08-registry/08-PATTERNS.md

<interfaces>
<!-- 실행자는 아래 계약을 그대로 사용. 코드베이스 재탐색 불필요. -->

store 기존 SELECT 패턴 (src/stocksig/io/fundamentals_store.py):
  get_store() -> sqlite3.Connection  (더블체크 락 + WAL, L107-120)
  count_rows(ticker) — "SELECT COUNT(*) FROM raw_facts WHERE ticker=?", (ticker,)  (L155-163)
  get_last_accession — "SELECT last_accession FROM delta_state WHERE ticker=? AND source=?"  (L137-144)

raw_facts 컬럼 (SCHEMA L52-66):
  ticker, source, quarter('YYYYQn'), field, value(REAL NULL=결손),
  unit, accession, period_start, period_end, period_type('instant'|'duration'),
  reprt_code('11013'|'11012'|'11014'|'11011'), fetched_at

upsert_quarters(rows) rows = 12-tuple (컬럼 순서 = SCHEMA 순서, L81-91):
  (ticker, source, quarter, field, value, unit, accession,
   period_start, period_end, period_type, reprt_code, fetched_at)

기존 fixture (재사용):
  tests/fixtures/dart_005930_finstate.py — 005930 finstate_all 실응답 (thstrm_amount 문자열 포함)
  tests/fixtures/edgar_aapl_facts.py — AAPL EDGAR facts 실응답
  tests/conftest.py::_isolated_fundamentals_db (autouse) — tmp DB 자동 격리

기존 행 builder analog (tests/test_fundamentals_store.py L22-44):
  _row(ticker="AAPL", source="EDGAR", quarter="2026Q1", field="revenue",
       value=1000.0, accession="acc-0001") -> 12-tuple  (디폴트 인자 팩토리)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: raw 진실 확정 spike — DART 분기/누적 + EDGAR Q4 갭</name>
  <files>tests/test_raw_semantics_spike.py</files>
  <read_first>
    - .planning/phases/08-registry/08-RESEARCH.md (Pitfall 1·2, Open Q1·Q2, Assumptions A1·A2, State of the Art)
    - tests/fixtures/dart_005930_finstate.py (DART 실응답 — thstrm_amount 형식)
    - tests/fixtures/edgar_aapl_facts.py (EDGAR 실응답)
    - src/stocksig/io/dart_account_map.py (L16-24 확정 005930 IS 값 + thstrm_amount 주석)
    - src/stocksig/io/edgar_client.py L96-114 (_EDGAR_DURATION_CONCEPTS — by_period_length(3)만 추출)
  </read_first>
  <behavior>
    - test_dart_quarter_semantics: 005930 반기(reprt_code=11012)·3분기(11014) finstate_all
      fixture에서 revenue thstrm_amount를 읽어, 그 값이 분기 단독값인지(≈1개 분기 규모)
      누적값인지(≈2·3개 분기 규모)를 단언. 공식 가이드(DS003: 분/반기 손익 thstrm_amount=3개월값,
      누적은 thstrm_add_amount)와 fixture 실값을 교차 단언. 결과를 모듈 docstring/주석에 "확정:
      DART 손익 thstrm_amount = 분기 단독값(또는 누적)" 으로 박제.
    - test_edgar_q4_gap_absent: edgar_aapl_facts fixture(또는 by_period_length(3) 추출 결과)에서
      캘린더 Q4(연 마지막 분기) revenue 행이 부재함을 단언 — Q4 단독 duration도 FY(12개월) duration도
      저장 raw에 없음. "EDGAR Q4 = 빈값+사유 기본 채택(D-05 일관), FY 저장 추가는 범위 밖" 박제.
  </behavior>
  <action>
    `tests/test_raw_semantics_spike.py` 신규 생성. 네트워크 호출 0 — 기존 fixture
    `dart_005930_finstate.py`/`edgar_aapl_facts.py`의 *저장된 실응답*만 읽어 단언한다(spike이지만
    재현 가능한 fixture 기반, 실시간 외부 호출 금지). 두 테스트 함수:
    (1) `test_dart_quarter_semantics` — DART thstrm_amount가 분기 단독값임을 확정(RESEARCH State
    of the Art·Pitfall 2). 확정 결과를 본 테스트 파일 상단 docstring에 한국어로 명시: 분기 단독값이면
    "DART도 EDGAR처럼 단순 4분기 합 TTM — YTD 분해 작업 불필요(08-03이 분해 로직 미구현)".
    (2) `test_edgar_q4_gap_absent` — EDGAR raw에 캘린더 Q4 손익 단독값·FY가 부재함을 확정(Pitfall 1,
    Assumption A2). 확정 결과 박제: "Q4 유량 지표 = 빈값+사유(D-05), Q4=FY−9M 보정은 FY raw 부재로
    수행 불가 → 08-03 엔진이 Q4 보정 로직 미구현, Q4 행 자연 결손". 두 결정은 08-03 SUMMARY 입력이
    되도록 docstring에 "→ 08-03 엔진 산식 방침" 섹션으로 적는다. 0/-999999 대체 금지(D-05).
  </action>
  <verify>
    <automated>uv run pytest tests/test_raw_semantics_spike.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_raw_semantics_spike.py` 파일 존재 + `def test_dart_quarter_semantics` + `def test_edgar_q4_gap_absent` 포함
    - `uv run pytest tests/test_raw_semantics_spike.py -x -q` exits 0
    - 파일 docstring에 "DART 손익 thstrm_amount = 분기 단독값" 및 "EDGAR Q4 = 빈값+사유" 확정 문구 + "→ 08-03 엔진 산식 방침" 섹션 포함
    - 네트워크/실시간 외부 호출 코드 없음 (fixture import만)
  </acceptance_criteria>
  <done>두 raw-data 진실이 fixture 기반 테스트로 확정·박제되고, 08-03 엔진의 DART TTM·EDGAR Q4 산식 방침이 결정됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store fetch_raw_quarters 헬퍼 + raw 행 builder fixture + 엔진 테스트 RED 스캐폴드</name>
  <files>src/stocksig/io/fundamentals_store.py, tests/fixtures/raw_quarters.py, tests/test_metrics_engine.py</files>
  <read_first>
    - src/stocksig/io/fundamentals_store.py L137-163 (count_rows / get_last_accession SELECT + ?-바인딩 analog)
    - tests/test_fundamentals_store.py L22-44 (_row 12-tuple 디폴트 인자 팩토리 analog)
    - tests/conftest.py L24-42 (_isolated_fundamentals_db autouse — tmp DB 자동 격리)
    - .planning/phases/08-registry/08-RESEARCH.md (Test Map L447-454: -k 마커 목록)
  </read_first>
  <behavior>
    - test_fetch_raw_quarters_returns_sorted: upsert_quarters로 2026Q1·2025Q4 revenue 행을 심고
      fetch_raw_quarters("AAPL") 호출 → quarter 오름차순 정렬 list 반환, 각 행에 (quarter, source,
      field, value, period_type, reprt_code, unit) 포함.
    - test_fetch_raw_quarters_empty: 미존재 ticker → 빈 list.
    - raw_row fixture builder: 디폴트 인자로 12-tuple 반환, value=None(결손)·source/field/quarter
      오버라이드 가능.
  </behavior>
  <action>
    (A) `fundamentals_store.py`에 `fetch_raw_quarters(ticker: str) -> list[tuple]` 추가 —
    "SELECT quarter, source, field, value, period_type, reprt_code, unit FROM raw_facts
    WHERE ticker=? ORDER BY quarter", (ticker,) ?-바인딩(ASVS V5, f-string/% SQL 금지). `get_store()`
    재사용(신규 connection 금지). analog = `count_rows`(L155-163).
    (B) `tests/fixtures/raw_quarters.py` 신규 — `raw_row(ticker="AAPL", source="EDGAR",
    quarter="2026Q1", field="revenue", value=1000.0, unit="USD", accession="acc-0001",
    period_type="duration", reprt_code=None)` 디폴트 인자 12-tuple 팩토리(test_fundamentals_store
    `_row` 스타일). 결손 행은 value=None으로. EDGAR 3개월(duration)·DART 분기(reprt_code 지정)·BS
    instant(period_type="instant")·결손 분기를 모두 표현 가능해야 함.
    (C) `tests/test_metrics_engine.py` 신규 — RED 스캐폴드. RESEARCH Test Map의 -k 마커별 테스트
    함수 *시그니처+`pytest.skip("08-03 Wave 2에서 구현")` 또는 실패 단언*을 둔다:
    `test_type_rules`, `test_reproduce`, `test_ttm_missing`, `test_provenance_or_pershare`,
    `test_edgar_q4`. import는 `from stocksig.io.metrics_engine import ...`(08-03이 채움)이 아직
    실패하므로 모듈 미존재 단계에서는 함수 본문을 `pytest.skip`으로 두어 collect만 통과시킨다.
    fetch_raw_quarters 두 테스트(test_fetch_raw_quarters_returns_sorted / _empty)는 실제 GREEN으로
    구현(store 헬퍼 검증).
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics_engine.py tests/fixtures/raw_quarters.py -x -q 2>&1; uv run pytest tests/test_metrics_engine.py -k "fetch_raw_quarters" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `fundamentals_store.py` contains `def fetch_raw_quarters(ticker` and `WHERE ticker=?`
    - `fundamentals_store.py`에 fetch_raw_quarters의 SQL에 f-string/`%` 보간 없음 (전부 `?` 바인딩)
    - `tests/fixtures/raw_quarters.py` contains `def raw_row(`
    - `tests/test_metrics_engine.py` 존재 + `test_type_rules`/`test_ttm_missing`/`test_reproduce`/`test_provenance_or_pershare`/`test_edgar_q4` 함수 포함
    - `uv run pytest tests/test_metrics_engine.py -k "fetch_raw_quarters" -x -q` exits 0 (store 헬퍼 GREEN)
    - `uv run pytest tests/test_metrics_engine.py -q` collect 통과(스캐폴드는 skip, 에러 0)
  </acceptance_criteria>
  <done>엔진(08-03)이 읽을 store 조회 경로·행 builder·테스트 -k 마커 스캐폴드가 준비되고, 전 스위트 collect 무에러</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장된 raw_facts → 계산 헬퍼 | 유일한 입력 경계. 외부 네트워크·인증·사용자 입력 없음(오프라인 로컬 계산). 신뢰 대상 = 이미 Phase 7이 저장한 SQLite raw |
| DB 파일 경로 | `data/fundamentals.db` 로컬 파일. 테스트는 conftest autouse로 tmp 격리 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | fetch_raw_quarters SQL (ticker 보간) | mitigate | `?` 파라미터 바인딩 강제(ASVS V5). f-string/`%` SQL 금지 — 기존 store 전 SQL 바인딩 패턴 준수 |
| T-08-02 | (데이터 무결성) | 잘못된 raw 분기값 해석 | mitigate | 본 plan의 spike가 DART 분기/누적·EDGAR Q4 진실을 확정 → 08-03 오계산 차단 |
| T-08-S | Spoofing/Auth/XSS/SSRF/세션 | — | N/A | N/A — 오프라인 로컬 계산, 외부 입력·네트워크·인증 없음 |
| T-08-SC | Tampering | 패키지 설치 | N/A | 신규 패키지 설치 없음(stdlib + 기존 pandas/numpy) — RESEARCH Package Legitimacy Audit 비어 있음 |

high 심각도 위협 없음 — 순수 오프라인 계산 층, 공격 표면 최소.
</threat_model>

<verification>
- `uv run pytest tests/test_raw_semantics_spike.py tests/test_metrics_engine.py -q` 그린(스캐폴드 skip 허용)
- `uv run pytest -q` 전 스위트 회귀 0 (store 헬퍼 additive — 기존 경로 불변)
- spike 결과(DART 분기/누적·EDGAR Q4)가 08-01 SUMMARY에 명시되어 08-03 입력이 됨
</verification>

<success_criteria>
- DART thstrm_amount 분기/누적 의미가 확정·박제됨 (FUND-09 산식 정확성 선행)
- EDGAR Q4 갭이 확정되고 Q4=빈값+사유 방침 박제됨 (D-05 일관)
- fetch_raw_quarters 헬퍼 GREEN + ?-바인딩 (ASVS V5)
- 엔진 테스트 -k 마커 스캐폴드 존재 + collect 무에러
- 전 스위트 회귀 0
</success_criteria>

<output>
Create `.planning/phases/08-registry/08-01-SUMMARY.md` when done.
**필수 기록:** (1) DART thstrm_amount 확정 결과(분기 단독/누적) + 그에 따른 08-03 DART TTM 방침,
(2) EDGAR Q4 갭 확정 + Q4 처리 방침(빈값+사유), (3) fetch_raw_quarters 시그니처. → 08-03 직접 입력.
</output>
