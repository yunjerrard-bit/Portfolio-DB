---
phase: 08-registry
plan: 03
type: tdd
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/stocksig/io/metrics_engine.py
  - tests/test_metrics_engine.py
autonomous: true
requirements: [FUND-09]

must_haves:
  truths:
    - "저장 raw만으로 분기 매트릭스 전체(분기×지표 MetricCell)가 외부 재호출 없이 산출된다(D-06)"
    - "저량=최근분기값 / 유량=TTM 직전4분기 합 / 하이브리드=분자TTM÷분모최근값 규칙으로 계산된다(SC2·D-03 — 하이브리드 분모=최근 분기 시점값, 기초·기말 평균 아님)"
    - "TTM 4분기 중 결손 분기가 있으면 해당 TTM 지표는 빈값+사유, 0 대체·부분합산 없음(SC4·D-05)"
    - "PER/PEG/GPM/OPM가 저장 raw로 재현되고, ROE/PBR/PCR/PSR/ROA 신규 지표가 동일 원천에서 즉시 산출된다(SC3)"
    - "가격 의존 4종(PER/PBR/PCR/PSR)은 per-share 분모만 산출, 비율은 호출자가 price 주입(D-07)"
    - "per-metric provenance 라벨(EDGAR/DART/yf/Naver, 혼합='+'결합)이 MetricCell.source에 보존된다(SC5/D-08)"
    - "sanity bounds 밖 값은 빈값+사유로 차단된다(D-05 단위오류/이상치 게이트)"
    - "registry(저장 raw 4분기 합산)가 새 단일 원천(canonical)이며, 레거시 표시값과의 미세 차이는 '더 일관된 값'으로 수용·문서화된다(D-04)"
    - "폴백 fetch(DART→Naver→yf)는 registry 책임이 아니라 Phase 10 오케스트레이션 몫이며, registry는 저장된 어느 source raw든 균일 소비하고 1차 결손 시 빈값+사유로 처리한다(D-09)"
  artifacts:
    - path: "src/stocksig/io/metrics_engine.py"
      provides: "분기 매트릭스 계산 엔진 (순수 함수) — registry 순회 + TTM + per-share + sanity + provenance"
      contains: "def compute_matrix"
      min_lines: 120
    - path: "tests/test_metrics_engine.py"
      provides: "유형별·TTM결손·재현·per-share/provenance·Q4·sanity 단언 (08-01 스캐폴드 채움)"
      contains: "def test_ttm_missing"
  key_links:
    - from: "src/stocksig/io/metrics_engine.py"
      to: "src/stocksig/io/fundamentals_store.fetch_raw_quarters"
      via: "raw 조회 (08-01 헬퍼)"
      pattern: "fetch_raw_quarters"
    - from: "src/stocksig/io/metrics_engine.py"
      to: "src/stocksig/io/metrics_registry.REGISTRY"
      via: "registry 순회 (08-02)"
      pattern: "REGISTRY|MetricType"
    - from: "src/stocksig/io/metrics_engine.py"
      to: "src/stocksig/io/fundamentals._is_missing / MetricCell"
      via: "결손 게이트·셀 모델 이식/재사용(WR-01, Phase 10 계약 동일)"
      pattern: "_is_missing|MetricCell"
---

<objective>
저장된 분기별 raw로부터 **9종 지표의 분기 매트릭스 전체**를 외부 재호출 없이 계산하는 순수 엔진을
구현한다(FUND-09 핵심). 유형별 산출 규칙(저량/유량 TTM/하이브리드), TTM 결손 빈값+사유, per-share
분모/가격 분리, sanity bounds, per-metric provenance를 모두 충족한다.

Purpose: Phase 9(트렌드 엑셀 매트릭스)·Phase 10(시트1 최신열)이 공통으로 읽는 **단일 원천** 백엔드
계약을 완성한다 — 두 출력의 값이 드리프트 없이 일치(설계노트).
Output: `metrics_engine.py`(compute_matrix + 유형별 헬퍼) + 08-01 테스트 스캐폴드 채운 GREEN 스위트.

선행: 08-01(DART/EDGAR raw 진실·fetch_raw_quarters·테스트 스캐폴드) + 08-02(REGISTRY). 두 SUMMARY를
반드시 읽고 DART TTM·EDGAR Q4 방침을 그대로 따른다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-registry/08-CONTEXT.md
@.planning/phases/08-registry/08-RESEARCH.md
@.planning/phases/08-registry/08-PATTERNS.md
@.planning/phases/08-registry/08-01-SUMMARY.md
@.planning/phases/08-registry/08-02-SUMMARY.md

<interfaces>
<!-- 08-01·08-02가 제공한 계약. 실행자는 그대로 사용. -->

08-01 store 헬퍼 (src/stocksig/io/fundamentals_store.py):
  fetch_raw_quarters(ticker: str) -> list[tuple]
    각 행 = (quarter, source, field, value, period_type, reprt_code, unit), quarter 오름차순

08-01 진실 (08-01-SUMMARY.md 읽고 확정값 적용):
  - DART thstrm_amount 분기/누적 의미 → DART TTM 산식 방침
  - EDGAR Q4 갭 → Q4 처리 방침(빈값+사유 기본)

08-01 fixture (tests/fixtures/raw_quarters.py):
  raw_row(...) -> 12-tuple 행 builder (결손 분기 value=None 포함)

08-02 registry (src/stocksig/io/metrics_registry.py):
  MetricType: STOCK / FLOW_TTM / HYBRID / PER_SHARE / DERIVED
  MetricDef(name, mtype, numerator, denominator, is_ratio_0_1, price_denominator)
  REGISTRY: tuple[MetricDef, ...] (9종 + per-share 분모 정의)

이식/재사용 (src/stocksig/io/fundamentals.py — 신규 작성 금지, Don't Hand-Roll):
  _is_missing(x) -> bool   (L72-78, None/NaN 단일 게이트 WR-01)
  MetricCell(value, source, note)   (L32-44, Phase 10 계약과 동일 구조 — 새 dataclass 금지)
  _empty_cell(note)   (L56-57)
  _compute_margin(numer, denom)  (L112-118 — GPM/OPM/ROE/ROA 비율 시작점)
  _compute_per/_compute_peg  (L81-109 — PER/PEG 재현 + 엣지케이스: EPS≤0/성장률≤0/전년EPS 0)

캘린더 분기 키 산술 (신규 헬퍼 — Pitfall 5 경계 테스트 필수):
  "YYYYQn" ±N. Q1→직전=전년 Q4. pd.Period(q, freq="Q") 또는 stdlib 산술.
  analog: dart_client._calendar_quarter_key / edgar_client L117
</interfaces>

<sanity_bounds>
<!-- RESEARCH Sanity Bounds 권고표 (ASSUMED — 느슨하게 시작). 밖=빈값+사유. -->
GPM: 0~1 (여유 -0.5~1.5, is_ratio_0_1)   OPM: -2~1.5
ROE: -2~2 (±200%)   ROA: -1~1
PER: 0<PER<~1000 (EPS≤0→빈값)   PEG: 0<PEG<~10 (기존 _compute_peg)
PBR: 0<PBR<~100 (BPS≤0 자본잠식→빈값)   PSR: 0<PSR<~100   PCR: OCF음수→빈값+사유
</sanity_bounds>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 분기 정규화 + TTM 합 + 캘린더 분기 산술 (유형 계산 코어)</name>
  <files>src/stocksig/io/metrics_engine.py, tests/test_metrics_engine.py</files>
  <read_first>
    - .planning/phases/08-registry/08-01-SUMMARY.md (DART 분기/누적·EDGAR Q4 확정 방침 — 산식 분기 결정)
    - src/stocksig/io/fundamentals.py L72-118 (_is_missing/_empty_cell/_compute_margin 이식 기준)
    - .planning/phases/08-registry/08-RESEARCH.md (Pattern 2·3, Pitfall 1·2·3·5, Anti-Patterns)
    - src/stocksig/io/dart_client.py L187-195 (_REPRT_TO_QUARTER / _calendar_quarter_key analog)
    - tests/fixtures/raw_quarters.py (08-01 행 builder)
  </read_first>
  <behavior>
    - test_prior_4_quarters_boundary: _prior_4_quarters("2026Q1")==["2026Q1","2025Q4","2025Q3","2025Q2"]
      (Q1→전년 Q4 경계, Pitfall 5). "2026Q3"→["2026Q3","2026Q2","2026Q1","2025Q4"].
    - test_type_rules: 저량=최근분기값(_recent) / 유량=직전4분기합(_ttm_sum) / 하이브리드=분자TTM÷분모최근.
    - test_ttm_missing: 4분기 중 1개 value=None → _ttm_sum 반환 None(전체 빈값), 3개만 합산 안 됨,
      0 대체 안 됨(SC4·D-05). 사유 note에 결손 분기 언급.
    - test_dart_quarter_semantics_applied: 08-01 확정대로 DART 손익을 분기값으로 처리(분기 단독 확정 시
      단순 4분기 합; 누적 확정 시 thisQ−직전Q 분해). 08-01-SUMMARY 방침과 일치.
    - test_edgar_q4: 08-01 확정대로 EDGAR 캘린더 Q4 손익 부재 → 해당 분기 유량 지표 빈값+사유(FY−9M
      보정 미수행). raw에 Q4 행 없으면 자연 결손.
  </behavior>
  <action>
    `metrics_engine.py` 신규 — 계산 코어. (1) `_is_missing`/`_empty_cell`/`MetricCell`은
    `from stocksig.io.fundamentals import _is_missing, MetricCell, _empty_cell`로 *재사용*(새 정의 금지,
    Phase 10 계약 동일). (2) `_calendar_quarter_offset(q: str, n: int) -> str`와 `_prior_4_quarters(q)`
    헬퍼 — "YYYYQn" ±N 산술, Q1→전년 Q4 경계 정확. (3) `_recent(raw_by_qf, field, quarter)` = 해당
    분기 시점값(저량 분모용, D-03). (4) `_ttm_sum(raw_by_qf, field, quarter)` = `_prior_4_quarters`의
    4개 값 조회 → `any(_is_missing(v))` 시 None 반환(부분합산·0 대체 절대 금지, SC4). 4개 모두 존재 시
    sum. (5) 분기 정규화 `_normalize_quarters(rows)`: fetch_raw_quarters 행 → `{(quarter, field):
    value}` dict + source 라벨 보존. DART 손익은 08-01-SUMMARY 확정 방침대로 처리(분기 단독 확정 →
    추가 분해 없음 / 누적 확정 → thisQ−직전Q, 직전 결손 시 빈값+사유). EDGAR Q4는 raw 부재대로 자연
    결손(빈값+사유) — FY−9M 보정 미구현(08-01 방침). pandas `rolling().sum()` 무비판 사용 금지
    (Anti-Pattern — min_periods 부분합산이 SC4 위반). 08-01 fixture `raw_row`로 테스트.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics_engine.py -k "prior_4_quarters or type_rules or ttm_missing or dart_quarter or edgar_q4" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `metrics_engine.py` contains `def _ttm_sum` and `def _prior_4_quarters` and `def _recent`
    - `metrics_engine.py`가 fundamentals에서 `_is_missing`·`MetricCell` import (새 dataclass/게이트 미정의)
    - `_prior_4_quarters("2026Q1")` 첫 원소가 전년 Q4 포함 (경계 GREEN)
    - 4분기 중 1개 None → `_ttm_sum` returns None (부분합산·0 대체 없음 — grep로 `or 0`/`fillna(0)` 부재 확인)
    - `metrics_engine.py`에 pandas `rolling(` 부분합산 패턴 없음 또는 명시 결손 게이트 동반
    - `uv run pytest tests/test_metrics_engine.py -k "prior_4_quarters or type_rules or ttm_missing or dart_quarter or edgar_q4" -x -q` exits 0
  </acceptance_criteria>
  <done>유형별 산출 규칙·TTM 결손 정책·분기 산술·DART/EDGAR raw 의미 처리가 08-01 방침대로 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: compute_matrix — registry 순회 + per-share 분모 + sanity + provenance + PER/PEG 재현</name>
  <files>src/stocksig/io/metrics_engine.py, tests/test_metrics_engine.py</files>
  <read_first>
    - .planning/phases/08-registry/08-02-SUMMARY.md (REGISTRY 9종·MetricType·price_denominator 계약)
    - src/stocksig/io/fundamentals.py L81-118 (_compute_per/_compute_peg/_compute_margin 재현 기준)
    - src/stocksig/io/fundamentals.py L153-176, L289 (provenance source 부여 + "+".join 병합 패턴)
    - .planning/phases/08-registry/08-RESEARCH.md (Pattern 4·5, Sanity Bounds 표, Pitfall 4)
  </read_first>
  <behavior>
    - test_compute_matrix_shape: compute_matrix(ticker) → {metric_name: {quarter: MetricCell}} 분기 매트릭스
      전체(D-06). 최신값=마지막 분기 열.
    - test_reproduce: 저장 raw(EDGAR/DART)만으로 PER/PEG/GPM/OPM 산출 — 분모 EPS_ttm·매출 등이 raw에서
      계산됨(외부 재호출 0). PER 비율 자체는 price 주입 함수로(D-07).
    - test_new_metrics: ROE/ROA/PBR/PCR/PSR가 동일 원천에서 산출(SC3) — ROE=net_income TTM÷total_equity
      최근값, PBR 분모=BPS=total_equity÷shares.
    - test_provenance_or_pershare: MetricCell.source가 사용 raw field의 source 라벨(동일→그 source,
      혼합→"EDGAR+yf" "+"결합, D-08). per-share 분모(EPS_ttm/BPS/SPS/OCF_ps)는 가격 비의존으로 산출되고
      price_ratio(denom_cell, price)가 별도로 가격 주입(D-07).
    - test_sanity_bounds: GPM>1.5 또는 ROE>2 등 범위 밖 → 빈값+"sanity 범위 밖" 사유(빈값, 0 아님).
    - test_shares_missing_pershare: shares_outstanding 결손(KR 흔함) → per-share 분모 빈값+"발행주식수
      미존재" 사유 (정상 동작, Pitfall 4).
  </behavior>
  <action>
    `metrics_engine.py`에 (1) `compute_matrix(ticker, fetch_fn=fetch_raw_quarters) -> dict[str,
    dict[str, MetricCell]]` — fetch_fn으로 raw 조회 → `_normalize_quarters` → 각 분기 × REGISTRY 순회
    → `compute_cell(mdef, quarter, raw_by_qf)`로 셀 산출. fetch_fn 주입 가능(테스트 격리·DB 비결합).
    (2) `compute_cell`: mtype 분기 — STOCK→_recent 분자/분모, FLOW_TTM→_ttm_sum 분자·_recent 분모(분모가
    shares면 최근값), HYBRID→_ttm_sum 분자÷_recent 분모(D-03), PER_SHARE→분자(유량은 TTM·저량은 최근)
    ÷최근 shares. 비율 셀은 `_compute_margin` 이식 산식 + sanity bounds 게이트(<context> sanity_bounds
    표). (3) `price_ratio(denom_cell, price) -> MetricCell` — D-07: 분모≤0/결손 또는 price 결손 → 빈값+
    사유, else price/denom. PER 엣지케이스(EPS≤0)·PEG(_compute_peg: 성장률≤0/전년EPS 0)는 fundamentals
    산식 그대로 이식. (4) provenance: `_merge_provenance(used_sources)` — 동일 source면 그 라벨, 혼합이면
    정렬 "+"결합(fundamentals L289 `"+".join` 패턴). MetricCell.note는 한국어. 결손=None(0 금지, D-05).
    08-01 테스트 스캐폴드의 -k 마커(test_reproduce/test_provenance_or_pershare 등)를 실제 GREEN으로 채움.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `metrics_engine.py` contains `def compute_matrix` and `def compute_cell` and `def price_ratio`
    - compute_matrix 반환 = `{metric: {quarter: MetricCell}}` (분기 매트릭스 전체, D-06)
    - PER/PEG/GPM/OPM + ROE/ROA/PBR/PCR/PSR 모두 산출 경로 존재 (REGISTRY 9종 순회)
    - 가격 의존 4종은 price_ratio로 가격 주입 분리 — compute_cell이 PER 비율을 직접 계산하지 않음(D-07)
    - sanity 범위 밖 → MetricCell.value is None + note contains 'sanity' (0 대체 없음)
    - shares 결손 → per-share 분모 MetricCell.value is None + note contains '발행주식수'
    - provenance 혼합 시 MetricCell.source contains '+' (예 'EDGAR+yf')
    - `uv run pytest tests/test_metrics_engine.py -x -q` exits 0 (08-01 스캐폴드 전부 GREEN, skip 0)
  </acceptance_criteria>
  <done>9종 지표 분기 매트릭스가 저장 raw만으로 산출되고, per-share/가격분리·sanity·provenance·재현이 모두 GREEN — Phase 9/10 단일 원천 계약 충족</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장된 raw_facts → metrics_engine | 유일한 입력 경계. fetch_raw_quarters로 조회한 로컬 SQLite raw만 신뢰. 외부 네트워크·인증·사용자 입력 없음(D-09: registry는 fetch 안 함) |
| 가격 주입 | price_ratio의 price 인자는 호출자(Phase 9/10)가 주입 — Phase 8은 가격 파이프라인과 비결합 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | (데이터 무결성) | 잘못된 분기값 해석 → TTM 오계산 | mitigate | 08-01 spike 확정 방침 적용(DART 분기/누적·EDGAR Q4). _ttm_sum 결손 게이트로 부분합산 차단(SC4) |
| T-08-05 | (데이터 무결성) | 결손 0 오염 → 잘못된 색 신호(Core Value) | mitigate | _is_missing(None/NaN) 단일 게이트(WR-01). 0/-999999 sentinel 절대 금지(D-05). sanity bounds로 이상치 빈값화 |
| T-08-06 | (데이터 무결성) | sanity 미적용 → 단위 오류값 전파 | mitigate | sanity bounds 게이트(범위 밖=빈값+사유) — 단위 오류·이상치 차단 |
| T-08-S3 | Auth/Session/Injection/SSRF/XSS | — | N/A | N/A — 순수 계산. registry는 외부 fetch 안 함(D-09). SQL은 08-01 store 헬퍼(?-바인딩)만 경유. API 키 미진입 |
| T-08-SC | Tampering | 패키지 설치 | N/A | 신규 패키지 없음(stdlib + 기존 pandas/numpy, fundamentals 재사용) |

high 심각도 위협 없음 — 오프라인 로컬 계산. 가장 큰 실질 위험은 "분기값 의미 오해"이며 08-01 spike + 결손 게이트로 완화.
</threat_model>

<verification>
- `uv run pytest tests/test_metrics_engine.py -q` 전부 GREEN (skip 0)
- `uv run pytest -q` 전 스위트 회귀 0 — 특히 시트1 회귀(test_sheet_portfolio·test_history_integration·
  test_fundamentals) 그린(Core Value 불변, Phase 8은 fundamentals.py 미수정)
- compute_matrix가 외부 네트워크 호출 0 (fetch_fn = store 조회만)
- grep로 `or 0`/`fillna(0)`/`-999999` 부재 확인 (D-05 결손=None)
</verification>

<success_criteria>
- 저장 raw만으로 9종 분기 매트릭스 산출 — 외부 재호출 0 (FUND-09 SC2·SC3·D-06)
- TTM 4분기 결손 → 빈값+사유, 부분합산·0 대체 없음 (SC4·D-05)
- PER/PEG/GPM/OPM 재현 + ROE/PBR/PCR/PSR/ROA 신규 즉시 산출 (SC3)
- per-share 분모/가격 주입 분리 (D-07) + per-metric provenance 보존 (SC5/D-08)
- sanity bounds 게이트 동작
- 전 스위트 회귀 0 (Core Value 보호)
</success_criteria>

<output>
Create `.planning/phases/08-registry/08-03-SUMMARY.md` when done.
**필수 기록:** compute_matrix/compute_cell/price_ratio 시그니처·매트릭스 반환 구조·provenance 병합 규칙·
sanity bounds 적용값·DART/EDGAR 분기 처리 최종 방침. → Phase 9(매트릭스 렌더)·Phase 10(최신열·가격 주입)
직접 입력. D-04 canonical 드리프트(레거시 시트1 표시값과 미세차) 수용 문서화.
</output>
