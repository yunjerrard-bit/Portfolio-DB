---
phase: 09-trend-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/history_fixtures.py
  - tests/test_quarter_price.py
  - tests/test_trend_color.py
  - src/stocksig/io/quarter_price.py
  - src/stocksig/compute/trend_color.py
autonomous: true
requirements: [FUND-10]

must_haves:
  truths:
    - "분기말 종가가 OHLCV에서 분기키(YYYYQn)별로 조달되고 최신=현재가가 분리된다 (D-09 / SC4)"
    - "동종 산업군 표본<3 또는 산업='' 셀은 상대색이 무색이다 (D-07)"
    - "밸류에이션(PER/PEG/PBR/PCR/PSR)은 낮을수록 초록·수익성(ROE/ROA/GPM/OPM)은 높을수록 초록 (D-06)"
    - "전년동기(4분기 전) 대비 ↑/↓ 화살표가 산출되고 전년 결손 시 생략된다 (D-08)"
    - "네트워크 0 fixture(compute_matrix fetch_fn stub + OHLCV monkeypatch)로 모든 테스트가 외부 호출 없이 돈다"
  artifacts:
    - path: "src/stocksig/io/quarter_price.py"
      provides: "quarter_end_prices(ticker) -> ({YYYYQn: close}, current_price)"
      contains: "resample"
    - path: "src/stocksig/compute/trend_color.py"
      provides: "relative_bucket(metric, value, peer_values, industry) + yoy_glyph(cell_q, cell_q_prior)"
      contains: "LOWER_IS_BETTER"
    - path: "tests/fixtures/history_fixtures.py"
      provides: "다종목·다산업 raw_quarters stub + 합성 OHLCV builder"
    - path: "tests/test_quarter_price.py"
      provides: "분기말 종가·현재가 단언"
    - path: "tests/test_trend_color.py"
      provides: "상대색 방향/표본 게이트/YoY 글리프 단언"
  key_links:
    - from: "src/stocksig/io/quarter_price.py"
      to: "stocksig.io.market.fetch_ohlcv_cached"
      via: "import + resample('QE').last()"
      pattern: "fetch_ohlcv_cached"
    - from: "src/stocksig/compute/trend_color.py"
      to: "stocksig.io.fundamentals._is_missing"
      via: "결손 게이트 재사용(신규 정의 금지)"
      pattern: "_is_missing"
---

<objective>
Phase 9 렌더 층의 **순수 계산 기반 2종 + 네트워크 0 테스트 스캐폴드**를 만든다 (Wave 0 + Wave 1 통합).
- `io/quarter_price.py`: 보유 10년치 OHLCV → `{YYYYQn: 분기말 종가}` + 현재가 (D-09).
- `compute/trend_color.py`: (분기열×산업) 상대 순위 → 초록/무색/빨강 bucket (D-05/06/07) + 전년동기 YoY 화살표 글리프 (D-08).
- 다종목·다산업 fixture + 두 테스트 파일(전부 네트워크 0 — `fetch_fn` stub + `fetch_ohlcv_cached` monkeypatch).

Purpose: 다운스트림 시트 writer(Plan 02)·오케스트레이션(Plan 03)이 소비할 검증된 순수 함수와 fixture를 먼저 확정해 scavenger-hunt를 차단한다. 신규 산식·외부 호출 0 (Don't Hand-Roll — 가격은 OHLCV resample, 결손은 `_is_missing`, YoY 키는 `_calendar_quarter_offset`).
Output: 2 신규 소스 모듈 + 1 fixture + 2 테스트 파일.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trend-render/09-CONTEXT.md
@.planning/phases/09-trend-render/09-RESEARCH.md
@.planning/phases/09-trend-render/09-PATTERNS.md
@.planning/phases/09-trend-render/09-VALIDATION.md

<interfaces>
<!-- 재사용 계약 (직접 read 확정). 신규 정의 금지 — 전부 import. -->

From src/stocksig/io/fundamentals.py:
- `class MetricCell: value: float|None; source: str|None; note: str|None`
- `def _is_missing(x: float|None) -> bool`  # None 또는 NaN 동시 게이트 (WR-01)
- `def _empty_cell(note: str|None=None) -> MetricCell`

From src/stocksig/io/metrics_engine.py:
- `def compute_matrix(ticker, fetch_fn=fetch_raw_quarters) -> dict[str, dict[str, MetricCell]]`  # 분기축 오름차순
- `def price_ratio(denom_cell: MetricCell, price: float|None) -> MetricCell`
- `def compute_peg_cell(per_value, eps_ttm, eps_prior) -> MetricCell`
- `def _calendar_quarter_offset(q: str, n: int) -> str`  # ("2026Q1",-4) -> "2025Q1"

From src/stocksig/io/metrics_registry.py:
- `REGISTRY: tuple[MetricDef,...]`  # 9 지표 + 4 분모 metric(EPS_ttm/BPS/SPS/OCF_ps)
- 가격 의존 4종 매핑: PER→EPS_ttm, PBR→BPS, PCR→OCF_ps, PSR→SPS (MetricDef.price_denominator)

From src/stocksig/io/market.py:
- `def fetch_ohlcv_cached(ticker: str) -> pd.DataFrame`  # columns=[Open,High,Low,Close,Volume], index=DatetimeIndex, 24h TTL

From tests/fixtures/raw_quarters.py:
- `def raw_row(ticker="AAPL", source="EDGAR", quarter="2026Q1", field="revenue", value=1000.0, unit="USD", accession="acc-0001", period_type="duration", reprt_code=None) -> tuple`  # 12-tuple

방향 상수 (D-06):
- LOWER_IS_BETTER = {"PER","PEG","PBR","PCR","PSR"}   # 낮을수록 초록
- HIGHER_IS_BETTER = {"ROE","ROA","GPM","OPM"}        # 높을수록 초록
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: quarter_price.py — 분기말 종가 + 현재가 (D-09) RED→GREEN</name>
  <read_first>
    - src/stocksig/io/market.py L101 `fetch_ohlcv_cached` (반환 DataFrame 형태·index)
    - 09-PATTERNS.md §"quarter_price.py" (resample("QE").last() + to_period("Q").astype(str))
    - 09-RESEARCH.md Pattern 1 + Pitfall 4 (키 표기 YYYYQn 일치)
    - tests/fixtures/raw_quarters.py (fixture 스타일 — 디폴트 인자 팩토리)
    - tests/test_metrics_engine.py 머리 (네트워크 0 정책·monkeypatch 패턴)
  </read_first>
  <behavior>
    - 합성 OHLCV(DatetimeIndex 4분기+) → quarter_end_prices가 {"2024Q1":..,"2024Q2":..} 키를 to_period("Q").astype(str) 표기로 반환
    - 각 분기 값 = 그 분기 마지막 거래일 Close (휴장일은 resample last가 자동 처리)
    - current_price == Close.dropna().iloc[-1]
    - 분기키 표기가 metrics_engine._calendar_quarter_offset 출력("2026Q1")과 정확히 일치 (Pitfall 4 단언)
  </behavior>
  <action>
    `src/stocksig/io/quarter_price.py`에 `quarter_end_prices(ticker: str) -> tuple[dict[str, float], float]` 정의. `from stocksig.io.market import fetch_ohlcv_cached`로 OHLCV 조달(캐시 HIT 시 외부 호출 0), `close = df["Close"].dropna()`, `qe = close.resample("QE").last()`(pandas 2.2+ — 구 "Q" 금지), `keys = qe.index.to_period("Q").astype(str)`, `qmap = dict(zip(keys, qe.to_numpy()))`, `current_price = float(close.iloc[-1])` 반환. 빈 시계열 가드(빈 dict + None 또는 명시 처리 — note는 호출자). 신규 분기 경계 산술 금지(resample 위임, Don't Hand-Roll).
    테스트는 `tests/test_quarter_price.py`에 작성하고 OHLCV는 `fetch_ohlcv_cached`를 monkeypatch한 합성 DataFrame으로 주입(네트워크 0). fixture builder는 Task 3에서 `tests/fixtures/history_fixtures.py`에 추가하되, 본 태스크에서 최소 inline 합성 DataFrame으로 RED→GREEN 후 Task 3 fixture로 정리 가능.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_quarter_price.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_quarter_price.py::test_quarter_end_close_and_current` GREEN
    - quarter_end_prices 반환 dict 키가 정확히 "YYYYQn" 표기(예 "2024Q1"), `_calendar_quarter_offset("2024Q2",-1)=="2024Q1"`와 동일 표기 단언
    - current_price == 합성 Close 마지막값 (approx)
    - 테스트 실행 중 yfinance/네트워크 호출 0 (monkeypatch로 fetch_ohlcv_cached 대체)
    - `resample("QE")` 사용(grep `resample("QE")` 1건 이상, 구 `resample("Q")` 0건)
  </acceptance_criteria>
  <done>합성 OHLCV에서 분기말 종가 dict + 현재가가 정확한 YYYYQn 키로 반환되고 네트워크 0으로 검증된다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: trend_color.py — 상대색 bucket(D-05/06/07) + YoY 글리프(D-08) RED→GREEN</name>
  <read_first>
    - src/stocksig/compute/color_rules.py L1-28 (순수 함수 스타일·색 상수 GREEN_100/RED_100 — 함수 미수정, 상수만 참조)
    - src/stocksig/io/fundamentals.py L72 `_is_missing` (결손 게이트 — 재사용)
    - src/stocksig/io/metrics_engine.py L69 `_calendar_quarter_offset` (YoY 4분기 전 키)
    - 09-PATTERNS.md §"trend_color.py" (relative_bucket / yoy_glyph 시그니처·모집단 정의)
    - 09-RESEARCH.md Pattern 4 + Pitfall 3 (모집단=(분기열,산업) 2차원, 표본<3 무색)
  </read_first>
  <behavior>
    - relative_bucket(metric, value, peer_values, industry): industry=="" 또는 유효 peer(_is_missing 제외) < 3 → "무색" (D-07)
    - LOWER_IS_BETTER 지표(PER 등)는 value가 peer 하위 분위면 "초록", 상위면 "빨강"; HIGHER_IS_BETTER(ROE 등)는 방향 반전 (D-06)
    - 동값/동순위는 "무색"(중립) 처리 (RESEARCH A4 정책 확정)
    - value 자체가 _is_missing이면 "무색"
    - yoy_glyph(cell_q, cell_q_prior): 둘 중 하나라도 None 또는 value _is_missing → "" (D-08 전년 결손 생략); cell_q.value > prior → " ▲", < → " ▼", == → ""
  </behavior>
  <action>
    `src/stocksig/compute/trend_color.py`에 모듈 상수 `LOWER_IS_BETTER = {"PER","PEG","PBR","PCR","PSR"}`·`HIGHER_IS_BETTER = {"ROE","ROA","GPM","OPM"}`(D-06) 정의. `relative_bucket(metric: str, value, peer_values: list, industry: str) -> str`("초록"|"무색"|"빨강") 구현 — `from stocksig.io.fundamentals import _is_missing`로 유효 peer 필터, `industry=="" or len(valid)<3` 무색 게이트(권장 N=3, D-07), 유효 peer 내 value 3분위(상/중/하) 산정 후 LOWER_IS_BETTER면 방향 반전, 동률 무색. `yoy_glyph(cell_q, cell_q_prior) -> str` 구현 — 유니코드 글리프 " ▲"/" ▼"/""(icon_set 기각 — RESEARCH). 4분기 전 키 산술은 호출자(Plan 03)가 `_calendar_quarter_offset(q,-4)`로 조회 후 두 셀 주입; 본 함수는 두 셀만 비교. 순수 함수만(외부 의존 0·네트워크 0). 색 베이킹용 hex 상수는 정의하지 않음(Plan 02 history_workbook이 color_rules에서 import).
    테스트는 `tests/test_trend_color.py`에 작성 — 합성 peer 리스트·MetricCell 직접 구성(import만으로 실행, 네트워크 0).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_trend_color.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_trend_color.py` GREEN — 다음 단언 포함: PER 최저값+peer 3종 → "초록", PER 최고값 → "빨강", ROE 최고값 → "초록"(방향 반전), peer 2종 → "무색", industry="" → "무색", value=None → "무색"
    - `tests/test_trend_color.py::test_yoy_glyph` GREEN — cell_q>prior → " ▲", < → " ▼", prior 결손(None/NaN) → ""
    - grep `_is_missing` in trend_color.py ≥ 1 (신규 결손 게이트 정의 0 — `def _is_missing` 0건)
    - 테스트 실행 네트워크 0 (순수 함수·import만)
  </acceptance_criteria>
  <done>상대색 3-bucket이 방향(D-06)·표본 게이트(D-07)를 정확히 반영하고 YoY 글리프가 전년 결손을 생략한다.</done>
</task>

<task type="auto">
  <name>Task 3: 다종목·다산업 네트워크 0 fixture 통합</name>
  <read_first>
    - tests/fixtures/raw_quarters.py (raw_row 12-tuple 팩토리 — 재사용)
    - src/stocksig/io/metrics_engine.py L299-338 (compute_matrix fetch_fn 주입점, fetch_fn은 7-tuple 기대 vs raw_row 12-tuple — 변환 필요 여부 확인: _normalize_quarters 입력 형태)
    - src/stocksig/io/fundamentals_store.py L166 `fetch_raw_quarters` 반환 7-tuple (quarter,source,field,value,period_type,reprt_code,unit) — stub이 모방할 형태
    - 09-VALIDATION.md Wave 0 Requirements (fixture 항목)
  </read_first>
  <action>
    `tests/fixtures/history_fixtures.py`에 (a) `fetch_fn` stub 팩토리 — 다종목(US 1·KR 1 최소)·다산업·다분기 raw 행을 `compute_matrix(ticker, fetch_fn=stub)`이 받는 형태(7-tuple: quarter,source,field,value,period_type,reprt_code,unit)로 반환하는 함수(`raw_quarters.raw_row` 재사용 후 7-tuple 투영 또는 직접 구성 — `_normalize_quarters` 입력 계약에 맞춤), (b) 합성 OHLCV builder(DatetimeIndex 다분기 Close 시계열, `fetch_ohlcv_cached` monkeypatch용)를 추가. Task 1·2 테스트가 inline으로 구성한 합성 데이터를 이 fixture로 치환해 재사용(중복 제거). 모든 fixture는 결정적(seed 고정·하드코딩 값), 외부 호출 0.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_quarter_price.py tests/test_trend_color.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/fixtures/history_fixtures.py` 존재 — fetch_fn stub + OHLCV builder export
    - stub이 반환한 행으로 `compute_matrix("AAPL", fetch_fn=stub)`가 KeyError 없이 매트릭스 반환(다분기·다산업)
    - Task 1·2 테스트가 fixture 사용으로 리팩터된 후에도 GREEN 유지(2 파일 통과)
    - 전 스위트 회귀 0: `.venv/Scripts/python.exe -m pytest -q` → 341 passed + 신규 통과(회귀 0, baseline 341 유지)
  </acceptance_criteria>
  <done>다종목·다산업 결정적 fixture가 compute_matrix fetch_fn·OHLCV monkeypatch에 주입 가능하고 네트워크 0으로 두 테스트를 지원한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB(data/fundamentals.db) → 렌더 코드 | 읽기 전용 — 신규 SQL 미작성, 기존 `?`-바인딩 함수만 (이 플랜은 DB 미접근, fetch_fn stub만) |
| OHLCV 캐시/yfinance → quarter_price | 외부 시세 — 캐시 우선, 테스트는 monkeypatch로 차단 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | quarter_price.py OHLCV 입력 | accept | 시세는 로컬 캐시/yfinance, 단일 사용자 로컬 도구. 신규 SQL·네트워크 쓰기 경로 없음 |
| T-09-02 | Info Disclosure | 테스트 fixture | mitigate | fixture는 합성 데이터(실제 키·비밀 미포함), 네트워크 0 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy Audit — slopcheck 대상 없음). 전부 기설치 의존 재사용 |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_quarter_price.py tests/test_trend_color.py -q` 전부 GREEN
- `.venv/Scripts/python.exe -m pytest -q` — baseline 341 passed 유지 + 신규 통과(회귀 0)
- 신규 모듈에 `def _is_missing`·`def _compute_peg`·신규 MetricCell 정의 0건(재사용만)
- `resample("QE")` 사용·`resample("Q")` 미사용
</verification>

<success_criteria>
- quarter_price.quarter_end_prices가 분기말 종가 dict(YYYYQn) + 현재가를 네트워크 0으로 산출 (D-09 / SC4 기반)
- trend_color.relative_bucket이 방향(D-06)·표본 게이트(D-07)를 정확히 반영, yoy_glyph가 전년 결손 생략 (D-08)
- 다종목·다산업 결정적 fixture가 다운스트림(Plan 02/03) 테스트에 재사용 가능
- 시트1 회귀 0(이 플랜은 시트1 파일 미접근)
</success_criteria>

<output>
Create `.planning/phases/09-trend-render/09-01-SUMMARY.md` when done
</output>
