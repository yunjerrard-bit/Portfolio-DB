---
phase: 09-trend-render
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/stocksig/output/history_workbook.py
  - src/stocksig/output/sheet_metric_matrix.py
  - src/stocksig/output/sheet_raw.py
  - src/stocksig/output/sheet_snapshot.py
  - tests/test_history_sheets.py
autonomous: true
requirements: [FUND-10]

must_haves:
  truths:
    - "지표별 시트가 식별 5열(티커·기업명·시장·티어·산업) + 분기 열(최신 왼쪽)로 그려진다 (D-01/02/SC2)"
    - "A열(티커)만 freeze, 헤더행은 freeze 안 함 (D-04)"
    - "결손/sanity-밖 셀은 '-' + 사유 코멘트 (D-11 — 0/빈칸 sentinel 금지)"
    - "상대색이 정적 add_format으로 베이킹되고 YoY 화살표가 셀 텍스트에 결합된다 (D-05/08)"
    - "[원천] 시트가 raw long 7-tuple, [최신 스냅샷] 시트가 종목 1행×전 지표 최신값 (D-13/SC3)"
    - "시트1 portfolio 모듈은 import도 호출도 수정도 안 한다 (Core Value 불변)"
  artifacts:
    - path: "src/stocksig/output/history_workbook.py"
      provides: "트렌드 전용 make_history_workbook(path) -> (Workbook, formats) — green/red/plain/header/comment Format"
      contains: "constant_memory"
    - path: "src/stocksig/output/sheet_metric_matrix.py"
      provides: "write_metric_sheet(wb, ws, metric, rows, display_quarters, formats)"
    - path: "src/stocksig/output/sheet_raw.py"
      provides: "[원천] long 시트 writer"
    - path: "src/stocksig/output/sheet_snapshot.py"
      provides: "[최신 스냅샷] 종목 1행×지표 writer"
    - path: "tests/test_history_sheets.py"
      provides: "openpyxl read-back 셀/색/코멘트/freeze 단언"
  key_links:
    - from: "src/stocksig/output/history_workbook.py"
      to: "stocksig.compute.color_rules (GREEN_100/GREEN_900/RED_100/RED_900)"
      via: "색 상수 import (로직 미수정)"
      pattern: "from stocksig.compute.color_rules import"
    - from: "src/stocksig/output/sheet_metric_matrix.py"
      to: "stocksig.compute.trend_color (relative_bucket/yoy_glyph)"
      via: "셀 색 bucket + 화살표 결합"
      pattern: "relative_bucket|yoy_glyph"
    - from: "src/stocksig/output/sheet_metric_matrix.py"
      to: "stocksig.io.fundamentals._is_missing"
      via: "결손 '-' 게이트"
      pattern: "_is_missing"
---

<objective>
트렌드 워크북 작성 층 — **별도 워크북 팩토리 + 3종 시트 writer**.
- `history_workbook.py`: 트렌드 전용 `make_history_workbook` (시트1 45키 Format 캐시와 비결합, Pitfall 5). 초록/빨강/무색(plain) 정적 Format + header + comment.
- `sheet_metric_matrix.py`: 지표별 매트릭스 시트 (식별 5열 D-02 + 분기 열 최신 왼쪽 D-01, 상대색 정적 베이킹 D-05, YoY 글리프 결합 D-08, 결손 '-'+코멘트 D-11, freeze A열만 D-04).
- `sheet_raw.py`: [원천] raw long 시트 (fetch_raw_quarters 7-tuple).
- `sheet_snapshot.py`: [최신 스냅샷] 종목 1행 × 전 지표 최신값 (D-13).

Purpose: XlsxWriter 정적 색 베이킹(CLAUDE.md — conditional_format/icon_set 기각)으로 이중 인코딩을 시트에 굽는다. 식별 열은 sheet_portfolio `_COL`/`PORTFOLIO_COLUMNS`를 **import하지 않고 동일 헤더 리터럴로 트렌드 전용 정의**(시트1 결합 회피). 시트1 모듈은 읽기 전용 참조(Core Value 불변).
Output: 4 신규 output 모듈 + 1 openpyxl read-back 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trend-render/09-CONTEXT.md
@.planning/phases/09-trend-render/09-RESEARCH.md
@.planning/phases/09-trend-render/09-PATTERNS.md
@.planning/phases/09-trend-render/09-01-SUMMARY.md

<interfaces>
<!-- 직접 read 확정 계약. 식별열은 import 금지·동일 리터럴 재정의. -->

From src/stocksig/output/writer.py (패턴만 차용 — make_workbook 직접 호출 금지):
- `xlsxwriter.Workbook(str(p), {"constant_memory": False, "nan_inf_to_errors": True})`
- `p.parent.mkdir(parents=True, exist_ok=True)`
- `wb.add_format({...})`, `formats["header"] = wb.add_format({"bold": True, "align": "center"})`

From src/stocksig/compute/color_rules.py (상수만 import — 함수 미수정):
- `GREEN_100="#C8E6C9"`, `GREEN_900="#1B5E20"`, `RED_100="#FFCDD2"`, `RED_900="#B71C1C"`

From src/stocksig/output/sheet_portfolio.py (읽기 전용 모델 — import 금지):
- 식별열 헤더 리터럴(트렌드에서 동일 재정의): `["티커","기업명","시장","티어","산업"]` + 명명 인덱스 dict 패턴
- `_write_fund_cell` 패턴: 값 있으면 write_number + write_comment(note or source); 결손이면 write_blank
  → **트렌드 차이(D-11): write_blank 대신 `ws.write_string(row,col,"-")` + write_comment(사유)**
- freeze 패턴: 시트1은 `ws.freeze_panes(5,1)` → **D-04: `ws.freeze_panes(0,1)`(A열만, 헤더행 미고정)**
- `ws.set_column(col, col, width)` 개별 폭 (시장/티어 최소 너비)

From src/stocksig/compute/trend_color.py (Plan 01):
- `relative_bucket(metric, value, peer_values, industry) -> "초록"|"무색"|"빨강"`
- `yoy_glyph(cell_q, cell_q_prior) -> str`  # " ▲"|" ▼"|""
- `LOWER_IS_BETTER`, `HIGHER_IS_BETTER`

From src/stocksig/io/fundamentals.py:
- `MetricCell(value, source, note)`, `_is_missing(x) -> bool`

From src/stocksig/io/fundamentals_store.py:
- `fetch_raw_quarters(ticker) -> list[tuple]`  # (quarter,source,field,value,period_type,reprt_code,unit) 7-tuple, period_end 미포함
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: history_workbook.py — 트렌드 전용 워크북 팩토리 + Format 캐시</name>
  <read_first>
    - src/stocksig/output/writer.py L70-142 `make_workbook` (워크북 옵션·parent mkdir·add_format 패턴 — 직접 호출 금지, 패턴만)
    - src/stocksig/compute/color_rules.py L16-21 (GREEN/RED 상수 — import만, 함수 미수정)
    - 09-PATTERNS.md §"history_workbook.py" (별도 Format 셋 근거·Pitfall 5)
    - 09-RESEARCH.md §"history_workbook" (constant_memory/nan_inf_to_errors)
  </read_first>
  <action>
    `src/stocksig/output/history_workbook.py`에 `make_history_workbook(path) -> tuple[xlsxwriter.Workbook, dict]` 정의. writer.py 패턴 차용(직접 호출 금지): `p = Path(path); p.parent.mkdir(parents=True, exist_ok=True)`, `wb = xlsxwriter.Workbook(str(p), {"constant_memory": False, "nan_inf_to_errors": True})`. `from stocksig.compute.color_rules import GREEN_100, GREEN_900, RED_100, RED_900`(상수만). formats dict: `green`={bg_color GREEN_100, font_color GREEN_900, num_format "#,##0.00"}, `red`={bg_color RED_100, font_color RED_900, num_format "#,##0.00"}, `plain`={num_format "#,##0.00"}(무색 D-07), `header`={bold, align center}, 텍스트 셀용 `green_text`/`red_text`/`plain_text`(YoY 글리프 결합 시 문자열 셀 — num_format 미적용, RESEARCH 방법 A). **WARNING-2(표시 정합) 보강: 비율 지표(MetricDef.is_ratio_0_1=True인 GPM/OPM 등)는 시트1과 동일하게 퍼센트로 보이도록, 값 텍스트를 Task 2에서 `f"{value*100:.1f}%"`로 포맷한다(저장은 0~1 비율). 따라서 Format 자체는 텍스트 셀(green_text/red_text/plain_text)을 공유하면 충분 — 별도 percent num_format 불필요(텍스트로 베이킹).** `make_workbook`(시트1)·`write_portfolio_sheet`는 import·호출 금지. `[원천]` 행수 크면 constant_memory=True 옵션화 가능하나 기본 False.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "from stocksig.output.history_workbook import make_history_workbook; import tempfile,os; p=os.path.join(tempfile.mkdtemp(),'h.xlsx'); wb,f=make_history_workbook(p); assert {'green','red','plain','header'} <= set(f); wb.close(); assert os.path.exists(p)"</automated>
  </verify>
  <acceptance_criteria>
    - `make_history_workbook` 반환 formats에 green/red/plain/header 키 존재, wb.close() 후 파일 생성
    - grep `make_workbook` in history_workbook.py = 0 (시트1 팩토리 미호출)
    - grep `from stocksig.compute.color_rules import` 존재(상수만), `decide_` 미참조
    - color_rules.py·writer.py·sheet_portfolio.py 미수정(git diff 0)
  </acceptance_criteria>
  <done>트렌드 전용 워크북 팩토리가 초록/빨강/무색/header Format을 시트1 캐시와 비결합으로 제공한다.</done>
</task>

<task type="auto">
  <name>Task 2: sheet_metric_matrix.py — 지표 매트릭스 시트(식별열+분기열+이중인코딩+freeze)</name>
  <read_first>
    - src/stocksig/output/sheet_portfolio.py L37-63(식별열 모델), L112-131(_write_fund_cell 결손 패턴), L307/L336(set_column/freeze_panes) — 읽기 전용 모델, import 금지
    - src/stocksig/compute/trend_color.py (relative_bucket/yoy_glyph — Plan 01)
    - src/stocksig/io/fundamentals.py L72 `_is_missing` / L33 `MetricCell`
    - 09-PATTERNS.md §"sheet_metric_matrix.py" (식별열 트렌드 전용 정의·D-11 "-"·D-04 freeze(0,1))
    - 09-RESEARCH.md Pattern 4 + Pitfall 1/2/3 (reversed 분기·합집합 .get·모집단 (분기,산업))
  </read_first>
  <action>
    `src/stocksig/output/sheet_metric_matrix.py`에 식별열 헤더 리터럴 `_IDENT_COLUMNS = ["티커","기업명","시장","티어","산업"]` + 명명 인덱스 dict(트렌드 전용 — sheet_portfolio `_COL` import 금지)를 정의. `write_metric_sheet(wb, ws, metric: str, ticker_rows, display_quarters, formats, peer_lookup, prior_lookup)` 구현 (peer_lookup/prior_lookup은 WARNING-3대로 호출자 주입 — 아래 (4) 참조):
    (1) 헤더 행: 식별 5열 + display_quarters(이미 최신 왼쪽 = reversed, Plan 03이 산출) 헤더 write(formats["header"]);
    (2) 종목 행마다 식별 5열 write_string(시장/티어/산업 None 폴백 "");
    (3) 분기 셀: 그 (분기 열, 산업 그룹) 모집단의 유효값으로 `relative_bucket(metric, value, peer_values, industry)`(D-05/06/07) → green/red/plain Format 선택, `yoy_glyph(cell_q, cell_q_prior)`(D-08) 글리프 결합, `_is_missing(value)`이면 `ws.write_string(row,col,"-", formats["plain"])` + `ws.write_comment(row,col, 사유)`(D-11 — write_blank/0 금지), 값 있으면 `ws.write_string(row,col, f"{text_val}{glyph}", fmt_text)` + provenance 코멘트(D-12 보조). **값 텍스트(WARNING-2 표시 정합): 비율 지표는 퍼센트, 아니면 소수 2자리** — `text_val = f"{value*100:.1f}%" if is_ratio else f"{value:.2f}"`. `is_ratio`는 모듈 상단에서 `_IS_RATIO = {m.name: m.is_ratio_0_1 for m in REGISTRY}` 1회 구성해 조회(GPM/OPM 등 True → 27% 표시로 시트1 정합; 신규 산식 아님, 표시 분기만);
    (4) `ws.freeze_panes(0, 1)`(A열만, 헤더행 미고정 — D-04), 시장/티어 열 `ws.set_column`으로 최소 폭. **peer/prior 책임 확정(WARNING-3): 호출자(Plan 03 run_history)가 (분기열×산업) 2차원 모집단과 4분기 전 셀을 계산해 `peer_lookup(metric, quarter, industry) -> list[float]`(유효 peer 값)·`prior_lookup(metric, ticker, quarter) -> MetricCell|None`로 주입한다(시그니처는 09-03 정의와 동일한 3-arg(metric 포함)으로 통일 — wave3 정합). writer는 모집단을 자체 구성하지 않는다(책임 단일화 — wave3 통합 시 시그니처 재작업 방지, Pitfall 3 모집단=(분기열,산업) 2차원 보장은 호출자 측).** 분기 미보유 셀은 `.get` 가드 → "-"(Pitfall 2).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_history_sheets.py -k matrix -x -q</automated>
  </verify>
  <acceptance_criteria>
    - openpyxl read-back: 시트 A1=="티커", B1=="기업명", E1=="산업", F1==display_quarters[0](최신 분기)
    - 결손 셀 value=="-" 이고 `.comment.text`에 사유 존재(D-11)
    - 산업 표본<3 그룹 셀은 fill이 무색(plain — bg 없음), 표본≥3·LOWER_IS_BETTER 최저값 셀 fill rgb endswith "C8E6C9"(초록 D-05/06)
    - YoY 증가 셀 value에 "▲" 포함(D-08), 전년 결손 셀 글리프 없음
    - `ws.freeze_panes` == "B1"(A열만, 헤더행 미고정 — D-04)
    - 비율 지표(GPM/OPM 등 is_ratio_0_1=True) 셀 텍스트가 퍼센트 표기("%"포함, 예 0.27→"27.0%"), 비-비율 지표(PER 등)는 소수("." 포함, "%" 없음) — WARNING-2 시트1 표시 정합
    - `write_metric_sheet` 시그니처에 `peer_lookup`·`prior_lookup` 인자 존재(호출자 주입 — writer가 모집단 자체 구성 안 함, WARNING-3)
    - grep `_COL` import from sheet_portfolio = 0; grep `_is_missing` ≥ 1(재사용)
  </acceptance_criteria>
  <done>지표 매트릭스 시트가 식별 5열+최신왼쪽 분기열·정적 상대색·YoY 글리프·결손 '-'코멘트·A열 freeze로 그려진다.</done>
</task>

<task type="auto">
  <name>Task 3: sheet_raw.py [원천] + sheet_snapshot.py [최신 스냅샷]</name>
  <read_first>
    - src/stocksig/io/fundamentals_store.py L166 `fetch_raw_quarters` 반환 7-tuple(quarter,source,field,value,period_type,reprt_code,unit) — period_end 미포함
    - src/stocksig/output/sheet_portfolio.py L313-334(헤더+행 루프), L112-131(_write_fund_cell — 스냅샷 셀 모델) — 읽기 전용
    - 09-PATTERNS.md §"sheet_raw.py"/"sheet_snapshot.py" (long 행·1행/종목 + "-")
    - 09-RESEARCH.md Open Q2 (스냅샷 PEG = 매트릭스 최신 열 셀 재사용)
  </read_first>
  <action>
    `src/stocksig/output/sheet_raw.py`에 `write_raw_sheet(ws, raw_by_ticker, formats)` — 헤더 한국어(티커·소스·분기·필드·값·기간유형·보고서코드·단위) + `fetch_raw_quarters(ticker)` 7-tuple을 long 행으로 write(provenance=source 열, D-12 중심). value None은 "-" 또는 빈칸(검증 시트라 D-11 일관 권장). 종목 정렬은 Plan 03 sorted_tickers 순서 그대로.
    `src/stocksig/output/sheet_snapshot.py`에 `write_snapshot_sheet(ws, snapshot_rows, formats)` — 헤더(식별 5열 + 9지표 이름 PER/PEG/GPM/OPM/PBR/PCR/PSR/ROE/ROA) + 종목 1행 × 전 지표 **최신 열 셀 재사용**(재계산 없이 매트릭스 최신 분기 셀, D-13/Open Q2). 결손/PEG 미산출 셀 "-" + 코멘트(`_is_missing` 게이트). 신규 산식·재계산 금지.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_history_sheets.py -k "raw or snapshot" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - openpyxl read-back: "[원천]" 시트(또는 sanitize명)에 헤더 행 + raw long 행 존재, 값 결손 행은 "-" 또는 빈칸 일관
    - "[최신 스냅샷]" 시트에 식별 5열 + 9지표 헤더, 종목 1행에 최신값(매트릭스 최신 열과 동일 값), PEG 결손 시 "-"
    - 스냅샷 셀이 매트릭스 최신 열 셀 재사용(재계산 흔적 없음 — 신규 price_ratio/compute_peg_cell 호출은 Plan 03 오케스트레이션에서만)
    - grep `_is_missing` ≥ 1(재사용), 신규 결손 게이트 정의 0
  </acceptance_criteria>
  <done>[원천] long 시트와 [최신 스냅샷] 1행/종목 시트가 7-tuple·매트릭스 최신 셀 재사용으로 작성된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 시트1 portfolio 모듈 ↔ 트렌드 모듈 | Core Value 경계 — 트렌드는 시트1 모듈을 읽기만(색 상수 import), 함수/로직/시그니처 미수정 |
| 지표 시트명 ↔ Excel | 시트명은 고정 리터럴(PER/PEG/...) — 동적 시트명 인젝션 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | 시트1 Format 캐시 오염 | mitigate | 트렌드 전용 워크북 팩토리 신설(Pitfall 5) — make_workbook 미호출, 색 상수만 import |
| T-09-04 | Tampering | Excel 시트명 인젝션 | mitigate | 지표 시트명 고정 리터럴, 동적 시트명 시 sheet_portfolio `_sanitize_sheet_name` 패턴 재사용 |
| T-09-05 | Info Disclosure | 셀 코멘트 provenance | accept | source 라벨(EDGAR/DART/yf)만 — 비밀·키 미노출 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Audit) |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest tests/test_history_sheets.py -q` 전부 GREEN
- `.venv/Scripts/python.exe -m pytest -q` — 시트1 회귀 0(baseline 341 유지 + 신규)
- git diff: `sheet_portfolio.py`·`color_rules.py`·`writer.py` 변경 0줄(Core Value 불변)
- 트렌드 모듈에서 `make_workbook`/`write_portfolio_sheet`/`decide_*` 미호출, `_COL` import 0
</verification>

<success_criteria>
- 지표 매트릭스 시트 = 식별 5열(D-02) + 분기 열 최신 왼쪽(D-01) + 정적 상대색(D-05/06/07) + YoY 글리프(D-08) + 결손 '-'코멘트(D-11) + A열 freeze(D-04) — SC2
- [원천](raw long)·[최신 스냅샷](1행/종목) 시트 — SC3/D-13
- 시트1 portfolio 파일·로직 완전 불변 (Core Value)
</success_criteria>

<output>
Create `.planning/phases/09-trend-render/09-02-SUMMARY.md` when done
</output>
