---
phase: 09-trend-render
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - src/stocksig/io/history_render.py
  - main.py
  - tests/test_history_render.py
autonomous: true
requirements: [FUND-10]

must_haves:
  truths:
    - "history 서브커맨드 실행 시 fundamentals_history_YYYYMMDD.xlsx가 DB에서 별도 파일로 렌더된다 (D-14/D-15/SC1)"
    - "시트1 portfolio_*.xlsx 산출 흐름(main_run.run)과 완전 분리된다 (D-15)"
    - "종목이 미국→한국 그룹 후 그룹 내 알파벳순으로 정렬된다 (D-03)"
    - "가격 의존 4종 과거=분기말 종가·최신=현재가로 price_ratio 주입, PEG 분기별 compute_peg_cell (D-09/10/SC4)"
    - "DB 미적재(count_rows()==0) 시 한국어 안내 후 정상 종료(예외 아님) (D-15)"
    - "기존 portfolio 흐름·시트1 색 신호는 회귀 없이 동작 (Core Value 불변)"
  artifacts:
    - path: "src/stocksig/io/history_render.py"
      provides: "run_history(tickers_path, output_dir) -> Path|None 오케스트레이션 엔트리"
      contains: "compute_matrix"
    - path: "main.py"
      provides: "argparse 서브커맨드 history (main_run.run과 분리)"
      contains: "add_subparsers"
    - path: "tests/test_history_render.py"
      provides: "SC1~4·D-09/10/11·시트1 불변 통합 단언"
  key_links:
    - from: "src/stocksig/io/history_render.py"
      to: "stocksig.io.metrics_engine (compute_matrix/price_ratio/compute_peg_cell/_calendar_quarter_offset)"
      via: "지표 매트릭스 소비 + 가격 주입 + PEG (외부 호출 0)"
      pattern: "compute_matrix|price_ratio|compute_peg_cell"
    - from: "src/stocksig/io/history_render.py"
      to: "stocksig.io.quarter_price.quarter_end_prices"
      via: "분기말 종가 + 현재가 주입 (D-09)"
      pattern: "quarter_end_prices"
    - from: "main.py"
      to: "stocksig.io.history_render.run_history"
      via: "history 서브커맨드 늦은 import (main_run.run 비결합)"
      pattern: "run_history"
---

<objective>
오케스트레이션 엔트리 + CLI 배선 + 통합 검증.
- `io/history_render.py`: `run_history(tickers_path, output_dir)` — DB 미적재 게이트(count_rows==0 한국어 안내, D-15) → 종목 정렬(US→KR, 알파벳 D-03) → ticker별 `compute_matrix`(외부 호출 0) + `quarter_end_prices`(D-09) → 가격 의존 4종 `price_ratio` 주입·분기별 PEG `compute_peg_cell`(D-10) → 다종목 매트릭스 합집합·reversed(최신 왼쪽 D-01) → history_workbook + 9 지표 시트(Plan 02) + [원천] + [최신 스냅샷] → `fundamentals_history_YYYYMMDD.xlsx`(D-14).
- `main.py`: argparse 서브커맨드 `history` 추가 — `main_run.run`과 완전 분리(D-15), 기존 portfolio 흐름 하위호환.
- 통합 테스트: SC1~4 + D-09/10/11 + 시트1 불변(회귀) — 네트워크 0(Plan 01 fixture stub + OHLCV monkeypatch).

Purpose: Phase 8 엔진·Phase 7 store·Plan 01/02를 배선해 사용자가 별도 트렌드 엑셀을 한 번의 서브커맨드로 얻게 한다(FUND-10). 신규 산식·외부 펀더멘털 호출 0 — 전부 검증된 함수 소비.
Output: 1 신규 오케스트레이션 모듈 + main.py 수정 + 1 통합 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-trend-render/09-CONTEXT.md
@.planning/phases/09-trend-render/09-RESEARCH.md
@.planning/phases/09-trend-render/09-PATTERNS.md
@.planning/phases/09-trend-render/09-01-SUMMARY.md
@.planning/phases/09-trend-render/09-02-SUMMARY.md

<interfaces>
<!-- 직접 read 확정 계약. -->

From src/stocksig/io/metrics_engine.py:
- `compute_matrix(ticker, fetch_fn=fetch_raw_quarters) -> {metric: {quarter: MetricCell}}`  # 분기축 오름차순 → reversed 필수
- `price_ratio(denom_cell: MetricCell, price: float|None) -> MetricCell`
- `compute_peg_cell(per_value, eps_ttm, eps_prior) -> MetricCell`
- `_calendar_quarter_offset(q, n) -> str`  # ("2026Q1",-4)->"2025Q1"
- 가격 의존 분모 매핑: PER→EPS_ttm, PBR→BPS, PCR→OCF_ps, PSR→SPS

From src/stocksig/io/fundamentals_store.py:
- `count_rows(ticker=None) -> int`  # DB 적재 판정 (==0 → 안내)
- `fetch_raw_quarters(ticker) -> list[tuple]`  # [원천] 입력

From src/stocksig/io/quarter_price.py (Plan 01):
- `quarter_end_prices(ticker) -> tuple[dict[str,float], float]`  # ({YYYYQn:종가}, 현재가)

From src/stocksig/output/history_workbook.py (Plan 02):
- `make_history_workbook(path) -> (Workbook, formats)`
From src/stocksig/output/sheet_metric_matrix.py / sheet_raw.py / sheet_snapshot.py (Plan 02):
- `write_metric_sheet(...)`, `write_raw_sheet(...)`, `write_snapshot_sheet(...)`

From src/stocksig/io/input.py / company.py / market_kind.py:
- `read_tickers_extended(path) -> list[TickerSpec(symbol, tier, industry)]`
- `fetch_company_name(ticker) -> str`
- `classify_market(symbol) -> "US"|"KR"`  # D-03 그룹 정렬

From src/stocksig/io/metrics_registry.py:
- `REGISTRY` — 지표 시트 목록(9종) + 분모 metric

From main.py (수정 대상):
- 기존 argparse: --tickers/--env/--output-dir/--summary-only → `main_run.run(...)`; 늦은 import 컨벤션; UTF-8 reconfigure
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: history_render.py — 오케스트레이션 엔트리(정렬·가격주입·PEG·매트릭스 재구성·시트 배선)</name>
  <read_first>
    - src/stocksig/main_run.py L235-391 (run 흐름·import 블록·카운터·한국어 로깅·예외 격리 type(exc).__name__·파일명 date.today())
    - src/stocksig/io/metrics_engine.py L299-338 (compute_matrix 계약·분기축 오름차순·PEG 3단 docstring L309-324)
    - src/stocksig/io/fundamentals_store.py L155(count_rows)/L166(fetch_raw_quarters)
    - src/stocksig/io/quarter_price.py (Plan 01) / output 시트 writer 3종(Plan 02)
    - 09-PATTERNS.md §"history_render.py" (DB 게이트·합집합 reversed·.get 가드·D-03 정렬·파일명)
    - 09-RESEARCH.md Pattern 2/3 + Pitfall 1/2 (reversed·합집합·KeyError 가드)
  </read_first>
  <action>
    `src/stocksig/io/history_render.py`에 `run_history(tickers_path: str, output_dir: str) -> Path | None` 구현:
    (1) `from stocksig.io.fundamentals_store import count_rows` — `count_rows()==0`이면 한국어 안내(`"펀더멘털 DB가 비어 있습니다. 먼저 \`uv run python main.py\`를 실행해 분기 펀더멘털을 적재한 뒤 다시 \`history\`를 실행하세요."`) print 후 `return None`(예외 아님, D-15);
    (2) `read_tickers_extended` → US/KR `classify_market` 그룹화 후 각 그룹 내 `sorted(symbol)` → sorted_tickers(D-03);
    (3) `per_ticker = {t: compute_matrix(t) for t in sorted_tickers}`(외부 호출 0); `all_quarters = sorted({q for m in per_ticker.values() for cells in m.values() for q in cells})`; `display_quarters = list(reversed(all_quarters))`(최신 왼쪽 D-01, Pitfall 1);
    (4) 종목·분기별 가격 의존 4종: `qmap, current = quarter_end_prices(t)`, `price = current if q==display_quarters[0] else qmap.get(q)`, `cell = price_ratio(matrix[denom][q], price)` (PER→EPS_ttm/PBR→BPS/PCR→OCF_ps/PSR→SPS); PEG 분기별 3단 계약: `per = price_ratio(matrix["EPS_ttm"][q], price)`, `qp = _calendar_quarter_offset(q,-4)`, `eps_prior = matrix["EPS_ttm"][qp].value if qp in matrix["EPS_ttm"] else None`, `peg = compute_peg_cell(per.value, matrix["EPS_ttm"][q].value, eps_prior)` (D-10, KeyError `.get`/`in` 가드 Pitfall 2);
    (4b) **상대색 모집단·YoY 구성(WARNING-3 — 오케스트레이터 책임):** 지표·분기·산업별 peer 모집단과 4분기 전 셀을 여기서 계산해 Plan 02 writer에 주입한다. `peer_lookup(metric, quarter, industry) -> list[float]` = 같은 산업 종목들의 해당 (지표,분기) 유효값 리스트(`_is_missing` 제외); `prior_lookup(metric, ticker, quarter) -> MetricCell|None` = `matrix[metric].get(_calendar_quarter_offset(quarter,-4))`. 산업은 TickerSpec.industry(빈값 "" 허용 — relative_bucket이 무색 처리). writer는 이 lookup만 호출(모집단 자체 구성 안 함).
    (5) `make_history_workbook(out_dir / f"fundamentals_history_{date.today():%Y%m%d}.xlsx")`(D-14), REGISTRY 9 지표 시트(`write_metric_sheet(..., peer_lookup, prior_lookup)` 주입) + [원천](fetch_raw_quarters) + [최신 스냅샷] write(Plan 02 writer), `wb.close()`, path 반환.
    예외 격리: 종목별 try/except로 한 종목 실패가 전체 렌더를 막지 않음, `logger.warning("... 실패(%s)", type(exc).__name__)`(키 미노출). `main_run.run`·시트1 모듈 import/호출 금지(D-15 분리).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_history_render.py -k "render or peg or missing or layout" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `count_rows()==0` monkeypatch 시 run_history가 None 반환 + 안내 print(예외 미발생) — `test_db_empty_guard`
    - 정렬: US 종목군이 KR보다 먼저, 그룹 내 알파벳순 — `test_us_kr_alpha_order`
    - display_quarters[0] == 최신 분기키(엔진 오름차순 reversed 검증) — `test_matrix_layout_latest_left`
    - 분기별 PEG: 최신 분기 PEG = compute_peg_cell(현재가 PER, EPS_now, EPS_prior) 결과와 일치 — `test_peg_per_quarter`
    - 미보유 분기 셀 KeyError 없이 "-"(Pitfall 2 `.get` 가드)
    - grep `main_run` in history_render.py = 0; grep `compute_matrix`/`quarter_end_prices` ≥ 1
    - 네트워크 0: compute_matrix fetch_fn stub + quarter_end_prices/fetch_ohlcv_cached monkeypatch (Plan 01 fixture)
  </acceptance_criteria>
  <done>run_history가 DB→정렬→가격주입→분기별 PEG→매트릭스 재구성→9지표+원천+스냅샷 시트를 별도 파일로 렌더하고 main_run과 분리된다.</done>
</task>

<task type="auto">
  <name>Task 2: main.py — history 서브커맨드 배선(main_run.run 분리, 하위호환)</name>
  <read_first>
    - main.py L37-81 (기존 argparse 구조·늦은 import·종료코드·UTF-8 reconfigure)
    - 09-PATTERNS.md §"main.py" (서브커맨드 추가·하위호환·늦은 import)
    - 09-RESEARCH.md §"CLI 서브커맨드 배선" (add_subparsers 패턴)
  </read_first>
  <action>
    `main.py`에 `sub = parser.add_subparsers(dest="cmd")` 추가, `p_hist = sub.add_parser("history", help="펀더멘털 트렌드 엑셀 렌더 (DB → fundamentals_history_*.xlsx)")` + `p_hist.add_argument("--tickers", default="tickers.txt")` + `p_hist.add_argument("--output-dir", default="output")`. `args.cmd == "history"`이면 늦은 import `from stocksig.io.history_render import run_history` 후 `path = run_history(args.tickers, args.output_dir)`(None이면 안내 이미 출력됨 — 깔끔 종료 코드 0), else 기존 `from stocksig.main_run import run` 흐름 그대로(하위호환 — 서브커맨드 없으면 portfolio 흐름). 기존 --env/--summary-only 플래그·기본 동작·예외 처리·`완료: {path}` print 유지.
    **CLI 검증은 셸 체인 대신 pytest 케이스로(WARNING-4 이식성):** `tests/test_history_render.py`에 `test_history_cli_help`/`test_history_cli_dispatch` 추가 — `subprocess`로 `[sys.executable, "main.py", "history", "--help"]` 호출해 returncode 0·"펀더멘털 트렌드" 출력 단언하고, `history` 분기가 `run_history`를·기본 분기가 `main_run.run`을 호출함을 monkeypatch로 단언. `&&` 셸 체인 금지(Windows/PowerShell 이식성).
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_history_render.py -k cli -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python main.py history --help` 종료코드 0 + "펀더멘털 트렌드" 도움말 출력
    - `python main.py --help`(서브커맨드 없음)도 정상 — 기존 portfolio 흐름 하위호환(--tickers/--env/--output-dir/--summary-only 유지)
    - history 분기가 `run_history` 호출, 기본 분기가 `main_run.run` 호출(서로 다른 엔트리 — D-15 분리)
    - run_history None 반환 시(DB 미적재) 종료코드 0(예외 아님)
  </acceptance_criteria>
  <done>history 서브커맨드가 run_history를 main_run.run과 분리해 호출하고 기존 portfolio 흐름은 하위호환된다.</done>
</task>

<task type="auto">
  <name>Task 3: 통합 검증 — SC1(별도 파일·시트1 불변)·SC3(원천/스냅샷)·전 스위트 회귀 0</name>
  <read_first>
    - tests/test_sheet_portfolio.py L13-24 (openpyxl read-back·네트워크 0 정책)
    - tests/fixtures/history_fixtures.py (Plan 01 stub·OHLCV builder)
    - 09-VALIDATION.md Per-Task Verification Map (SC1~4 테스트명)
    - 09-RESEARCH.md §"워크북 셀 검증" (openpyxl load_workbook 단언 예시)
  </read_first>
  <action>
    `tests/test_history_render.py`에 통합 단언 추가(Plan 01 fixture 재사용, 네트워크 0):
    (1) `test_separate_file_sheet1_untouched` — run_history가 `fundamentals_history_YYYYMMDD.xlsx`를 생성(파일명 date 스탬프 D-14)하고, 같은 디렉터리의 시트1 `portfolio_*.xlsx`는 생성/변경하지 않음(트렌드 흐름이 시트1 모듈 미호출 — git diff sheet_portfolio.py 0 보강);
    (2) `test_raw_and_snapshot_sheets` — openpyxl로 [원천]·[최신 스냅샷] 시트 존재·내용(raw long 행 + 1행/종목 최신값) 단언(SC3/D-13);
    (3) `test_missing_dash_comment` — 결손 셀 value=="-" + `.comment.text` 사유(D-11);
    (4) `test_freeze` — 지표 시트 `freeze_panes=="B1"`(A열만 D-04).
    fetch_fn stub + `quarter_end_prices`/`fetch_ohlcv_cached`/`count_rows`/`fetch_raw_quarters`/`fetch_company_name` monkeypatch로 네트워크·DB 비결합. 마지막에 전 스위트 회귀 확인.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_history_render.py::test_separate_file_sheet1_untouched` GREEN — 트렌드 파일 생성, portfolio_*.xlsx 미생성·미변경
    - `test_raw_and_snapshot_sheets`·`test_missing_dash_comment`·`test_freeze` GREEN (SC3·D-11·D-04)
    - 전 스위트 `.venv/Scripts/python.exe -m pytest -q` GREEN — baseline 341 passed 유지 + Phase 9 신규(회귀 0)
    - git diff: `sheet_portfolio.py`·`color_rules.py`·`writer.py`·`main_run.py` 변경 0줄(Core Value·시트1 흐름 불변)
    - 통합 테스트 네트워크·실DB 호출 0(monkeypatch·fixture stub)
  </acceptance_criteria>
  <done>별도 파일 생성·시트1 불변·[원천]/[최신 스냅샷]·결손/freeze가 openpyxl로 검증되고 전 스위트 회귀 0이다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB(data/fundamentals.db) → run_history | 읽기 전용 — count_rows/fetch_raw_quarters/compute_matrix 기존 `?`-바인딩 함수만, 신규 SQL 0 |
| tickers.txt → run_history | 사용자 입력 파일 — 기존 검증된 read_tickers_extended 파싱 |
| CLI args(main.py) → run_history | argparse 표준 파싱, 경로 인자만(셀 값·시트명에 미진입) |
| 시트1 portfolio 흐름 ↔ 트렌드 흐름 | D-15 완전 분리 — main_run.run·시트1 모듈 미호출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | DB SELECT | mitigate | 신규 SQL 미작성 — store의 `?`-바인딩 함수만 호출(T-08-01 불변) |
| T-09-07 | Info Disclosure | 예외 로그 | mitigate | 종목별 except에서 `type(exc).__name__`만 로깅(API 키·예외 원문 미노출, main_run L348 패턴) |
| T-09-08 | Denial of Service | 종목 1개 실패가 전체 렌더 차단 | mitigate | 종목별 try/except 격리 — 한 종목 실패해도 나머지 렌더 진행 |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | 신규 패키지 0 (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` 전 스위트 GREEN — baseline 341 + Phase 9 신규(회귀 0)
- `python main.py history --help` 종료코드 0; `python main.py --help` 기존 흐름 하위호환
- git diff: 시트1 모듈(`sheet_portfolio.py`/`color_rules.py`/`writer.py`)·`main_run.py` 변경 0줄
- history_render.py에서 `main_run` import 0(D-15 분리), 신규 SQL·신규 산식 0
</verification>

<success_criteria>
- 사용자가 `uv run python main.py history`로 `fundamentals_history_YYYYMMDD.xlsx`를 별도 파일로 생성 (FUND-10 / SC1 / D-14/D-15)
- 9 지표 매트릭스(최신 왼쪽 D-01) + [원천] + [최신 스냅샷] (SC2/SC3/D-13)
- 가격 의존 4종 과거=분기말 종가·최신=현재가, 분기별 PEG (SC4/D-09/D-10)
- DB 미적재 시 한국어 안내 후 정상 종료 (D-15)
- 시트1 색 신호·portfolio 흐름 완전 불변 (Core Value) — 전 회귀 스위트 그린
</success_criteria>

<output>
Create `.planning/phases/09-trend-render/09-03-SUMMARY.md` when done
</output>
