---
phase: 10-1-store-registry
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/stocksig/io/metrics_engine.py
  - src/stocksig/io/history_render.py
  - src/stocksig/io/fundamentals_view.py
  - tests/test_fundamentals_view.py
  - tests/test_metrics_engine.py
autonomous: true
requirements: [FUND-11]
must_haves:
  truths:
    - "compute_matrix 최신열 + last_close 주입 → FundamentalsResult{per,peg,gpm,opm} 4셀이 값·source·note와 함께 변환된다(D-08, D-09)"
    - "단일 분기 가격 주입 코어(inject_prices_for_quarter)를 시트1·트렌드가 동일하게 호출해 드리프트가 구조적으로 차단된다(D-06)"
    - "어댑터 4셀 value가 sheet_snapshot 최신열 셀과 동일하다(드리프트 0, 동일 fixture·가격)"
    - "PEG 셀 source가 PER 셀 source를 승계한다(compute_peg_cell source=None 회귀 차단)"
    - "DB 빈 종목(latest_q=None) → 4셀 빈칸 + 한국어 사유, 색 신호 무영향(D-02, D-10)"
    - "기존 history_render 14개 통합 테스트가 _inject_prices 리팩터 후에도 무손상이다"
  artifacts:
    - path: "src/stocksig/io/fundamentals_view.py"
      provides: "matrix_to_fundamentals 어댑터 + provenance 라벨 합성"
      contains: "def matrix_to_fundamentals"
    - path: "src/stocksig/io/metrics_engine.py"
      provides: "inject_prices_for_quarter 단일분기 공유 코어"
      contains: "def inject_prices_for_quarter"
    - path: "tests/test_fundamentals_view.py"
      provides: "어댑터·드리프트·parity·provenance·빈DB 단위 테스트"
      exports: ["test_adapter_maps_latest_column", "test_sheet1_matches_snapshot", "test_peg_provenance_inherited", "test_missing_db_blank"]
  key_links:
    - from: "src/stocksig/io/history_render.py"
      to: "metrics_engine.inject_prices_for_quarter"
      via: "_inject_prices 분기 루프가 단일분기 코어 호출"
      pattern: "inject_prices_for_quarter"
    - from: "src/stocksig/io/fundamentals_view.py"
      to: "fundamentals.FundamentalsResult / MetricCell / _empty_cell"
      via: "import 재사용(신규 dataclass 0)"
      pattern: "from stocksig.io.fundamentals import"
---

<objective>
시트1 펀더멘털 단일 원천 이관의 **계산·변환 계층**을 만든다. 두 산출물:
1. `metrics_engine._inject_prices`(다분기)에서 **단일 분기 코어** `inject_prices_for_quarter`를 추출해 시트1·트렌드가 공유한다(D-06).
2. `compute_matrix` 최신열을 시트1 writer 입력 계약 `FundamentalsResult{per,peg,gpm,opm}`로 변환하는 **얇은 어댑터** `matrix_to_fundamentals`를 신설한다(D-08). 신규 산식 0 — 기존 `price_ratio`/`compute_peg_cell`/`MetricCell`/`FundamentalsResult`/`_empty_cell` 재사용.

Purpose: 시트1·트렌드 양쪽이 **같은 코드·같은 입력**(last_close·최신분기)을 쓰게 해 두 파일 값 드리프트를 구조적으로 차단한다(SC1). 어댑터가 `FundamentalsResult`를 정확히 공급해 `sheet_portfolio.py` 0줄 수정(Core Value 보호)을 가능하게 한다.

Output: `src/stocksig/io/fundamentals_view.py`(신규), `metrics_engine.py` 공유 코어, `history_render._inject_prices` 비파괴 리팩터, Wave 0 테스트 6종.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-1-store-registry/10-CONTEXT.md
@.planning/phases/10-1-store-registry/10-RESEARCH.md
@.planning/phases/10-1-store-registry/10-PATTERNS.md
@.planning/phases/10-1-store-registry/10-VALIDATION.md

<interfaces>
<!-- 어댑터·코어가 의존하는 기존 계약. import 재사용 — 신규 정의 0 (D-04). -->

From src/stocksig/io/fundamentals.py (보존 계약, L32-78):
```
@dataclass
class MetricCell: value: float | None; source: str | None; note: str | None
@dataclass
class FundamentalsResult: per: MetricCell; peg: MetricCell; gpm: MetricCell; opm: MetricCell
def _empty_cell(note: str | None = None) -> MetricCell  # value=None,source=None,note=note
def _is_missing(x: float | None) -> bool                # None or NaN
```

From src/stocksig/io/metrics_engine.py:
```
def compute_matrix(ticker, fetch_fn=...) -> dict[str, dict[str, MetricCell]]  # {metric: {quarter: cell}}, 분기 오름차순·최신=마지막열
def price_ratio(denom_cell, price) -> MetricCell        # 분모None/≤0·price결손=빈셀+사유
def compute_peg_cell(per_value, eps_ttm, eps_prior) -> MetricCell  # source=None 반환 (L282) — L5 LANDMINE
def _calendar_quarter_offset(q, n) -> str               # "2026Q2",-4 → "2025Q2"
```

From src/stocksig/io/history_render.py (추출 원본 L70-94):
```
_PRICE_DEPENDENT: dict[str,str]   # {PER:EPS_ttm, PBR:BPS, PCR:OCF_ps, PSR:SPS} — REGISTRY 도출
def _inject_prices(matrix, quarters, qmap, current, latest_q) -> None  # in-place, 분기 루프
```

From tests/fixtures/history_fixtures.py (네트워크 0 재사용):
```
fetch_fn_stub        # compute_matrix(ticker, fetch_fn=fetch_fn_stub): AAPL(EDGAR/tech)·005930.KS(DART/semis) 5분기 7-tuple
build_ohlcv          # fetch_ohlcv_cached monkeypatch용 합성 OHLCV
TICKER_INDUSTRY
```

From src/stocksig/output/sheet_snapshot.py (드리프트 비교 기준점, L57-66):
```
write_snapshot_sheet(ws, snapshot_rows, formats)  # 종목 1행 × 9지표 = 매트릭스 최신열 셀 재사용
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inject_prices_for_quarter 단일분기 코어 추출 + history_render 비파괴 재배선</name>
  <files>src/stocksig/io/metrics_engine.py, src/stocksig/io/history_render.py, tests/test_metrics_engine.py</files>
  <read_first>
    - src/stocksig/io/history_render.py (L38, L70-94 `_inject_prices` 추출 원본·`_PRICE_DEPENDENT`)
    - src/stocksig/io/metrics_engine.py (price_ratio·compute_peg_cell·_calendar_quarter_offset 시그니처)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§공유 헬퍼 inject_prices_for_quarter, 추출 후 시그니처 L100-115)
    - tests/test_history_render.py (기존 _inject_prices 통합 동작 — 무손상 확인)
  </read_first>
  <behavior>
    - Test (test_metrics_engine.py::test_inject_prices_for_quarter): 단일 분기 q·가격 주입 시 가격 의존 4종(PER/PBR/PCR/PSR)이 price_ratio 결과로, PEG가 compute_peg_cell(PER.value, eps_now, eps_prior=4분기전) 결과로 matrix에 in-place 채워진다.
    - Test: price=None(가격 결손) → 4종 모두 빈 셀+사유, PEG도 None value.
    - Regression: 기존 history_render _inject_prices 호출 경로(run_history 14 통합 테스트) value 동일 — 리팩터 전후 출력 불변.
  </behavior>
  <action>
    `metrics_engine.py`에 단일 분기 공유 코어 `inject_prices_for_quarter(matrix: dict, q: str, price: float | None, eps_map: dict) -> None`(in-place)를 추가한다. 본문은 PATTERNS.md L100-115 그대로: `_PRICE_DEPENDENT` 각 (metric, denom)에 `matrix.setdefault(metric, {})[q] = price_ratio(matrix.get(denom, {}).get(q), price)` 주입 후, PEG 3단 — `per = matrix.get("PER", {}).get(q)`·`eps_now = eps_map.get(q)`·`eps_prior = eps_map.get(_calendar_quarter_offset(q, -4))` → `matrix.setdefault("PEG", {})[q] = compute_peg_cell(per_value, eps_now_v, eps_prior_v)`(각 셀 None 가드는 `.value if ... is not None else None`). `_PRICE_DEPENDENT`는 현재 `history_render.py:38`에 있으므로 metrics_engine로 옮기거나 metrics_engine에서 REGISTRY로 재도출해 history_render가 import하도록 한다(순환 import 주의 — REGISTRY는 metrics_registry 모듈). 그 뒤 `history_render._inject_prices`(L70-94)의 시그니처·외부 동작(matrix in-place, quarters/qmap/current/latest_q)을 **유지**하되 분기 루프 본문이 `price = current if (latest_q is not None and q == latest_q) else qmap.get(q)` 계산 후 `inject_prices_for_quarter(matrix, q, price, eps_map)`만 호출하도록 비파괴 치환한다(D-06 추출, PATTERNS L117 비파괴 계약). 신규 산식·신규 dataclass 0.
  </action>
  <verify>
    <automated>uv run pytest tests/test_metrics_engine.py::test_inject_prices_for_quarter tests/test_history_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def inject_prices_for_quarter" src/stocksig/io/metrics_engine.py` 1건.
    - `history_render._inject_prices` 본문이 `inject_prices_for_quarter` 를 호출(`grep -n "inject_prices_for_quarter" src/stocksig/io/history_render.py` ≥1건), 자체 price_ratio/compute_peg_cell 직접 호출 중복 없음.
    - `uv run pytest tests/test_history_render.py -q` 회귀 0(기존 통과 수 유지).
    - 신규 `test_inject_prices_for_quarter` GREEN.
  </acceptance_criteria>
  <done>단일분기 코어가 metrics_engine에 존재하고, history_render 트렌드 경로가 그 코어를 호출하며 기존 통합 테스트가 무손상.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: matrix_to_fundamentals 어댑터 + provenance 라벨 합성 (fundamentals_view.py 신규)</name>
  <files>src/stocksig/io/fundamentals_view.py, tests/test_fundamentals_view.py</files>
  <read_first>
    - src/stocksig/io/fundamentals.py (L32-78 MetricCell/FundamentalsResult/_empty_cell/_is_missing — import 재사용)
    - src/stocksig/io/metrics_engine.py (Task 1 산출 inject_prices_for_quarter + compute_matrix 반환 구조)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§어댑터 필드 매핑 L53-66, §provenance 라벨 L62)
    - .planning/phases/10-1-store-registry/10-RESEARCH.md (Q3 어댑터 매핑·Q5 provenance·L1 호출순서·L5 PEG source·L8 빈DB)
    - tests/fixtures/history_fixtures.py (fetch_fn_stub·build_ohlcv — 테스트 입력)
    - src/stocksig/output/sheet_snapshot.py (L57-66 최신열 셀 — 드리프트 비교 기준점)
  </read_first>
  <behavior>
    - Test (test_fundamentals_view.py::test_adapter_maps_latest_column): compute_matrix(AAPL,fetch_fn_stub) → inject_prices_for_quarter(latest_q, last_close) → matrix_to_fundamentals(matrix, latest_q) 가 per=matrix["PER"][latest_q]·peg=matrix["PEG"][latest_q]·gpm=matrix["GPM"][latest_q]·opm=matrix["OPM"][latest_q] value를 정확히 매핑.
    - Test (test_sheet1_matches_snapshot): 동일 fixture·동일 last_close·동일 latest_q → 어댑터 4셀 value == sheet_snapshot 최신열 동일 지표 셀 value (드리프트 0, SC1).
    - Test (test_peg_provenance_inherited): PEG.source == PER.source (compute_peg_cell source=None 승계, L5).
    - Test (test_missing_db_blank): latest_q=None 또는 빈 matrix → 4셀 모두 value=None·note="조회 실패: DB 분기 데이터 없음" (D-02/L8).
    - Test (test_price_source_parity): runner last_close 경로(df.iloc[-1].get("Close"))와 quarter_price.current(Close.dropna().iloc[-1])가 동일 OHLCV fixture에서 같은 float (L4 가드).
  </behavior>
  <action>
    신규 모듈 `src/stocksig/io/fundamentals_view.py`를 만든다. `from stocksig.io.fundamentals import FundamentalsResult, MetricCell, _empty_cell, _is_missing`로 계약 재사용(신규 dataclass 0, D-04). `matrix_to_fundamentals(matrix: dict, latest_q: str | None) -> FundamentalsResult`: 내부 `cell_or_empty(metric)` = `matrix.get(metric, {}).get(latest_q) if latest_q else None`, None이면 `_empty_cell("조회 실패: DB 분기 데이터 없음")`(D-02). per=PER·peg=PEG·gpm=GPM·opm=OPM 매핑(RESEARCH Q3 표). provenance 라벨 합성 `_provenance_note(cell, latest_q)` = `f"{cell.source} · {latest_q}"`(값 있는 셀, 예 "EDGAR · 2026Q2"/"DART+yf · 2026Q2"), source 없으면 `cell.note` 그대로(결손 사유 보존, D-09/D-10). 값 있는 셀의 note를 이 합성값으로 덮어쓴다. **L5 LANDMINE**: PEG 셀은 `compute_peg_cell` 결과라 source=None — 어댑터에서 PEG.source가 None이고 value가 있으면 `peg.source = per.source` 승계 후 라벨 합성(metrics_engine.py:283 docstring 패턴). **L1 호출순서**: 이 어댑터는 가격 주입을 하지 않는다 — 호출자가 반드시 `compute_matrix → inject_prices_for_quarter(latest_q, last_close) → matrix_to_fundamentals` 순으로 호출해야 PER/PEG가 채워짐을 모듈 docstring에 명시. MetricCell은 fundamentals 재사용이므로 타입 변환·복사 없이 그대로 FundamentalsResult에 넣는다. `tests/test_fundamentals_view.py`를 신설해 behavior 5종을 fetch_fn_stub·build_ohlcv·conftest 격리 fixture로 작성(네트워크 0). last_close는 build_ohlcv 마지막 Close로 고정, snapshot 비교는 sheet_snapshot이 쓰는 동일 매트릭스 최신열 셀과 대조.
  </action>
  <verify>
    <automated>uv run pytest tests/test_fundamentals_view.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def matrix_to_fundamentals" src/stocksig/io/fundamentals_view.py` 1건.
    - `grep -v '^#' src/stocksig/io/fundamentals_view.py | grep -c "class MetricCell\|class FundamentalsResult\|def _compute_"` → 0 (신규 dataclass·산식 정의 없음, D-04 import 재사용).
    - test_adapter_maps_latest_column·test_sheet1_matches_snapshot·test_peg_provenance_inherited·test_missing_db_blank·test_price_source_parity 5종 GREEN.
    - `uv run pytest tests/test_fundamentals_view.py -q` 5 passed.
  </acceptance_criteria>
  <done>어댑터가 매트릭스 최신열을 FundamentalsResult로 무변환 매핑하고, PEG source 승계·빈DB 빈칸·드리프트 0·가격 parity가 단위 테스트로 단언됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store(SQLite) → 계산/어댑터 | DB raw → MetricCell. fetch_raw_quarters `?`-바인딩(Phase 7 확립), 신규 SQL 0 |
| 어댑터 note 합성 → 시트1 호버 주석 | source·분기 라벨이 사용자 가시 텍스트로 출력 — API 키 미포함 보장 필요 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | fundamentals_view `_provenance_note` 라벨 합성 | mitigate | note = `cell.source`("EDGAR"/"DART"/"DART+yf")·`latest_q`("2026Q2")만 보간 — EDGAR UA·OPENDART_API_KEY 미참조. source는 metrics_engine `_merge_provenance` 산출 상수 라벨뿐(원문 URL·키 비포함). CR-01 보존 |
| T-10-02 | Tampering | inject_prices_for_quarter in-place 매트릭스 변형 | accept | 순수 in-place 변환·외부 입력 경계 없음. 입력은 store 계산 매트릭스(신뢰)·last_close(로컬 OHLCV) |
| T-10-03 | Information Disclosure | 어댑터/코어 예외 로깅 | mitigate | Plan 02/03 호출자 종목별 try/except가 `type(exc).__name__`만 로깅(공유 패턴) — 본 plan 코어는 예외 메시지에 키/원문 보간 금지 |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 패키지 0(순수 내부 이관) — 설치 task 없음. slopcheck 대상 없음 |
</threat_model>

<verification>
- `uv run pytest tests/test_metrics_engine.py tests/test_history_render.py tests/test_fundamentals_view.py -x -q` — 어댑터·코어·엔진·트렌드 GREEN (회귀 0).
- `git diff src/stocksig/output/sheet_portfolio.py` — 빈 출력(이 plan은 writer 미접촉, Core Value 보호).
- `grep -n "fetch_edgar_quarterly_raw\|fetch_dart_quarterly_raw" src/stocksig/io/*.py` — store 경로 추출기 존재 확인(L2 오삭제 방지 — 이 plan은 제거 안 함).
</verification>

<success_criteria>
- inject_prices_for_quarter 단일분기 코어가 metrics_engine에 존재하고 history_render가 호출(드리프트 구조적 차단 D-06).
- matrix_to_fundamentals 어댑터가 fundamentals_view.py에 존재, MetricCell/FundamentalsResult 무변환 매핑.
- 드리프트 0(어댑터==스냅샷)·PEG provenance 승계·빈DB 빈칸·가격 parity 단위 테스트 GREEN.
- 신규 dataclass·산식·외부 호출 0(D-04 import 재사용).
</success_criteria>

<output>
Create `.planning/phases/10-1-store-registry/10-01-SUMMARY.md` when done
</output>
