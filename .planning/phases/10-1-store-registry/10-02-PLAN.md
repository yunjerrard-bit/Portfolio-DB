---
phase: 10-1-store-registry
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/stocksig/main_run.py
  - tests/test_history_integration.py
autonomous: true
requirements: [FUND-11]
must_haves:
  truths:
    - "run 흐름이 PASS1(시세·기업명 fan-out) → SYNC(sync_ticker_history DB 적재) → READ(compute_matrix+가격주입+어댑터) → PASS2(시트1 write) 순서로 정렬된다(D-01)"
    - "_fundamentals_with_auth 클로저·fetch_fundamentals 호출이 store/registry 읽기로 대체된다(D-03)"
    - "시트1 경로에서 fetch_fundamentals/fetch_edgar_cached/fetch_dart_cached 외부 호출이 0이다(단일 원천)"
    - "READ 단계가 res.enriched_df.iloc[-1].get('Close') 같은 last_close 경로로 가격을 주입한다(D-07, L4 동일성)"
    - "인증 실패 소스는 SYNC 루프에서만 skip하고 store 읽기(compute_matrix)에는 skip을 적용하지 않는다(L7)"
  artifacts:
    - path: "src/stocksig/main_run.py"
      provides: "sync→read→write 재배선 run 흐름"
      contains: "matrix_to_fundamentals"
    - path: "tests/test_history_integration.py"
      provides: "single_source·no_legacy_fetch 호출 카운트 0 단언"
      contains: "no_legacy_fetch"
  key_links:
    - from: "src/stocksig/main_run.py"
      to: "fundamentals_view.matrix_to_fundamentals + metrics_engine.compute_matrix/inject_prices_for_quarter"
      via: "READ 단계 종목별 어댑터 호출 → res.fundamentals 재할당"
      pattern: "matrix_to_fundamentals"
    - from: "src/stocksig/main_run.py SYNC 루프"
      to: "fundamentals_delta.sync_ticker_history"
      via: "PASS2 write 앞으로 이동"
      pattern: "sync_ticker_history"
---

<objective>
`main_run.run`의 펀더멘털 산출 경로를 구 fetch에서 store/registry 읽기로 **재배선**한다(D-01/D-03). 현재 흐름(PASS1 fetch_fundamentals → PASS2 write → SYNC)을 **PASS1(시세·기업명만) → SYNC(DB 적재) → READ(compute_matrix → inject_prices_for_quarter → matrix_to_fundamentals) → PASS2(시트1 write)** 순으로 뒤집는다. `_fundamentals_with_auth` 클로저·`fetch_fundamentals` 호출을 제거하고, READ 단계가 어댑터로 `res.fundamentals`를 재할당해 `sheet_portfolio.py`가 무변경 소비하게 한다.

Purpose: 시트1이 sync로 적재된 store를 읽어 단일 원천화 → 트렌드 엑셀과 값 일치(SC1)·평소 외부 펀더멘털 호출 ≈0(FUND-11). 시트1 writer는 0줄 수정(Core Value 보호).

Output: 재배선된 `main_run.run`, `tests/test_history_integration.py` 단일원천·호출 0 통합 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-1-store-registry/10-CONTEXT.md
@.planning/phases/10-1-store-registry/10-RESEARCH.md
@.planning/phases/10-1-store-registry/10-PATTERNS.md
@.planning/phases/10-1-store-registry/10-VALIDATION.md
@.planning/phases/10-1-store-registry/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01 산출 + 기존 run 흐름 계약. -->

From src/stocksig/io/fundamentals_view.py (Plan 01 산출):
```
def matrix_to_fundamentals(matrix: dict, latest_q: str | None) -> FundamentalsResult
```

From src/stocksig/io/metrics_engine.py:
```
def compute_matrix(ticker, fetch_fn=...) -> dict[str, dict[str, MetricCell]]  # 외부 호출 0 (SQLite SELECT)
def inject_prices_for_quarter(matrix, q, price, eps_map) -> None              # Plan 01 산출, in-place
```

From src/stocksig/io/fundamentals_delta.py:
```
def sync_ticker_history(symbol, source) -> ...   # 접수번호 델타 DB 적재 (Phase 7)
```

From src/stocksig/runner.py:
```
@dataclass
class TickerResult: spec; enriched_df; fundamentals; ...   # frozen 아님 → res.fundamentals 재할당 가능 (L34)
# run_all(specs, classify_market, pipeline, fundamentals_fn=None, company_name_fn=...) — fundamentals_fn=None 시 fundamentals=None (L147 하위호환)
# last_close = df.iloc[-1].get("Close") (worker 내부 L100) → READ 단계는 res.enriched_df.iloc[-1].get("Close")
```

현재 main_run.run 구조(변경 지점):
- L272-279 `_fundamentals_with_auth` 클로저 → **제거**
- L283-289 `run_all(..., fundamentals_fn=_fundamentals_with_auth, ...)` → `fundamentals_fn=None`
- L334-353 SYNC 루프(sync_ticker_history, 인증 skip 포함) → **PASS2(L299 write_portfolio_sheet) 앞으로 이동**
- PASS2 직전 신규 READ 단계 삽입
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run 흐름 재배선 — PASS1 → SYNC → READ(어댑터) → PASS2</name>
  <files>src/stocksig/main_run.py</files>
  <read_first>
    - src/stocksig/main_run.py (L260-380 run 흐름 전체 — 클로저·run_all·PASS2·SYNC 루프·요약)
    - src/stocksig/runner.py (L30-165 TickerResult·run_all·fundamentals_fn=None 하위호환·last_close 경로 L100)
    - src/stocksig/io/fundamentals_view.py (Plan 01 matrix_to_fundamentals)
    - src/stocksig/io/metrics_engine.py (compute_matrix·inject_prices_for_quarter)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§main_run.py 재배선 권고 순서 L169-185·재할당 L181·L7 인증결합 금지)
    - .planning/phases/10-1-store-registry/10-RESEARCH.md (Q4 변경 지점 표·주의사항·L7)
  </read_first>
  <action>
    `main_run.run`을 RESEARCH Q4 / PATTERNS L169-185 권고 순서로 재배선한다. (1) `_fundamentals_with_auth` 클로저(L272-279) **제거**. (2) `run_all(...)` 호출(L283-289)의 `fundamentals_fn=_fundamentals_with_auth` → `fundamentals_fn=None`(runner.py:147 하위호환 — fan-out은 시세·기업명만, last_close 확보). (3) SYNC 루프(L334-353 `for s in specs: ... sync_ticker_history(s.symbol, source)`, 인증 실패 소스 skip·종목별 try/except·`type(exc).__name__`만 로깅)를 **PASS2 write_portfolio_sheet(L299-318) 앞으로 이동**(D-01). (4) SYNC 직후·PASS2 직전에 **신규 READ 단계** 삽입: `by_symbol = {r.spec.symbol: r for r in results}`로 results 순회하며 종목별로 `matrix = compute_matrix(res.spec.symbol)`(외부 호출 0) → `quarters = sorted(set(q for m in matrix.values() for q in m))`로 `latest_q = quarters[-1] if quarters else None` → `last_close = res.enriched_df.iloc[-1].get("Close")`(L4 동일성 — runner.py:100과 같은 경로) → `latest_q`가 있으면 `inject_prices_for_quarter(matrix, latest_q, last_close, matrix.get("EPS_ttm", {}))`(L1 호출순서) → `res.fundamentals = matrix_to_fundamentals(matrix, latest_q)`(TickerResult 재할당, Open Q1 권고). 종목별 try/except로 `type(exc).__name__`만 로깅(펀더멘털 결손 ≠ 티커 실패, CR-01). **L7 금지**: READ 단계의 compute_matrix에 `skip_edgar`/`skip_dart` 적용하지 말 것(store 읽기는 인증 무관) — 인증 skip은 SYNC 루프에만 유지. PASS2 `write_portfolio_sheet`는 무변경. 요약 캐시 줄(L370 "펀더멘털 HIT/MISS")은 이 plan에서 건드리지 않는다(Plan 03 캐시 제거와 함께 정리 — 지금 제거하면 stats KeyError 위험).
  </action>
  <verify>
    <automated>uv run pytest tests/test_history_integration.py tests/test_metrics_engine.py tests/test_fundamentals_view.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_fundamentals_with_auth" src/stocksig/main_run.py` → 0 (클로저 제거).
    - `grep -n "matrix_to_fundamentals\|inject_prices_for_quarter\|compute_matrix" src/stocksig/main_run.py` 각 ≥1건 (READ 단계 배선).
    - main_run에서 SYNC 루프(`sync_ticker_history`)가 `write_portfolio_sheet` **호출 라인보다 앞 줄번호**에 위치(소스 순서 단언).
    - `grep -n "skip_edgar\|skip_dart" src/stocksig/main_run.py` 결과가 SYNC/ping 영역에만 존재, READ/compute_matrix 인접에는 없음(L7).
    - `uv run pytest tests/test_history_integration.py -q` 회귀 0.
  </acceptance_criteria>
  <done>run이 sync→read→write 순으로 동작하고 시트1 펀더멘털이 어댑터 경유 store 값으로 채워진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 단일원천·외부호출 0 통합 테스트 (single_source / no_legacy_fetch)</name>
  <files>tests/test_history_integration.py</files>
  <read_first>
    - tests/test_history_integration.py (기존 mocker.spy·_setup_mock_yfinance·_disable_history 패턴 L51-188)
    - tests/fixtures/history_fixtures.py (fetch_fn_stub·build_ohlcv·TICKER_INDUSTRY)
    - src/stocksig/main_run.py (Task 1 재배선 결과)
    - src/stocksig/io/fundamentals.py (fetch_fundamentals — spy 대상)
    - src/stocksig/io/edgar_client.py / dart_client.py (fetch_edgar_cached / fetch_dart_cached — spy 대상)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§test_history_integration 확장 L255-274)
  </read_first>
  <behavior>
    - Test (no_legacy_fetch): run() 1회 실행 시 `mocker.spy(fundamentals, "fetch_fundamentals")`·`mocker.spy(edgar_client, "fetch_edgar_cached")`·`mocker.spy(dart_client, "fetch_dart_cached")` 모두 `call_count == 0` (시트1 경로 구 fetch 미호출).
    - Test (single_source): run() 흐름이 sync_ticker_history(DB 적재) 후 시트1 펀더멘털을 채운다 — DB에 적재된 종목은 4셀 value 존재, 외부 펀더멘털 fetch 호출 0(compute_matrix는 SQLite SELECT만).
    - 네트워크 0: yfinance Ticker mock·fetch_fn_stub·conftest 격리 DB/캐시 fixture 사용, probe는 _disable_history 패턴으로 차단.
  </behavior>
  <action>
    `tests/test_history_integration.py`에 `no_legacy_fetch`·`single_source` 테스트를 추가한다. 기존 `_setup_mock_yfinance`/`_disable_history`/`mocker.spy` 패턴(L51-188)을 차용. `no_legacy_fetch`: run 진입 전 `mocker.spy`로 `stocksig.io.fundamentals.fetch_fundamentals`·`stocksig.io.edgar_client.fetch_edgar_cached`·`stocksig.io.dart_client.fetch_dart_cached`를 감싸고, run 1회 후 세 spy `call_count == 0` 단언. `single_source`: conftest `_isolated_fundamentals_db`에 fixture raw를 사전 적재(또는 sync stub로 적재)한 뒤 run 실행, 시트1 산출 결과(write_portfolio_sheet에 전달된 results의 `res.fundamentals`)가 store 매트릭스 최신열에서 온 값임을 단언 — 외부 펀더멘털 fetch 호출 0. compute_matrix는 fetch_fn 주입 또는 격리 DB로 SQLite만 읽도록 한다. 테스트 OHLCV는 build_ohlcv monkeypatch로 네트워크 0.
  </action>
  <verify>
    <automated>uv run pytest tests/test_history_integration.py -k "no_legacy_fetch or single_source" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "no_legacy_fetch\|single_source" tests/test_history_integration.py` ≥2 (두 테스트 존재).
    - test_..._no_legacy_fetch: 세 spy `call_count == 0` 단언 GREEN.
    - test_..._single_source: store 경유 4셀 값·외부 호출 0 단언 GREEN.
    - `uv run pytest tests/test_history_integration.py -q` 전체 GREEN(기존 14 + 신규, 회귀 0).
  </acceptance_criteria>
  <done>시트1 경로의 구 fetch 미호출(0)과 store 단일원천 읽기가 통합 테스트로 단언됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run 오케스트레이션 → 외부 API | SYNC 루프만 외부(EDGAR/DART) 접촉. READ(compute_matrix)는 store SQLite만 — 외부 경계 없음 |
| 종목별 예외 → 로그 | sync·READ 예외가 로그로 — API 키·예외 원문 미보간 필요(CR-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | SYNC·READ 종목별 try/except 로깅 | mitigate | `logger.warning("%s | ... %s", sym, type(exc).__name__)` — 예외 원문·URL·OPENDART_API_KEY·EDGAR UA 미보간(기존 main_run.py:348-353 패턴 유지). CR-01·T-04-03 준수 |
| T-10-05 | Tampering | run_all fundamentals_fn=None 전환 | accept | runner.py:147 하위호환 — fundamentals=None 안전 경로. READ 단계가 재할당으로 채움 |
| T-10-06 | Information Disclosure | 인증 결합 잔재(skip_edgar/dart를 store 읽기에 적용) | mitigate | L7 — store 읽기는 인증 무관, skip은 SYNC 루프에만. acceptance_criteria grep 위치 단언으로 강제 |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 패키지 0 — 설치 task 없음 |
</threat_model>

<verification>
- `uv run pytest tests/test_history_integration.py tests/test_metrics_engine.py tests/test_fundamentals_view.py -x -q` — 재배선·단일원천 GREEN.
- `git diff src/stocksig/output/sheet_portfolio.py` — 빈 출력(writer 무변경, Core Value 보호).
- `grep -n "fetch_edgar_quarterly_raw\|fetch_dart_quarterly_raw" src/stocksig/io/*.py` — store 추출기 존재(L2 오삭제 방지 — 이 plan 미제거).
</verification>

<success_criteria>
- run 흐름 sync→read→write 정렬, `_fundamentals_with_auth`·`fetch_fundamentals` 시트1 경로 제거(D-01/D-03).
- 시트1 경로 구 fetch 호출 0·store 단일원천 읽기 통합 테스트 GREEN.
- 인증 skip은 SYNC 루프에만(L7), 시트1 writer 무변경.
</success_criteria>

<output>
Create `.planning/phases/10-1-store-registry/10-02-SUMMARY.md` when done
</output>
