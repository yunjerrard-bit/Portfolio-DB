---
phase: 10-1-store-registry
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/stocksig/io/fundamentals.py
  - src/stocksig/io/edgar_client.py
  - src/stocksig/io/dart_client.py
  - src/stocksig/io/cache.py
  - src/stocksig/main_run.py
  - tests/test_sheet_portfolio.py
autonomous: true
requirements: [FUND-11]
must_haves:
  truths:
    - "구 펀더멘털 fetch 경로(fetch_fundamentals/_fill_us/_fill_kr/_default_edgar/_default_dart + fetch_edgar_cached/fetch_dart_cached)가 제거된다(D-03)"
    - ".cache/fundamentals 캐시 헬퍼(_FUND_DIR/get_fund/put_fund/make_fund_key/_fund_cache)가 제거되고 OHLCV·기업명 캐시는 무손상이다(D-05/L3)"
    - "보존 계약(MetricCell/FundamentalsResult/_is_missing/_compute_per/_compute_peg/_compute_margin)과 store 추출기(fetch_edgar_quarterly_raw/fetch_dart_quarterly_raw)는 제거되지 않는다(D-04/L2)"
    - "요약 '펀더멘털 HIT/MISS' 줄과 fund_hit/fund_miss stats 참조가 함께 정리되어 KeyError 회귀가 없다(L6)"
    - "시트1 σ-bucket 색 신호·하이퍼링크·freeze·기업명 열·실패행 마커가 회귀 없이 동작한다(Core Value)"
  artifacts:
    - path: "src/stocksig/io/fundamentals.py"
      provides: "구 fetch 경로 제거·보존 계약 유지"
      contains: "_compute_peg"
    - path: "tests/test_sheet_portfolio.py"
      provides: "σ-bucket 색 신호 불변 + D-02 빈DB 빈칸 회귀 테스트"
      contains: "color"
  key_links:
    - from: "src/stocksig/io/cache.py"
      to: "OHLCV 캐시 (_get_cache/get_ohlcv/_DEFAULT_DIR)"
      via: "펀더멘털 헬퍼만 제거·OHLCV 미접촉"
      pattern: "_DEFAULT_DIR"
    - from: "src/stocksig/output/sheet_portfolio.py"
      to: "σ-bucket 색 서식"
      via: "0줄 diff (writer 무변경)"
      pattern: "decide_sigma"
---

<objective>
구 펀더멘털 경로(Plan 02에서 호출자가 사라진 죽은 코드)를 안전하게 제거하고(D-03/D-05), 시트1 색 신호 회귀 0을 검증한다. 제거 대상: `fundamentals.py`의 `fetch_fundamentals`/`_fill_us`/`_fill_kr`/`_default_edgar`/`_default_dart`, `edgar_client.fetch_edgar_cached`/`dart_client.fetch_dart_cached`, `cache.py`의 `.cache/fundamentals` 헬퍼군, `main_run` 요약 "펀더멘털 HIT/MISS" 줄. 보존: 공유 계약(D-04)·store per-quarter 추출기(L2)·OHLCV/기업명 캐시(L3).

Purpose: 중복 fetch·계산·7일 캐시 경로 제거로 단일 원천 완성(FUND-11 SC2). OHLCV 캐시·Core Value 색 신호 무손상 보증.

Output: 정리된 4개 io/run 모듈, `tests/test_sheet_portfolio.py` 색 회귀·D-02 빈칸 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-1-store-registry/10-CONTEXT.md
@.planning/phases/10-1-store-registry/10-RESEARCH.md
@.planning/phases/10-1-store-registry/10-PATTERNS.md
@.planning/phases/10-1-store-registry/10-VALIDATION.md
@.planning/phases/10-1-store-registry/10-02-SUMMARY.md

<interfaces>
<!-- 제거 대상 vs 보존 대상 정확 분리 (D-03/D-04/L2/L3). -->

제거 대상 (Plan 02 후 호출자 0인 죽은 코드):
```
fundamentals.py:  fetch_fundamentals (L296), _fill_us (L123), _fill_kr (L192),
                  _default_edgar (L347), _default_dart (L371)
edgar_client.py:  fetch_edgar_cached (L256)
dart_client.py:   fetch_dart_cached (L299)
cache.py (L106-150): _FUND_DIR, _FUND_TTL_SECONDS, _fund_cache, _get_fund_cache,
                     make_fund_key, get_fund, put_fund  + _stats fund_hit/fund_miss 키
main_run.py: 요약 줄 "... 펀더멘털 HIT %d/MISS %d ..." (L370) + stats["fund_hit"]/["fund_miss"] 참조
```

절대 제거 금지 (D-04 / L2 / L3):
```
fundamentals.py:  MetricCell, FundamentalsResult, _empty_cell, _is_missing (WR-01),
                  _compute_per, _compute_peg, _compute_margin (metrics_engine import 재사용)
edgar_client.py:  fetch_edgar_quarterly_raw (store 경로 — L2)
dart_client.py:   fetch_dart_quarterly_raw (store 경로 — L2)
cache.py:         _DEFAULT_DIR(.cache/ohlcv), _get_cache, get_ohlcv, put_ohlcv, make_key,
                  _cache_lock(공유 lock), _NAME_DIR/get_company_name/put_company_name(기업명, L3)
fundamentals_store.py: fetch_raw_quarters, count_rows, get_delta_stats (store 읽기)
```

From src/stocksig/io/cache.py (get_cache_stats — fund 키 제거 후 회귀 방지):
```
def get_cache_stats() -> dict   # ohlcv_hit/ohlcv_miss/name_hit/name_miss 유지, fund_hit/fund_miss 제거
```

From src/stocksig/output/sheet_portfolio.py (무변경 목표, 색 신호 = 펀더멘털과 무관 L121):
```
_write_fund_cell(...)  # 펀더멘털 4셀 = (SigmaBucket.DEFAULT, num_fmt) — 색 없음
# decide_sigma_bucket 등 σ-bucket 색 로직 (L174-203) — 펀더멘털 4셀과 완전 무관
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 구 fetch 경로 + .cache/fundamentals 헬퍼 제거 + 요약 줄 정리</name>
  <files>src/stocksig/io/fundamentals.py, src/stocksig/io/edgar_client.py, src/stocksig/io/dart_client.py, src/stocksig/io/cache.py, src/stocksig/main_run.py</files>
  <read_first>
    - src/stocksig/io/fundamentals.py (전체 — 제거 대상 L123/192/296/347/371 vs 보존 L32-78·_compute_* 정확 구분)
    - src/stocksig/io/edgar_client.py (L256 fetch_edgar_cached 제거 vs fetch_edgar_quarterly_raw 보존 — L2)
    - src/stocksig/io/dart_client.py (L299 fetch_dart_cached 제거 vs fetch_dart_quarterly_raw 보존 — L2)
    - src/stocksig/io/cache.py (L23/65/81 OHLCV 보존 vs L106-150 펀더멘털 제거 — L3, get_cache_stats fund 키)
    - src/stocksig/main_run.py (L357-377 요약 줄·stats 참조 — L6)
    - .planning/phases/10-1-store-registry/10-RESEARCH.md (Q1 호출사슬·정확 제거 범위·건드리면 안 되는 것)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§캐시 분리 L311-313·절대 제거 금지 L321-323)
  </read_first>
  <action>
    Plan 02에서 호출자가 사라진 죽은 코드를 제거한다(D-03/D-05). `fundamentals.py`: `fetch_fundamentals`(L296)·`_fill_us`(L123)·`_fill_kr`(L192)·`_default_edgar`(L347)·`_default_dart`(L371) 및 이들 전용 import/헬퍼 제거. **보존**: `MetricCell`/`FundamentalsResult`/`_empty_cell`/`_is_missing`/`_compute_per`/`_compute_peg`/`_compute_margin`(D-04 — metrics_engine가 import 재사용). `edgar_client.py`: `fetch_edgar_cached`(L256) 제거, **`fetch_edgar_quarterly_raw` 절대 보존**(L2 — 함수명 정확 구분). `dart_client.py`: `fetch_dart_cached`(L299) 제거, **`fetch_dart_quarterly_raw` 절대 보존**(L2). `cache.py`: `_FUND_DIR`/`_FUND_TTL_SECONDS`/`_fund_cache`/`_get_fund_cache`/`make_fund_key`/`get_fund`/`put_fund`(L106-150) 제거, `_stats`에서 `fund_hit`/`fund_miss` 키 제거, `get_cache_stats` 반환 dict에서 `fund_hit`/`fund_miss` 제거. **OHLCV(`_DEFAULT_DIR`/`_get_cache`/`get_ohlcv`/`put_ohlcv`/`make_key`)·기업명(`_NAME_DIR`/`get_company_name`/`put_company_name`)·공유 `_cache_lock` 절대 미접촉**(L3). `main_run.py`: 요약 줄(L369-377)의 `· 펀더멘털 HIT %d/MISS %d` 토막과 `stats["fund_hit"]`/`stats["fund_miss"]` 인자를 제거(L6 — KeyError 회귀 방지). OHLCV·기업명 HIT/MISS 줄은 유지. 히스토리 델타 줄(L378-)은 단일원천 호출 현황 대표로 유지(RESEARCH Open Q2 권고 — 펀더멘털 캐시 줄 제거).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py tests/test_cache_isolation.py tests/test_history_integration.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "def fetch_fundamentals\|def _fill_us\|def _fill_kr\|def fetch_edgar_cached\|def fetch_dart_cached" src/stocksig/io/` → 0 (구 fetch 제거).
    - `grep -c "def fetch_edgar_quarterly_raw" src/stocksig/io/edgar_client.py` → 1 AND `grep -c "def fetch_dart_quarterly_raw" src/stocksig/io/dart_client.py` → 1 (L2 store 추출기 보존).
    - `grep -c "def _compute_per\|def _compute_peg\|def _compute_margin\|class MetricCell\|class FundamentalsResult\|def _is_missing" src/stocksig/io/fundamentals.py` → 6 (D-04 보존).
    - `grep -c "_FUND_DIR\|make_fund_key\|get_fund\|put_fund\|fund_hit\|fund_miss" src/stocksig/io/cache.py` → 0; `grep -c "_DEFAULT_DIR\|def get_ohlcv\|def make_key" src/stocksig/io/cache.py` → 3 (OHLCV 무손상, L3).
    - `grep -c "fund_hit\|fund_miss\|펀더멘털 HIT" src/stocksig/main_run.py` → 0 (L6 요약 정리).
    - `uv run pytest tests/test_cache.py tests/test_cache_isolation.py -q` GREEN (OHLCV 캐시 무손상 — DoS 방지).
  </acceptance_criteria>
  <done>구 fetch·펀더멘털 캐시·요약 줄이 제거되고, store 추출기·보존 계약·OHLCV/기업명 캐시가 무손상이며 전 스위트 import 깨짐 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 시트1 σ-bucket 색 신호 불변 + D-02 빈DB 빈칸 회귀 테스트</name>
  <files>tests/test_sheet_portfolio.py</files>
  <read_first>
    - tests/test_sheet_portfolio.py (기존 시트1 writer 테스트 패턴)
    - tests/test_history_integration.py (L133-188 test_sheet1_unchanged_by_history — openpyxl 셀·서식 불변 단언 패턴)
    - src/stocksig/output/sheet_portfolio.py (L112-131 _write_fund_cell·L174-203 σ-bucket 색·L248-264 소비부)
    - src/stocksig/io/fundamentals_view.py (matrix_to_fundamentals — 빈DB 시 _empty_cell)
    - .planning/phases/10-1-store-registry/10-PATTERNS.md (§test_sheet_portfolio 확장 L278-284·§sheet_portfolio 입력계약)
    - .planning/phases/10-1-store-registry/10-VALIDATION.md (색 신호 회귀 0·D-02 빈DB)
  </read_first>
  <behavior>
    - Test (-k color): 이관 후 write_portfolio_sheet 산출 xlsx를 openpyxl로 읽어 σ-bucket 조건부 색 서식·셀 값이 고정 기대값과 동일(펀더멘털 4셀은 값 동치, 색 영역 불변) — Core Value 회귀 0.
    - Test (D-02 빈DB): store 분기 데이터 없는 종목(latest_q=None → 어댑터 _empty_cell) → PER/PEG/GPM/OPM 4셀이 write_blank + 한국어 사유 주석, σ-bucket 색 셀은 영향 없음.
    - 네트워크 0: 합성 results·격리 fixture 사용.
  </behavior>
  <action>
    `tests/test_sheet_portfolio.py`에 색 회귀·빈DB 테스트를 추가한다. `test_history_integration.py::test_sheet1_unchanged_by_history`(L133-188) openpyxl 셀·서식 비교 패턴 차용. 색 테스트(`-k color` 매칭 이름, 예 `test_sigma_color_unchanged_after_migration`): 합성 results로 `write_portfolio_sheet` 호출 → 산출 xlsx를 openpyxl로 읽어 σ-bucket 셀(가격/σ 영역)의 채움색·폰트색이 고정 기대값과 일치 단언(펀더멘털 4셀은 `(DEFAULT, num_fmt)`로 색 없음 확인). 빈DB 테스트(`test_missing_db_fund_cells_blank`): `res.fundamentals = matrix_to_fundamentals({}, None)`(빈 매트릭스)로 설정한 종목 결과를 write → PER/PEG/GPM/OPM 셀이 빈칸이고 주석이 한국어 사유("조회 실패…")이며, 같은 행의 σ-bucket 색 셀은 정상 색을 유지함을 단언(D-02 빈칸이 색 신호 무영향). 모든 입력은 합성·격리 fixture로 네트워크 0.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sheet_portfolio.py -k "color or missing_db" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_sheet_portfolio.py -k color -x -q` GREEN (σ-bucket 색 불변).
    - `test_missing_db_fund_cells_blank` GREEN — 4셀 빈칸+한국어 사유, σ-bucket 색 무영향.
    - `git diff src/stocksig/output/sheet_portfolio.py` 빈 출력 (writer 0줄 diff — Core Value 구조적 보장, L9).
    - `uv run pytest -q` 전체 GREEN (≥375 baseline + 신규, 회귀 0).
  </acceptance_criteria>
  <done>시트1 σ-bucket 색 신호 불변·D-02 빈칸 동작이 회귀 테스트로 단언되고 sheet_portfolio.py 0줄 diff가 확인됨.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cache.py 편집 → OHLCV 캐시 | 펀더멘털 헬퍼 제거가 OHLCV/기업명 캐시를 침범하면 기능 마비(DoS) |
| 코드 제거 → 보존 계약 | _compute_*·store 추출기 오삭제 시 단일 원천·델타 경로 붕괴 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Denial of Service | cache.py 펀더멘털 헬퍼 제거 시 OHLCV 오삭제 | mitigate | L3 — `_DEFAULT_DIR`/`_get_cache`/`get_ohlcv`/`make_key`/`_cache_lock` 미접촉. acceptance_criteria grep(OHLCV 3건 존재) + `test_cache.py`/`test_cache_isolation.py` GREEN 가드 |
| T-10-08 | Tampering | store per-quarter 추출기 오삭제(L2) | mitigate | `fetch_edgar_quarterly_raw`/`fetch_dart_quarterly_raw` 보존 — grep 1건씩 단언. 제거는 `*_cached`만 |
| T-10-09 | Information Disclosure | 요약 줄 정리 후 잔존 로깅 | mitigate | OHLCV/기업명 HIT/MISS·델타 줄은 정수 카운트만(T-04-01) — API 키·예외 원문 미포함. CR-01 보존 |
| T-10-10 | Tampering | 보존 산식 헬퍼(_compute_*) 오삭제 → 드리프트 | mitigate | D-04 — `_compute_per`/`_compute_peg`/`_compute_margin` 보존, grep 6건(계약 전체) 단언. metrics_engine import 무손상 |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | 신규 패키지 0·제거만 — 설치 task 없음 |
</threat_model>

<verification>
- `uv run pytest -q` — 전체 스위트 GREEN(≥375, 회귀 0).
- `git diff src/stocksig/output/sheet_portfolio.py` — 빈 출력(Core Value 색 신호 구조적 보장).
- `grep -c "def fetch_edgar_quarterly_raw" src/stocksig/io/edgar_client.py` → 1, `grep -c "def fetch_dart_quarterly_raw" src/stocksig/io/dart_client.py` → 1 (L2 store 추출기 보존).
- `uv run pytest tests/test_cache.py tests/test_cache_isolation.py -q` — OHLCV 캐시 무손상(L3 DoS 방지).
</verification>

<success_criteria>
- 구 fetch 경로·`.cache/fundamentals` 헬퍼·요약 펀더멘털 줄 제거(D-03/D-05/L6), 단일 원천 완성(SC2).
- 보존 계약(D-04)·store 추출기(L2)·OHLCV/기업명 캐시(L3) 무손상.
- 시트1 σ-bucket 색 신호 불변·D-02 빈칸·sheet_portfolio 0줄 diff(Core Value 회귀 0).
- 전 스위트 GREEN(회귀 0).
</success_criteria>

<output>
Create `.planning/phases/10-1-store-registry/10-03-SUMMARY.md` when done
</output>
