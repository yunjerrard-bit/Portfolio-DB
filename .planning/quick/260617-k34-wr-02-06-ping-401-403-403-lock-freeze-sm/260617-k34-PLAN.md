---
phase: quick-260617-k34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stocksig/io/auth_check.py
  - src/stocksig/io/cache.py
  - tests/test_auth_check.py
  - tests/test_freeze_panes.py
  - tests/test_smoke_n_tickers.py
autonomous: true
requirements: [WR-02, WR-03, WR-04, WR-05, WR-06, IN-02]

must_haves:
  truths:
    - "일시 장애(5xx)나 transient 예외가 발생해도 ping_edgar/ping_dart 는 (True, None) 을 반환해 1차 소스를 차단하지 않는다"
    - "인증 실패 판정은 HTTP 401/403 에만 국한된다 (구조적 status_code 기반)"
    - "403 판별이 '403' in str(e) 문자열 매칭이 아니라 e.response.status_code == 403 으로 이루어진다"
    - "ping_dart 의 except 절에 미사용 e 바인딩이 없다 (ruff F841 청정)"
    - "캐시 싱글톤(_get_cache/_get_fund_cache/_get_name_cache) 초기화가 double-checked lock 으로 보호되어 동시 첫 호출에서도 단일 인스턴스만 생성된다"
    - "test_freeze_panes / test_smoke_n_tickers 가 실제 SEC/EDGAR/DART/Naver/yf 네트워크 호출을 0회 수행한다"
    - "전체 uv run pytest -q 가 네트워크 없이 그린 유지된다"
  artifacts:
    - path: "src/stocksig/io/auth_check.py"
      provides: "tenacity 재시도 + 401/403 한정 인증 판정 + 구조적 403 + F841 정리"
      contains: "stop_after_attempt"
    - path: "src/stocksig/io/cache.py"
      provides: "double-checked lock 으로 보호된 3개 캐시 싱글톤 초기화"
      contains: "_cache_lock"
    - path: "tests/test_auth_check.py"
      provides: "5xx/transient→(True,None), 403→(False,403노트) 회귀 단언"
      contains: "status_code"
    - path: "tests/test_freeze_panes.py"
      provides: "ping + fetch_fundamentals stub 로 네트워크 0 보장"
      contains: "ping_edgar"
    - path: "tests/test_smoke_n_tickers.py"
      provides: "fetch_fundamentals stub + ping-failure spy 의 원본 위임 보정"
      contains: "fetch_fundamentals"
  key_links:
    - from: "src/stocksig/io/auth_check.py"
      to: "tenacity"
      via: "_edgar_probe/_dart_probe 데코레이터"
      pattern: "stop_after_attempt"
    - from: "src/stocksig/io/cache.py"
      to: "threading.Lock"
      via: "double-checked locking"
      pattern: "with _cache_lock"
---

<objective>
04-REVIEW.md 의 안정성 권고 5건(WR-02~06) + 정리 1건(IN-02)을 닫는다. 모두 리뷰에 확정 Fix 코드 스니펫이 명시된 항목이다.

Purpose:
- 인증 일시 장애 false-negative 제거 — 5xx/transient 1회로 200티커 전체의 1차 소스가 꺼지는 것을 방지 (WR-02).
- 403 판별을 문자열 매칭에서 구조적 status_code 기반으로 교체해 오진/미탐 제거 (WR-03).
- 캐시 싱글톤 race 로 인한 Windows 핸들 누수 차단 (WR-04).
- "네트워크 없음" 을 표방하는 freeze/smoke 테스트가 실제로 외부 호출 0회를 충족하도록 stub 보완 (WR-05/06).
- ruff F841 청소 (IN-02).

Output:
- auth_check.py: tenacity 재시도 + 401/403 한정 + 구조적 403 + F841 제거.
- cache.py: double-checked lock 3개소.
- 테스트 격리 + 회귀 단언.

비범위(이번 플랜에서 다루지 않음): CR-01(펀더멘털 키 누설), WR-01(NaN 가드), IN-01/03/04/05/06. 별도 처리.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@.planning/milestones/v1.0-phases/04-quality-robustness/04-REVIEW.md

@src/stocksig/io/auth_check.py
@src/stocksig/io/cache.py
@tests/test_auth_check.py
@tests/test_freeze_panes.py
@tests/test_smoke_n_tickers.py

<interfaces>
<!-- 실행자가 코드베이스를 탐색하지 않도록 핵심 계약을 미리 제공 -->

tenacity 사용 패턴 (이미 src/stocksig/io/market.py, company.py 에서 동일 스택 사용):
```
from tenacity import (
    retry,
    retry_if_exception_type,
    stop_after_attempt,
    wait_exponential,
    wait_random,
)
```
- market.py 의 retry 스택: `wait=wait_exponential(multiplier=1, min=2, max=30) + wait_random(0, 1)`, `stop=stop_after_attempt(5)`.
- 이번 probe 재시도는 가볍게: `stop=stop_after_attempt(3)`, `wait=wait_exponential(multiplier=1, min=1, max=8)` 권장 (긴 대기 불필요).

auth_check.py 현재 구조:
- `@throttled_edgar def _edgar_probe() -> None`: `httpx.get(_EDGAR_PING_URL, headers=..., timeout=10)` 후 `resp.raise_for_status()`. (raise_for_status 는 4xx/5xx 모두 HTTPStatusError raise)
- `def ping_edgar() -> tuple[bool, str|None]`: `try: _edgar_probe() except Exception as e:` 에서 `note = _EDGAR_403_NOTE if "403" in str(e) else _EDGAR_FAIL_NOTE` (← WR-03 대상), `return False, note`. 성공 시 `(True, None)`.
- `@throttled_dart def _dart_probe() -> str`: status 문자열 반환. `ping_dart()` 의 `except Exception as e:` 의 e 미사용 (← IN-02), status in `_DART_VALID_KEY_STATUS={"000","013","020"}` → OK.
- 고정 노트 상수: `_EDGAR_403_NOTE = "EDGAR 403 (UA 확인)"`, `_EDGAR_FAIL_NOTE = "EDGAR 인증 실패"`, `_DART_FAIL_NOTE = "DART 인증 실패"`.
- `import httpx` 이미 존재.

cache.py 현재 구조:
- `import threading` 이미 존재. `_stats_lock = threading.Lock()` 패턴 존재 (별개 lock).
- 싱글톤 3개: `_cache`(`_DEFAULT_DIR=.cache/ohlcv`), `_fund_cache`(`_FUND_DIR=.cache/fundamentals`), `_name_cache`(`_NAME_DIR=.cache/company`).
- 각 `_get_*()` 는 `if _x is None: _DIR.mkdir(...); _x = Cache(str(_DIR))` (lock 없음).

main_run.py 심볼 바인딩 (테스트 monkeypatch 대상 — 모두 모듈 전역 이름):
- L46 `from stocksig.io.auth_check import AuthStatus, ping_dart, ping_edgar`
- L48 `from stocksig.io.fundamentals import fetch_fundamentals`
- L47 `from stocksig.io.company import fetch_company_name`
- L268-269: 클로저 `_fundamentals_with_auth` 가 **bare name** `fetch_fundamentals(...)` 를 호출 → call-time 모듈 전역 resolve → `monkeypatch.setattr(main_mod, "fetch_fundamentals", ...)` 가 적중한다.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task A: auth_check.py — WR-02 재시도+401/403 한정 + WR-03 구조적 403 + IN-02 F841</name>
  <files>src/stocksig/io/auth_check.py, tests/test_auth_check.py</files>
  <behavior>
    회귀 단언(먼저 작성 → RED):
    - test: EDGAR 5xx (httpx.HTTPStatusError, status 503) → ping_edgar() == (True, None) (인증 문제 아님, 스킵 안 함).
    - test: EDGAR transient 일반 예외 (RuntimeError/httpx.ConnectError) → ping_edgar() == (True, None).
    - test: EDGAR 403 (HTTPStatusError status 403) → (False, "EDGAR 403 (UA 확인)").
    - test: EDGAR 401 (HTTPStatusError status 401) → (False, _EDGAR_FAIL_NOTE) [401 도 인증 실패로 한정].
    - 기존 test_ping_edgar_success / _403 / _generic_exception / _note_never_leaks_ua_email 는 의미 유지하되,
      _generic_exception 의 단언은 신 계약(일반 예외→(True,None))과 충돌하므로 5xx/403 기반 케이스로 갱신.
    - test: DART transient 예외 → ping_dart() == (True, None) (status 코드 false-negative 와 분리; 예외/네트워크 장애는 인증 문제 아님).
    - test: DART status "010"(무효 키) → (False, _DART_FAIL_NOTE) 유지 (status 분기는 변경 없음).
    - 소스 단언 회귀: test_source_notes_have_no_exception_interpolation 유지 ('{e}' 보간 없음).
  </behavior>
  <action>
    WR-02 (재시도 + 401/403 한정): _edgar_probe / _dart_probe 에 tenacity retry 데코레이터를 추가한다 — stop_after_attempt(3) + wait_exponential(multiplier=1, min=1, max=8). 데코레이터 순서는 throttle 이 outer, retry 가 inner (market.py 의 throttled_yahoo→retry 패턴과 동일하게 @throttled_edgar 가 위, @retry 가 아래). retry 대상은 transient 로 한정하지 말고 (probe 는 본래 가벼운 호출이므로) 기본 동작(모든 예외 재시도)으로 두되, 3회 소진 후 마지막 예외가 ping_* 의 except 로 전파되게 한다.

    ping_edgar 의 except 블록을 재구성: `except httpx.HTTPStatusError as e:` 에서 `if e.response.status_code in (401, 403): return False, (_EDGAR_403_NOTE if e.response.status_code == 403 else _EDGAR_FAIL_NOTE)` — WR-03 의 구조적 403 판별을 여기서 흡수(403→UA 노트, 401→일반 실패 노트). `return True, None`(그 외 4xx 가 아닌 5xx 등 서버측 오류 — 인증 문제 아님). 이어서 `except Exception: return True, None` (transient — per-call 흡수 경로에 맡김). 단, 예외 원문 e 는 절대 note 에 보간하지 않는다 (T-04-03; 기존 보안 단언 유지). retry 가 RetryError 로 감쌀 수 있으므로, RetryError 인 경우 `e.last_attempt.exception()` 으로 원 예외를 꺼내거나 — 더 단순하게 `reraise=True` 를 retry 데코레이터에 설정해 원 예외가 그대로 전파되도록 한다(reraise=True 권장, except 분기 단순화).

    WR-03 문자열 매칭 제거: 기존 `note = _EDGAR_403_NOTE if "403" in str(e) else _EDGAR_FAIL_NOTE` 줄을 완전히 삭제하고 위 구조적 status_code 분기로 대체한다.

    ping_dart 재구성: `except httpx.HTTPStatusError as e:` 추가 — 401/403 만 (False, _DART_FAIL_NOTE), 그 외 5xx 는 (True, None). `except Exception:` (e 바인딩 제거 — IN-02 F841) → transient 이므로 (True, None). status 분기(`if status in _DART_VALID_KEY_STATUS: ... else (False, _DART_FAIL_NOTE)`)는 그대로 유지 — 무효 키 status("010" 등)는 인증 실패로 남긴다. 주의: _dart_probe 는 httpx 를 직접 쓰지 않고 OpenDartReader 경유라 HTTPStatusError 가 안 올 수 있으므로, ping_dart 의 핵심 변경은 (1) 일반 예외→(True,None) (2) e 바인딩 제거 두 가지다.

    docstring 의 사유 설명(403/실패 분기)을 신 계약에 맞게 갱신. tenacity import 는 파일 상단에 추가.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auth_check.py -q</automated>
  </verify>
  <done>
    - 5xx/transient → (True, None), 401/403 → (False, note) 단언 그린.
    - 소스에 '403' in str(e) 부재, e.response.status_code 사용.
    - ping_dart except 절에 미사용 e 없음.
    - PowerShell 검증: `Select-String -Path src/stocksig/io/auth_check.py -Pattern 'status_code','stop_after_attempt'` 가 모두 매치, `Select-String -Path src/stocksig/io/auth_check.py -Pattern '"403" in str'` 무매치.
    - `uv run ruff check src/stocksig/io/auth_check.py` F841 없음.
    - commit: `fix(quick-260617-k34): auth ping 재시도+401/403 한정 + 구조적 403 + F841 (WR-02/03, IN-02)`
  </done>
</task>

<task type="auto">
  <name>Task B: cache.py — WR-04 double-checked lock (ohlcv/fund/company 3개소)</name>
  <files>src/stocksig/io/cache.py</files>
  <action>
    모듈 상단(기존 `_stats_lock = threading.Lock()` 부근 또는 싱글톤 선언부 근처)에 `_cache_lock = threading.Lock()` 을 추가한다. 싱글톤 생성 전용 단일 lock 하나로 3개 _get_*() 를 모두 보호한다(_stats_lock 과 분리 — 카운터 lock 과 섞지 않음).

    _get_cache() / _get_fund_cache() / _get_name_cache() 세 함수를 double-checked locking 으로 교체:
    - 바깥 `if _x is None:` (lock 없는 fast-path) → `with _cache_lock:` → 안쪽 `if _x is None:`(재확인) → `_DIR.mkdir(parents=True, exist_ok=True); _x = Cache(str(_DIR))`. 이미 초기화된 경우 lock 없이 즉시 반환(hot path 비용 0).
    - mkdir/Cache 생성 로직은 기존과 동일하게 유지 (_DEFAULT_DIR/_FUND_DIR/_NAME_DIR, TTL 무관).

    get_ohlcv/get_fund/get_company_name 의 _stats 카운터 lock(_stats_lock) 은 건드리지 않는다. 테스트 fixture 가 `monkeypatch.setattr(cache_mod, "_cache", None)` 등으로 싱글톤을 리셋하는 동작과 호환되어야 한다(전역 변수 이름 불변).
  </action>
  <verify>
    <automated>uv run pytest tests/test_cache.py tests/test_cache_isolation.py -q</automated>
  </verify>
  <done>
    - 3개 _get_*() 모두 with _cache_lock + double-checked 패턴.
    - 기존 캐시 hit/miss·격리 테스트 무회귀 그린.
    - PowerShell 검증: `(Select-String -Path src/stocksig/io/cache.py -Pattern 'with _cache_lock').Count` 가 3 (각 _get_* 1회).
    - `uv run ruff check src/stocksig/io/cache.py` 청정.
    - commit: `fix(quick-260617-k34): 캐시 싱글톤 double-checked lock 3개소 (WR-04)`
  </done>
</task>

<task type="auto">
  <name>Task C: 테스트 격리 — WR-05 freeze fixture stub + WR-06 smoke fixture stub + ping-failure spy 보정</name>
  <files>tests/test_freeze_panes.py, tests/test_smoke_n_tickers.py</files>
  <action>
    WR-05 (tests/test_freeze_panes.py, fixture mock_pipeline_env L65-99): 현재 fetch_ohlcv + fetch_company_name 만 stub 한다. ping_edgar/ping_dart/fetch_fundamentals stub 을 추가한다. fixture 내 `import stocksig.main_run as main_run_mod` 가 이미 있으므로 그 모듈에:
    - `monkeypatch.setattr(main_run_mod, "ping_edgar", lambda: (True, None))`
    - `monkeypatch.setattr(main_run_mod, "ping_dart", lambda: (True, None))`
    - `monkeypatch.setattr(main_run_mod, "fetch_fundamentals", lambda t, m, lc, **kw: None)`  (frozen panes 검증에 펀더멘털 불필요)
    freeze 단언 "B6" 는 불변이어야 한다 — stub 추가는 시트 구조에 영향 없음.

    WR-06 (tests/test_smoke_n_tickers.py, fixture mock_pipeline_env L64-90): 현재 fetch_ohlcv + ping stub 만 있고 fetch_fundamentals 미stub. fixture 의 `import stocksig.main_run as main_mod` 직후에 추가:
    - `monkeypatch.setattr(main_mod, "fetch_fundamentals", lambda ticker, market, last_close, **kw: None)`
    이로써 run() 경유 모든 테스트가 실 EDGAR/DART/Naver/yf 를 치지 않는다.

    ping-failure spy 보정 (test_ping_failure_propagates_skip_edgar, L385-407): 이 테스트의 _spy_fund 는 L397 에서 `main_mod.fetch_fundamentals(...)` 를 호출하는데, 위 fixture stub 이 적용된 뒤 spy 가 `main_mod.fetch_fundamentals` 를 spy 자신으로 재설정하면 (1) 무한 재귀 또는 (2) 스텁(None) 위임이 되어 의도(real fetch 의 skip_edgar 전파 확인)가 깨진다. 이 테스트는 fetch_fundamentals 의 **실제 호출 인자(skip_edgar)** 만 검증하면 충분하므로, spy 가 원본 real 함수를 다시 호출할 필요가 없다. 보정:
    - spy 가 원본으로 위임하지 않도록 변경 — `_spy_fund` 가 인자만 captured 에 기록하고 `return None` 을 반환하도록 수정한다(real fetch_fundamentals 호출 제거). 이렇게 하면 fixture stub 과 무관하게 네트워크 0 + skip_edgar 전파 단언(c[2] is True)은 그대로 유효하다.
    - 또는 real 함수 검증이 꼭 필요하면 `from stocksig.io.fundamentals import fetch_fundamentals as _real_fund` 를 spy 정의 전에 캡처해 `return _real_fund(...)` 로 위임하되 그 경우 `yf_fn`/소스 stub 추가가 필요하므로, 더 단순하고 네트워크 0 인 전자(return None)를 채택한다.
    단언 `us_calls` / `all(c[2] is True ...)` 는 불변 유지.
  </action>
  <verify>
    <automated>uv run pytest tests/test_freeze_panes.py tests/test_smoke_n_tickers.py -q</automated>
  </verify>
  <done>
    - freeze 3개 테스트 그린, "B6" 단언 불변.
    - smoke 전 테스트 그린, ping-failure 전파 테스트(skip_edgar=True) 무회귀.
    - 네트워크 0 충족 — fixture 에 ping + fetch_fundamentals stub 존재.
    - PowerShell 검증: `Select-String -Path tests/test_freeze_panes.py -Pattern 'ping_edgar','fetch_fundamentals'` 매치, `Select-String -Path tests/test_smoke_n_tickers.py -Pattern 'fetch_fundamentals'` 매치.
    - commit: `test(quick-260617-k34): freeze/smoke fixture 네트워크 0 stub + ping-failure spy 보정 (WR-05/06)`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 프로세스 → SEC/DART HTTP | 인증 ping probe 가 외부 서비스에 식별 정보(UA 이메일, crtfc_key)를 전송 |
| 예외 객체 → 로그/Excel note | 예외 원문에 UA/API 키가 포함될 수 있음 (기 식별된 위협 T-04-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k34-01 | Information Disclosure | auth_check.ping_edgar/ping_dart note | mitigate | 예외 원문 e 를 note 에 보간 금지 유지 — Task A 가 구조적 status_code 분기로 교체하되 고정 한국어 노트만 반환. 기존 보안 단언(test_*_note_never_leaks_*) 회귀로 보장 |
| T-k34-02 | Denial of Service | auth ping false-negative | mitigate | WR-02: 5xx/transient 를 (True, None) 으로 처리해 1차 소스 차단(self-DoS) 방지. retry 3회 한정으로 무한 대기 방지 |
| T-k34-03 | Tampering | 캐시 핸들 누수 | mitigate | WR-04: double-checked lock 으로 중복 Cache 인스턴스/Windows 핸들 누수 차단 |
| T-k34-SC | Tampering | 패키지 설치 | accept | 신규 패키지 설치 없음 — tenacity 는 이미 스택에 존재(market.py/company.py 사용). 설치 게이트 불필요 |
</threat_model>

<verification>
순차 검증 (PowerShell, Windows, 네트워크 없음):
1. `uv run pytest tests/test_auth_check.py -q` — Task A 회귀 그린
2. `uv run pytest tests/test_cache.py tests/test_cache_isolation.py -q` — Task B 무회귀
3. `uv run pytest tests/test_freeze_panes.py tests/test_smoke_n_tickers.py -q` — Task C 격리 그린
4. `uv run pytest -q` — 전체 스위트 그린 (네트워크 없음)
5. `uv run ruff check src/stocksig/io/auth_check.py src/stocksig/io/cache.py` — F841 등 0
6. `Select-String -Path src/stocksig/io/auth_check.py -Pattern '"403" in str'` — 무매치 (WR-03 문자열 매칭 제거 확인)
</verification>

<success_criteria>
- WR-02: ping_edgar/ping_dart 가 5xx/transient 에서 (True, None) 반환, 인증 실패는 401/403 한정. tenacity 재시도 적용. 단위 테스트 증명.
- WR-03: 403 판별이 e.response.status_code == 403 구조적 기반. '403' in str(e) 소스에서 제거.
- IN-02: ping_dart except 절 미사용 e 제거, ruff F841 청정.
- WR-04: 3개 캐시 싱글톤 double-checked lock. 기존 캐시 테스트 무회귀.
- WR-05/06: freeze/smoke fixture 가 ping + fetch_fundamentals stub 으로 외부 호출 0회. ping-failure 전파 테스트 무회귀.
- 전체 `uv run pytest -q` 네트워크 없이 그린.
</success_criteria>

<output>
Create `.planning/quick/260617-k34-wr-02-06-ping-401-403-403-lock-freeze-sm/260617-k34-SUMMARY.md` when done
</output>
